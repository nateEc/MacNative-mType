# Typebar 人工验收记录

本表将“自动化测试通过”与“真实 macOS 交互已验收”分开记录。只有 `状态：已验收` 的条目可作为人工验收证据；其余条目不得据此宣称完成。

## 当前构建

- 应用：`Typebar.app`
- 自动化基线：客户端 525 项、服务端 79 项通过（2026-09-10）
- 参考基线：Monkeytype 官方源码提交 `91bd24bb8513785c7364cbea29296ff7adafac41`，只读盘点使用

## 待执行场景

| ID | 场景 | 操作 | 期望结果 | 状态 |
| --- | --- | --- | --- | --- |
| SMK-01 | release 候选包主流程冒烟 | 启动独立候选 `.app`，输入一个完整英文词，随后放弃测试并重开 | 主界面、输入焦点、实时指标、节奏引导和下一键提示均可用；放弃不会保存完成成绩 | 已验收 |
| APP-01 | 关于窗口与项目归属 | 从 macOS 应用菜单打开“关于 Typebar”；关闭后按 ⇧⌘K 分别搜索“关于”和“隐私”再打开；检查版本、链接与各说明 | 两个入口打开同一独立原生窗口；显示当前 bundle 版本/构建、本机优先和清晰联网边界、四项指标定义、Typebar 源码/问题反馈/固定参考项目链接；不显示广告，不冒充 Monkeytype 品牌、支持者或贡献者，也不声称存在仓库未提供的许可证 | 待验收（版本回退与中英文命令发现由客户端自动化覆盖；依用户要求不启动 GUI） |
| APP-02 | 版本历史与发布说明 | 分别从应用菜单、关于窗口和 ⇧⌘K 搜索“版本历史”打开；联网读取后继续加载更早发布，再断网重试并点击一条发布链接 | 三个入口打开同一独立原生窗口；仅显示 Typebar 自己的正式 GitHub Release，标题、标签、日期和纯文本说明可读且链接回对应发布；草稿/预发布不出现，分页不重复，断网显示明确重试且不影响离线练习 | 待验收（解析、筛选、仓库端点、外链约束与中英文命令发现由客户端自动化覆盖；依用户要求不启动 GUI） |
| MAC-01 | 本机数据启动恢复 | 在隔离副本中让 SwiftData 文件暂时不可打开后启动候选应用；记录恢复页，再还原文件权限并重启 | 应用保持运行并显示原数据文件路径、错误详情、“在 Finder 中显示”和“复制诊断信息”；不会删除、替换原文件或进入可写空库。还原后重启会重新打开原数据 | 待验收（成功/失败决策、单次打开尝试、原路径与错误保留由客户端自动化覆盖；依用户要求不启动 GUI） |
| INP-02 | 组合输入 | 用 macOS 中文输入法输入一段简体中文提示，在“关闭 / 下方显示 / 替换当前字符”间切换，确认组合中间态不提交，确认后才进入计分 | 无重复/丢失字符；组合态按所选策略呈现；可完成测试 | 待验收（中间态与提交由自动化覆盖） |
| INP-09 | 失焦提醒延迟 | 开启“输入框失焦提示”，在练习输入框外点击后立刻回点输入框；再失焦并等待超过 1 秒；最后关闭该开关重复 | 短暂失焦不显示提醒；连续失焦满约 1 秒后才显示恢复输入提示，重新获焦立即移除；关闭开关后始终不显示，输入和计分不变 | 待验收（延迟门控与其他焦点/大小写规则由客户端测试覆盖） |
| MOD-01 | 引语审核 | 启动配置了审核密钥的服务，输入密钥并读取待审核引语，批准一项 | 仅显示引语/举报上下文；批准后从待审核队列移除 | 待验收 |
| MOD-02 | 资料举报审核 | 创建两账户并提交资料举报，审核员读取资料队列并标为已处理 | 不显示举报人或邮箱；被举报账户资料不自动改变 | 待验收 |
| ACC-01 | 听写模式 | 在设置启用“听写模式”，重开练习 | 系统语音朗读提示，文字隐藏，输入仍按原提示计分 | 待验收（已验证 UI） |
| ACC-02 | 排行榜隐身 | 登录自建服务，在账户设置切换“从 WPM 和 XP 排行榜隐藏我” | 开关即时更新；说明明确成绩、XP、同步和本机历史保留 | 待验收 |
| ACC-03 | 密码重置 | 部署一个受信任 HTTPS 投递 webhook，在设置选择“重置密码”，请求已注册与未注册邮箱，然后用收到的码设置新密码；最后分别用旧、新密码登录，并检查另一设备会话 | 两类邮箱请求都显示相同的通用成功提示；码在 20 分钟内只能使用一次；完成后旧密码和所有旧会话都失效，新密码可登录。未配置投递时明确显示服务端不可用，绝不声称已发送邮件 | 待验收（令牌哈希、过期、一次性、会话撤销、未知邮箱同响应、投递配置边界由服务端自动化覆盖） |
| ACC-04 | 邮箱验证 | 部署一个受信任 HTTPS 投递 webhook，注册账户并取得首封验证邮件；在设置重发，再分别尝试旧码与新码；改邮箱后完成新邮箱收到的验证码。 | 注册与改邮箱后自动投递，重发会使旧码失效；新码仅能使用一次、24 小时后过期；验证成功立即显示已验证且会话继续有效；改邮箱会复位验证状态。未配置投递时明确显示服务端不可用，绝不声称已发送邮件 | 待验收（令牌哈希、过期、一次性、重发与改邮箱撤销、路由和资料状态由服务端自动化覆盖） |
| ACC-05 | 第三方登录 | 配置一个 GitHub 或 Google OAuth 客户端，并在原生账户设置中依次测试新用户登录、已有用户关联和移除关联方式；再为一个纯第三方账户添加、移除密码方式。 | 授权页仅请求所需公开资料和已验证邮箱范围；取消、过期或重放授权不会登录；新用户可补充展示名，已有账户可关联或移除方式但不能移除最后一种登录方式。移除第三方方式时，密码账户须验证当前密码；纯第三方账户须在系统授权窗口重新确认另一已关联提供商。添加密码也须重新确认第三方身份；重新验证凭据只能使用一次且五分钟内过期。移除密码前必须验证当前密码且仍保留第三方方式。 | 待验收（服务端授权码、PKCE、回调、一次性状态、注册、关联、密码增删和重新验证路径已自动化覆盖；原生端编译及旧服务响应兼容性已自动化覆盖，真实提供商账户待人工验收） |
| ACC-06 | 撤销所有设备会话 | 以密码账户在两台 Mac 登录，在其中一台输入当前密码并确认“撤销所有设备会话”；另以纯第三方账户在两台 Mac 登录，在其中一台按系统授权窗口重新确认身份后撤销。 | 两种账户的两台设备都立即退出自建服务；再次使用旧令牌的请求被拒绝，本机离线练习历史保留。 | 待验收（服务端会话撤销、当前及第二会话同时失效，以及重新验证凭据的一次性和过期由自动化覆盖） |
| ACC-07 | 开发者密钥与私有成绩 | 登录自建服务，在设置创建一个名称为 `results-uploader` 的开发者密钥，保存显示的一次性明文；用该值向 `POST /v1/results` 提交成绩，再以同一 `X-Typebar-Access-Key` 读取 `GET /v1/results` 和该成绩详情；随后禁用并删除该密钥，重试读取/上传；最后尝试用该值读取 `/v1/profiles/me`。 | 明文只在创建后显示，列表不显示明文；启用状态下只能读取或上传该账户的最小成绩元数据，且列表记录最近使用时间。禁用或删除后读取和上传被拒绝；密钥不能读取资料或执行账户操作。 | 待验收（创建、哈希化持久化、跨账户隔离、分页过滤、禁用/删除和路由范围由 60 项服务端自动化覆盖） |
| ACC-08 | 清除服务端成绩 | 用密码账户在设置输入当前密码并选择“清除所有服务端成绩”；另以纯第三方账户重复，完成系统授权确认。分别查看服务端近期成绩、XP、排行榜与本机历史。 | 每次都需要二次确认；只清除当前账户的服务端成绩和相应 XP，本机历史、同步与其他账户不变。密码错误、过期或重复使用的重新验证凭据被拒绝；开发者密钥不能调用删除路由。 | 待验收（密码确认、用户隔离、路由认证和删除数量由 60 项服务端自动化覆盖；纯第三方真实授权待人工验收） |
| ACC-09 | 服务端成绩标签 | 登录自建服务，在设置为一条近期服务端成绩添加 `review` 与 `café`，移除其中一个；再用另一账户、开发者密钥和重复或超限标签分别请求同一更新路由。 | 当前账户的标签立即刷新，最多五个且每个 1–24 个字符；首尾空白被移除，大小写或重音差异的重复标签被拒绝。其他账户、开发者密钥和不存在的成绩均不能修改；旧数据没有标签时显示为空。 | 待验收（正常保存、跨账户隔离、开发者密钥拒绝与重复标签拒绝由 60 项服务端自动化覆盖） |
| ACC-10 | 徽章目录与奖励通知 | 先提交一条持续 60 秒但事件数为零的合法结果；再用新账户提交 9 条覆盖五种语言、四种模式但均低于 100 WPM、单条不足一分钟的成绩，第 10 条恰好 100 WPM、60 秒且零误差，最后使累计时长恰好达到一小时。选择“模式巡游”公开展示，重复上传末条成绩，再删除全部服务端成绩并用另一账户查看。 | 零输入结果不解锁或推进任何徽章。十枚原创徽章按首次／十次完成、98% 准确率、连续一分钟零误差、80／100 WPM、累计 15／60 分钟、五种语言和四种模式的边界逐项解锁；每枚只通知一次并显示原创标题与 SF Symbol。重复成绩不重复通知，另一账户看不到；删除通知不删除徽章，删除全部服务端成绩则撤销全部派生徽章与当前选择。 | 待验收（零输入与阈值边界、客户端动态展示、旧通知解码、服务端新解锁差分、选择、撤销、幂等和隔离由客户端 387 项／服务端 74 项自动化覆盖；依用户要求不启动 GUI） |
| ACC-11 | 重置服务端公开个人最佳 | 先上传一条较快标准档成绩并记录服务端历史、XP、徽章和榜单；在账户设置重新确认身份并重置服务端个人最佳，再上传一条较慢同档成绩。另用第二账户查看。 | 重置后旧成绩仍在，XP、徽章、完成/开始次数、练习时长、活动和排行榜不变，但公开最佳 WPM、稳定度及标准档 PB 清空；较慢的新接收成绩成为新 PB。第二账户不变，本机历史与本机 PB 不变；密码错误、无认证和失效重新验证均被拒绝。 | 待验收（服务端纪元派生、授权、隔离、持久化往返、路由认证及客户端新旧响应解码由客户端 374 项／服务端 73 项自动化覆盖；依用户要求不启动 GUI） |
| ACC-12 | 导出全部服务端成绩 CSV | 为同一账户准备超过一页的服务端成绩，在账户设置点击“导出全部 CSV…”，选择临时目录并用 Numbers 或文本编辑器打开；导出读取期间另提交一条成绩后重试。 | 稳定时文件包含该账户全部服务端成绩而非只含最近二十条，UTC 文件名、列顺序、标签引号/逗号转义正确；不含提示、输入、回放、邮箱、令牌、本机历史或其他账户。分页期间总数变化时不打开保存面板并提示重试，稳定后可重新导出。 | 待验收（完整分页、顺序、快照变化/重复页拒绝、CSV 字段、UTC 名称、转义和敏感字段排除由客户端 374 项自动化覆盖；依用户要求不启动 GUI） |
| ACC-13 | 完整重置账户数据 | 先导出归档与服务端 CSV；准备本机成绩、两类预设、保存文本、自定义设置/背景/字体，以及服务端成绩、资料、密钥、同步档案、通知、好友、屏蔽、投稿和固定统计日边界。重新验证后执行“重置服务端与这台 Mac…”，再重复一次，并用另一账户检查。 | 首次操作永久清空约定的服务端与本机可变数据；账户身份、登录会话、好友、屏蔽、投稿、统计日边界及其他账户保持不变。同步检查点归零且旧档案不回灌；重复执行结果相同。模拟本机清理失败时明确显示“服务端已重置、本机未完成”，再次执行可完成；旧服务拒绝新路由时本机数据不删除。 | 待验收（服务端授权、保留/清除边界、账户隔离、幂等、持久化与路由，以及本机四类模型、设置、文件清理和同步检查点由客户端 374 项／服务端 73 项自动化覆盖；依用户要求不启动 GUI） |
| HIS-01 | 筛选后的本机 CSV | 保存几条不同模式、标签和语言的本机成绩；在练习历史用筛选只留下其中一部分，选择“导出 CSV…”，再用 Numbers 或文本编辑器打开文件。 | 保存面板提供 UTC 命名的 CSV；只导出当前筛选集，并包含成绩、配置、标签与本机派生指标。逗号和引号标签保持合法 CSV 转义；提示、实际输入、回放、账户资料和远端数据均不出现。 | 待验收（CSV 字段、UTC 名称、空集、转义与敏感内容排除由客户端自动化覆盖） |
| SOC-03 | 公开资料稳定度 | 让一个账户上传两条 WPM 与稳定度不同的完成成绩；从全局榜打开其资料卡，再搜索或添加为好友并刷新好友列表；最后查询一条缺少稳定度的旧服务响应。 | 资料卡和好友行显示最佳 WPM 与最高稳定度；最高稳定度可来自非最高 WPM 的另一条成绩。缺失字段显示 `0%`，不会泄露邮箱、令牌或本机回放。 | 待验收（服务端聚合、范围校验后的成绩、客户端旧响应回退由自动化覆盖） |
| SOC-04 | 公开标准档个人最佳 | 在一个账户完成两个 30 秒档成绩（其中较快者的稳定度较低），再完成 15 秒和 10 词成绩；从榜单打开资料卡。 | 卡片显示 15/30/60/120 秒及 10/25/50/100 词中有成绩的标准档。30 秒卡取较快成绩，却保留它自身的准确率、稳定度、语言和日期；未完成标准档不显示。提示、输入回放、邮箱和令牌不会显示。 | 待验收（标准档聚合、同档取较快成绩和响应内容由服务端自动化覆盖） |
| SOC-05 | 公开活动日历与账户日界 | 登录后把“公开连续练习日界”固定为 `UTC+1`，在 UTC 午夜后、该日界前完成练习，再跨过日界完成第二轮；重启服务后再次查看资料卡并尝试改为其他值。另搜索同一账户、查看好友与通知列表。 | 首轮归入前一逻辑练习日，第二轮归入新日；资料卡标出 `UTC+1`，活动格与当前/最长连续天数使用同一边界。重启后选择保留且再次设置被拒绝；成绩原时间不变。本机日界不被上传。搜索、好友和通知不包含 365 天活动、提示、回放、邮箱或令牌。 | 待验收（范围/半小时校验、跨 UTC 午夜聚合、一次锁定、持久化、认证路由与旧服务字段回退由自动化覆盖；依用户要求不启动 GUI） |
| SOC-06 | 公开资料编辑与隐私 | 在账户设置填写简介、键盘说明、GitHub、X / Twitter 用户名和 HTTPS 网站，打开另一账户的资料卡；随后关闭“在公开资料显示练习活动”并重新打开资料卡。 | 所有非空资料字段与链接可见，邮箱、令牌、提示和本机回放始终不可见；关闭活动后活动日历消失，其他公开资料和成绩保持可见。无效网站、超长简介或键盘说明会被拒绝；资料举报可选择不当简介或链接。 | 待验收（服务端字段校验、公开响应、活动隐私和客户端旧响应回退由自动化覆盖） |
| SYN-02 | 分页拉取与原子检查点 | 在自建服务中为同一账户准备至少 101 条版本化变更，最后一条为有效 Typebar 归档；在另一台 Mac 登录并点击“拉取”，完成后再次拉取。再把最后归档换成无效 JSON，确认失败后修正并重试。 | 首次拉取会读完所有分页并只合并一次最终归档；再次拉取显示没有新归档。无效归档不会推进本机游标或版本；修正后可从同一检查点成功拉取。 | 待验收（服务端分页、用户隔离、最终全局游标推进和客户端构建由自动化覆盖） |
| SYN-03 | 多账户与多服务器隔离 | 在服务器 A 的账户甲上传归档并拉取到最新游标；退出后登录同服账户乙，确认能从其游标 0 拉取；再把服务地址切到服务器 B 并登录，最后切回服务器 A，点击“恢复此服务器会话”。尝试在任一网络请求进行中编辑服务地址。 | 三个作用域的归档版本与游标互不继承；切换服务器立即清除当前显示身份；恢复按钮只读取当前服务地址的钥匙串令牌并恢复该服务器会话。活动请求期间地址不可编辑。抓包确认服务器 B 从未收到服务器 A 的 Bearer 令牌。升级旧版时首次同步可重放但不跳过远端归档；降级旧版最多要求重新登录。 | 待验收（URL 规范化、账户/服务器检查点隔离、令牌槽隔离、端点切换与活动请求门控由客户端自动化覆盖；真实双服务器钥匙串与抓包待人工验收） |
| SYN-04 | 同步冲突审计 | 在服务器 A 的账户甲与两台 Mac 分别创建同名但内容不同的预设、文本、主题和自定义键盘布局；两端先后上传以触发版本冲突。打开同步页，再切换到同服账户乙和服务器 B，最后切回账户甲并清空记录。 | 账户甲会保留本机版本并为远端差异另存四类带“同步冲突”标记的副本；同步页逐条显示副本类型、最终名称和时间，最多保留最近 50 条。账户乙与服务器 B 均看不到账户甲记录；切回后记录恢复。清空只删除账户甲在服务器 A 的本机审计，不删除副本、归档、远端数据或同步游标。记录中没有文本正文、成绩、归档载荷或凭据。 | 待验收（四类冲突明细、唯一内容不误报、URL/账户隔离、容量、持久化、损坏恢复与清空由客户端自动化覆盖；依用户要求本批不启动 GUI） |
| RANK-01 | 榜单稳定度 | 使用两账户各完成一轮不同节奏的有效练习，刷新全局榜和好友榜；再让一个旧客户端或旧服务端记录参与查询。 | 每条榜单成绩显示本机派生的稳定度百分比；全局/好友筛选与排序不因展示字段改变。缺失稳定度的旧请求、记录或响应显示 `0%`，且不会导致请求失败。 | 待验收（服务端范围校验、持久化、旧字段回退与榜单返回由自动化覆盖） |
| RANK-02 | 当前账户排名 | 用至少三名用户建立不同 WPM/XP；让当前账户排在全局可见列表以外，再在“同步”依次刷新全局与好友 WPM、XP 榜，并开启隐身后重复。 | 常规会话显示当前范围下自己的实际名次，即使不在前 25/100 名；好友榜只按接受关系重排。无合资格成绩或隐身时明确没有个人条目；开发者密钥和未登录请求被拒绝。 | 待验收（前榜外、好友隔离、隐身、常规会话认证与路由响应由 60 项服务端自动化覆盖；原生端旧服务降级待人工验收） |
| RANK-03 | 昨日 WPM 榜 | 用同一账户在服务端今天、昨天和本 ISO 周前分别提交有效成绩；在“同步”切换“今天”“昨天”“本周”，再查看全局和好友范围及个人排名。 | “昨天”仅显示服务端前一个完整日的最佳成绩，不混入今天或更早成绩；全局、好友和个人名次共享该时间范围。 | 待验收（服务端日边界和个人名次由自动化覆盖；原生范围选择待人工验收） |
| RANK-05 | 完成页今日 WPM 名次 | 以可见账户完成并发布一轮成绩，再以另一账户提交更快的同模式同语言成绩并重试第一条；点击完成页名次。随后测试昨日成绩、排行榜隐身和旧服务响应。 | 回执显示当前同模式同语言今日全局名次；重复 UUID 不改变首次成绩或 XP，但会按当前榜单重新返回其名次。点击后打开已预设并刷新相同模式、语言和“今天”的榜单。昨日成绩、隐身账户与旧服务不显示名次入口 | 待验收（今日/历史日、隐身、重复 UUID 权威记录、可选字段解码及结构化客户端状态由自动化覆盖；依用户要求不启动 GUI） |
| RANK-04 | 上周 XP 榜 | 用两个账户分别在本 ISO 周和上一个完整 ISO 周提交有效成绩；在“同步”切换“本周”“上周”，查看全局/好友榜与当前账户名次；再连接缺少范围回显的旧服务并选择“上周”。 | 两个 ISO 周成绩严格隔离，当前范围回显一致；旧服务会明确提示不支持上周 XP 榜，绝不把可能的本周数据展示为上周。 | 待验收（两周隔离、范围回显、排名和非法范围由 60 项服务端自动化覆盖；原生范围选择和旧服务提示待人工验收） |
| TST-03 | 引语长度、收藏与本机搜索模式 | 选择引语模式，在“按长度”选短与长并保存预设；切到“收藏”确认跨长度候选；再切到“搜索”，输入两个关键词、手选一条并重开，最后应用刚才的预设 | 长度模式仅显示所选档；收藏模式忽略长度并显示该语言全部收藏；搜索同时匹配标题/内容、锁定所选引语且重开不换条；预设恢复长度模式与集合，关键词不会上传或进入分享链接；无收藏时回到长度模式 | 部分验收（旧搜索 UI 已实机验收；六值映射、模式筛选、锁定、分享往返和旧配置回退由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-04 | 超长引语 | 选择引语模式并在长度菜单选择“超长引语” | 显示当前语言的 Typebar 自有长篇内容且可开始练习 | 已验收 |
| TST-11 | 乌克兰语离线练习 | 在语言菜单选择「Українська」，分别开始字数与引语练习；用 macOS 乌克兰语输入源输入包含 `ї`、`є`、`ґ` 的提示词。 | 词流和四档引语均来自 Typebar 原创内容；词间以空格提交，三个乌克兰语特有字符按系统输入法正常计分。朗读使用系统 `uk-UA` voice，百科入口仅在用户明确选择实时内容时请求乌克兰语页面。 | 待验收（词库、引语、混排、弱项复练、朗读、百科入口及服务端白名单由客户端/服务端自动化覆盖） |
| TST-12 | 乌克兰语 Latin 离线练习 | 在语言菜单选择「Українська (Latin)」，分别开始字数与引语练习；输入 `yizha yednist grunt`，再启用“知识短文”。 | 词流和四档引语只使用 Typebar 自创 ASCII Latin 内容并按空格提交；错词复练仍在同一自有词库内。知识短文不会发起/应用会产生西里尔内容的百科替换，保持离线 Latin 提示；系统朗读使用 `uk-UA` voice。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-13 | Dutch 离线与百科练习 | 在语言菜单选择「Nederlands」，分别开始字数、引语和“知识短文”练习；用 macOS 荷兰语输入源输入 `één`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求荷兰语页面。`één` 的组合重音输入、错词复练和系统 `nl-NL` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-14 | Danish 离线与百科练习 | 在语言菜单选择「Dansk」，分别开始字数、引语和“知识短文”练习；用 macOS 丹麦语输入源输入 `åndedræt`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求丹麦语页面。`æ`、`ø`、`å` 的系统输入、错词复练和系统 `da-DK` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-15 | Norwegian Bokmål 离线与百科练习 | 在语言菜单选择「Norsk bokmål」，分别开始字数、引语和“知识短文”练习；用 macOS 挪威语输入源输入 `fjær`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 Bokmål 的 `no` 页面。`æ`、`ø`、`å` 的系统输入、错词复练和系统 `nb-NO` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-16 | Hungarian 离线与百科练习 | 在语言菜单选择「Magyar」，分别开始字数、引语和“知识短文”练习；用 macOS 匈牙利语输入源输入 `tűz`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `hu` 页面。长短重音 `á/é/í/ó/ö/ő/ú/ü/ű` 的系统输入、错词复练和系统 `hu-HU` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-19 | Swedish 离线与百科练习 | 在语言菜单选择「Svenska」，分别开始字数、引语和“知识短文”练习；用 macOS 瑞典语输入源输入 `sjö`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `sv` 页面。`å`、`ä`、`ö` 的系统输入、错词复练和系统 `sv-SE` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-20 | Greek 离线与百科练习 | 在语言菜单选择「Ελληνικά」，分别开始字数、引语和“知识短文”练习；用 macOS 希腊语输入源输入 `πρωί`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `el` 页面。重音字符的系统输入、错词复练和系统 `el-GR` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-120 | Koine Greek、Pig Latin 与 Lorem Ipsum | 分别选择「Ἑλληνιστικὴ Κοινή」「Pig Latin」「Lorem Ipsum · Typebar」完成字数与引语练习；Koine 使用 macOS 希腊语输入源，后两项确认简化输入被禁用，再将三项加入自选多语练习。 | 三项均显示 Typebar 自有内容和四档文本并以空格提交；Koine 的百科／朗读为 `el`／`el-GR`；Pig Latin 确定性转换 Typebar 自有英语内容，Lorem 不引用参考文本；三项可混排，社区投稿、撤回、成绩与排行榜均接受对应语言 ID。 | 待验收（词流、转换、四档文本、混排、配置元数据和服务端数据面由客户端 370 项／服务端 70 项自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-121 | 五字母英语 | 选择「English · Five Letter」完成字数与四档引语练习，再加入自选多语练习并提交一条社区引语与成绩 | 所有离线词流和引语单词均恰好五个字母；百科／朗读使用 `en`／`en-US`，简化输入可选；投稿、撤回、成绩与排行榜接受 `englishFiveLetter` | 待验收（词长、四档文本、混排、配置元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-122 | Kokanu | 选择「Kokanu」完成字数与四档引语练习，再加入自选多语练习并提交一条社区引语与成绩 | 显示 Typebar 独立编写的 Kokanu 拉丁转写内容并以空格提交；知识短文／朗读使用 `xxs`／`xxs-Lat`，Zipf 显示不支持提示、简化输入可选；服务端接受 `kokanu` | 待验收（词流、四档文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-123 | Likanu | 选择「Likanu」完成字数与四档引语练习，再加入自选多语练习并提交一条社区引语与成绩 | Typebar 自写音节解析器把自有 Kokanu 内容转换为 Likanu，保留组合上划线、原生连写和 Likanu 标点；知识短文／朗读使用 `xxs`／`xxs-Uixs`，简化输入可选；服务端接受 `likanu` | 待验收（转写、词流、四档文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-124 | 三种英语专题词集 | 分别选择「English · Commonly Misspelled」「English · Contractions」「English · Double Letter」完成字数与四档引语练习，再加入自选多语并提交社区引语与成绩 | 易错词显示正确拼法；缩写词流每词含直撇号；双字母词流每词含相邻重复字符。三者禁用简化输入、以 `en`／`en-US` 获取知识短文和朗读、显示 Zipf 不支持，并由服务端接受各语言 ID | 待验收（词集约束、四档文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-125 | Legal 与 Medical 英语 | 分别选择「English · Legal」「English · Medical」完成字数与四档引语练习，再加入自选多语并提交社区引语与成绩 | 两项显示各自 Typebar 自写领域内容并使用 `en`／`en-US`；Legal 可选简化输入且 Zipf 状态未知，Medical 禁用简化输入且显示 Zipf 不支持；服务端接受两个语言 ID | 待验收（领域词流、四档文本、混排、差异元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-126 | Shakespearean English | 选择「English · Shakespearean」完成字数与四档引语练习，再加入自选多语并提交社区引语与成绩 | 显示 Typebar 自写古体词流及仿古文本，不出现复制的莎士比亚原文；禁用简化输入、使用 `en`／`en-US`、Zipf 状态未知；服务端接受 `englishShakespearean` | 待验收（古体词流、四档文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-127 | Swedish Diacritics | 选择「Svenska · Å Ä Ö」完成字数与四档引语练习，再加入自选多语并提交社区引语与成绩 | 每个词流候选均为 4–6 字符且包含 `å`、`ä` 或 `ö`；使用 `sv`／`sv-SE`、保留简化输入且 Zipf 状态未知；服务端接受 `swedishDiacritics` | 待验收（词形约束、四档原创文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-128 | Portuguese accents and cedilla | 选择「Português · Acentos e cedilha」完成字数与四档引语练习，再加入自选多语并提交社区引语与成绩 | 每个词流候选均包含葡萄牙语重音字母或 `ç`；使用 `pt`／`pt-PT`、保留简化输入且 Zipf 状态未知；服务端接受 `portugueseAccents` | 待验收（词形约束、四档原创文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-129 | Russian abbreviations | 选择「Русский · Аббревиатуры」完成字数与四档引语练习，再加入自选多语并提交社区引语与成绩 | 自有词流全部为不超过 6 字符的无空格、无数字西里尔 token，以大写缩略词为主并保留少量小写形式；使用 `ru`／`ru-RU`、禁用简化输入且 Zipf 不支持；服务端接受 `russianAbbreviations` | 待验收（词形比例、四档原创文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-130 | Ukrainian endings | 分别选择「Українська · Закінчення」与「Українська (Latynka) · Закінчення」完成字数和四档引语练习，再加入自选多语并提交社区引语与成绩 | 原生词流仅含 1–4 字符乌克兰西里尔词尾；Latynka 词流仅含 1–5 字符的 `a-zïğš` 并覆盖三个扩展字符。两项禁用简化输入、以 `en`／`en-US` 走缺省在线／朗读路径、Zipf 状态未知；服务端接受两个独立语言 ID | 待验收（词形边界、四档原创文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-131 | Bangla letters 与连写塑形 | 选择「বাংলা · অক্ষর」完成字数和四档引语练习，再加入自选多语并提交社区引语与成绩；分别检查 Bangla、Indic、RTL、Korean、Likanu、Tibetan 与 Yiddish 提示 | 字符流为 62 个 Typebar 独立编排的 Bengali Unicode 字母、符号及组合序列，保持固定 56/3/2/1 标量长度分布；使用 `bn`／`bn-BD`、禁用简化输入且 Zipf 状态未知。全部 26 个固定 `joiningScript` 语言均启用原生连写行距和逐字隐藏保护；服务端接受 `banglaLetters` | 待验收（字符边界、四档原创文本、混排、固定元数据、全量连写集合和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-132 | Git 专项 | 选择「Git」完成字数和四档引语练习，再加入自选多语并提交社区引语与成绩 | 自有词流包含 52 个唯一、全小写 ASCII 的 Git 命令／概念 token，保持固定长度分布，只有 4 项包含符号且符号仅为 `-`／`@`；独立 `@` 作为一字符 Git 引用 token 正常生成、输入和计分，不会被装饰标点清理；使用 `en`／`en-US`、禁用简化输入且 Zipf 状态未知；服务端接受 `git` | 待验收（词形结构、四档原创文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-133 | toki pona ku 词集 | 分别选择「toki pona · ku suli」与「toki pona · ku lili」完成字数和四档引语练习，再加入自选多语并提交社区引语与成绩 | ku suli 是 Typebar 自有基础集合的严格超集，ku lili 与其互斥；两项禁用简化输入、走 `en`／`en-US` 缺省路径且 Zipf 状态未知；服务端接受两个独立语言 ID | 待验收（独立词集、四档自有文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-134 | Old English | 选择「Old English」完成字数和四档引语练习，输入含 `æ` 与 `þ` 的词，再加入自选多语并提交社区引语与成绩 | 79 个 Typebar 独立编写词均为 2–11 字符，只含 ASCII 小写字母与 `æ/þ`；使用 LTR 空格词界、`en`／`en-US` 缺省路径、可选简化输入和 Zipf 未知状态；服务端接受 `oldEnglish` | 待验收（词形边界、四档原创文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-135 | Français · Bitoduc | 选择「Français · Bitoduc」完成字数和四档引语练习，输入含连字符及 `çèéêîô` 的词，再加入自选多语并提交社区引语与成绩 | 显示 72 个 Typebar 独立编写的科技法语与趣味复合词，不读取 bitoduc.fr 内容；每项为 5–14 字符单 token，使用 `fr`／`fr-fr`、可选简化输入和 Zipf 未知状态；服务端接受 `frenchBitoduc` | 待验收（词形边界、四档原创文本、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-136 | Streaming Emotes · Typebar | 选择该模式完成字数和四档 token 流练习，输入大小写、数字、下划线及轻量表情符号，再加入自选多语并提交社区内容与成绩 | 显示 80 个 Typebar 完全虚构的无空格 emote token，不包含 Twitch 名称、聊天数据或资产；使用 `en`／`en-US`、禁用简化输入且 Zipf 状态未知；服务端接受 `twitchEmotes` | 待验收（token 结构、四档自有内容、混排、固定元数据和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-137 | Arcade Horror Phrases · Typebar | 分别关闭和开启标点完成 25 词练习，再开启数字并测试四档引语、混排、投稿与成绩 | 64 段 Typebar 原创多词短语按最终词数截断；关闭标点时为小写纯词，开启时保留短语自身大小写和标点，数字位仍替换为 1/2/3；使用 `en`／`en-US`、禁用简化输入且 Zipf 不支持；服务端接受 `typingOfTheDead` | 待验收（section 结构、原始标点、词数、数字、四档内容、混排和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-138 | Creature Index 1k · Typebar | 完成普通、标点和数字练习，抽查多词及特殊符号条目，再测试四档引语、混排、投稿与成绩 | 1,025 个 Typebar 原创虚构生物条目保持 3–12 字符、28 个多词、17 个符号及 1 个数字结构；使用 `en`、简化输入缺省且 Zipf 不支持；服务端接受 `pokemon1k` | 待验收（规模、结构、最终词数、四档内容、混排和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-139 | Arena Strategy Terms · Typebar | 分别关闭和开启标点完成练习，抽查题名大小写、多词及特殊条目，再开启数字并测试四档引语、混排、投稿与成绩 | 442 个 Typebar 原创竞技场术语保持 3–25 字符、229 个多词、61 个符号及 2 个数字结构；关闭标点时转小写，开启时保留题名大小写并合成标点；服务端接受 `arenaStrategy` | 待验收（规模、结构、最终词数、大小写、标点、数字、四档内容、混排和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-140 | Русский · Краткие формы | 分别选择基础与 1k 入口，完成普通、标点和数字练习，再测试四档引语、混排、投稿与成绩 | 基础 200 条、扩展 880 条且完整包含基础集；保持各自长度、多词、符号、数字、大小写及纯西里尔结构，使用 `ru`／`ru-RU`、禁用简化输入且 Zipf 不支持；服务端接受两个独立 ID | 待验收（双规模、包含关系、结构、生成、四档内容、混排和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-141 | தமிழ் · பழைய தொகுப்பு | 选择该独立入口完成普通、标点、数字和四档引语练习，再加入自选多语并提交社区内容与成绩 | 460 条 Typebar 原创 joining-script 项保持 3–13 Unicode 标量、唯一一条多词项和 Tamil 区段字符；使用 `ta`／`ta-IN`、可选简化输入且 Zipf 状态未知；服务端接受 `tamilOld` | 待验收（规模、结构、生成、四档内容、混排和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-142 | English 数字规模入口 | 分别选择 1k／5k／10k／25k／450k 完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 五个入口保持独立 ID，实际词数为 1,000／5,000／9,944／24,141／450,029；原创索引词流保持各自长度、大小写、符号、禁用简化输入和 Zipf 支持状态；普通出题只生成请求的词，服务端接受五个 ID | 待验收（身份、规模、结构、按需生成、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-143 | Español 数字规模入口 | 分别选择 1k／10k／650k 完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 三个入口保持独立 ID，实际词数为 998／9,990／646,579；原创索引词流保持各自长度、大小写、符号、多词和非 ASCII 结构，使用 `es`／`es-ES`、可选简化输入和 Zipf 未知状态；服务端接受三个 ID | 待验收（身份、规模、结构、按需生成、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-144 | Français 数字规模入口 | 分别选择 1k／2k／10k 完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 三个入口保持独立 ID，实际词数为 1,394／2,041／10,251；原创索引词流保持各自长度、大小写、标点、多词和非 ASCII 结构，使用 `fr`／`fr-FR`、可选简化输入和 Zipf 未知状态；服务端接受三个 ID | 待验收（身份、规模、结构、按需生成、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-145 | Deutsch 数字规模入口 | 分别选择 1k／10k／250k 完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 三个入口保持独立 ID，实际词数为 988／9,994／239,243；原创索引词流保持各自长度、名词大写、标点、多词和非 ASCII 结构，使用 `de`／`de-DE`、可选简化输入和 Zipf 未知状态；服务端接受三个 ID | 待验收（身份、规模、结构、按需生成、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-146 | Română 七档数字规模入口 | 分别选择 1k／5k／10k／25k／50k／100k／200k 完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 七个入口保持独立 ID，实际词数与标称值一致；原创索引词流保持各自长度、标点和非 ASCII 结构，使用 `ro`／`ro-RO`、可选简化输入和 Zipf 未知状态；服务端接受七个 ID | 待验收（身份、规模、结构、按需生成、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-147 | Polski 六档数字规模入口 | 分别选择 2k／5k／10k／20k／40k／200k 完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 六个入口保持独立 ID，实际词数为 2,338／5,000／10,000／20,000／40,000／199,979；原创索引词流保持各自长度、大小写、标点和非 ASCII 结构，使用 `pl`／`pl-PL`、可选简化输入和 Zipf 未知状态；服务端接受六个 ID | 待验收（身份、规模、结构、按需生成、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-148 | Беларуская 六档数字规模入口 | 分别选择 1k／5k／10k／25k／50k／100k 完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 六个入口保持独立 ID，实际词数为 997／5,044／10,725／24,133／52,817／106,381；原创西里尔索引词流保持各自长度、大小写、标点和全量非 ASCII 结构，使用 `be`／`be-BY`，1k 禁用简化输入而其余五档保留；服务端接受六个 ID | 待验收（身份、规模、结构、按需生成、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-149 | Русский 六档数字规模入口 | 分别选择 1k／5k／10k／25k／50k／375k 完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 六个入口保持独立 ID，实际词数为 996／4,971／9,996／26,037／51,682／376,092；原创西里尔索引词流保持各自长度、大小写、标点、多词和全量非 ASCII 结构，使用 `ru`／`ru-RU`，1k／5k 支持 Zipf 且只有 5k 禁用简化输入；服务端接受六个 ID | 待验收（身份、规模、结构、按需生成、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-150 | Português 五档数字规模入口 | 分别选择 1k／3k／5k／320k／550k 完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 五个入口保持独立 ID，实际词数为 1,000／3,043／5,665／318,601／558,207；原创索引词流保持各自长度、大小写、标点、多词、数字、符号和非 ASCII 结构，使用 `pt` 百科及逐档朗读标识，5k 与两档大型入口禁用简化输入；服务端接受五个 ID | 待验收（身份、规模、结构、按需生成、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-151 | Français 600k 数字规模入口 | 选择 600k 完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 入口保持独立 `french600k` ID，实际词数为 633,941；原创索引词流保持 1–33 字符、无大写、34 个标点项、无多词或数字项及 282,793 个非 ASCII 项，使用 `fr`／`fr-FR`、可选简化输入和 Zipf 未知状态；服务端接受该 ID | 待验收（身份、规模、结构、按需生成、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-152 | Arabic 两档数字规模入口 | 分别选择 العربية 10k 与 العربية المصرية 1k，完成普通、标点、数字和四档引语练习，再测试简化输入、预设、分享、归档、投稿与成绩 | 两个入口保持独立 `arabic10k`／`arabicEgypt1k` ID，实际词数为 9,281／1,141；原创索引词流保持固定长度、大小写、前导／内部／双空格和全量非 ASCII 结构，均按 RTL joining-script 显示并使用 `ar` 百科、`ar-SA`／`ar-EG` 朗读；仅 10k 自动应用基础 Arabic 的简化输入偏好；服务端接受两个 ID | 待验收（身份、规模、结构、RTL、连写、简化输入、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-153 | Korean 两档数字规模入口 | 分别选择 한국어 1k 与 5k，完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 两个入口保持独立 `korean1k`／`korean5k` ID，实际词数为 975／4,201；原创索引词流保持 1–5／1–6 字符和全量纯 Hangul，均使用普通空格词界、joining-script、`ko` 百科、`ko-KR` 朗读，并禁用简化输入与大写锁定提示；服务端接受两个 ID | 待验收（身份、规模、结构、连写、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-154 | Thai 六档数字规模入口 | 分别选择 ไทย 1k／5k／10k／20k／50k／60k，完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 六个入口保持独立 ID 和 1,000／5,000／10,000／18,737／50,000／60,000 项规模；原创索引词流逐档保持 Unicode 长度、组合标记、标点、空格、数字、非字母和 Thai 区段结构，均使用普通空格词界、`th` 百科、`th-TH` 朗读、禁用简化输入且不标记 joining-script；服务端接受六个 ID | 待验收（身份、规模、Unicode 结构、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-155 | Norsk bokmål 五档数字规模入口 | 分别选择 1k／5k／10k／150k／600k，完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 五个入口保持独立 ID 和 1,000／5,000／10,000／142,938／614,970 项规模；原创按需索引词流逐档保持长度、大小写、标点、数字、非字母和非 ASCII 结构，使用空格词界、`no` 百科、`nb-NO` 朗读和可选简化输入；前三档支持 Zipf，后两档未知；服务端接受五个 ID | 待验收（身份、规模、结构、按需生成、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-156 | Norsk nynorsk 五档数字规模入口 | 分别选择 1k／5k／10k／100k／400k，完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 五个入口保持独立 ID 和 1,000／5,000／9,939／104,745／410,719 项规模；原创按需索引词流逐档保持长度、大小写、标点、空格、数字、非字母和非 ASCII 结构，使用空格词界、`nn` 百科、`nn-NO` 朗读和可选简化输入；前三档支持 Zipf，后两档未知；服务端接受五个 ID | 待验收（身份、规模、结构、按需生成、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-157 | 简体中文四档数字规模入口 | 分别选择 1k／5k／10k／50k，完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 四个入口保持独立 ID 和 1,000／5,000／10,000／50,000 项规模；原创按需 CJK 索引词流逐档保持 2–5／2–6／2–7／2–9 字符及 Unicode 数字、标点、大写、非 ASCII 和纯 CJK 结构，使用无空格词界、`zh` 百科、`zh-CN` 朗读，禁用简化输入与大写锁定提示；服务端接受四个 ID | 待验收（身份、规模、结构、按需生成、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-158 | 繁體中文四档数字规模入口 | 分别选择 1k／5k／10k／50k，完成普通、标点、数字和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 四个入口保持独立 ID 和 1,000／4,991／9,974／49,925 项规模；原创按需繁体 CJK 索引词流逐档保持 2–5／2–6／2–7／1–9 字符及 Unicode 数字、标点、大写、非 ASCII、纯 CJK 和数字标点交叠结构，使用无空格词界、`zh` 百科、`zh-TW` 朗读，禁用简化输入与大写锁定提示并支持 Zipf；服务端接受四个 ID | 待验收（身份、规模、结构、按需生成、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-159 | नेपाली 1k 数字规模入口 | 选择「नेपाली · 1k · Typebar」，用 macOS Nepali 输入源完成普通、标点和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 入口保持独立 ID 和 1,000 项原创天城文内容，具有 1–7 字素、1–11 Unicode 标量与 927 个含组合标记条目；使用空格词界、连写保护、`ne` 百科、`ne-NP` 朗读，禁用简化输入与大写锁定提示，不进入多语混排；服务端接受该 ID | 待验收（身份、规模、Unicode 结构、按需生成、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-160 | Azərbaycanca 1k 数字规模入口 | 选择「Azərbaycanca · 1k · Typebar」，完成普通、标点和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 入口保持独立 ID 和 989 项原创内容，具有 2–7 字符与 668 个非 ASCII 条目；使用 LTR 空格词界、`az` 百科、`az-AZ` 朗读，保留简化输入与大写锁定提示，不进入多语混排；服务端接受该 ID | 待验收（身份、规模、Unicode 结构、按需生成、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-161 | Malagasy 1k 数字规模入口 | 选择「Malagasy · 1k · Typebar」，完成普通、标点和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 入口保持独立 ID 和 975 项原创内容，具有 3–23 字符及固定大写、标点、非 ASCII 和交叠数量；禁用简化输入，使用默认 `en` 百科、`en-US` 朗读且不进入多语混排；服务端接受该 ID | 待验收（身份、规模、结构、按需生成、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-162 | Bahasa Melayu 1k 数字规模入口 | 选择「Bahasa Melayu · 1k · Typebar」，完成普通、标点和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 入口保持独立 ID 和 1,000 项原创内容，具有 2–16 字符及固定大写、标点和内部空格数量；使用 `ms` 百科、`ms-MY` 朗读且不进入多语混排；服务端接受该 ID | 待验收（身份、规模、结构、按需生成、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-163 | Монгол 10k 数字规模入口 | 选择「Монгол · 10k · Typebar」，完成普通、标点和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 入口保持独立 ID 和 9,219 项原创内容，具有 1–18 字符、897 个大写项及全非 ASCII 字母结构；禁用简化输入，使用默认 `en` 百科、`en-US` 朗读且不进入多语混排；服务端接受该 ID | 待验收（身份、规模、结构、按需生成、输入元数据、四档内容和服务端数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-164 | Українська 1k 数字规模入口 | 选择「Українська · 1k · Typebar」，完成普通、标点和四档引语练习，再测试预设、分享、归档、投稿与成绩 | 独立 ID 保持 1,000 项、1–18 字符、15 个标点项及全非 ASCII 结构；服务端接受该 ID | 待验收（身份、聚合、按需生成、输入元数据、四档内容和数据面由自动化覆盖；依用户要求不启动 GUI） |
| TST-165 | Українська 10k 数字规模入口 | 选择「Українська · 10k · Typebar」并重复 TST-164 流程 | 独立 ID 保持 9,998 项、1–30 字符、210 个标点项及全非 ASCII 结构；服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-166 | Українська 50k 数字规模入口 | 选择「Українська · 50k · Typebar」并重复 TST-164 流程 | 独立 ID 保持 49,991 项、1–31 字符、10 个大写项、1,980 个标点项、2 个数字项与固定交叠；服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-167 | Українська (Latin) 1k 数字规模入口 | 选择「Українська (Latin) · 1k · Typebar」并重复配置、分享、引语、投稿与成绩流程 | 独立 ID 保持 1,000 项、1–18 字符、16 个标点项与 288 个非 ASCII 项，并阻止百科原文破坏 Latin 书写练习；服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-168 | Українська (Latin) 10k 数字规模入口 | 选择「Українська (Latin) · 10k · Typebar」并重复 TST-167 流程 | 独立 ID 保持 9,998 项、1–30 字符、214 个标点项、3,214 个非 ASCII 项与固定交叠；服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-169 | Українська (Latin) 50k 数字规模入口 | 选择「Українська (Latin) · 50k · Typebar」并重复 TST-167 流程 | 独立 ID 保持 49,991 项、1–31 字符、10 个大写项、2,005 个标点项、2 个数字项、18,670 个非 ASCII 项与固定交叠；服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-170 | Bahasa Indonesia 1k 数字规模入口 | 选择「Bahasa Indonesia · 1k · Typebar」，完成普通、标点、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,020 项、2–14 字符、2 个大写项和 2 个标点项；按固定异常使用 `hu` 百科与 `hu-HU` 朗读，服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-171 | Bahasa Indonesia 10k 数字规模入口 | 选择「Bahasa Indonesia · 10k · Typebar」并重复 TST-170 流程 | 独立 ID 保持 13,769 项、2–13 字符和 494 个标点项；使用 `id` 百科与 `id-ID` 朗读，服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-172 | کوردی ناوەندی 2k 数字规模入口 | 选择「کوردی ناوەندی · 2k · Typebar」，用 Central Kurdish 输入源完成普通、标点、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,486 项、1–11 字符、4 个内部空格项、RTL 连写、`ckb` 百科与朗读；服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-173 | کوردی ناوەندی 4k 数字规模入口 | 选择「کوردی ناوەندی · 4k · Typebar」并重复 TST-172 流程 | 独立 ID 保持 4,256 项、1–11 字符、3 个内部空格项、RTL 连写、`ckb` 百科与朗读；服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-174 | Swiss German 1k 数字规模入口 | 选择「Swiss German · 1k · Typebar」，完成普通、自定义、四档引语、预设、分享、社区投稿与成绩流程 | 独立 ID 保持 1,000 项、2–16 字符及固定大小写、标点、空格与非 ASCII 聚合；全部可见 `ß` 转换为 `ss`，使用 `de`／`de-CH`，投稿被拒绝但成绩与排行榜被接受 | 待验收（自动化覆盖并保持旧基础档引语 ID；依用户要求不启动 GUI） |
| TST-175 | Swiss German 2k 数字规模入口 | 选择「Swiss German · 2k · Typebar」并重复 TST-174 流程 | 独立 ID 保持 2,000 项、2–29 字符及固定大小写、标点、数字、非 ASCII 与交叠聚合；可见转换和数据面边界与 1k 一致 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-176 | Afrikaans 1k 数字规模入口 | 选择「Afrikaans · 1k · Typebar」，完成普通、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,000 项、2–19 字符及固定大小写、标点、非 ASCII 与交叠聚合；禁用简化输入，使用 `af`／`af-ZA`，服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-177 | Afrikaans 10k 数字规模入口 | 选择「Afrikaans · 10k · Typebar」并重复 TST-176 流程 | 独立 ID 保持 8,164 项、2–22 字符及固定大小写、标点、非 ASCII 与交叠聚合；保留简化输入，服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-178 | Italiano 1k 数字规模入口 | 选择「Italiano · 1k · Typebar」，完成普通、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,159 项、1–14 字符、1 个大写项和 34 个非 ASCII 项；使用 `it`／`it-IT`，服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-179 | Italiano 7k 数字规模入口 | 选择「Italiano · 7k · Typebar」并重复 TST-178 流程 | 独立 ID 保持 7,154 项、1–18 字符、2 个大写项和 136 个非 ASCII 项；不进入多语混排 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-180 | Italiano 60k 数字规模入口 | 选择「Italiano · 60k · Typebar」并重复 TST-178 流程 | 独立 ID 保持 60,442 项、1–18 字符、38 个标点项与 2 个独立符号项，无非 ASCII 或数字项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-181 | Italiano 280k 数字规模入口 | 选择「Italiano · 280k · Typebar」并重复 TST-178 流程 | 独立 ID 保持 279,833 个小写 ASCII 单 token 和 1–25 字符；普通出题不物化整表，服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-182 | Esperanto 1k 数字规模入口 | 选择「Esperanto · 1k · Typebar」，完成普通、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,000 项、1–15 字符和 137 个非 ASCII 项；保留简化输入并支持 Zipf | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-183 | Esperanto 10k 数字规模入口 | 选择「Esperanto · 10k · Typebar」并重复 TST-182 流程 | 独立 ID 保持 10,000 项、1–17 字符和 1,560 个非 ASCII 项；服务端接受该 ID | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-184 | Esperanto 25k 数字规模入口 | 选择「Esperanto · 25k · Typebar」并重复 TST-182 流程 | 独立 ID 保持 24,998 项、1–17 字符和 4,528 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-185 | Esperanto 36k 数字规模入口 | 选择「Esperanto · 36k · Typebar」并重复 TST-182 流程 | 独立 ID 保持 36,342 项、1–40 字符和 6,843 个非 ASCII 项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-186 | Esperanto X-sistemo 1k 数字规模入口 | 选择「Esperanto · X-sistemo · 1k · Typebar」，完成 TST-182 全流程 | 独立 ID 保持 999 项、1–15 字符和 4 个非 ASCII 项；禁用简化输入并支持 Zipf | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-187 | Esperanto X-sistemo 10k 数字规模入口 | 选择「Esperanto · X-sistemo · 10k · Typebar」并重复 TST-186 流程 | 独立 ID 保持 9,993 项、1–17 字符和 38 个非 ASCII 项；Zipf 保持未知提示 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-188 | Esperanto X-sistemo 25k 数字规模入口 | 选择「Esperanto · X-sistemo · 25k · Typebar」并重复 TST-186 流程 | 独立 ID 保持 24,970 项、1–18 字符和 213 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-189 | Esperanto X-sistemo 36k 数字规模入口 | 选择「Esperanto · X-sistemo · 36k · Typebar」并重复 TST-186 流程 | 独立 ID 保持 36,296 项、1–40 字符和 277 个非 ASCII 项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-190 | Esperanto H-sistemo 1k 数字规模入口 | 选择「Esperanto · H-sistemo · 1k · Typebar」，完成 TST-182 全流程 | 独立 ID 保持 999 项、1–15 字符和 4 个非 ASCII 项；禁用简化输入并支持 Zipf | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-191 | Esperanto H-sistemo 10k 数字规模入口 | 选择「Esperanto · H-sistemo · 10k · Typebar」并重复 TST-190 流程 | 独立 ID 保持 9,969 项、1–17 字符和 38 个非 ASCII 项；支持 Zipf | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-192 | Esperanto H-sistemo 25k 数字规模入口 | 选择「Esperanto · H-sistemo · 25k · Typebar」并重复 TST-190 流程 | 独立 ID 保持 24,922 项、1–18 字符和 213 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-193 | Esperanto H-sistemo 36k 数字规模入口 | 选择「Esperanto · H-sistemo · 36k · Typebar」并重复 TST-190 流程 | 独立 ID 保持 36,131 项、1–40 字符和 277 个非 ASCII 项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-194 | Ελληνικά 1k 数字规模入口 | 选择「Ελληνικά · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 993 个全非 ASCII 小写词项和 1–16 字符；使用 `el`／`el-GR`、简化输入和 Zipf 未知语义 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-195 | Ελληνικά 5k 数字规模入口 | 选择「Ελληνικά · 5k · Typebar」并重复 TST-194 流程 | 独立 ID 保持 4,963 个全非 ASCII 小写词项和 1–20 字符 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-196 | Ελληνικά 10k 数字规模入口 | 选择「Ελληνικά · 10k · Typebar」并重复 TST-194 流程 | 独立 ID 保持 9,936 个全非 ASCII 小写词项和 1–21 字符 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-197 | Ελληνικά 25k 数字规模入口 | 选择「Ελληνικά · 25k · Typebar」并重复 TST-194 流程 | 独立 ID 保持 24,836 个全非 ASCII 小写词项和 1–21 字符；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-198 | Greeklish 1k 数字规模入口 | 选择「Greeklish · 1k · Typebar」，完成普通、四档引语、预设、分享、投稿与成绩流程，并尝试启用知识短文 | 独立 ID 保持 990 项、1–16 字符和 12 个非 ASCII 异常项；禁用简化输入与百科替换，并明确不支持 Zipf | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-199 | Greeklish 5k 数字规模入口 | 选择「Greeklish · 5k · Typebar」并重复 TST-198 流程 | 独立 ID 保持 4,926 项、1–20 字符和 26 个非 ASCII 异常项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-200 | Greeklish 10k 数字规模入口 | 选择「Greeklish · 10k · Typebar」并重复 TST-198 流程 | 独立 ID 保持 9,808 项、1–21 字符和 49 个非 ASCII 异常项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-201 | Greeklish 25k 数字规模入口 | 选择「Greeklish · 25k · Typebar」并重复 TST-198 流程 | 独立 ID 保持 24,273 项、1–21 字符和 116 个非 ASCII 异常项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-202 | Qırımtatarca 1k 数字规模入口 | 选择「Qırımtatarca · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,000 项、1–17 字符和 526 个非 ASCII 项；使用 `crh`／`crh-CRH`，禁用简化输入 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-203 | Qırımtatarca 5k 数字规模入口 | 选择「Qırımtatarca · 5k · Typebar」并重复 TST-202 流程 | 独立 ID 保持 5,000 项、1–17 字符、2 个标点项和 2,752 个非 ASCII 项，标点项均与非 ASCII 交叠 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-204 | Qırımtatarca 10k 数字规模入口 | 选择「Qırımtatarca · 10k · Typebar」并重复 TST-202 流程 | 独立 ID 保持 10,000 项、1–17 字符、2 个标点项和 5,560 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-205 | Qırımtatarca 15k 数字规模入口 | 选择「Qırımtatarca · 15k · Typebar」并重复 TST-202 流程 | 独立 ID 保持 15,082 项、1–18 字符、1 个非 ASCII 大写项、4 个标点项和 8,400 个非 ASCII 项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-206 | Къырымтатарджа 1k 数字规模入口 | 选择「Къырымтатарджа · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,000 个全非 ASCII 项和 1–18 字符；使用 `crh`／`crh-CRH`，禁用简化输入 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-207 | Къырымтатарджа 5k 数字规模入口 | 选择「Къырымтатарджа · 5k · Typebar」并重复 TST-206 流程 | 独立 ID 保持 5,000 个全非 ASCII 项、1–19 字符和 2 个标点交叠项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-208 | Къырымтатарджа 10k 数字规模入口 | 选择「Къырымтатарджа · 10k · Typebar」并重复 TST-206 流程 | 独立 ID 保持 10,000 个全非 ASCII 项、1–20 字符和 2 个标点交叠项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-209 | Къырымтатарджа 15k 数字规模入口 | 选择「Къырымтатарджа · 15k · Typebar」并重复 TST-206 流程 | 独立 ID 保持 15,082 个全非 ASCII 项、1–20 字符、1 个大写项和 4 个标点交叠项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-210 | Occitan 1k 数字规模入口 | 选择「Occitan · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,000 项、1–14 字符和 217 个非 ASCII 项；使用 `oc`／`oc-FR`、简化输入和 Zipf 未知语义 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-211 | Occitan 2k 数字规模入口 | 选择「Occitan · 2k · Typebar」并重复 TST-210 流程 | 独立 ID 保持 2,000 项、1–16 字符和 499 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-212 | Occitan 5k 数字规模入口 | 选择「Occitan · 5k · Typebar」并重复 TST-210 流程 | 独立 ID 保持 5,000 项、1–17 字符和 1,205 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-213 | Occitan 10k 数字规模入口 | 选择「Occitan · 10k · Typebar」并重复 TST-210 流程 | 独立 ID 保持 10,000 项、1–23 字符和 2,387 个非 ASCII 项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-214 | Taqbaylit 1k 数字规模入口 | 选择「Taqbaylit · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,000 项、3–14 字符、139 个标点项、321 个非 ASCII 项及 35 个交叠项；使用 `kab`、简化输入且不支持 Zipf | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-215 | Taqbaylit 2k 数字规模入口 | 选择「Taqbaylit · 2k · Typebar」并重复 TST-214 流程 | 独立 ID 保持 2,000 项、3–14 字符、299 个标点项、670 个非 ASCII 项及 62 个交叠项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-216 | Taqbaylit 5k 数字规模入口 | 选择「Taqbaylit · 5k · Typebar」并重复 TST-214 流程 | 独立 ID 保持 5,000 项、3–17 字符、736 个标点项、1,665 个非 ASCII 项及 179 个交叠项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-217 | Taqbaylit 10k 数字规模入口 | 选择「Taqbaylit · 10k · Typebar」并重复 TST-214 流程 | 独立 ID 保持 10,000 项、3–22 字符、1,420 个标点项、3,319 个非 ASCII 项及 345 个交叠项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-218 | Hebrew 1k 数字规模入口 | 选择「עברית · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,000 个全非 ASCII 项及 2–10 字符；使用 RTL、连写、`he`／`he-IL`，并按该规模档固定元数据保留简化输入 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-219 | Hebrew 5k 数字规模入口 | 选择「עברית · 5k · Typebar」并重复 TST-218 流程 | 独立 ID 保持 5,000 个全非 ASCII 项及 2–13 字符；不进入多语混排 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-220 | Hebrew 10k 数字规模入口 | 选择「עברית · 10k · Typebar」并重复 TST-218 流程 | 独立 ID 保持 10,000 个全非 ASCII 项及 2–13 字符；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-221 | Persian 1k 数字规模入口 | 选择「فارسی · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,000 个全非 ASCII 项及 2–10 字符；使用 RTL、连写、`fa`／`fa-IR` 并禁用简化输入 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-222 | Persian 5k 数字规模入口 | 选择「فارسی · 5k · Typebar」并重复 TST-221 流程 | 独立 ID 保持 5,000 个全非 ASCII 项及 2–12 字符；不进入多语混排 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-223 | Persian 20k 数字规模入口 | 选择「فارسی · 20k · Typebar」并重复 TST-221 流程 | 独立 ID 保持 21,715 个全非 ASCII 项、2–82 字符、12 个标点项、27 个空白项、2 个组合标记项及 24 个格式字符项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-224 | Urdu 1k 数字规模入口 | 选择「اردو · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 934 个全非 ASCII 项、2–17 字符、50 个多词项、1 个标点项及 1 个组合标记项；使用 RTL、连写、`ur`／`ur-PK` 并禁用简化输入 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-225 | Urdu 5k 数字规模入口 | 选择「اردو · 5k · Typebar」并重复 TST-224 流程 | 独立 ID 保持 4,981 个全非 ASCII 项、1–15 字符和 72 个多词项；普通出题不物化整表且不进入多语混排 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-226 | Tamil 1k 数字规模入口 | 选择「தமிழ் · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 951 项、1–19 标量、1–11 字素及 942 个组合标记项；使用连写、`ta`／`ta-IN`、Zipf 支持并禁用简化输入 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-227 | Telugu 1k 数字规模入口 | 选择「తెలుగు · 1k · Typebar」并重复 TST-226 流程 | 独立 ID 保持 901 项、1–18 标量、1–11 字素、879 个组合标记项和 5 个多词项；使用 `te`／`te-IN` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-228 | Bangla 10k 数字规模入口 | 选择「বাংলা · 10k · Typebar」并重复 TST-226 流程 | 独立 ID 保持 9,734 项、1–16 标量、1–10 字素、9,392 个组合标记项和 15 个标点项；使用 `bn`／`bn-BD` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-229 | Hindi 1k 数字规模入口 | 选择「हिन्दी · 1k · Typebar」并重复 TST-226 流程 | 独立 ID 保持 999 项、1–12 标量、1–7 字素及 956 个组合标记项；使用 `hi`／`hi-IN` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-230 | Gujarati 1k 数字规模入口 | 选择「ગુજરાતી · 1k · Typebar」并重复 TST-226 流程 | 独立 ID 保持 1,004 项、1–19 标量、1–12 字素、900 个组合标记项和 33 个多词项；使用 `gu`／`gu-IN`，普通出题不物化整表且不进入多语混排 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-231 | Turkish 1k 数字规模入口 | 选择「Türkçe · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,026 项、1–13 字符、462 个非 ASCII 项和 1 个多词项；使用 `tr`／`tr-TR` 并保留简化输入 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-232 | Turkish 5k 数字规模入口 | 选择「Türkçe · 5k · Typebar」并重复 TST-231 流程 | 独立 ID 保持 5,016 项、1–17 字符、1 个大写项、36 个多词项及 2,155 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-233 | Kazakh 1k 数字规模入口 | 选择「Қазақша · 1k · Typebar」并重复 TST-231 流程 | 独立 ID 保持 990 个全非 ASCII 项、2–16 字符、4 个标点项和 3 个多词项；按固定缺省使用 `en`／`en-US` 并禁用简化输入 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-234 | Kyrgyz 1k 数字规模入口 | 选择「Кыргызча · 1k · Typebar」并重复 TST-231 流程 | 独立 ID 保持 849 个全非 ASCII 项、2–13 字符、2 个大写项、11 个标点项和 10 个多词项；使用 `ky`／`ky-KY` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-235 | Tatar 1k 数字规模入口 | 选择「Татарча · 1k · Typebar」并重复 TST-231 流程 | 独立 ID 保持 1,001 个全非 ASCII 项、1–15 字符和 124 个大写项；使用 `tt` 并保持 Zipf 支持 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-236 | Tatar 5k 数字规模入口 | 选择「Татарча · 5k · Typebar」并重复 TST-235 流程 | 独立 ID 保持 5,004 个全非 ASCII 项、1–17 字符和 785 个大写项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-237 | Tatar 9k 数字规模入口 | 选择「Татарча · 9k · Typebar」并重复 TST-235 流程 | 独立 ID 保持 9,034 个全非 ASCII 项、1–19 字符和 1,542 个大写项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-238 | Uzbek 1k 数字规模入口 | 选择「Oʻzbekcha · 1k · Typebar」并重复 TST-231 流程 | 独立 ID 保持 821 项、2–18 字符、6 个大写项、3 个标点项、1 个多词项和 135 个非 ASCII 项；使用 `uz`／`uz-UZ` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-239 | Uzbek 70k 数字规模入口 | 选择「Oʻzbekcha · 70k · Typebar」并重复 TST-238 流程 | 独立 ID 保持 76,595 项、1–25 字符、4 个标点项、44 个多词项和 11,030 个非 ASCII 项；普通出题不物化整表且不进入多语混排 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-240 | Czech 1k 数字规模入口 | 选择「Čeština · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 899 项、1–15 字符、16 个大写项、2 个标点项与 532 个非 ASCII 项；使用 `cs`／`cs-CZ` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-241 | Czech 10k 数字规模入口 | 选择「Čeština · 10k · Typebar」并重复 TST-240 流程 | 独立 ID 保持 9,629 项、1–19 字符、418 个大写项、5 个标点项与 6,245 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-242 | Slovak 1k 数字规模入口 | 选择「Slovenčina · 1k · Typebar」并重复 TST-240 流程 | 独立 ID 保持 1,001 项、1–13 字符与 524 个非 ASCII 项；使用 `sk`／`sk-SK` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-243 | Slovak 10k 数字规模入口 | 选择「Slovenčina · 10k · Typebar」并重复 TST-242 流程 | 独立 ID 保持 9,944 项、1–15 字符、41 个大写项与 6,046 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-244 | Slovenian 1k 数字规模入口 | 选择「Slovenščina · 1k · Typebar」并重复 TST-240 流程 | 独立 ID 保持 1,023 项、1–13 字符与 227 个非 ASCII 项；使用 `sl`／`sl-SI` 并保持 Zipf 支持 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-245 | Slovenian 5k 数字规模入口 | 选择「Slovenščina · 5k · Typebar」并重复 TST-244 流程 | 独立 ID 保持 4,971 项、1–17 字符、2 个标点项与 1,213 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-246 | Croatian 1k 数字规模入口 | 选择「Hrvatski · 1k · Typebar」并重复 TST-240 流程 | 独立 ID 保持 1,108 项、1–14 字符、9 个多词项与 226 个非 ASCII 项；使用 `hr`／`hr-HR` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-247 | Dutch 1k 数字规模入口 | 选择「Nederlands · 1k · Typebar」并重复 TST-240 流程 | 独立 ID 保持 1,000 项、1–15 字符与 1 个非 ASCII 项；使用 `nl`／`nl-NL` 并禁用简化输入 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-248 | Dutch 10k 数字规模入口 | 选择「Nederlands · 10k · Typebar」并重复 TST-247 流程 | 独立 ID 保持 9,998 项、1–30 字符、131 个大写项、181 个标点项、62 个多词项、80 个非 ASCII 项与 1 个数字项；普通出题不物化整表且不进入多语混排 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-249 | Danish 1k 数字规模入口 | 选择「Dansk · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 954 项、1–14 字符、2 个大写项与 181 个非 ASCII 项；使用 `da`／`da-DK`，不复制受限授权词值 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-250 | Danish 10k 数字规模入口 | 选择「Dansk · 10k · Typebar」并重复 TST-249 流程 | 独立 ID 保持 9,624 项、1–24 字符、31 个大写项、2,159 个非 ASCII 项与 9 个数字项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-251 | Swedish 1k 数字规模入口 | 选择「Svenska · 1k · Typebar」并重复 TST-249 流程 | 独立 ID 保持 994 项、1–12 字符与 332 个非 ASCII 项；使用 `sv`／`sv-SE` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-252 | Finnish 1k 数字规模入口 | 选择「Suomi · 1k · Typebar」并重复 TST-249 流程 | 独立 ID 保持 1,000 项、2–14 字符与 240 个非 ASCII 项；使用 `fi`／`fi-FI` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-253 | Finnish 10k 数字规模入口 | 选择「Suomi · 10k · Typebar」并重复 TST-252 流程 | 独立 ID 保持 9,906 项、2–15 字符与 2,453 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-254 | Estonian 1k 数字规模入口 | 选择「Eesti · 1k · Typebar」并重复 TST-249 流程 | 独立 ID 保持 1,000 项、2–15 字符、1 个标点项与 233 个非 ASCII 项；使用 `et`／`et-EE` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-255 | Estonian 5k 数字规模入口 | 选择「Eesti · 5k · Typebar」并重复 TST-254 流程 | 独立 ID 保持 5,000 项、2–23 字符、5 个标点项与 1,235 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-256 | Estonian 10k 数字规模入口 | 选择「Eesti · 10k · Typebar」并重复 TST-254 流程 | 独立 ID 保持 10,000 项、2–23 字符、13 个标点项与 2,471 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-257 | Icelandic 1k 数字规模入口 | 选择「Íslenska · 1k · Typebar」并重复 TST-249 流程 | 独立 ID 保持 1,000 项、1–12 字符与 462 个非 ASCII 项；按固定缺省使用 `en`／`en-US`，不继承基础 Icelandic 的区域路径 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-258 | Irish 1k 数字规模入口 | 选择「Gaeilge · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,000 项、1–13 字符、397 个非 ASCII 项与 1 个组合标记项；使用 `ga`／`ga-IE` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-259 | Filipino 1k 数字规模入口 | 选择「Filipino · 1k · Typebar」并重复 TST-258 流程 | 独立 ID 保持 1,000 个 ASCII 项与 1–11 字符；禁用简化输入，按固定缺省使用 `en`／`en-US` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-260 | Hungarian 1k 数字规模入口 | 选择「Magyar · 1k · Typebar」并重复 TST-258 流程 | 独立 ID 保持 1,000 项、1–14 字符、7 个大写项、1 个标点项与 524 个非 ASCII 项；使用 `hu`／`hu-HU` 并明确不支持 Zipf | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-261 | Hungarian 2k 数字规模入口 | 选择「Magyar · 2k · Typebar」并重复 TST-260 流程 | 独立 ID 保持 2,452 项、1–21 字符、7 个大写项、1 个标点项与 1,479 个非 ASCII 项；Zipf 保持未知 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-262 | Welsh 1k 数字规模入口 | 选择「Cymraeg · 1k · Typebar」并重复 TST-258 流程 | 独立 ID 保持 1,000 项、1–14 字符、13 个大写项、46 个标点项与 15 个非 ASCII 项；按固定缺省使用 `en`／`en-US` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-263 | Lithuanian 1k 数字规模入口 | 选择「Lietuvių · 1k · Typebar」并重复 TST-258 流程 | 独立 ID 保持 990 项、1–13 字符与 292 个非 ASCII 项；按固定缺省使用 `en`／`en-US` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-264 | Lithuanian 3k 数字规模入口 | 选择「Lietuvių · 3k · Typebar」并重复 TST-263 流程 | 独立 ID 保持 2,978 项、1–17 字符与 982 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-265 | Latvian 1k 数字规模入口 | 选择「Latviešu · 1k · Typebar」并重复 TST-258 流程 | 独立 ID 保持 930 项、2–18 字符、3 个大写项、1 个标点项、6 个多词项与 463 个非 ASCII 项；使用 `lv`／`lv` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-266 | Maltese 1k 数字规模入口 | 选择「Malti · 1k · Typebar」并重复 TST-258 流程 | 独立 ID 保持 927 项、1–15 字符、5 个大写项、8 个标点项、7 个多词项与 288 个非 ASCII 项；使用 `mt`／`mt` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-267 | Vietnamese 1k 数字规模入口 | 选择「Tiếng Việt · 1k · Typebar」，完成普通、百科、四档引语、预设、分享、投稿与成绩流程 | 独立 ID 保持 1,000 项、1–7 字符与 870 个非 ASCII 项；按固定缺省使用 `en`／`en-US` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-268 | Vietnamese 5k 数字规模入口 | 选择「Tiếng Việt · 5k · Typebar」并重复 TST-267 流程 | 独立 ID 保持 5,000 项、1–7 字符与 4,450 个非 ASCII 项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-269 | Pinyin 1k 数字规模入口 | 选择「Pinyin · 1k · Typebar」并重复 TST-267 流程 | 独立 ID 保持 612 项、1–6 字符与 601 个非 ASCII 项；按固定缺省使用 `en`／`en-US` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-270 | Pinyin 10k 数字规模入口 | 选择「Pinyin · 10k · Typebar」并重复 TST-269 流程 | 独立 ID 保持 1,293 项、1–6 字符与 1,223 个非 ASCII 项 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-271 | Hausa 1k 数字规模入口 | 选择「Hausa · 1k · Typebar」并重复 TST-267 流程 | 独立 ID 保持 829 项、1–19 字符、17 个标点项、138 个多词项与 44 个非 ASCII 项；使用 `ha`／`ha` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-272 | Bemba 1k 数字规模入口 | 选择「Ichibemba · 1k · Typebar」并重复 TST-267 流程 | 独立 ID 保持 1,001 个 ASCII 项与 1–15 字符；使用 `bem`／`bem` 并明确不支持 Zipf | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-273 | Bemba 10k 数字规模入口 | 选择「Ichibemba · 10k · Typebar」并重复 TST-272 流程 | 独立 ID 保持 10,001 项、1–19 字符与 2 个非 ASCII 项；普通出题不物化整表 | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-274 | Catalan 1k 数字规模入口 | 选择「Català · 1k · Typebar」并重复 TST-267 流程 | 独立 ID 保持 1,000 项、1–16 字符、60 个大写项、8 个标点项与 182 个非 ASCII 项；按固定缺省使用 `en`／`en-US` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-275 | Frisian 1k 数字规模入口 | 选择「Frysk · 1k · Typebar」并重复 TST-267 流程 | 独立 ID 保持 909 项、2–13 字符、2 个标点项与 103 个非 ASCII 项；使用 `fy`／`fy-FY` | 待验收（自动化覆盖；依用户要求不启动 GUI） |
| TST-34 | Greeklish 离线练习 | 在语言菜单选择「Greeklish」，分别开始字数与引语练习；用普通 ASCII 键盘输入 `kalimera charti`，再启用“知识短文”。 | 词流和四档引语均为 Typebar 原创 ASCII 内容、以空格提交；知识短文不会请求或应用希腊字母原文，保持 Latin 提示。混排、错词复练、结果、服务端投稿/排行榜和系统 `el-GR` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-35 | Norwegian Nynorsk 离线与百科练习 | 在语言菜单选择「Norsk nynorsk」，分别开始字数、引语和“知识短文”练习；用 macOS 挪威语输入源输入 `vindauge`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `nn` 页面。Nynorsk 拼写、错词复练和系统 `nn-NO` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-36 | Afrikaans 离线与百科练习 | 在语言菜单选择「Afrikaans」，分别开始字数、引语和“知识短文”练习；用 macOS 输入源输入 `môre reën`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `af` 页面。组合变音输入、错词复练和系统 `af-ZA` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-37 | Filipino 离线与百科练习 | 在语言菜单选择「Filipino」，分别开始字数、引语和“知识短文”练习；输入 `umaga pagsasanay`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `tl` 页面。错词复练和系统 `fil-PH` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-38 | Catalan 离线与百科练习 | 在语言菜单选择「Català」，分别开始字数、引语和“知识短文”练习；用 macOS 加泰罗尼亚语输入源输入 `matí paciència`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `ca` 页面。组合重音输入、错词复练和系统 `ca-ES` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-39 | Indonesian 离线与百科练习 | 在语言菜单选择「Bahasa Indonesia」，分别开始字数、引语和“知识短文”练习；输入 `pagi kesabaran`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `id` 页面。错词复练和系统 `id-ID` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-40 | Malay 离线与百科练习 | 在语言菜单选择「Bahasa Melayu」，分别开始字数、引语和“知识短文”练习；输入 `pagi tingkap`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `ms` 页面。错词复练和系统 `ms-MY` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-41 | Arabic RTL 离线与百科练习 | 在语言菜单选择「العربية」，确认“Arabic 快速输入”默认开启，分别开始字数、引语和“知识短文”练习；用 macOS Arabic 输入源输入 `نافذة طريق`，关闭快速输入并重开，再切换卷带和独立光标样式。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；快速输入开启时词流中的短元音、tanwin、shadda、sukun 与常见 alef 变体被归一，关闭后保留完整提示。提示右对齐并按 RTL 呈现，卷带和独立 TextKit 光标覆盖安全关闭，当前字形状态仍正确显示。知识短文仅在明确启用时请求 `ar` 页面，系统朗读为 `ar-SA`。Arabic 不出现在默认/自选多语混排。 | 待验收（离线内容、快速输入归一化与持久化、RTL 策略、`ar` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-42 | Hebrew RTL 离线与百科练习 | 在语言菜单选择「עברית」，分别开始字数、引语和“知识短文”练习；用 macOS Hebrew 输入源输入 `חלון דרך`，再切换卷带和独立光标样式。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；提示右对齐并由 macOS 完成 RTL 双向排版，卷带和独立 TextKit 光标覆盖安全关闭，当前字形状态仍正确显示。知识短文仅在明确启用时请求 `he` 页面，系统朗读为 `he-IL`。Hebrew 不出现在默认/自选多语混排，也不应用简化重音输入。 | 待验收（离线内容、完整输入会话、RTL 策略、`he` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-43 | Persian RTL 离线与百科练习 | 在语言菜单选择「فارسی」，分别开始字数、引语和“知识短文”练习；用 macOS Persian 输入源输入 `پنجره راه`，再切换卷带和独立光标样式。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；提示右对齐并由 macOS 完成 RTL 双向排版，卷带和独立 TextKit 光标覆盖安全关闭，当前字形状态仍正确显示。知识短文仅在明确启用时请求 `fa` 页面，系统朗读为 `fa-IR`。Persian 不出现在默认/自选多语混排，也不应用简化重音输入。 | 待验收（离线内容、完整输入会话、RTL 策略、`fa` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-44 | Urdu RTL 离线与百科练习 | 在语言菜单选择「اردو」，分别开始字数、引语和“知识短文”练习；用 macOS Urdu 输入源输入 `کھڑکی راستہ`，再切换卷带和独立光标样式。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；提示右对齐并由 macOS 完成 RTL 双向排版，卷带和独立 TextKit 光标覆盖安全关闭，当前字形状态仍正确显示。知识短文仅在明确启用时请求 `ur` 页面，系统朗读为 `ur-PK`。Urdu 不出现在默认/自选多语混排，也不应用简化重音输入。 | 待验收（离线内容、完整输入会话、RTL 策略、`ur` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-45 | Tamil 离线、组合输入与百科练习 | 在语言菜单选择「தமிழ்」，分别开始字数、引语和“知识短文”练习；用 macOS Tamil 输入源输入 `சாளரம் பாதை`，再以 Tamil 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文仅在明确启用时请求 `ta` 页面，系统朗读为 `ta-IN`；Tamil 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`ta` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-46 | Hindi 离线、组合输入与百科练习 | 在语言菜单选择「हिन्दी」，分别开始字数、引语和“知识短文”练习；用 macOS Hindi 输入源输入 `खिड़की रास्ता`，再以 Hindi 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文仅在明确启用时请求 `hi` 页面，系统朗读为 `hi-IN`；Hindi 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`hi` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-47 | Gujarati 离线、组合输入与百科练习 | 在语言菜单选择「ગુજરાતી」，分别开始字数、引语和“知识短文”练习；用 macOS Gujarati 输入源输入 `બારી પતંગ`，再以 Gujarati 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文仅在明确启用时请求 `gu` 页面，系统朗读为 `gu-IN`；Gujarati 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`gu` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-48 | Bangla 离线、组合输入与百科练习 | 在语言菜单选择「বাংলা」，分别开始字数、引语和“知识短文”练习；用 macOS Bangla 输入源输入 `জানালা ঘুড়ি`，再以 Bangla 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文仅在明确启用时请求 `bn` 页面，系统朗读为 `bn-BD`；Bangla 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`bn` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-49 | Thai 离线、词界与百科练习 | 在语言菜单选择「ไทย」，分别开始字数、引语和“知识短文”练习；用 macOS Thai 输入源输入 `หน้าต่าง ว่าว`，再以 Thai 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容。参考实际生成器在非 `nospace` 修饰器下使用空格提交，故普通 LTR 提示也以空格提交；知识短文仅在明确启用时请求 `th` 页面，系统朗读为 `th-TH`。Thai 可以参与默认/自选多语混排，且不应用简化重音输入。 | 待验收（离线内容、完整输入会话、空格词界、多语轮转、`th` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-50 | Nepali 离线、组合输入与百科练习 | 在语言菜单选择「नेपाली」，分别开始字数、引语和“知识短文”练习；用 macOS Nepali 输入源输入 `झ्याल बाटो`，再以 Nepali 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文仅在明确启用时请求 `ne` 页面，系统朗读为 `ne-NP`；Nepali 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`ne` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-51 | Kannada 离线、组合输入与百科练习 | 在语言菜单选择「ಕನ್ನಡ」，分别开始字数、引语和“知识短文”练习；用 macOS Kannada 输入源输入 `ಕಿಟಕಿ ರಸ್ತೆ`，再以 Kannada 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文仅在明确启用时请求 `kn` 页面，系统朗读为 `kn-IN`；Kannada 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`kn` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-52 | Telugu 离线、组合输入与百科练习 | 在语言菜单选择「తెలుగు」，分别开始字数、引语和“知识短文”练习；用 macOS Telugu 输入源输入 `కిటికీ దారి`，再以 Telugu 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文仅在明确启用时请求 `te` 页面，系统朗读为 `te-IN`；Telugu 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`te` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-53 | Malayalam 离线、组合输入与百科练习 | 在语言菜单选择「മലയാളം」，分别开始字数、引语和“知识短文”练习；用 macOS Malayalam 输入源输入 `ജാലകം വഴി`，再以 Malayalam 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文仅在明确启用时请求 `ml` 页面，系统朗读为 `ml-IN`；Malayalam 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`ml` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-54 | Sanskrit 离线、组合输入与百科练习 | 在语言菜单选择「संस्कृतम्」，分别开始字数、引语和“知识短文”练习；用 macOS Sanskrit 输入源输入 `वातायनम् मार्गः`，再以 Sanskrit 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文与系统朗读均精确使用参考配置的 `sa`，不补造地区后缀；Sanskrit 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`sa` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-55 | Sinhala 离线、组合输入与百科练习 | 在语言菜单选择「සිංහල」，分别开始字数、引语和“知识短文”练习；用 macOS Sinhala 输入源输入 `කවුළුව මාවත`，再以 Sinhala 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文与系统朗读均精确使用参考配置的 `si`，不补造地区后缀；Sinhala 可以参与默认/自选多语混排。参考未设 `noLazyMode`，所以不会强制关闭用户选择的简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、`si` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-56 | Khmer 离线、组合输入与百科练习 | 在语言菜单选择「ខ្មែរ」，分别开始字数、引语和“知识短文”练习；用 macOS Khmer 输入源输入 `បង្អួច ផ្លូវ`，再以 Khmer 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文请求 `km` 页面，系统朗读使用精确的 `km-KH`；Khmer 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`km` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-57 | Burmese 离线、组合输入与百科练习 | 在语言菜单选择「မြန်မာ」，分别开始字数、引语和“知识短文”练习；用 macOS Burmese 输入源输入 `ပြတင်းပေါက် လမ်း`，再以 Burmese 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；正常 LTR 提示使用 macOS 组合输入与原生文本排版。知识短文请求 `my` 页面，系统朗读使用精确的 `my-MM`；缅甸语可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、`my` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-58 | Lao 离线与百科练习 | 在语言菜单选择「ລາວ」，分别开始字数、引语和“知识短文”练习；用 macOS Lao 输入源输入 `ປ່ອງຢ້ຽມ ທາງ`，再以 Lao 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。知识短文与系统朗读均精确使用参考配置的 `lo`，不补造地区后缀；Lao 可以参与默认/自选多语混排。参考未设 `noLazyMode`，所以不会强制关闭用户选择的简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、`lo` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-59 | Amharic 离线与百科练习 | 在语言菜单选择「አማርኛ」，分别开始字数、引语和“知识短文”练习；用 macOS Amharic 输入源输入 `መስኮት መንገድ`，再以 Amharic 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。知识短文按参考 `am-ET` 的首段请求 `am` 页面，系统朗读使用精确的 `am-ET`；Amharic 可以参与默认/自选多语混排。参考未设 `noLazyMode`，所以不会强制关闭用户选择的简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、`am` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-60 | Armenian 离线与百科练习 | 在语言菜单选择「Հայերեն」，分别开始字数、引语和“知识短文”练习；用 macOS Armenian 输入源输入 `պատուհան ճանապարհ`，再以 Armenian 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考没有 BCP-47，因此知识短文和系统朗读分别按官方缺省路径使用 `en` 与 `en-US`；Armenian 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-61 | Georgian 离线与百科练习 | 在语言菜单选择「ქართული」，分别开始字数、引语和“知识短文”练习；用 macOS Georgian 输入源输入 `ფანჯარა გზა`，再以 Georgian 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考没有 BCP-47，因此知识短文和系统朗读分别按官方缺省路径使用 `en` 与 `en-US`；Georgian 可以参与默认/自选多语混排，但不应用简化重音输入。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-62 | Azerbaijani 离线与百科练习 | 在语言菜单选择「Azərbaycanca」，分别开始字数、引语和“知识短文”练习；用 macOS Azerbaijani 输入源输入 `pəncərə yol`，再以 Azerbaijani 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考将 BCP-47 设为 `az-AZ`，因此知识短文按首段请求 `az`，系统朗读精确使用 `az-AZ`；Azerbaijani 可以参与默认/自选多语混排，且不强制关闭简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、`az` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-63 | Belarusian 离线与百科练习 | 在语言菜单选择「Беларуская」，分别开始字数、引语和“知识短文”练习；用 macOS Belarusian 输入源输入 `акно дарога`，再以 Belarusian 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考将 BCP-47 设为 `be-BY`，因此知识短文按首段请求 `be`，系统朗读精确使用 `be-BY`；Belarusian 可以参与默认/自选多语混排，但会依 `noLazyMode` 禁用简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、`be` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-64 | Central Kurdish RTL、连写与百科练习 | 在语言菜单选择「کوردی ناوەندی」，分别开始字数、引语和“知识短文”练习；用 macOS Central Kurdish 输入源输入 `پەنجەرە ڕێگا`。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示使用原生 RTL 排版与连写字形。参考将 BCP-47 设为 `ckb`，因此知识短文与系统朗读均使用 `ckb`；Central Kurdish 不参与默认/自选多语混排，但不强制关闭简化输入。 | 待验收（离线内容、完整输入会话、RTL 排版、`ckb` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-65 | Lithuanian 离线与百科练习 | 在语言菜单选择「Lietuvių」，分别开始字数、引语和“知识短文”练习；用 macOS Lithuanian 输入源输入 `langas kelias`，再以 Lithuanian 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考没有 BCP-47，因此知识短文和系统朗读分别按官方缺省路径使用 `en` 与 `en-US`；Lithuanian 可以参与默认/自选多语混排，并保留简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-66 | Latvian 离线与百科练习 | 在语言菜单选择「Latviešu」，分别开始字数、引语和“知识短文”练习；用 macOS Latvian 输入源输入 `logs ceļš`，再以 Latvian 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考将 BCP-47 设为 `lv`，因此知识短文与系统朗读均精确使用 `lv`；Latvian 可以参与默认/自选多语混排，并保留简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、`lv` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-67 | Mongolian 离线与百科练习 | 在语言菜单选择「Монгол」，分别开始字数、引语和“知识短文”练习；用 macOS Mongolian 输入源输入 `ном цонх`，再以 Mongolian 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考仅定义 `noLazyMode: true`、没有 BCP-47，因此知识短文和系统朗读分别按官方缺省路径使用 `en` 与 `en-US`；Mongolian 可以参与默认/自选多语混排，但会禁用简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-68 | Irish 离线与百科练习 | 在语言菜单选择「Gaeilge」，分别开始字数、引语和“知识短文”练习；用 macOS Irish 输入源输入 `leabhar fuinneog`，再以 Irish 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考将 BCP-47 设为 `ga-IE`，因此知识短文按首段请求 `ga`，系统朗读精确使用 `ga-IE`；Irish 可以参与默认/自选多语混排，且不强制关闭简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、`ga` 解析、朗读及服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-69 | Galician 离线、百科与 Zipf 练习 | 在语言菜单选择「Galego」，分别开始字数、引语和“知识短文”练习；用 macOS Galician 输入源输入 `libro xanela`，再以 Galician 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考将 BCP-47 设为 `gl-ES` 且标记 `orderedByFrequency: true`，因此知识短文按首段请求 `gl`，系统朗读精确使用 `gl-ES`，并支持 Zipf 高频词；Galician 可以参与默认/自选多语混排，且不强制关闭简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、`gl` 解析、朗读、Zipf、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-70 | Marathi 离线、缺省百科与 Zipf 练习 | 在语言菜单选择「मराठी」，分别开始字数、引语和“知识短文”练习；用 macOS Marathi 输入源输入 `पुस्तक खिडकी`，再以 Marathi 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考仅定义 `noLazyMode: true` 与 `orderedByFrequency: true`、没有 BCP-47，因此知识短文和系统朗读分别按官方缺省路径使用 `en` 与 `en-US`；Marathi 可以参与默认/自选多语混排，支持 Zipf 高频词，但会禁用简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读、Zipf、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-71 | Albanian 离线与缺省百科练习 | 在语言菜单选择「Shqip」，分别开始字数、引语和“知识短文”练习；用 macOS Albanian 输入源输入 `libër dritare`，再以 Albanian 与 English 创建多语练习。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考仅定义 `name: albanian`、没有 BCP-47，因此知识短文和系统朗读分别按官方缺省路径使用 `en` 与 `en-US`；Albanian 可以参与默认/自选多语混排，且不强制关闭简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-72 | Bemba 离线、百科与 Zipf 提示 | 在语言菜单选择「Ichibemba」，分别开始字数、引语和“知识短文”练习；用 macOS Bemba 输入源输入 `buku amenshi`，再以 Bemba 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考将 BCP-47 设为 `bem`、显式标记 LTR，且 `orderedByFrequency: false`，因此知识短文和系统朗读均使用 `bem`；Bemba 可以参与默认/自选多语混排，不强制关闭简化输入；启用 Zipf 时显示“词表未按频率排序”七秒提示，但不关闭修饰器。 | 待验收（离线内容、完整输入会话、多语轮转、`bem` 解析、朗读、Zipf 提示、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-73 | Bosnian 离线、缺省百科与 Zipf 练习 | 在语言菜单选择「Bosanski」，分别开始字数、引语和“知识短文”练习；用 macOS Bosnian 输入源输入 `prozor svjetlo`，再以 Bosnian 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考仅定义 `name: bosnian` 与 `orderedByFrequency: true`、没有 BCP-47，因此知识短文和系统朗读分别按官方缺省路径使用 `en` 与 `en-US`；Bosnian 可以参与默认/自选多语混排，支持 Zipf 高频词且不强制关闭简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读、Zipf、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-74 | Esperanto Unicode 离线、缺省百科与 Zipf 练习 | 在语言菜单选择「Esperanto」，分别开始字数、引语和“知识短文”练习；输入 `ĉielo ĝardeno`，再以 Esperanto 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语为 Typebar 原创 Unicode 内容，涵盖 Esperanto 的组合变音字符，以空格提交。参考仅定义 `orderedByFrequency: true`、没有 BCP-47，因此知识短文和系统朗读分别按缺省路径使用 `en` 与 `en-US`；标准 Esperanto 可混排、支持 Zipf 且保留简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读、Zipf、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-75 | Esperanto X/H 转写模式 | 分别选择「Esperanto · X-sistemo」和「Esperanto · H-sistemo」，输入 `cxielo gxardeno` 与 `chielo ghardeno`；分别打开引语、知识短文和 Zipf。 | 两套 ASCII 转写拥有独立的 Typebar 原创词流和四档引语，且不会被 Unicode 版本替换；参考对两者设置 `noLazyMode: true`，故简化输入被禁用。两者都无 BCP-47，知识短文与朗读走 `en`／`en-US` 缺省分支；H 明确支持 Zipf，X 未声明排序，因而显示七秒“可能不支持”提示但不关闭修饰器。 | 待验收（三种离线内容、完整输入会话、多语轮转、缺省解析、朗读、Zipf 与服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-76 | Latin 离线、缺省百科与 Zipf 提示 | 在语言菜单选择「Latina」，分别开始字数、引语和“知识短文”练习；输入 `fenestra lumen`，再以 Latin 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考仅定义 `name: latin`、没有 BCP-47，因此知识短文和系统朗读分别按官方缺省路径使用 `en` 与 `en-US`；Latin 可以参与默认/自选多语混排，保留简化输入；启用 Zipf 时显示“可能不支持”七秒提示，但不关闭修饰器。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读、Zipf 提示、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-77 | Friulian 离线、百科与 Zipf 提示 | 在语言菜单选择「Friulian」，分别开始字数、引语和“知识短文”练习；输入 `soreli lune`，再以 Friulian 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考将 BCP-47 设为 `fur`，因此知识短文和系统朗读均精确使用 `fur`；Friulian 可以参与默认/自选多语混排，保留简化输入；启用 Zipf 时显示“可能不支持”七秒提示，但不关闭修饰器。 | 待验收（离线内容、完整输入会话、多语轮转、`fur` 解析、朗读、Zipf 提示、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-78 | Malagasy 离线、缺省百科与 Zipf 提示 | 在语言菜单选择「Malagasy」，分别开始字数、引语和“知识短文”练习；输入 `masoandro volana`，再以 Malagasy 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考仅定义 `noLazyMode: true`、没有 BCP-47，因此知识短文和系统朗读分别按官方缺省路径使用 `en` 与 `en-US`；Malagasy 可以参与默认/自选多语混排，但会禁用简化输入；启用 Zipf 时显示“可能不支持”七秒提示，但不关闭修饰器。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读、Zipf 提示、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-79 | Welsh 离线、缺省百科与 Zipf 提示 | 在语言菜单选择「Cymraeg」，分别开始字数、引语和“知识短文”练习；输入 `haul lleuad`，再以 Welsh 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考仅定义 `name: welsh`、没有 BCP-47，因此知识短文和系统朗读分别按官方缺省路径使用 `en` 与 `en-US`；Welsh 可以参与默认/自选多语混排，保留简化输入；启用 Zipf 时显示“可能不支持”七秒提示，但不关闭修饰器。 | 待验收（离线内容、完整输入会话、多语轮转、缺省 `en` 解析、朗读、Zipf 提示、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-80 | Hausa 离线、百科与 Zipf 提示 | 在语言菜单选择「Hausa」，分别开始字数、引语和“知识短文”练习；输入 `rana wata`，再以 Hausa 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考将 BCP-47 设为 `ha`，因此知识短文和系统朗读均精确使用 `ha`；Hausa 可以参与默认/自选多语混排，保留简化输入；启用 Zipf 时显示“可能不支持”七秒提示，但不关闭修饰器。 | 待验收（离线内容、完整输入会话、多语轮转、`ha` 解析、朗读、Zipf 提示、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-81 | Tatar 离线、百科与 Zipf 练习 | 在语言菜单选择「Татарча」，分别开始字数、引语和“知识短文”练习；用 macOS Tatar 输入源输入 `көн ай`，再以 Tatar 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 西里尔内容，以空格提交；普通 LTR 提示沿用 macOS 原生文本排版。参考将 BCP-47 设为 `tt` 并标记 `orderedByFrequency: true`，因此知识短文与系统朗读均精确使用 `tt`，并支持 Zipf 高频词；Tatar 可以参与默认/自选多语混排，且不强制关闭简化输入。 | 待验收（离线内容、完整输入会话、多语轮转、`tt` 解析、朗读、Zipf、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-82 | Uzbek 离线、百科与 Zipf 提示 | 在语言菜单选择「Oʻzbekcha」，分别开始字数、引语和“知识短文”练习；输入 `tong oy`，再以 Uzbek 与 English 创建多语练习并启用 Zipf 高频词。 | 词流和四档引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示遵循参考显式 LTR 设置并沿用 macOS 原生文本排版。参考将 BCP-47 设为 `uz-UZ`，因此知识短文按首段请求 `uz`，系统朗读精确使用 `uz-UZ`；Uzbek 可以参与默认/自选多语混排，保留简化输入；启用 Zipf 时显示“可能不支持”七秒提示，但不关闭修饰器。 | 待验收（离线内容、完整输入会话、多语轮转、`uz` 解析、朗读、Zipf 提示、服务端白名单由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-83 | Swiss German 词流、German 引语回退与社区边界 | 选择「Swiss German」，分别开始字数、引语和“知识短文”练习；确认 `straße` 在自定义文本中显示为 `strasse`，再启用 Zipf 并以 Swiss German 与 English 创建多语练习。 | 词流和四档引语只从 Typebar 自有 German 内容派生，所有可见 `ß` 均改为 `ss`，并以 LTR 空格提交。参考使用 `bcp47: de-CH`，所以知识短文按首段使用 `de`，系统朗读精确使用 `de-CH`；参考不设 `noLazyMode`，故保留简化输入。Zipf 显示可能不支持七秒提示但保留修饰器。Swiss German 可进入成绩和排行榜，却不出现在社区投稿选择中，社区引语来源不可选。 | 待验收（词流／引语派生、`ß` 转换、自定义文本、混排、`de` 解析、朗读、Zipf、成绩与排行榜及投稿拒绝由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-84 | Egyptian Arabic RTL、连写与百科练习 | 在语言菜单选择「العربية المصرية」，分别开始字数、引语和“知识短文”练习；用 macOS Arabic 输入源输入 `دلوقتي شباك`，再以该语言提交社区引语和成绩。 | 词流和四档引语均为 Typebar 原创埃及阿拉伯语内容，使用空格提交并由 macOS 进行 RTL 连写字形排版。参考定义 `rightToLeft: true`、`joiningScript: true` 与 `bcp47: ar-EG`，因此知识短文按首段请求 `ar`，系统朗读精确使用 `ar-EG`；它不进入未验收的双向多语混排。参考未设 `noLazyMode`，所以用户显式选择的简化输入仍然有效，但不会自动开启标准 Arabic 专用快捷偏好。 | 待验收（原创内容、完整输入会话、RTL 排版、`ar` 解析、朗读、简化输入、Zipf 提示、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-85 | Moroccan Arabic RTL、连写与 Zipf 边界 | 在语言菜单选择「العربية المغربية」，分别开始字数、引语和“知识短文”练习；用 macOS Arabic 输入源输入 `شرجم أتاي`，再启用 Zipf 并以该语言提交社区引语和成绩。 | 词流和四档引语均为 Typebar 原创摩洛哥阿拉伯语内容，使用空格提交并由 macOS 进行 RTL 连写字形排版。参考定义 `rightToLeft: true`、`joiningScript: true`、`orderedByFrequency: false` 与 `bcp47: ar-MA`，因此知识短文按首段请求 `ar`，系统朗读精确使用 `ar-MA`；它不进入未验收的双向多语混排。参考未设 `noLazyMode`，所以用户显式选择的简化输入仍然有效，但不会自动开启标准 Arabic 专用快捷偏好；Zipf 显示词表未按频率排序的提示且保留修饰器。 | 待验收（原创内容、完整输入会话、RTL 排版、`ar` 解析、朗读、简化输入、Zipf 提示、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-86 | Pashto RTL、连写与简化输入边界 | 在语言菜单选择「پښتو」，分别开始字数、引语和“知识短文”练习；用 macOS Pashto 输入源输入 `کړکۍ لار`，确认普通练习不可选择简化输入，再在自定义文本中确认可显式选择；启用 Zipf 并提交社区引语和成绩。 | 词流和四档引语均为 Typebar 原创 Pashto 内容，使用空格提交并由 macOS 进行 RTL 连写字形排版。参考定义 `rightToLeft: true`、`joiningScript: true`、`noLazyMode: true` 与 `bcp47: ps`，因此知识短文和系统朗读均精确使用 `ps`；它不进入未验收的双向多语混排。非自定义练习会禁用简化输入，而自定义文本保持该例外；未标记词频排序，Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、RTL 排版、`ps` 解析、朗读、普通/自定义简化输入边界、Zipf 提示、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-87 | Sindhi RTL、连写与 Zipf 边界 | 在语言菜单选择「سنڌي」，分别开始字数、引语和“知识短文”练习；用 macOS Sindhi 输入源输入 `ڪتاب رستو`，再启用 Zipf 并以该语言提交社区引语和成绩。 | 词流和四档引语均为 Typebar 原创 Sindhi 内容，使用空格提交并由 macOS 进行 RTL 连写字形排版。参考定义 `rightToLeft: true`、`joiningScript: true`、`orderedByFrequency: false` 与 `bcp47: sd`，因此知识短文和系统朗读均精确使用 `sd`；它不进入未验收的双向多语混排。参考未设 `noLazyMode`，所以用户显式选择的简化输入仍然有效，但不会自动开启标准 Arabic 专用快捷偏好；Zipf 显示词表未按频率排序的提示且保留修饰器。 | 待验收（原创内容、完整输入会话、RTL 排版、`sd` 解析、朗读、简化输入、Zipf 提示、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-88 | Occitan 离线、多语与百科练习 | 在语言菜单选择「Occitan」，分别开始字数、引语和“知识短文”练习；输入 `libre camin`，再以 Occitan 与 English 创建多语练习并启用 Zipf。 | 词流和四档引语均为 Typebar 原创 Occitan 内容，以 LTR 空格提交。参考定义 `bcp47: oc-FR`，因此知识短文按首段请求 `oc`，系统朗读精确使用 `oc-FR`；它进入默认/自选多语混排，保留显式简化输入。参考未标记词频排序，Zipf 显示可能不支持的提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、`oc` 解析、朗读、简化输入、Zipf 提示、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-89 | Oromo 离线、多语与 Zipf 练习 | 在语言菜单选择「Oromo」，分别开始字数、引语和“知识短文”练习；输入 `kitaaba karaa`，再以 Oromo 与 English 创建多语练习并启用 Zipf。 | 词流和四档引语均为 Typebar 原创 Oromo 内容，以 LTR 空格提交。参考定义 `bcp47: om` 与 `orderedByFrequency: true`，因此知识短文和系统朗读均精确使用 `om`；它进入默认/自选多语混排，保留显式简化输入和 Zipf 高频词。 | 待验收（原创内容、完整输入会话、多语轮转、`om` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-90 | Macedonian 离线、多语与简化输入边界 | 在语言菜单选择「Македонски」，分别开始字数、引语和“知识短文”练习；用 macOS Macedonian 输入源输入 `книга патека`，再以 Macedonian 与 English 创建多语练习并分别在普通及自定义文本中选择简化输入。 | 词流和四档引语均为 Typebar 原创 Macedonian 西里尔内容，以 LTR 空格提交。参考仅定义 `noLazyMode: true`，故知识短文和系统朗读严格走 `en`／`en-US` 缺省分支；它进入默认/自选多语混排。普通练习禁用简化输入，自定义文本保留例外；未标记词频排序，Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、缺省在线/朗读分支、普通/自定义简化输入边界、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-91 | Kazakh 离线、多语与简化输入边界 | 在语言菜单选择「Қазақша」，分别开始字数、引语和“知识短文”练习；用 macOS Kazakh 输入源输入 `кітап жол`，再以 Kazakh 与 English 创建多语练习并分别在普通及自定义文本中选择简化输入。 | 词流和四档引语均为 Typebar 原创 Kazakh 西里尔内容，以 LTR 空格提交。参考仅定义 `noLazyMode: true`，故知识短文和系统朗读严格走 `en`／`en-US` 缺省分支；它进入默认/自选多语混排。普通练习禁用简化输入，自定义文本保留例外；未标记词频排序，Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、缺省在线/朗读分支、普通/自定义简化输入边界、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-92 | Vietnamese 离线、多语与默认输入练习 | 在语言菜单选择「Tiếng Việt」，分别开始字数、引语和“知识短文”练习；用 macOS Vietnamese 输入源输入 `sách đường`, 再以 Vietnamese 与 English 创建多语练习并启用简化输入。 | 词流和四档引语均为 Typebar 原创 Vietnamese 内容，以 LTR 空格提交。参考未定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序，故知识短文和系统朗读严格走 `en`／`en-US` 缺省分支；它进入默认/自选多语混排，并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、缺省在线/朗读分支、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-93 | Jyutping 离线、多语与 BCP 中文百科 | 在语言菜单选择「Jyutping」，分别开始字数、引语和“知识短文”练习；输入 `nei5 hou2`，再以 Jyutping 与 English 创建多语练习并启用简化输入。 | 本地词流和四档引语均为 Typebar 原创 ASCII 加声调数字内容，以 LTR 空格提交。参考定义 `bcp47: zh-Hant`，故知识短文按首段请求 `zh` 并按中文词界分词，系统朗读精确使用 `zh-Hant`；它进入默认/自选多语混排，保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、`zh` 解析、中文词界、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-94 | Pinyin 离线、多语与默认输入练习 | 在语言菜单选择「Pinyin」，分别开始字数、引语和“知识短文”练习；输入 `ni hao`，再以 Pinyin 与 English 创建多语练习并启用简化输入。 | 本地词流和四档引语均为 Typebar 原创 ASCII 拼音内容，以 LTR 空格提交。基础、1k 和 10k 参考配置未定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序，故知识短文和系统朗读严格走 `en`／`en-US` 缺省分支；它进入默认/自选多语混排，并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、缺省在线/朗读分支、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-95 | Western Armenian 离线、多语与 BCP 练习 | 在语言菜单选择「Հայերէն (Արեւմտեան)」，分别开始字数、引语和“知识短文”练习；用 macOS Armenian 输入源输入 `բարեւ դուն`，再以 Western Armenian 与 English 创建多语练习并启用简化输入。 | 本地词流和四档引语均为 Typebar 原创西部亚美尼亚语正字法内容，以 LTR 空格提交。基础与 1k 参考配置定义 `bcp47: hyw`，故知识短文和系统朗读均精确使用 `hyw`；它进入默认/自选多语混排，并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、`hyw` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-96 | Bashkir 离线、多语与 Zipf 练习 | 在语言菜单选择「Башҡортса」，分别开始字数、引语和“知识短文”练习；用 macOS Bashkir 输入源输入 `китап юл`，再以 Bashkir 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Bashkir 西里尔内容，以 LTR 空格提交。参考定义 `bcp47: ba` 与 `orderedByFrequency: true`，故知识短文和系统朗读均精确使用 `ba`；它进入默认/自选多语混排，保留显式简化输入和 Zipf 高频词。 | 待验收（原创内容、完整输入会话、多语轮转、`ba` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-97 | Euskera 离线、多语与 BCP 练习 | 在语言菜单选择「Euskara」，分别开始字数、引语和“知识短文”练习；输入 `kaixo zu`, 再以 Euskera 与 English 创建多语练习并启用简化输入。 | 本地词流和四档引语均为 Typebar 原创 Basque 内容，以 LTR 空格提交。参考定义 `rightToLeft: false` 与 `bcp47: eu`，故知识短文和系统朗读均精确使用 `eu`；它进入默认/自选多语混排，并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、`eu` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-98 | Frisian 离线、多语与 BCP 练习 | 在语言菜单选择「Frysk」，分别开始字数、引语和“知识短文”练习；输入 `hoi do`, 再以 Frisian 与 English 创建多语练习并启用简化输入。 | 本地词流和四档引语均为 Typebar 原创 Frisian 内容，以 LTR 空格提交。基础与 1k 参考配置定义 `bcp47: fy-FY`，故知识短文按首段请求 `fy`，系统朗读精确使用 `fy-FY`；它进入默认/自选多语混排，并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、`fy` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-99 | isiZulu 离线、多语与缺省路径练习 | 在语言菜单选择「isiZulu」，分别开始字数、引语和“知识短文”练习；输入 `sawubona wena`, 再以 isiZulu 与 English 创建多语练习并启用简化输入。 | 本地词流和四档引语均为 Typebar 原创 isiZulu 内容，以 LTR 空格提交。参考未定义 BCP-47，故知识短文和系统朗读严格回退 `en` 与 `en-US`；它进入默认/自选多语混排，并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、缺省解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-100 | ʻŌlelo Hawaiʻi 离线、多语与 BCP 练习 | 在语言菜单选择「ʻŌlelo Hawaiʻi」，分别开始字数、引语和“知识短文”练习；输入 `aloha ʻoe`, 再以 Hawaiian 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Hawaiian 内容，以 LTR 空格提交。基础与 1k 参考配置定义 `bcp47: haw` 与 `orderedByFrequency: true`，故知识短文和系统朗读均精确使用 `haw`；它进入默认/自选多语混排，并保留显式简化输入和 Zipf 高频词。 | 待验收（原创内容、完整输入会话、多语轮转、`haw` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-101 | Taqbaylit 离线、多语与 BCP 练习 | 在语言菜单选择「Taqbaylit」，分别开始字数、引语和“知识短文”练习；输入 `azul kečč`, 再以 Kabyle 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Taqbaylit 内容，以 LTR 空格提交。参考组定义 `bcp47: kab` 与 `orderedByFrequency: false`，故知识短文和系统朗读均精确使用 `kab`；它进入默认/自选多语混排，保留显式简化输入。Zipf 显示不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、`kab` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-102 | Maltese 离线、多语与 BCP 练习 | 在语言菜单选择「Malti」，分别开始字数、引语和“知识短文”练习；输入 `bongu ktieb`, 再以 Maltese 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Maltese 内容，以 LTR 空格提交。参考组定义 `bcp47: mt`，不定义频率排序，故知识短文和系统朗读均精确使用 `mt`；它进入默认/自选多语混排并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、`mt` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-103 | toki pona 离线、多语与输入规则 | 在语言菜单选择「toki pona」，分别开始字数、引语和“知识短文”练习；输入 `sina pona`, 启用简化输入后再启动普通练习与自定义文本；以 toki pona 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 toki pona 内容，以 LTR 空格提交。参考组定义 `noLazyMode: true`，而未定义 BCP-47 或频率排序，故知识短文和系统朗读走 `en`／`en-US` 缺省路径；普通练习移除简化输入，自定义文本保留该例外。它进入默认/自选多语混排；Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、在线/朗读缺省、普通/自定义简化输入边界、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-104 | isiXhosa 离线、多语与 BCP 练习 | 在语言菜单选择「isiXhosa」，分别开始字数、引语和“知识短文”练习；输入 `molo incwadi`, 再以 isiXhosa 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Xhosa 内容，以 LTR 空格提交。主参考组定义 `bcp47: xh`，故用户可见的 Xhosa 知识短文和系统朗读均精确使用 `xh`；3k 组不重申 BCP-47，差异已由配置审计记录。它进入默认/自选多语混排并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、`xh` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-105 | Tibetan 连写、离线与 BCP 练习 | 在语言菜单选择「བོད་སྐད་」，分别开始字数、引语和“知识短文”练习；输入 `ང་ ཁྱེད་`，开启圆点效果后观察已完成 Tibetan 字形，再以 Tibetan 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Tibetan 内容，以 LTR 空格提交。参考组定义 `joiningScript: true`、`noLazyMode: true` 与 `bcp47: bo-TI`，故提示保留 macOS 原生塑形和较紧行距，圆点不会逐字替换连写簇；知识短文使用 `bo`，系统朗读精确使用 `bo-TI`。普通练习移除简化输入，自定义文本保留该例外；它进入默认/自选多语混排。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、连写塑形、圆点保护、多语轮转、`bo`/`bo-TI` 解析、普通/自定义简化输入边界、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-106 | Kyrgyz 离线、多语与 BCP 练习 | 在语言菜单选择「Кыргызча」，分别开始字数、引语和“知识短文”练习；输入 `салам китеп`，再以 Kyrgyz 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Kyrgyz 内容，以 LTR 空格提交。参考组定义 `bcp47: ky-KY`，故知识短文使用 `ky`，系统朗读精确使用 `ky-KY`；它进入默认/自选多语混排并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、`ky`/`ky-KY` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-107 | Yiddish RTL、连写与 BCP 练习 | 在语言菜单选择「ייִדיש」，分别开始字数、引语和“知识短文”练习；输入 `שלום בוך`，开启圆点效果后观察已完成 Yiddish 字形。 | 本地词流和四档引语均为 Typebar 原创 Yiddish 内容，以 RTL 空格提交。参考组定义 `rightToLeft: true`、`joiningScript: true` 与 `bcp47: yi`，故提示保留 macOS 原生 RTL/连写塑形和较紧行距，圆点不会逐字替换字形；知识短文和系统朗读均精确使用 `yi`。它保留显式简化输入，Zipf 显示可能不支持提示且保留修饰器；双向多语交互尚未验收，故不加入混排。 | 待验收（原创内容、完整输入会话、RTL/连写塑形、圆点保护、`yi` 解析、朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-108 | Udmurt 离线、多语与缺省回退 | 在语言菜单选择「Удмурт кыл」，分别开始字数、引语和“知识短文”练习；输入 `зечбур книга`，再以 Udmurt 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Udmurt 内容，以 LTR 空格提交。参考组未定义 BCP-47、RTL、连写、`noLazyMode` 或频率排序，故知识短文和系统朗读严格走 `en`／`en-US` 缺省路径；它进入默认/自选多语混排并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、在线/朗读缺省、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-109 | Yoruba 离线、多语与缺省回退 | 在语言菜单选择「Yorùbá」，分别开始字数、引语和“知识短文”练习；输入 `báwo ìwé`，再以 Yoruba 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Yoruba 声调内容，以 LTR 空格提交。参考组未定义 BCP-47、RTL、连写、`noLazyMode` 或频率排序，故知识短文和系统朗读严格走 `en`／`en-US` 缺省路径；它进入默认/自选多语混排并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、在线/朗读缺省、声调字符、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-110 | Swahili 离线、多语与输入规则 | 在语言菜单选择「Kiswahili」，分别开始字数、引语和“知识短文”练习；输入 `hujambo kitabu`，启用简化输入后分别启动普通练习与自定义文本；再以 Swahili 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Swahili 内容，以 LTR 空格提交。参考组定义 `noLazyMode: true`，而未定义 BCP-47 或词频排序，故知识短文和系统朗读走 `en`／`en-US` 缺省路径；普通练习移除简化输入，自定义文本保留该例外。它进入默认/自选多语混排；Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、在线/朗读缺省、普通/自定义简化输入边界、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-111 | Kinyarwanda 离线、多语与输入规则 | 在语言菜单选择「Ikinyarwanda」，分别开始字数、引语和“知识短文”练习；输入 `muraho igitabo`，启用简化输入后分别启动普通练习与自定义文本；再以 Kinyarwanda 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Kinyarwanda 内容，以 LTR 空格提交。参考组定义 `noLazyMode: true`、`orderedByFrequency: true` 与 `bcp47: rw-RW`，故知识短文使用 `rw`，系统朗读精确使用 `rw-RW`；普通练习移除简化输入，自定义文本保留该例外。它进入默认/自选多语混排并保留 Zipf 高频词。 | 待验收（原创内容、完整输入会话、多语轮转、`rw`/`rw-RW` 解析、普通/自定义简化输入边界、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-112 | Shona 离线、多语与缺省回退 | 在语言菜单选择「chiShona」，分别开始字数、引语和“知识短文”练习；输入 `mhoro bhuku`，再以 Shona 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Shona 内容，以 LTR 空格提交。参考组未定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序，故知识短文和系统朗读严格走 `en`／`en-US` 缺省路径；它进入默认/自选多语混排并保留显式简化输入。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、在线/朗读缺省、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-113 | Belarusian Łacinka 离线、多语与缺省回退 | 在语言菜单选择「Biełaruskaja łacinka」，分别开始字数、引语和“知识短文”练习；输入 `vytaju kniha`，再以 Łacinka 与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创拉丁转写内容，以 LTR 空格提交。参考组定义 `noLazyMode: false`，但未定义 BCP-47、RTL、连写或词频排序，故知识短文和系统朗读严格走 `en`／`en-US` 缺省路径；保留简化输入，进入默认/自选多语混排。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、在线/朗读缺省、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-114 | Crimean Tatar 双书写离线、多语与输入规则 | 分别选择「Qırımtatarca」与「Къырымтатарджа」，开始字数、引语和“知识短文”练习；分别输入 `qırım kitap` 与 `къырым китап`，启用简化输入后比较普通练习和自定义文本，再与 English 创建多语练习并启用 Zipf。 | 两种书写的本地词流和各自四档引语均为 Typebar 原创内容，以 LTR 空格提交，且不混用书写体系。固定参考的十个对应词表档位都定义 `noLazyMode: true` 与 `bcp47: crh-CRH`，故普通练习移除简化输入，自定义文本保留例外；知识短文使用 `crh`，系统朗读精确使用 `crh-CRH`。两者均进入默认/自选多语混排；Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、双书写分离、多语轮转、`crh`/`crh-CRH`、普通/自定义简化输入边界、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-115 | Klingon 离线、词内撇号与在线路径 | 在语言菜单选择「tlhIngan Hol」，分别开始字数、引语和“知识短文”练习；输入 `tlhIngan Hol Qapla'`，开启标点、数字、简化输入与 Zipf，再与 English 创建多语练习。 | 本地词流和四档引语均为 Typebar 原创 Klingon 内容，以 LTR 空格提交；大小写与词内 `'` 是目标文本的一部分，不能当成装饰标点剥离。基础与 1k 参考组均定义 `bcp47: tlh`，未定义 RTL、连写、`noLazyMode` 或词频排序，故知识短文和系统朗读均精确使用 `tlh`，保留简化输入；Zipf 显示可能不支持提示且保留修饰器。它进入默认/自选多语混排。 | 待验收（原创内容、完整输入会话、大小写、词内撇号、标点/数字、多语轮转、`tlh` 在线/朗读、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-116 | Quenya 离线、多语与缺省回退 | 在语言菜单选择「Quenya」，分别开始字数、引语和“知识短文”练习；输入 `elen calma`，再与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为 Typebar 原创 Quenya 内容，以 LTR 空格提交。固定参考配置未定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序，故知识短文和系统朗读严格走 `en`／`en-US` 缺省路径；保留显式简化输入，进入默认/自选多语混排。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、多语轮转、在线/朗读缺省、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-117 | Viossa 双变体、输入规则与缺省回退 | 分别选择「Viossa」与「Viossa · Njutro」，开始字数、引语和“知识短文”练习；分别输入 `tåvi möne` 与 `njütra sölu`，启用简化输入后比较普通练习和自定义文本，再与 English 创建多语练习并启用 Zipf。 | 两条本地词流和各自四档引语均为 Typebar 明示的原创练习 idiolect，以 LTR 空格提交，且不互相混用。固定参考均未定义 BCP-47、RTL 或连写，故知识短文和系统朗读使用 `en`／`en-US` 缺省路径；两者 `orderedByFrequency: false`，Zipf 显示不支持提示并保留修饰器。Viossa 保留简化输入；Njutro 定义 `noLazyMode: true`，普通练习移除简化输入、自定义文本保留例外。两者均进入默认/自选多语混排。 | 待验收（原创内容、完整输入会话、变体分离、多语轮转、在线/朗读缺省、普通/自定义简化输入边界、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-118 | Te reo Māori 离线、多语与缺省回退 | 在语言菜单选择「Te reo Māori」，分别开始字数、引语和“知识短文”练习；输入 `kōrero mārie`，再与 English 创建多语练习并启用 Zipf。 | 本地词流和四档引语均为保留 macron 的 Typebar 原创内容，以 LTR 空格提交。固定参考仅有 `maori_1k`，未定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序，故知识短文和系统朗读严格走 `en`／`en-US` 缺省路径；保留显式简化输入，进入默认/自选多语混排。Zipf 显示可能不支持提示且保留修饰器。 | 待验收（原创内容、完整输入会话、macron、多语轮转、在线/朗读缺省、简化输入、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-119 | Lojban 双词类与简化输入边界 | 分别选择「Lojban · gismu」与「Lojban · cmavo」，开始字数、引语和“知识短文”练习；分别输入 `bangu bridi` 与 `mi do .i`，启用简化输入后比较普通练习和自定义文本，再与 English 创建多语练习并启用 Zipf。 | 两条本地词流和各自四档引语均为 Typebar 原创内容，以 LTR 空格提交且不混用词类。gismu 保留五字母词根练习；cmavo 保留 `.` 和 `'` 的输入语义。固定参考均定义 `noLazyMode: true`、未定义 BCP-47、RTL、连写或词频排序，故普通练习移除简化输入、自定义文本保留例外，知识短文和系统朗读走 `en`／`en-US` 缺省路径，Zipf 显示可能不支持提示且保留修饰器。两者均进入默认/自选多语混排。 | 待验收（原创内容、完整输入会话、词类分离、`.`/`'`、多语轮转、普通/自定义简化输入边界、在线/朗读缺省、Zipf、投稿、成绩和排行榜由自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-21 | Japanese Katakana 离线练习 | 在语言菜单选择「日本語（カタカナ）」，分别开始字数与引语练习，再启用“知识短文”；用 macOS 日语片假名输入源输入 `カメラ`。 | 词流和四档引语均来自 Typebar 原创片假名内容、无空格提交；知识短文不会以可能包含汉字的远端文本替换当前提示。隐藏词界、结果文本与系统 `ja-JP` 朗读保持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-22 | Japanese Romaji 离线练习 | 在语言菜单选择「日本語（ローマ字）」，分别开始字数与引语练习，再启用“知识短文”；用普通 ASCII 键盘输入 `asa mado`。 | 词流和四档引语均来自 Typebar 原创 ASCII 罗马字内容、以空格提交；知识短文不会以可能包含汉字的日文原文替换当前提示。混排、结果与系统 `ja-JP` 朗读保持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-23 | Czech 离线与百科练习 | 在语言菜单选择「Čeština」，分别开始字数、引语和“知识短文”练习；用 macOS 捷克语输入源输入 `řeka` 与 `kůra`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `cs` 页面。`ř/ě/š/č/ž/ů` 的系统输入、错词复练和系统 `cs-CZ` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-24 | Bulgarian 离线与百科练习 | 在语言菜单选择「Български」，分别开始字数、引语和“知识短文”练习；用 macOS 保加利亚语输入源输入 `дъжд` 与 `път`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `bg` 页面。西里尔 `ъ/щ/ю/я` 的系统输入、错词复练和系统 `bg-BG` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-25 | Romanian 离线与百科练习 | 在语言菜单选择「Română」，分别开始字数、引语和“知识短文”练习；用 macOS 罗马尼亚语输入源输入 `dimineață` 与 `țărm`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `ro` 页面。组合变音 `ă/â/î/ș/ț` 的系统输入、错词复练和系统 `ro-RO` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-26 | Finnish 离线与百科练习 | 在语言菜单选择「Suomi」，分别开始字数、引语和“知识短文”练习；用 macOS 芬兰语输入源输入 `järvi` 与 `pöytä`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `fi` 页面。变音 `ä/ö` 的系统输入、错词复练和系统 `fi-FI` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-27 | Estonian 离线与百科练习 | 在语言菜单选择「Eesti」，分别开始字数、引语和“知识短文”练习；用 macOS 爱沙尼亚语输入源输入 `jõgi` 与 `ülesanne`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `et` 页面。变音 `õ/ä/ö/ü` 的系统输入、错词复练和系统 `et-EE` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-28 | Icelandic 离线与百科练习 | 在语言菜单选择「Íslenska」，分别开始字数、引语和“知识短文”练习；用 macOS 冰岛语输入源输入 `þolinmæði` 与 `æfing`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `is` 页面。特殊字母 `ð/þ/æ/ö` 的系统输入、错词复练和系统 `is-IS` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-29 | Slovak 离线与百科练习 | 在语言菜单选择「Slovenčina」，分别开始字数、引语和“知识短文”练习；用 macOS 斯洛伐克语输入源输入 `kľúč` 与 `stôl`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `sk` 页面。变音 `á/ä/č/ď/ľ/ô/ŕ/š/ť/ý/ž` 的系统输入、错词复练和系统 `sk-SK` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-30 | Slovenian 离线与百科练习 | 在语言菜单选择「Slovenščina」，分别开始字数、引语和“知识短文”练习；用 macOS 斯洛文尼亚语输入源输入 `ključ` 与 `šepet`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `sl` 页面。特殊字母 `č/š/ž` 的系统输入、错词复练和系统 `sl-SI` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-31 | Croatian 离线与百科练习 | 在语言菜单选择「Hrvatski」，分别开始字数、引语和“知识短文”练习；用 macOS 克罗地亚语输入源输入 `kuća` 与 `đak`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `hr` 页面。特殊字母 `č/ć/đ/š/ž` 的系统输入、错词复练和系统 `hr-HR` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-32 | Serbian Cyrillic 离线与百科练习 | 在语言菜单选择「Српски」，分别开始字数、引语和“知识短文”练习；用 macOS 塞尔维亚语西里尔输入源输入 `љубав`、`ђак` 与 `џеп`。 | 词流和四档引语均来自 Typebar 原创内容，以空格提交；知识短文仅在用户明确启用时请求 `sr` 页面。`љ/њ/ђ/ћ/џ/ч/ш/ж` 的系统输入、错词复练和系统 `sr-RS` 朗读维持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-33 | Serbian Latin 离线练习 | 在语言菜单选择「Srpski (Latin)」，分别开始字数与引语练习，再启用“知识短文”；输入 `ljubav đak džep`。 | 词流和四档引语只使用 Typebar 自创塞尔维亚语 Latin 内容并按空格提交；知识短文不会发起或应用可能产生西里尔文本的百科替换，保持离线 Latin 提示；错词复练和系统 `sr-RS` 朗读保持同一语言设置。 | 待验收（客户端与服务端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-17 | 本机词表筛选 | 切到自定义文本，点击“筛选词表…”，选择「Magyar」并设最短/最长词长为 `3`、允许字符为 `t ű z`、开启“仅使用允许字符”；预览后先替换、再以非精确筛选结果追加。 | 精确预览只保留 Typebar 自创的 `tűz`；排除与正则在精确模式下不生效。替换或追加均会立即进入自定义练习，且不会下载、导入或展示第三方词表。 | 待验收（字符、长度、正则、精确模式错误边界及共享原创词表来源由客户端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-18 | 本机自定义生成 | 切到自定义文本，点击“生成文本…”，填写 `a b`，设每词最少 `2`、最多 `4`、生成 `8` 词，先替换再以“数字”快捷填充追加；最后将最少片段设为 `4`、最多设为 `2`。 | 首次生成八个仅含 `a`/`b` 且长度为 2–4 的词，并立即进入自定义练习；追加保留原文本并以空格分隔。非法范围显示说明且不改变文本；生成内容不依赖网络或第三方词表。 | 待验收（片段范围、随机内容约束、空输入/零词数/10,000 字符边界由客户端自动化覆盖；依用户要求，本轮不启动 GUI） |
| MET-43 | 本机个人最佳表 | 在本机完成 English 30 秒、Español 30 秒、English 25 词和 English 30 秒“简化重音”各一轮，且 English 30 秒完成一条较快和一条同速较晚的成绩；打开历史的“个人最佳表…”，先后选择“时间”和“字数”。 | 时间表分别保留 English、Español 与简化重音的 30 秒最佳，同速 English 记录保留较早一条；字数表显示 25 词最佳。每行显示速度、Raw、准确率、稳定度、选项和日期；历史的“仅个人最佳”筛选与奖杯使用相同集合。全程不读取账户或网络数据。 | 待验收（同类分组、同速决胜、不合资格修饰器/混排/放弃/引语排除以及本机数据边界由客户端自动化覆盖；依用户要求，本轮不启动 GUI） |
| TST-10 | 完成页引语操作 | 分别完成一轮 Typebar 自有引语与已审核社区引语；在完成页收藏/评分前者，并登录后评价、撤销评价和举报后者 | 操作始终针对已完成轮次实际使用的引语；内置内容只更新本机收藏/评分。社区页显示聚合票数，评价立即刷新；举报说明最多 400 字、仅审核员可见，作者不会收到通知。未登录时明确提示需登录，不发出请求 | 待验收（完成目标的模式/来源/UUID 边界由客户端自动化覆盖；社区 API 的评分和举报由服务端自动化覆盖） |
| TST-08 | 禅模式自由输入 | 切换禅模式，输入任意不匹配文本、空格、Return 与 Tab，再按 Shift+Enter | 输入以正确状态显示、不产生错误且不会自动结束；空格/Return 分隔输入词、Tab 保留文本；Shift+Enter 显示结果 | 已验收（QA40；Shift+Enter 原生事件由自动化覆盖） |
| TST-09 | 保存长文本并恢复进度 | 在自定义模式保存 `amber harbor willow` 并勾选“作为长文本”，选择该条目，输入 `amber` 后双击 Shift+Enter；再次选择该条目，最后完整输入剩余文本 | 保存文本列表将其显示在“保存的长文本”并标注进度；中止结果说明进度已保存，再次选择只显示 `harbor willow`；完整完成后进度回到 `0 / 3 词`，编辑文本前须明确停止跟踪 | 待验收（词边界、SwiftData 和归档回归测试已通过） |
| CFG-04 | 命令面板中止长测试 | 选择 3600 秒计时或保存长文本，输入首字母，按 ⇧⌘K 搜索“中止长测试”；先取消确认，再重复并确认 | 普通测试不显示该命令；长测试显示入口。取消后继续原测试；确认后显示未保存结果，且不写入历史、统计、同步或发布 | 待验收（可用性策略与未保存结果回归已自动化覆盖） |
| CFG-05 | 命令面板主题切换 | 创建并收藏一个自定义主题，打开“跟随 macOS 深浅色自动切换”，按 ⇧⌘K 搜索“主题”并选择该主题 | 收藏主题排在其他主题前；选择后立即应用自定义主题并关闭“跟随 macOS”，重开设置后主题选择仍保留 | 待验收（收藏排序与命令标识回归已自动化覆盖） |
| CFG-06 | 命令面板应用预设 | 保存一个 60 秒测试预设后，切换到其他模式，按 ⇧⌘K 搜索该预设名称并选择 | 仅可读取的本机预设显示为“应用预设”；选择后立刻恢复时长、模式、语言和输入规则并生成新的练习 | 待验收（命令条目摘要与标识路由已自动化覆盖） |
| CFG-07 | 命令面板加载挑战 | 按 ⇧⌘K 搜索“沉稳三十”并选择；输入完成一轮后查看结果 | 命令会显示挑战说明和目标；选择后以挑战固定配置开始，结果页显示挑战通过或未通过的逐项结论 | 待验收（挑战命令目录标识已自动化覆盖） |
| CFG-08 | 命令面板收藏当前引语 | 进入引语模式，按 ⇧⌘K 搜索“收藏当前引语”并执行；再次打开命令面板并执行“取消收藏当前引语” | 引语模式且有当前引语时才显示对应命令；两次操作分别加入和移除本机收藏，不上传内容或改变当前提示 | 待验收（命令可见性与状态文字已自动化覆盖） |
| CFG-10 | 命令面板分组导航 | 在设置将“命令面板浏览”切换为“分组导航”，按 ⇧⌘K 打开面板，进入“主题与外观”；随后输入 `> 主题` | 初始显示功能分类；进入分类后只显示该分类命令；`>` 立即切换到全部命令搜索。关闭并重开设置后浏览偏好仍保留 | 待验收（分组、全局搜索和归档往返由客户端测试覆盖） |
| CFG-14 | 命令面板单列表 | 在设置将“命令面板浏览”切换为“单列表”，按 ⇧⌘K 打开面板；随后输入“主题” | 空查询时立即显示全部命令；输入后只保留匹配命令；关闭并重开设置后浏览偏好仍保留 | 待验收（空查询与筛选行为由客户端测试覆盖） |
| CFG-15 | 节奏伙伴压缩实时指标 | 将实时速度设为“文字”、准确率设为“关闭”后开启节奏伙伴；再把准确率设为“文字”并关闭节奏伙伴 | 开启后速度变为“迷你”、关闭的准确率保持关闭；关闭伙伴不会再改动速度或准确率样式 | 待验收（开启/关闭联动与非文字样式保留由客户端测试覆盖） |
| CFG-16 | 命令面板快速测试参数 | 在任意模式按 ⇧⌘K，依次执行“时间练习：60 秒”“时间练习：自定义…”并输入 2、“字数练习：自定义…”并输入 5001，再执行“开启标点”“开启数字”；另在输入页尝试 -1、小数和非数字 | 标准和自定义命令分别切换到对应模式、退出挑战并生成新提示；2 秒与 5001 词可进入预设及分享链接，5001 词不会一次性生成完整提示。0 表示无限；负数、小数、非数字和超过 JavaScript 安全整数的值不可应用。标点和数字各自改变现有内容开关而不改另一项 | 待验收（固定目录、输入边界、分享往返、按需提示策略和非法值拒绝由客户端测试覆盖；依用户要求不启动 GUI） |
| CFG-17 | 命令面板引语选择与挑战退出 | 在当前语言分别无收藏和有收藏时打开命令面板，检查“引语：本机收藏”；加载任一挑战后依次执行“引语长度：短”“搜索引语”，再另行加载挑战并执行“时间练习：30 秒” | 无收藏时不显示收藏命令，有当前来源收藏时才显示。长度命令切换到引语短档，搜索命令展示本地搜索控件；三种配置操作都会退出挑战，之后的提示不再受挑战配置覆盖 | 待验收（六种常驻引语路由、收藏可用性、非法值拒绝及配置命令挑战退出策略由客户端测试覆盖；依用户要求不启动 GUI） |
| CFG-18 | 命令面板切换语言与代码词表 | 在普通模式按 ⇧⌘K，分别搜索 `espanol`、`javascript react` 和 `simplifiedChinese` 并执行对应语言；再切到引语模式搜索 `codeSwift` 与 `mixedLanguages` | 重音无关的西班牙语搜索、代码显示名和固定 ID 均能命中并生成对应本机提示；每次选择退出活动挑战。引语模式不显示代码或混合入口，无法构造空引语练习 | 待验收（448 个目录项唯一性、逐项 ID 往返、显示名/原始 ID 搜索、引语过滤和非法值拒绝由客户端测试覆盖；依用户要求不启动 GUI） |
| CFG-19 | 命令面板练习行为设置 | 加载挑战并开始输入后，依次执行“保存完成成绩：关闭”和“引语重开：重复当前”；确认挑战和输入仍在。随后重新加载挑战并执行“难度：专家”，再次加载挑战后执行“英文拼写：英式” | 前两项只改变后续保存与引语重开行为，不退出挑战或清空当前输入；难度和拼写各自会退出挑战、生成新提示，并在设置中保持相同值。重新开启成绩保存和美式拼写可恢复原行为 | 待验收（九项固定目录、严格 ID 路由、重开/挑战退出差异及非法值拒绝由客户端测试覆盖；依用户要求不启动 GUI） |
| CFG-20 | 命令面板输入布局模拟 | 加载挑战并开始输入，按 ⇧⌘K 搜索 `colemak_dh_iso_wide`，选择“模拟布局：Colemak-DH Wide ISO”；再搜索“输入布局模拟：关闭”并执行 | 两次操作均退出挑战并重开；首次只把物理按键解释切换为对应原生布局，不改变视觉键盘选择；关闭后恢复 macOS 当前输入源。目录含固定参考的 `default + 239` 项，不显示 Nordic QWERTY 等五个 Typebar 自有布局 | 待验收（固定集合、239 项精确反向映射、严格 ID、搜索、设置应用与挑战退出由客户端自动化覆盖；依用户要求不启动 GUI） |
| CFG-21 | 命令面板声音设置 | 加载挑战并开始输入，按 ⇧⌘K 依次搜索并执行 `sound.soundVolume.0.1`、“键击提示音：灯芯”、“键击提示音：关闭”、“错误提示音：回声”、“错误提示音：关闭”、“倒计时提示音：结束前 5 秒”和“倒计时提示音：关闭” | 音量立即变为 10%；选择具体音型或倒计时秒数会启用并试听一次，关闭操作静默，键击/错误音仍记住所选音型。七次操作均不重开、不清空输入且不退出挑战。目录包含固定参考的 3 档音量、`off + 26` 个键击选项、`off + 4` 个错误选项和 `off/1/3/5/10` 倒计时选项，实际声音均为 Typebar 自有或 macOS 原生音，不使用参考音包 | 待验收（40 项固定目录、完整 ID/显示名搜索、逐项唯一映射、非法值拒绝、设置应用、试听意图与非重开策略由客户端自动化覆盖；实际听感依用户要求不启动 GUI） |
| CFG-22 | 命令面板光标呈现 | 加载挑战并输入部分文字，按 ⇧⌘K 依次执行“平滑光标：慢速”“光标样式：轮廓”“节奏光标样式：香蕉”和“重开沿用上一轮节奏：关闭” | 四项立即更新已有设置和可见光标，当前输入与挑战均保留；重新打开设置时值一致。目录含固定 4 档平滑值、主/节奏光标各 8 个 schema 值和重复节奏开/关；`default` 显示为 Typebar 条形光标，胡萝卜/香蕉/小猴由原生绘制。非法内部值 `bar` 不被接受 | 待验收（22 项固定目录、兼容值映射、完整 ID/显示名搜索、严格路由、设置持久化和非重开策略由客户端自动化覆盖；依用户要求不启动 GUI） |
| CFG-23 | 命令面板节奏目标来源 | 准备至少 12 条同设置成绩，其中过去 24 小时内有一条明显最高成绩；启用活动标签并加载挑战，按 ⇧⌘K 依次选择最近 10 次平均、过去 24 小时最佳和自定义速度。自定义页先取消，再以当前显示单位输入并应用 | 两个聚合模式分别使用按时间排序的最近 10 条和滚动 24 小时最高值，并按当前测试参数与活动标签筛选；选择模式会退出挑战并重开。取消自定义页不改变挑战或速度；应用后换算为规范 WPM、切换到自定义模式并重开。目录严格包含 `off/pb/tagPb/last/average/daily/custom` 七项，Typebar 原有全部平均与自然日平均仍可从设置选择且语义不变 | 待验收（固定目录、严格路由、最近十次排序、滚动时间窗、标签过滤、速度单位换算和旧模式保留由客户端自动化覆盖；依用户要求不启动 GUI） |
| CFG-24 | 命令面板显示选项 | 加载挑战并输入部分文字，按 ⇧⌘K 依次切换实时进度、实时速度、颜色、透明度、已输入效果、卷带和速度单位，确认输入未清空；再分别重新加载挑战并切换提示高亮与完整提示行 | 48 个固定选项均可按显示名和固定 ID 搜索并即时应用，不重新生成练习；普通显示切换保留挑战，提示高亮和完整提示行按固定行为退出挑战。命令面板速度单位只含 WPM/CPM/WPS/CPS，设置页仍保留 WPH。重新打开设置后所有选择一致 | 待验收（固定枚举总账、48 项顺序与严格路由、设置应用、搜索、非重开和挑战退出差异由客户端自动化覆盖；依用户要求不启动 GUI） |
| CFG-25 | 命令面板键盘提示设置 | 将物理输入模拟设为 Swiss German，加载挑战并开始输入；按 ⇧⌘K 依次执行按键反馈、分体矩阵、大写图例、完整按键集和大小 1.7；再选择视觉 Dvorak，最后选择“跟随输入模拟” | 前五项即时更新且不重开；只有模式切换退出挑战。显式 Dvorak 只改变键盘图、保留 Swiss German 物理输入，并退出挑战后重开；“跟随输入模拟”重开后显示 Swiss German，之后改变输入模拟时继续自动跟随。目录严格含 18 个离散值、一个 0.5–3.5/0.1 大小输入页及 `overrideSync + 239` 个视觉布局，不列五个 Typebar 自有布局 | 待验收（固定枚举与布局机器总账、严格 ID、逐项搜索、输入/视觉隔离、持续同步、范围/步长及挑战/重开策略由客户端自动化覆盖；依用户要求不启动 GUI） |
| CFG-26 | 命令面板行为设置 | 加载挑战并输入部分文字，按 ⇧⌘K 依次选择快速重开 Tab、开启盲打、开启完成后展开单词历史，再切换为手动分组；重新打开面板后用 `>` 搜索并逐项恢复 | 四次操作均立即写入既有设置，当前输入和挑战保持不变；快速重开严格提供 off/esc/tab/enter，另外三项分别提供两值，共十个命令。切换手动分组后面板按分类展示，输入 `>` 仍可搜索全部命令；重开应用后设置仍保留 | 待验收（十项固定目录、严格 ID、逐项搜索、设置持久化及非重开/非退出挑战策略由客户端自动化覆盖；依用户要求不启动 GUI） |
| CFG-27 | 命令面板练习阈值 | 加载挑战并输入部分文字，按 ⇧⌘K 依次选择最低整体速度自定义、最低准确率自定义和最低单词 Burst 弹性；每个输入页先取消，再以当前显示单位输入有效值并确认。另测试 0、301.25 WPM、101% 准确率、Burst 小数及非数字 | 取消保持当前输入和挑战；确认后退出挑战、持久化小数阈值并重开。0 关闭对应阈值，整体速度可超过 300 WPM，准确率只接受 0–100，Burst 只接受显示单位整数并按固定/弹性模式应用；七个固定命令均可按完整 ID 搜索 | 待验收（固定目录、严格路由、单位换算、数值边界、旧整数配置迁移、持久化及取消/确认差异由客户端自动化覆盖；依用户要求不启动 GUI） |
| MET-45 | 历史主曲线至少保留一条 | 在历史趋势中只保留“准确率”后关闭它；再只保留“速度”后关闭它，并分别切换“均值 10/100” | 关闭最后一条主曲线时自动恢复另一条；两条均值线仍可独立开关，不会导致主曲线消失 | 待验收（双向恢复和均值独立性由客户端测试覆盖） |
| CFG-11 | 恢复默认设置的本地资源边界 | 先导入本地背景图片和任一受支持的本地字体，修改任意练习/主题设置；在设置选择“恢复默认设置”并确认，重开设置 | 练习与外观设置恢复默认，本地背景图片被移除；本地字体文件仍覆盖名称字体，练习历史不删除 | 待验收（普通设置快照复位由客户端测试覆盖） |
| CFG-12 | 细粒度设置搜索 | 在设置依次搜索 `keyboard layout`、`keyboard missing`、`主题` 和一个不存在的词；检查“最佳匹配”列表及下方区域 | 完整词组只列出键盘布局；没有完整命中时列出同为最高分的键盘设置；主题显示具体主题设置及所属区域；无结果时显示空状态。搜索不修改任何偏好 | 待验收（全局最高分、并列结果、无结果、稳定 ID 和全部区域覆盖由客户端测试覆盖；依用户要求不启动 GUI） |
| CFG-13 | 独立简化输入开关 | 在设置搜索 `lazy mode`，开启“简化输入”；依次开始 Spanish、Hindi、Spanish 和自定义 Hindi 文本练习，再关闭并重新打开应用 | 开关位于输入设置而不出现在趣味修饰器列表；Spanish 提示简化重音，Hindi 普通练习临时禁用，切回 Spanish 自动恢复，自定义文本允许启用；重开应用后仍保留用户选择 | 待验收（设置分组、搜索、Unicode 转换、语言／多语／自定义边界及设置归档由客户端自动化覆盖；依用户要求不启动 GUI） |
| MET-35 | 结果稳定度与按键稳定度 | 分别以近乎等间隔和明显停顿/变速的节奏完成两轮同长度练习；查看完成页、历史行和两条成绩详情，检查历史摘要的最高/平均/近 10 次稳定度，并把近 28 日图切换为“平均稳定度”。最后导入一条没有回放的旧成绩。 | 均匀节奏的稳定度高于明显变速的一轮；结果页、列表、详情、摘要和每日图采用一致的本机派生值。旧成绩显示 `0%`，不会根据 WPM 或提示补造指标。 | 待验收（本机回放采样、空回放安全回退、历史聚合与公式映射由客户端测试覆盖） |
| MET-42 | 本机统计日分界 | 在设置将“连续练习日分界”首次设为 `+3 小时`，在分界前后各完成一轮；打开历史并尝试再次改为其他值，最后导出再导入设置 | 连续天数、近 28 日活动与热力图将分界前成绩归入前一统计日，分界后成绩归入新统计日；首次选择后控件锁定；归档往返保留该选择，原始成绩的完成时间不变 | 待验收（30 分钟步进范围、跨分界聚合、锁定、持久化及旧快照默认值由自动化覆盖） |
| VIS-07 | 节奏伙伴 | 在设置开启“显示节奏伙伴”，开始练习后先按左手键 A、再按右手键 H；点击面板外部失焦后重新开始测试 | 原创矢量伙伴只在进行中显示，分别反馈左右手按键；失焦和重开立即清空手部状态；开关重开设置后保留，不影响提示、输入或计分 | 待验收（物理手位状态、速度范围与归档往返由客户端测试覆盖） |
| VIS-08 | 键入能量效果 | 在设置依次选择柔和、高能、超载和极限，开始练习并各输入一次正确与错误字符；再开启 macOS“减少动态效果”、使输入框失焦并重开 | 每次接受输入均显示短暂原创粒子；柔和/超载按正误着色，高能/极限显示随机色；超载/极限短暂抖动面板。减少动态效果、失焦、关闭和重开都不保留动画，设置重开后保留，不改变输入或计分 | 待验收（五档映射、数量边界、正误色策略和设置归档由客户端测试覆盖） |
| VIS-11 | 本机与本地练习字体 | 在设置点“浏览…”，搜索一个当前 Mac 已安装的字体家族并选择，开始练习并切换光标样式；再手填 PostScript 名，并分别选择用户拥有的 TTF、OTF、WOFF、WOFF2 文件。另选择改名伪造扩展名的无效文件，确认旧字体仍在；最后移除本地文件并重开设置 | 浏览页按名称排序、忽略大小写和重音搜索，可选择字体或恢复系统设计；已安装字体同时用于提示和光标定位。四种本地格式均经 Core Text 成功读取并注册后覆盖名称选择，无效或当前系统不能加载的文件明确拒绝且不替换旧字体；移除后回退。名称按设置保留，本地文件只保留在当前 Mac、不随归档迁移、不安装或上传字体 | 待验收（目录清洗/搜索、名称解析、四种扩展名选择器白名单、无效文件拒绝、缺失回退与归档边界由客户端测试覆盖；本批以自生成 WOFF/WOFF2 做系统注册探针，依用户要求不启动 GUI） |
| SND-01 | 三类提示音音型与试听 | 在设置分别开启键击、错误和倒计时提示音；逐一试听 26 种键击音型，再为错误和倒计时各选一种音型试听；将倒计时设为结束前 1 秒，开始 5 秒计时练习，分别正确输入和故意输错，再关闭并重开设置 | 26 种键击音型均可选且听感可区分，错误与倒计时各有四种音型；试听不改变练习状态；键击只在接受输入后播放、错误只在错误数增加后播放、倒计时只在所选秒数播放一次；选择与音量在重开后保留，声音不可用时输入和计分仍正常 | 待验收（键击目录、合成 WAV、设置快照往返和倒计时单次触发策略已自动化覆盖；依用户要求不启动 GUI） |
| FUN-25 | 无空格结果词历史 | 启用“无空格”，以自定义 `amber harbor` 完成一轮，首词故意输错；展开结果单词历史与错词练习菜单 | 提示仍连续显示；结果保留两段目标/实际输入和各自 Burst；错词菜单只列首词，热力图与慢词入口按词段而非整段文本计算 | 已验收（QA45；热力图和慢词选择由自动化覆盖） |
| FUN-26 | 无空格字数进度 | 选择英语字数 2 词，启用“无空格”并重开；输入首词，再完成次词 | 连续提示下的实时进度从 `0/2` 变为 `1/2`，完成后结果单词历史为 `2/2 正确`，不依赖重新插入空格 | 已验收（QA46） |
| FUN-27 | 无空格 expert 词级失败 | 选择 expert 与“无空格”，以自定义 `amber harbor` 开始，首词输入一个错误字符并输入至词末 | 不等待空格或整个连续提示结束；首词末字符立即显示失败，且本次不保存为完成成绩 | 已验收（QA47） |
| FUN-28 | 无空格的输入边界 | 选择 normal 与“无空格”、遇错停下“单词”，以自定义 `amber harbor` 开始；先输入普通空格与全角空格，再将首词输为 `amxer` | 两种空白都不进入提示、不计错误；错误词末字符被拒绝，提示仍停在首词，删去错误再补全后才进入下一词 | 部分验收（QA48；实际退格修正由自动化覆盖） |
| INP-11 | Unicode 空白与严格空格 | 以 normal 自定义 `amber harbor` 开始，先在词首输入全角空格、再在词间输入全角空格；重开后开启严格空格，在词首输入全角空格 | normal 词首空白不进入输入、不计错；词间全角空格按普通空格提交；严格空格保留词首空白为一个可退格修正的错误，expert/master 也不会静默忽略该键 | 部分验收（QA49；Unicode 注入由自动化覆盖） |
| FUN-03 | 遇错清除当前词 | 在英语字数模式启用修饰器，正确输入首词及次词首字母，再输入一个错误字符 | 仅当前未提交词被清空，已提交词保留；重新输入可完成 | 待验收 |
| INP-06 | 信心模式 | 在设置依次选择“开启”和“最大”，在英语字数模式输入一个错误词后尝试退格 | 开启档不可退回修改上一错误词；最大档不可退格；与自由回退、遇错停下/删除互斥 | 待验收 |
| INP-08 | 反向 Shift | 分别启用“开启”和“按键位图”；以同侧与对侧 Shift 输入左/右手字符，并输入 Y、B、6；按键位图档再以 QMK 等外设重映射一个已知字符 | 同侧 Shift 作为一次错误计分但保留实际文本；对侧通过；Y/B/6 两侧均可用；按键位图按所选布局的原键位侧判定 | 待验收（策略、会话、回放由自动化覆盖） |
| INP-09 | 遇错停下与删除档位 | 在英语两词练习中依次选择“遇错停下”的字符/单词，以及“遇错删除”的字符、单词、字符（硬）、单词（硬）；于新词首字符处故意输错 | 字符停下拒绝误键；单词停下允许继续输入但拒绝错误词的空格；字符删除退回一个已接受字符，单词删除清空当前词；两个硬档分别返回上一词末尾或清空上一词；切换错误规则会关闭另一类和信心模式 | 已验收（QA38；全部档位策略、迁移与回放由自动化覆盖） |
| INP-10 | 最低单词 Burst 固定/弹性 | 在设置选择“固定”和“弹性”，调节 WPM 后关闭再打开设置；对同一较长词以低于固定阈值、但高于弹性阈值的可测 burst 提交；启用无空格后重复首词与次词的同类输入 | 三档可选且 WPM 持久化；固定按设定 WPM 判定，弹性随目标词长降低阈值；正确和错误词都会检查；无空格模式在每个原始词的末字符检查，不重新插入空格 | 已验收（QA39、QA44；无空格边界、循环和弹性词长由自动化覆盖） |
| CNT-04 | 代码语言与缩进 | 选择 Code · Swift、Code · JavaScript、Code · Python；输入一个含换行的片段，在自动插入的 Tab 行按退格，再完整输入片段 | 三种原创片段可开始；正确换行自动插入目标 Tab；开启“代码：退格反缩进”后退格删除缩进并返回上一行；有限测试完成后回放文本一致 | 已验收（隔离 release 候选） |
| CNT-05 | 官方代码选择集 | 打开语言菜单，选择 Code · Dockerfile、Code · CUDA、Code · VHDL、Code · 6502 Assembly 与任一已有代码语言 | 70 个官方当前代码语言/方言均可选择；每项生成非空的 Typebar 原创片段并可开始输入；不导入官方语料 | 部分验收（既有 69 项已验收；Dockerfile 自动化通过、菜单待隔离候选包） |
| VIS-03 | 单行卷带与完整行 | 选中英语字数模式，将“单行卷带”切为按词和按字符，调整停靠位置并输入多个词；关闭平滑滚动后再输入；关闭卷带后启用“显示完整提示行”，分别检查计时和无计时的字数/引语/自定义模式 | 当前输入位置分别按词/按字符保持在选定水平位置；关闭平滑后立即跳转；卷带激活时完整行选项不可用；完整行只在无计时字数、引语和自定义模式展开，计时模式保持有限、可滚动练习区；输入与指标不变 | 已验收（隔离 release 候选） |
| VIS-04 | 实时指标颜色与透明度 | 在设置选择“黑色”和 50% 透明度，开始英语练习并输入一个词；再选择主题强调色和 100%，关闭重开设置 | 进度、速度、Raw、Burst、准确率同步改变颜色/透明度；错误数与段落进度保持默认可读性；设置重开后保留，输入、计分、结果与回放不变 | 待验收（设置归档往返由自动化覆盖） |
| VIS-05 | 提示高亮范围 | 在常规英语字数测试依次选择“当前字符”“当前词”“当前词加后续 1/2/3 个词”和“关闭”，输入若干字符；再启用卷带、无空格、听写、记忆或任一预读遮挡模式并选择词范围 | 常规提示只突出所选的目标范围；关闭时不突出当前目标；特殊呈现中词范围降级为当前字符，输入、计分、结果与回放不变；设置重开后保留 | 待验收（范围策略与设置归档由自动化覆盖） |
| VIS-06 | 键盘提示模式、大小、图例、按键集与样式 | 在设置依次选择静态、按键反馈、下一键和关闭；在按键反馈中分别输入一个正确和错误字符；把大小调至 0.5、1.0、3.5；依次选择小写、大写、空白和动态图例，并按住 Shift、切换 Caps Lock 后观察字母、数字与标点；在 QWERTY 精简档观察数字行、切换到精简（数字行）与完整档，再选 AZERTY 精简档；依次选择错列、人体工学、矩阵、分体错列、分体矩阵、速录、速录矩阵；启用听写与 Simon 指令复查 | 静态不高亮键位；按键反馈以约 180ms 的强调色/红色短暂标记最近正确/错误按键；下一键跟随目标；键盘按所选倍率重排；动态图例随 Shift/Caps Lock 更新，数字和标点仅在 Shift 按下时显示 Shift 符号；空白档不显示图例。QWERTY 精简档隐藏数字行、下一键为数字时显示，精简（数字行）始终显示，完整档显示不可输入的功能键图示，AZERTY 精简档保留数字行。七种样式均立即改变提示几何；速录两档展示独立速录键位图示但不改变普通 macOS 键盘输入。除 Polish (Programmers) 明确定义的 Option/Shift+Option 层外，Option 字符仍由 macOS 原生输入处理，不显示未建模的 Option 图例。关闭隐藏键盘，听写也隐藏，Simon 始终强制下一键；设置重开后保留，输入、计分、结果与回放不变 | 待验收（模式/大小/图例/按键集/样式策略、归档迁移由自动化覆盖） |
| VIS-07 | 完成后随机主题 | 依次选择全部、收藏、浅色、深色、自定义和“跟随当前系统深浅色”；完成多轮有效测试，分别观察切换前后的主题。随后重启应用，并先后开启“跟随 macOS”与任一随机范围 | 每次完成后只从所选范围轮换且同一轮次池不重复；收藏范围含内置和自定义收藏，自动范围只选当前系统色调的内置主题；随机结果不会改写手动主题，重启后恢复手动选择；跟随 macOS 与随机范围互斥，旧“重开随机”偏好显示为“全部主题” | 待验收（范围、轮换、持久化隔离与旧归档迁移由客户端测试覆盖） |
| VIS-10 | 系统外观主题目标 | 先选择一个手动或自定义主题；开启“跟随 macOS 深浅色自动切换”，将浅色目标设为纸白、深色目标设为林地，再切换 macOS 外观或在两个外观下观察练习页；关闭开关并重开设置 | 自动切换时分别应用两个选定的内置主题；变更当前外观对应的目标立即更新；关闭后恢复原来的手动或自定义主题。两个目标和开关会随设置归档持久化，且与随机主题互斥 | 待验收（目标选择、互斥与设置归档由客户端测试覆盖） |
| VIS-08 | 翻转与彩色测试文字 | 在普通英语练习中分别关闭、单独开启和同时开启“翻转已输入与后续文本颜色”“彩色测试文字”，每种组合各输入一个正确字符与一个错误字符，再重开设置 | 默认时已输入文本更亮；翻转时后续提示更亮；彩色时已输入文本使用当前主题强调色；组合时后续提示使用强调色。错误和额外输入仍显示错误色，输入、计分、结果与回放均不改变，重开设置后保留 | 待验收（四种颜色策略与归档往返由客户端测试覆盖） |
| VIS-09 | 本地/远程自定义背景 | 分别填写一个有效 HTTPS PNG/JPG/GIF/WebP URL、无效 URL，并选择本地图片；依次选择覆盖、完整显示、填满并调整四项滤镜；重开设置后移除本地图片和 URL | 仅合规 URL 可应用，无效 URL 给出说明；本地图片优先于 URL，导入后背景即时更新。三种适配和滤镜即时改变图片呈现；移除本地图片后回退到 URL，移除 URL 或加载失败后回退原生背景。远程 URL、适配与滤镜持久化，本地图片只在本机，不上传也不在导出文件中 | 待验收（URL 白名单、滤镜归一化和设置归档由客户端测试覆盖） |
| CFG-03 | 快速重开键 | 在设置选择 Tab 和 Enter，回到英语练习区分别按下选定键 | 练习立即重开；关闭后不触发；⌘R 始终可重开 | 已验收（Tab/Enter；关闭与 ⌘R 由自动化覆盖） |
| CFG-09 | 页面快捷键提示 | 在设置关闭“显示快捷键提示”，回到未获焦的练习页；重新开启，依次选择关闭、Tab、Enter 与 Esc 快速重开，检查命令提示并按下对应动态入口；最后进入含 Tab 的自定义文本和禅模式，输入普通 Tab 后按 ⇧Tab，再按住 ⇧⌘P | 关闭时不显示提示；开启时，前三种快速重开显示 Esc / ⇧⌘P / ⇧⌘K，Esc 快速重开则显示 Tab / ⇧⌘P / ⇧⌘K；含 Tab 提示与禅模式显示 ⇧Tab 并保留普通 Tab 输入。各入口只打开一次面板，对应命令键不进入输入、计分、回放或按键活动；输入获焦时提示淡出、失焦后显示，重开设置后偏好保留 | 待验收（动态策略、设置归档、旧归档默认值及输入桥快捷键隔离由客户端测试覆盖；依用户要求不启动 GUI） |
| TST-05 | 引语重开策略 | 开启“重开时重复当前引语”，输入首字母后重开；完成后再重开 | 输入中重开保留当前引语；完成后重开换一条 | 已验收（隔离 release 候选） |
| TST-06 | 简化重音输入 | 选择德语或葡语并启用“简化重音输入” | 提示中的重音、变音和 ß 显示为普通拉丁字母；可输入完成 | 已验收（隔离 release 候选） |
| MET-05 | 节奏第二光标 | 设置自定义节奏，依次选择关闭、条形、轮廓、下划线、块状、胡萝卜、香蕉和小猴八档“节奏光标样式”并开始输入 | 关闭时不绘制第二光标；其余七档在当前输入光标之外显示独立目标位置标记，切换样式立即改变原创原生呈现；主光标样式不被改写 | 待验收（八档目录与固定 schema 对账由客户端测试覆盖） |
| INP-12 | 错误字符显示 | 设置为“替换并在下方显示目标”，在英语测试中故意输错一个字母 | 主字形显示实际输入，下面显示目标字形；切换到关闭恢复只显示目标字形；指标与回放不变 | 待验收 |
| MET-06 | 速度单位 | 在设置依次选择 CPM、WPS、CPS、WPH | WPM、Raw、Burst 三项同步换算并更新标题；历史和结果仍为 WPM | 待验收 |
| MET-10 | 结果页小数 | 在设置开启“结果页固定显示两位小数”，完成一轮练习；切换 WPH 后再完成一轮并重开设置 | 结果页的速度、Raw 速度和准确率显示两位小数，速度标题随单位切换；开关持久化；保存和同步仍为规范 WPM | 已验收（隔离 release 候选；WPH 换算由自动化覆盖） |
| MET-11 | 速度图起点 | 完成至少两次不同速度练习，打开历史；关闭“速度图从零开始”后重开设置并完成一轮可显示结果轨迹的练习 | 历史趋势图与结果速度轨迹 Y 轴可不从零起算，开关持久化；完成次数、练习分钟和错误图仍从零开始 | 待验收（设置可见性与持久化已验证） |
| MET-33 | 历史曲线选择 | 完成至少三条速度和准确率不同的练习，打开历史；依次关闭速度、准确率、10 次均值和 100 次均值，再关闭并重开应用 | 速度、准确率与两个均值可独立显示；速度图含历史个人最佳包络，底部显示按实际键入时长计算的每小时变化；选择会持久化 | 待验收（均值、包络、趋势和归档往返由客户端测试覆盖） |
| MET-34 | 结束前闲置保护 | 开始 10 秒限时练习，输入一个字符后静置至结束；再重开并在最后 5 秒内再输入一个字符 | 第一轮计时器不暂停，结果显示“闲置无效”、总用时、闲置秒数及百分比、有效键入时长，且不写入历史、统计、同步或发布；第二轮正常完成并可保存 | 待验收（一秒区间、结束判定、归档兼容和有效时长统计由客户端测试覆盖） |
| MET-04 | 历史时间、难度、内容、档位与修饰器 | 准备在最近 24 小时边界内、边界外及最近 7/30/90 天的本机完成成绩，其中至少各有普通、专家与大师难度，且覆盖仅标点、仅数字、两者都有和两者都无的内容配置，并完成短、中、长与超长引语，以及 15/30/60/120 秒、10/25/50/100 词和各一条自定义档位；再分别完成无修饰器、仅一个修饰器和多个修饰器的成绩。在历史页依次选择五档时间范围、三档难度、标点/数字的包含、不包含/全部、四档引语长度，以及时长和字数的任意多选组合（含仅自定义和全部取消）；最后多选修饰器、单选无修饰器，保存一个非“全部”的筛选预设后切换并重开该预设。 | 时间范围使用滚动 24 小时、7 天、30 天和 90 天窗口，边界成绩包含在内；难度、两个内容条件、实际完成的引语长度、时长/字数档位和修饰器均只保留相应配置的成绩，修饰器多选按任一命中。列表、汇总、图表和热力图同步收窄。筛选预设会恢复所有条件，旧预设按全部时间、全部难度且不限制内容选项、引语长度、档位或修饰器打开。 | 待验收（时间边界、筛选合取、实际引语长度、档位多选、修饰器任一命中和旧预设解码由客户端测试覆盖） |
| MET-50 | 筛选联动的账户统计摘要 | 准备至少十一条 WPM、Raw、准确率、稳定度和有效键入时长不同的本机完成成绩，其中一条带有已保存的 AFK 时长；另开始一轮并在输入后重开三次，再完成该轮。打开历史后先查看全部，再用任一筛选排除一部分，并依次切换 WPM、CPM、WPS、CPS、WPH；导出 CSV 和 v3 归档后重新导入。 | 摘要随筛选同步显示开始次数、完成数及向下取整的完成率、保留一位小数的每次完成重开比、有效键入时长、逐条估算后求和的词数，以及 WPM、Raw、准确率、稳定度各自的最高、平均和最近 10 次。开始次数等于完成数加筛选结果中的重开数；未输入即重开不增加计数。估算词数使用与 WPM 相同的完整测试时长，不因 AFK 再扣一次。最近 10 次按完成时间倒序选择，速度与 Raw 的标题和值随当前单位变化。CSV 的 `restart_count` 和归档导入后的值均为 3；无匹配结果时统计归零，不以旧摘要冒充当前筛选结果。 | 待验收（开始/完成派生、旧记录默认 0、逐条词数四舍五入、AFK 时长边界、最高/平均、倒序最近十条、空集合、CSV、v2/v3 归档与旧 SwiftData store 副本迁移由客户端自动化覆盖；依用户要求本轮不启动 GUI） |
| MET-51 | 筛选联动的每日活动指标 | 在相邻三天分别准备 0、1、2 条本机完成成绩；第三天的两条成绩使用不同速度、准确率、稳定度并合计重开三次。打开历史的近 28 日图，依次选择完成次数、练习分钟、平均速度、最高速度、平均准确率、平均稳定度和每次完成重开；关闭“图表从零开始”，切换一个会排除其中一条成绩的筛选，再依次切换 WPM、CPM、WPS、CPS、WPH。 | 无成绩日期仍占据日期轴；计数、分钟和重开比为 0，平均/最高类不出现伪造的零值柱。两条成绩所在日分别显示未舍入的日均值、最高速度及 `1.5` 次重开/完成，筛选后所有值只由剩余成绩重算。速度轴标题和值随单位切换，关闭从零开始后按真实速度范围缩放；其他百分比和次数不换算。VoiceOver 可读出每根有值柱的日期和当前指标值。 | 待验收（按日分组、小数均值、重开比、统计日界和空白日期传播由客户端自动化覆盖；依用户要求本轮不启动 GUI） |
| MET-52 | 历史排序与渐进显示 | 准备至少 25 条本机成绩，其中两条速度相同但完成时间不同。打开历史，依次按日期、速度、Raw、准确率和稳定度切换升降序；点击两次“再显示”，再改变筛选和排序；最后按当前排序导出 CSV，并从当前可见页删除一条非首条成绩。 | 初始只显示 10 条且日期最新在前；每次增加最多 10 条，25 条时依次为 10/20/25。五列升降序都正确，同速度项以较新成绩在前且刷新不跳位。筛选或排序后回到最多 10 条；摘要和图表仍包含全部匹配项。CSV 包含全部匹配项并遵循当前排序，不只导出可见页；删除的是点击行对应成绩。 | 待验收（五字段双向排序、同值稳定决胜、分页边界由客户端自动化覆盖；依用户要求本轮不启动 GUI） |
| MET-53 | 单位感知的速度直方图 | 准备速度落在低档、中间空档和高档的至少五条成绩，在历史中依次切换 WPM、CPM、WPS、CPS、WPH，并用 VoiceOver 检查柱形；再导入一条异常超大 WPM 的合法归档成绩。 | 每种单位都显示对应单位名和 10/50/0.5/5/250 档宽；分布从零档开始，中间没有成绩的档仍可见，所有柱计数之和始终等于当前筛选结果数。小数单位使用无歧义的半开区间；异常值不会卡死历史窗口，而进入最后一个“≥”档。切换单位不改历史成绩、CSV 或统计中的规范 WPM。 | 待验收（五单位换算、零起点空档、边界计数守恒与 512 档资源上限由客户端自动化覆盖；依用户要求本轮不启动 GUI） |
| MET-54 | 历史趋势图成绩选择 | 准备三条完成时间和指标不同的成绩，其中选择时间可落在两条正中。打开历史，在速度图和准确率图中分别拖动选择，切换列表排序并加载更多，再用“查看成绩”“清除选择”；最后选择一条后改变筛选，并重新选择后删除该成绩。 | 两图选择同一最近成绩并显示同步规则线；正中位置稳定选较新项。摘要显示速度、Raw、准确率、稳定度、日期、模式参数、语言、难度、标点/数字及 PB，查看按钮打开对应详情。排序/分页不改变选择；清除、筛选变化和删除所选成绩都会移除规则线与摘要，不跳到相邻成绩。VoiceOver 提示图表可拖动选择。 | 待验收（最近点、等距决胜与空集合由客户端自动化覆盖；macOS 14 API 可用性由目标部署编译验证；依用户要求本轮不启动 GUI） |
| MET-55 | 图表当前筛选摘要 | 打开默认历史，再依次组合最近 30 天、计时/字数、15 秒/自定义时长、空字数选择、专家、包含标点、无数字匹配、英西双语、短/长引语、一个修饰器、focus 标签和排除 PB；横向滚动并用 VoiceOver 读取。最后应用一个旧单语言筛选预设。 | 默认只显示“筛选：全部成绩”。组合后只出现十二类真实限制，顺序固定，空字数明确显示“无匹配档位”；长标签不会撑宽窗口，每项可独立朗读。摘要与列表、统计和图表条件一致，旧预设显示其有效单语言约束；查看摘要不会改变筛选。 | 待验收（默认折叠、复杂筛选顺序、无匹配状态及 effective 旧预设视图由客户端自动化覆盖；依用户要求本轮不启动 GUI） |
| MET-56 | 每日练习分钟趋势线 | 准备至少三个不同日期的本机成绩，中间留一个无成绩日期且每日有效练习分钟整体递增。打开历史，将近 28 日指标切换为练习分钟，再切换为完成次数和平均速度，并用 VoiceOver 检查趋势端点。 | 练习分钟图显示一条次要色虚线，按实际有成绩日期的分钟值拟合并只覆盖首末有效日；中间空白日不会被当作零分钟拉低趋势。其他指标不显示该线；不足两个有效日期时安全隐藏。趋势端点可朗读，切换不会更改成绩或筛选。 | 待验收（真实日期间隔、空白日排除、乱序输入、非有限值及不足两点由客户端自动化覆盖；依用户要求本轮不启动 GUI） |
| MET-57 | 历史行信息摘要 | 准备一条 30 秒成绩、一条无限字数成绩和一条旧配置缺失成绩；为首条添加 `Focus`、`focus`、`review` 标签并确保四类字符统计均非零。将历史窗口保持最小宽度，查看各行、悬停长摘要并打开详情。 | 行首显示“时间 30 秒”或“字数 无限”，旧记录显示“未知”；字符顺序固定为匹配/错位/额外/跳过，大小写重复标签只保留首次显示形式。长内容不撑宽窗口，悬停可见全文；Raw、稳定度、PB、行点击、删除、排序与分页保持可用，详情数据与行摘要一致。 | 待验收（模式参数、无限/未知回退、字符顺序和标签规范化由客户端自动化覆盖；依用户要求本轮不启动 GUI） |
| MET-36 | 历史快捷筛选 | 将练习区切换到 60 秒、西班牙语、专家、包含标点、无数字和一个修饰器；准备匹配及每项都不同的本机成绩。打开历史先点击“当前测试设置”，再修改任意筛选项并点击“全部”。 | 当前设置只保留匹配模式、语言、难度、内容、时长/字数档、引语长度和任一所选修饰器的成绩；已附成绩标签不影响该快捷筛选。点击全部会恢复所有本机成绩。 | 待验收（当前配置到筛选条件的映射和匹配由客户端测试覆盖） |
| MET-37 | 历史多选语言 | 准备英文、西班牙语、德语及一条无法读取配置的旧成绩。在历史页仅勾选英文和西班牙语，随后全选、全部取消；保存一个多选预设后重开应用，再应用一个旧单语言预设。 | 两种选中语言按任一命中；全选保留所有成绩（包括无法解码语言），全不选不显示成绩；新预设恢复多选集合，旧预设仍只显示其原语言。 | 待验收（多选、全选回退与旧预设解码由客户端测试覆盖） |
| MET-38 | 历史多选标签 | 准备无标签、仅“focus”、仅“review”和同时有两个标签的成绩。历史页选择“无标签 + focus”、仅 review、全部取消，保存该多选预设并重开；再应用旧单标签预设。 | 无标签加 focus 显示无标签、focus 和双标签成绩；仅 review 显示 review 与双标签成绩；全不选为空；多选预设恢复集合，旧预设保持单标签含义。 | 待验收（动态选择、任一命中和旧预设解码由客户端测试覆盖） |
| MET-39 | 历史个人最佳四态 | 准备一条个人最佳与一条非个人最佳成绩。历史页依次选择全部、仅个人最佳、排除个人最佳、无匹配项；保存“排除”预设后重开，并应用一个旧“仅个人最佳”预设。 | 四个状态分别显示两条、仅 PB、仅非 PB、空列表；新预设恢复“排除”，旧预设仍恢复“仅 PB”。 | 待验收（四态匹配与旧布尔预设解码由客户端测试覆盖） |
| MET-40 | 历史多选模式 | 准备计时、字数、引语三条成绩。历史页仅选计时与字数，再全选、再全部取消；保存双选预设并重开，再应用一个旧单引语预设。 | 双选仅显示计时和字数；全选显示全部；空选显示空列表；新预设恢复双选，旧预设仍只显示引语。 | 待验收（多选匹配与旧单选预设解码由客户端测试覆盖） |
| MET-41 | 历史多选难度 | 准备普通、专家、大师三条成绩。历史页仅选普通与专家，再全选、再全部取消；保存双选预设并重开，再应用一个旧单大师预设。 | 双选仅显示普通和专家；全选显示全部；空选显示空列表；新预设恢复双选，旧预设仍只显示大师。 | 待验收（多选匹配与旧单选预设解码由客户端测试覆盖） |
| MET-42 | 历史标点与数字四态 | 准备分别含和不含标点、数字的成绩。对标点和数字各自选择全部、包含、不包含、无匹配项；保存一个数字“无匹配项”预设并重开。 | 三个常规状态按内容保留对应成绩；无匹配项均显示空列表；新预设恢复无匹配项。 | 待验收（四态匹配与预设往返由客户端测试覆盖） |
| MET-49 | 历史多选引语长度 | 准备短、中、长引语与一条非引语成绩。仅选择短和长，再全选、再全部取消；保存双选预设并重开，再应用一个旧单长预设。 | 双选显示短、长和非引语；全选显示全部；空选仅显示非引语；新预设恢复双选，旧预设仍只显示长引语。 | 待验收（多选、非引语回退桶及旧预设解码由客户端测试覆盖） |
| MET-44 | 完成页本机标签 | 完成一轮开启“保存完成成绩”的练习，在完成页添加 `focus`、`café`，再点按移除其中一个并打开历史详情；随后关闭保存完成成绩完成一轮。 | 标签立即显示并写入同一条本机历史；大小写/重音重复、空白、超长和超过五个均不会新增。历史详情展示相同标签；未保存结果不显示标签入口，也不写入历史、同步或远端成绩。 | 待验收（标签归一化、SwiftData 往返和仅完成/保存时持久化由客户端自动化覆盖） |
| VIS-05 | German QWERTZ 布局 | 在设置中选择 German QWERTZ 并打开动态键盘提示；依次练习 `z y ü ß < >`，用 ISO 键盘输入 `< >`，重开应用后查看设置。 | 键盘提示分别高亮 Z、Y、Ü、ß 和 ISO 键；Shift 后数字 `2` 显示 `"`；输入正确计分，设置会恢复为 German QWERTZ。 | 待验收（物理 keycode 与提示映射由客户端测试覆盖） |
| VIS-06 | Swiss German / Swiss French 布局 | 分别选择 Swiss German、Swiss French 并打开动态键盘提示；练习 `z è é ü ö ç £ < >`，检查 Shift 后 `1`。 | 两种布局都高亮 ISO 键；Swiss German 的 `ü`/`è` 与 `ö`/`é` 共享对应物理键，Swiss French 的 `è`/`ü` 与 `é`/`ö` 顺序相反；Shift 后 `1` 显示 `+`，重开后所选布局保留。 | 待验收（物理 keycode 与提示映射由客户端测试覆盖） |
| VIS-07 | UK / Spanish / Italian QWERTY 布局 | 分别选择 UK、Spanish、Italian QWERTY 并打开动态键盘提示；练习 `£ # ~ @ ñ ¿ è ò à ù ç < >`。 | UK 正确显示/高亮 `£ # ~ @`；Spanish 正确显示 ñ 和 ¿；Italian 正确显示 è/ò/à/ù/ç 与 ISO 键；所选布局重开后保留。 | 待验收（物理 keycode 与提示映射由客户端测试覆盖） |
| VIS-08 | Portuguese QWERTY（ISO / ANSI）布局 | 分别选择 Portuguese QWERTY (ISO)、Portuguese QWERTY (ANSI) 并打开动态键盘提示；练习 `ç º ª « » < > ~ ^`。 | ISO 正确显示 `«`/`»` 与 `< >` section 键，ANSI 在数字行显示 `< >` 且顶行显示 `~ ^`；两种布局均正确高亮 `ç`、`º`、`ª`，重开后所选布局保留。 | 待验收（物理 keycode 与提示映射由客户端测试覆盖） |
| VIS-09 | Latin American QWERTY 布局 | 选择 Latin American QWERTY 并打开动态键盘提示；练习 `| ° ñ ¿ ¡ [ < >`，再按该布局空白死键。 | 前述字符显示、提示和输入正确；ISO `< >` 键正确；空白死键仍由 macOS 输入法处理，不被 Typebar 误转为空格或其他字符。 | 待验收（物理 keycode 与提示映射由客户端测试覆盖） |
| VIS-11 | Turkish Q 布局 | 选择 Turkish Q 并打开动态键盘提示；练习 `ğ ü ı İ ş ö ç , ;`，使用 ISO 键盘输入 `,` / `;`，重开应用后查看设置。 | 提示分别高亮对应土耳其字母和 ISO 键；物理键码输出与目标一致，重开后 Turkish Q 仍作为键盘图、输入模拟和 Layout Fluid 可选项。布局 JSON、死键和 Option 层不由 Typebar 导入或模拟。 | 待验收（字符、ISO 标点与归档由客户端测试覆盖） |
| VIS-12 | Russian JCUKEN 布局 | 选择 Russian JCUKEN 并打开动态键盘提示；练习 `ё й ж э я ь ю ,`，使用物理 `Ё`、`Й`、`Ж`、`Ь`、`.` 和 ISO `< >` 键，重开应用后查看设置。 | 提示分别高亮 Cyrillic、逗号和 ISO 键；物理键码输出与目标一致，重开后 Russian JCUKEN 仍作为键盘图、输入模拟和 Layout Fluid 可选项。映射为 Typebar 独立实现，不导入布局 JSON；死键和 Option 层仍交给 macOS。 | 待验收（字符、Shift、ISO 标点与归档由客户端测试覆盖） |
| VIS-13 | Ukrainian JCUKEN 布局 | 选择 Ukrainian JCUKEN 并打开动态键盘提示；练习 `ґ ї і є я ь ю ,`，使用物理 `Ґ`、`Ї`、`І`、`Є`、`.` 和 ISO `< >` 键，重开应用后查看设置。 | 提示分别高亮乌克兰语特有 Cyrillic、逗号和 ISO 键；物理键码输出与目标一致，重开后 Ukrainian JCUKEN 仍作为键盘图、输入模拟和 Layout Fluid 可选项。映射为 Typebar 独立实现，不导入布局 JSON；死键和 Option 层仍交给 macOS。 | 待验收（字符、Shift、ISO 标点与归档由客户端测试覆盖） |
| VIS-14 | Bulgarian Cyrillic · Typebar 布局 | 选择 Bulgarian Cyrillic · Typebar 并打开动态键盘提示；练习 `я ъ щ ч ь ?`，使用物理 `Q`、`Y`、`]`、`;`、`X`、`/` 和 ISO `< >` 键，重开应用后查看设置。 | 提示分别高亮 Typebar 自写的西里尔和 ISO 键；物理键码输出与目标一致，重开后该布局仍作为键盘图、输入模拟和 Layout Fluid 可选项。它不是官方或系统 BDS 布局的导入；需要精确系统布局时仍选 macOS 当前输入源。 | 待验收（字符、Shift、ISO 标点与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-15 | Serbian Cyrillic · Typebar 布局 | 选择 Serbian Cyrillic · Typebar 并打开动态键盘提示；练习 `љ ђ ћ ж џ _`，使用物理 `Q`、`]`、`'`、`Z`、`X`、`/` 和 ISO `< >` 键，重开应用后查看设置。 | 提示分别高亮 Typebar 自写的塞尔维亚语西里尔和 ISO 键；物理键码输出与目标一致，重开后该布局仍作为键盘图、输入模拟和 Layout Fluid 可选项。它不是官方或系统塞尔维亚语布局资产的导入；需要精确系统布局时仍选 macOS 当前输入源。 | 待验收（字符、Shift、ISO 标点与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-16 | Hungarian QWERTZ · Typebar 布局 | 选择 Hungarian QWERTZ · Typebar 并打开动态键盘提示；练习 `í ő ű é á ö ü ?`，使用物理 `Z`、`P`、`\\`、`;`、`'`、`.`、`/` 和 `=` 键，重开应用后查看设置。 | 提示分别高亮 Typebar 自写的 Magyar 重音元音、QWERTZ Y/Z 和练习标点；物理键码输出与目标一致，重开后该布局仍作为键盘图、输入模拟和 Layout Fluid 可选项。它不导入官方或系统匈牙利语布局资产；未收录的死键和 Option 层仍由 macOS 当前输入源处理。 | 待验收（字符、Shift、标点与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-17 | Greek Alphabetic · Typebar 布局 | 选择 Greek Alphabetic · Typebar 并打开动态键盘提示；练习 `α π ω ί ς ! ?`，使用物理 `Q`、`D`、`'`、`C`、`M`、`1`、左上角 grave 键和 Shift，重开应用后查看设置。 | 提示分别高亮 Typebar 自写的基本/重音 Greek 字符、词末 sigma 与练习标点；物理键码输出与目标一致，重开后该布局仍作为键盘图、输入模拟和 Layout Fluid 可选项。它不是官方或 macOS 希腊语布局资产的导入；未收录字符、死键和 Option 层仍由 macOS 当前输入源处理。 | 待验收（字符、Shift、词库覆盖与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-18 | Polish (Programmers) Option 布局 | 在设置中选择 Polish (Programmers) 并打开动态键盘提示；分别按 Option+`A/E/L/Z/X/C/N/O/S/U` 与 Shift+Option+`A`，再用 Control+Option+`A`；将其加入 Layout Fluid 后重开。 | Option/Shift+Option 动态显示并输入 `ą/ę/ł/ż/ź/ć/ń/ó/ś/€` 与 `Ą`；Control+Option 不被模拟而继续由 macOS 处理；设置与 Layout Fluid 序列持久化。映射独立编写，不导入 Monkeytype 布局 JSON。 | 待验收（提示、动态图例、输入、反查、Control+Option 边界、归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-19 | Swedish QWERTY 布局 | 在设置中选择 Swedish QWERTY 并打开动态键盘提示；练习 `å ö ä ½`，使用物理 `Å`、`;`、`'`、`§` 与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别高亮瑞典字符与数字行 `½`，物理键码输出与目标一致，设置与 Layout Fluid 序列持久化。死键和 Option 层仍由 macOS 处理；映射独立编写，不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-20 | Danish QWERTY 布局 | 在设置中选择 Danish QWERTY 并打开动态键盘提示；练习 `å æ ø §`，使用物理 `Å`、`;`、`'`、`½` 与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别高亮丹麦字符与数字行 `§`，物理键码输出与目标一致，设置与 Layout Fluid 序列持久化。死键和 Option/AltGr 层仍由 macOS 处理；映射独立编写，不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-21 | Norwegian QWERTY 布局 | 在设置中选择 Norwegian QWERTY 并打开动态键盘提示；练习 `å ø æ`，使用物理 `Å`、`;`、`'` 与 Shift；将其加入 Layout Fluid 后重开。 | 提示、物理键码和反查与 Nordic 兼容映射一致；设置与 Layout Fluid 序列持久化，旧 Nordic 选项可继续使用。死键和 Option/AltGr 层仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-22 | ANSI Colemak-DH 布局 | 在设置中选择 ANSI Colemak-DH 并打开下一键提示；练习 `b g d h z`，使用物理 QWERTY `T/G/C/M/B` 与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别定位 Colemak-DH 的 B/G、D/V、H/M 与 ANSI angle 的 X/C/D/V/Z 键，物理键码输出与反查一致，设置与 Layout Fluid 序列持久化。ISO/wide/DH 其他变体仍由系统或自定义布局处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-23 | Turkish F 布局 | 在设置中选择 Turkish F 并打开下一键提示；练习 `ğ ı i İ ü ö ç ş`、数字行和 ISO `< >`，使用物理 QWERTY `E/R/S/G/X/B/'` 与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别定位 Turkish F 的特有字母、数字行与 ISO 键，`I` 与 `İ` 的 Shift 行不互换，物理键码输出与反查一致，设置与 Layout Fluid 序列持久化。AltGr 与组合式死键仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-24 | Bulgarian Phonetic Traditional 布局 | 在设置中选择 Bulgarian Phonetic Traditional 并打开下一键提示；练习 `ч я ъ ш щ ю ь ѝ № € < >`，使用实体 Backtick、Q、Y、[、]、\\、ISO 键、X、E、6、`,`、`.` 与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别定位字母、数字行、ISO 物理键上的 `ю` 与 `< >` 标点，`ь` 的 Shift 输出为 `ѝ`，物理键码与反查一致，设置与 Layout Fluid 序列持久化。AltGr 仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-25 | Belarusian 布局 | 在设置中选择 Belarusian 并打开下一键提示；练习 `ё ў і № ' . , < >`，并验证反斜杠；使用实体 Backtick、O、C、E、]、反斜杠、斜杠、ISO 键与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别定位白俄罗斯语特有字母、数字行、右侧引号、反斜杠/斜杠和 ISO 键；物理键码与反查一致，设置与 Layout Fluid 序列持久化。Option/AltGr 仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-26 | Macedonian 布局 | 在设置中选择 Macedonian 并打开下一键提示；练习 `љ њ ѕ ѓ ж ќ ѐ џ „ ‘ ;`，使用实体 Q、W、Y、]、反斜杠、单引号、ISO 键、X、2、7、`,` 与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别定位马其顿语特有字母、ISO 键、数字引号层和区域标点；物理键码与反查一致，设置与 Layout Fluid 序列持久化。Option/AltGr 仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-27 | Pashto 布局 | 在设置中选择 Pashto 并打开下一键提示；练习 `ض ْ ۲ ځ ک ئ ظ ې ذ‌ و ، ږ / ؟`，使用实体 Q、Z、X、B、N、M、`,`、`.`、`/`、数字行与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别定位波斯-阿拉伯字母、东阿拉伯数字、组合音标、ZWJ/ZWNJ 与区域标点；普通/Shift 物理键码、反查、设置与 Layout Fluid 序列一致。Pashto ISO 键不输出字符，AltGr 和方向控制仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、组合字符、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-28 | Estonian 布局 | 在设置中选择 Estonian 并打开下一键提示；练习 `õ ü ö ä ˇ < > + ? -`，使用实体 `[`、`]`、`;`、单引号、反引号、ISO 键、`-`、`/` 与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别定位爱沙尼亚语特有字母、ISO 键、数字行与区域标点；普通/Shift 物理键码、反查、设置与 Layout Fluid 序列一致。AltGr 与组合式死键仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-29 | Persian (Standard) 布局 | 在设置中选择 Persian (Standard) 并打开下一键提示；练习 `ض ْ ۴ ﷼ ش ؤ ظ ك ر ٰ ذ‌ د ٔ پ و > . < / ؟`，使用实体 Q、4、A、Z、V、B、N、M、`,`、`.`、`/`、ISO 键与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别定位波斯字母、东阿拉伯数字、Rial sign、组合音标、ZWJ/ZWNJ、ISO 与区域标点；普通/Shift 物理键码、反查、设置与 Layout Fluid 序列一致。AltGr 与方向控制仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、组合字符、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-30 | Arabic (101) 布局 | 在设置中选择 Arabic (101) 并打开下一键提示；练习 `ذ ّ ض َ ف لإ ل لأ لا لآ ش ِ ئ ْ ظ ؟`，使用实体反引号、Q、T、G、B、A、Z、X、`/`、ISO 键与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别定位阿拉伯字母、组合音标、连字完整文本输出、ISO 与区域标点；普通/Shift 实体键、完整输出反查、设置与 Layout Fluid 序列一致。AltGr、ZWJ/ZWNJ 与方向控制仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、组合字符、连字、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-31 | Hebrew 布局 | 在设置中选择 Hebrew 并打开下一键提示；练习 `ק ר א ש ד ג ך ף צ ת ץ`，使用实体 E、R、T、A、S、D、L、`;`、M、`,`、`.`、ISO 键与 Shift；将其加入 Layout Fluid 后重开。 | 提示分别定位希伯来字母、Shift 拉丁/标点层和 ISO 键；普通/Shift 实体键、反查、设置与 Layout Fluid 序列一致。AltGr、Caps Lock 元音点和方向控制仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-32 | Arabic (102) 布局 | 在设置中选择 Arabic (102) 并打开下一键提示；练习 `ذ ّ ـ ئ ء ْ ؤ ِ ر ٍ لا لآ`，使用实体反斜杠、ISO、Z、X、C、V、B 与 Shift；将其加入 Layout Fluid 后重开。 | 提示图例与普通/Shift 实体键保留 Arabic 102 的区域差异、组合音标和连字；反查、设置与 Layout Fluid 序列一致。AltGr、ZWJ/ZWNJ 与方向控制仍由 macOS 处理；不导入 Monkeytype 布局 JSON。 | 待验收（提示、普通/Shift 输入、组合字符、连字、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-33 | Japanese Hiragana 布局 | 在设置中选择 Japanese Hiragana 并打开下一键提示；练习 `ろ あ ぁ わ を ゛ 「 ゜ 」 つ っ ね 、 る 。 め ・`，分别使用对应 ANSI 实体键和 Shift；将其加入 Layout Fluid 后重开。 | 四行 47 个物理位置均显示假名；Shift 正确产生小假名、`を`、日文括号及 `、。・`，普通/Shift 输入、反查、设置与 Layout Fluid 序列一致。Option 不由该内置模拟接管；不复制或打包参考布局资产。 | 待验收（全部 47 个普通/Shift 位置、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-34 | Colemak Angle/Wide 与 Norman 布局 | 分别选择 ANSI Colemak Angle、ANSI Colemak Wide、ANSI Norman 并打开下一键提示；Angle 练习 `x c v b z`，Wide 练习数字行 `6 = 7`、`[ ] /` 和右手字母，Norman 练习 `d k e y h p`；每项都使用 Shift，再加入 Layout Fluid 后重开。 | 三项分别显示独立排列；全部 47 个 ANSI Base/Shift 位置都有确定输出，提示、输入、反查、设置与 Layout Fluid 序列一致。Option 不由这些模拟接管；不复制或打包参考布局资产。 | 待验收（3×47 个 Base/Shift 位置、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-35 | Colemak-DHv、Programmer Workman 与 Turkish E 布局 | 分别选择 ANSI Colemak-DHv、Programmer Workman、Turkish E 并打开下一键提示；DHv 练习 `c f d k v h` 和移位标点，Programmer Workman 练习数字行的符号普通层及 `{ }`，Turkish E 练习 `ü ı ğ ö ç ş` 与 ISO `< >`；每项都使用 Shift，再加入 Layout Fluid 后重开。 | 前两项的 47 个 ANSI、Turkish E 的 48 个 ISO Base/Shift 位置均有确定输出；提示、输入、反查、设置与 Layout Fluid 序列一致。Option 不由这些模拟接管；不复制或打包参考布局资产。 | 待验收（全部 Base/Shift 位置、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-36 | MTGAP ASRT、Halmak、QGMLWB/QGMLWY 与 QWPR 布局 | 分别选择五项布局并打开下一键提示；逐项练习其主字母行，Halmak 另练习 Shift+9/0 与 Shift+逗号/句号；每项加入 Layout Fluid 后重开。 | 五项分别显示独立排列；每项 47 个 ANSI Base/Shift 位置均有确定输出，Halmak 的非标准 Shift 层不退化为 QWERTY；提示、输入、反查、设置与 Layout Fluid 序列一致。Option 不由这些模拟接管；不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-37 | Programmer 与区域 Dvorak 布局 | 分别选择 Programmer Dvorak、Programmer Dvorak Prime、German Dvorak、German Dvorak Improved、Spanish Dvorak 并打开下一键提示；两项 Programmer 重点练习全部数字行符号及 Shift，区域布局练习 `ä ö ü ß ñ ç` 与额外 ISO 键；每项加入 Layout Fluid 后重开。 | 两项 ANSI 的 47 个、三项 ISO 的 48 个 Base/Shift 位置均有确定输出；Prime、Improved 与普通版本不混用符号层，区域字符位置正确，提示、输入、反查、设置与 Layout Fluid 序列一致。Option 和死键不由这些模拟接管；不复制或打包参考布局资产。 | 待验收（全部 Base/Shift 位置、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-38 | 瑞典与法语多层布局 | 分别选择 Swedish Colemak、Swedish Dvorak、French Dvorak、French AZERTY (AFNOR)、French Bépo 并打开下一键提示；练习 `å ä ö é è ç œ`，AFNOR/Bépo 另逐行使用 Option 与 Shift+Option，AFNOR 验证 Option+Space/Shift+Option+Space；每项加入 Layout Fluid 后重开。 | 五项各 48 个 ISO 位置的 Base/Shift 正确；AFNOR/Bépo 的 Option/Shift+Option 图例和输入一致，未分配层不会回退到系统字符，AFNOR 空格层分别产生 NBSP/窄 NBSP；提示、反查、设置与 Layout Fluid 序列一致，不复制或打包参考布局资产。 | 待验收（全部物理层、空格层、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-39 | Bépo AFNOR 与 Alpha 布局 | 分别选择 French Bépo (AFNOR)、ANSI Alpha 并打开下一键提示；Bépo AFNOR 逐行练习 Base/Shift/Option/Shift+Option 与空格，重点检查 `‰ £ ɵ ’ å ọ` 及未分配层；Alpha 依次练习四行和 Shift；两项加入 Layout Fluid 后重开。 | Bépo AFNOR 的 48 个 ISO 四层位置与普通空格、Alpha 的 47 个 ANSI Base/Shift 位置均与各自提示一致；未分配 Option 层不回退到系统字符，反查、设置与 Layout Fluid 序列持久化；不复制或打包参考布局资产。 | 待验收（全部物理层、空层语义、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-40 | Hands Down ANSI 布局 | 分别选择 ANSI Hands Down、Alt、Neu 与 Neu Inverted 并打开下一键提示；逐行练习 Base/Shift，Neu 两项重点检查数字行 `? < >`、引号键的 `[ ]` 及倒置上下排；每项加入 Layout Fluid 后重开。 | 四项各 47 个 ANSI 位置均与独立排列一致，Neu 的非标准 Shift 符号能正确显示、输入与反查；设置和 Layout Fluid 序列持久化。Promethium 不出现在原生选项中，因为其独立 `R` 拇指键无法由当前标准 Mac 物理模型无损表达；不复制或打包参考布局资产。 | 待验收（4×47 个 Base/Shift 位置、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-41 | MTGAP、Ina、Soul 与 Niro 布局 | 分别选择 MTGAP、MTGAP Full、Ina、Soul、Niro 并打开下一键提示；逐行练习 Base/Shift，前三项重点检查非标准数字与标点配对，并在精简按键模式比较 MTGAP 与 Full；每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示、输入和反查一致；MTGAP Full 在精简模式仍显示数字行，普通 MTGAP 不强制显示；设置和 Layout Fluid 序列持久化，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、数字行策略、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-42 | TypeHack、ISRT 与 Engram 布局 | 分别选择 TypeHack、ISRT、ISRT Angle、Engram、Engrammer 并打开下一键提示；逐行练习 Base/Shift，TypeHack/Engram 重点检查非标准数字与标点配对，比较 ISRT 两项底行，并在精简按键模式检查 Engram 两项；每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示、输入和反查一致；ISRT Angle 保留独立底行，Engram 与 Engrammer 在精简模式仍显示数字行；设置和 Layout Fluid 序列持久化，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、数字行策略、变体差异、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-43 | Semimak 与 Canary 变体 | 分别选择 Semimak、Semimak JQ、Semimak JQC、Canary、Canary Matrix 并打开下一键提示；逐行练习 Base/Shift，比较 Semimak 三项的 J/Q/C 位置及两项 Canary 的字母与标点差异；每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示、输入和反查一致；三个 Semimak 变体和两个 Canary 变体不会互相退化，设置和 Layout Fluid 序列持久化；不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、变体差异、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-44 | Boo、APT 与 Middlemak 布局 | 分别选择 Boo、Boo Mangle、APT、APT Angle、Middlemak 并打开下一键提示；逐行练习 Base/Shift，Boo 两项重点检查 `?/`、数字符号和 `@/^`，比较 APT 两项底行，并在精简按键模式检查 Boo Mangle；每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示、输入和反查一致；Boo Mangle 在精简模式仍显示数字行，APT Angle 保留独立底行；设置和 Layout Fluid 序列持久化，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、符号层、数字行策略、变体差异、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-45 | Middlemak-NH、Foalmak、Quartz、Arensito 与 ARTS | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，比较 Middlemak-NH 与普通 Middlemak，检查 Foalmak 两个句点实体键及 Quartz 重排数字行，并将每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示和输入一致；Foalmak 两个句点键都可输入且视觉反查稳定显示第一处，Quartz 在精简模式仍显示数字行；设置和 Layout Fluid 序列持久化，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、重复输出反查、数字行策略、变体差异与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-46 | Capewell Dvorak、Colman、Heart、Klauser 与 Oneproduct | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，检查 Capewell Dvorak 重排数字行、Klauser 分散标点及 Oneproduct 的 `?`/`/` 实体键，并将每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示和输入一致；Capewell Dvorak 在精简模式仍显示数字行，Oneproduct 的问号键普通层输出 `?`、Shift 输出 `/`；反查、设置和 Layout Fluid 序列持久化，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、非标准符号层、数字行策略、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-47 | Pine、Real、Sertain、CTGAP 与 Graphite | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，检查 Real 重排数字行及 Graphite 的 `'→_`、`,→?`、`-→"`、`/→<`，并将每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示和输入一致；Real 在精简模式仍显示数字行，Graphite 的非标准 Shift 字符、下一键提示与反查落在正确实体键；设置和 Layout Fluid 序列持久化，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、非标准符号层、数字行策略、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-48 | Focal、Zenith、Dhorf、Gust 与 Recurva | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，检查 Focal、Zenith、Dhorf、Recurva 的分散标点及 Gust 重排的 `[]` 数字行，并将每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示和输入一致；Gust 在精简模式不强制显示数字行，即使下一键是 `[` 或 `]`，仍需选择“精简（数字行）”或“完整”才显示该行；实体输入、反查、设置和 Layout Fluid 序列持久化，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、数字行隐藏策略、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-49 | Pine v4、Three、Asset、Dwarf 与 Flaw | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，重点检查 Pine v4 与 Pine 的差异、Asset 主行，以及 Dwarf 的逗号/句号 Shift 输出，并将每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示和输入一致；Dwarf 的逗号输出 `>`、句号输出 `<`，反查指向对应实体键；设置和 Layout Fluid 序列持久化，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、非标准 Shift、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-50 | STNDC、UCIEA、Whorf、Whorf 6 与 Whorfmax | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，检查 STNDC、UCIEA 的重排数字行和符号输出，以及三种 Whorf 的独立字母排列，并将每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示和输入一致；STNDC 与 UCIEA 在精简模式仍显示数字行，其非标准 Shift 动态图例、下一键提示及反查落在正确实体键；设置和 Layout Fluid 序列持久化，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、数字行策略、非标准 Shift、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-51 | Octa8、Nerps、Gallium、Gallium Angle 与 Gallium v2 | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，重点比较三种 Gallium 的 V/J/G/Z 实体位置，并将每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示和输入一致；三种 Gallium 保持独立排列，下一键提示、反查、设置和 Layout Fluid 序列均使用所选变体，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、变体差异、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-52 | Nila、Noctum、Cascade、Vylet 与 Romak | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，重点核对 Cascade 与 Vylet 的逗号/句点 Shift 输出，并将每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示和输入一致；Cascade 与 Vylet 保留各自非标准符号配对，下一键提示、反查、设置和 Layout Fluid 序列均使用所选布局，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、非标准符号层、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-53 | Scythe、Inqwerted、Rain、Night 与 Night STIC | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，重点核对 Night 数字行末端的 `=` 与反斜杠实体位置，并将每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示和输入一致；Night 保留独立数字行，下一键提示、反查、设置和 Layout Fluid 序列均使用所选布局，不复制或打包参考布局资产。 | 待验收（5×47 个 Base/Shift 位置、Night 数字行、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-54 | Whix2、Haruka、Kuntum、Kuntem 与 Kuntem-JQ | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，重点确认 Whix2 的 7 个空位不显示字符、不产生输入，并比较三个 Kuntum/Kuntem 变体。 | Haruka 与三个变体各 47 个 ANSI 位置一致；Whix2 的 40 个赋值位置正常输入、7 个空位不输出且不参与下一键反查；设置和 Layout Fluid 保持所选布局，不复制或打包参考布局资产。 | 待验收（完整 Base/Shift、Whix2 空位、变体差异、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-55 | BEAKL Zi、Snorkle、MALTRON、PRSTEN 与 RSTHD | 分别选择五项布局并打开下一键提示；逐行练习 Base/Shift，检查四项双拇指提示，并在普通 Mac 上按 Space 核对第五行首项输出；将每项加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置一致；BEAKL Zi、MALTRON 与 RSTHD 的物理 Space 分别输出其首个拇指字母，PRSTEN 输出空格，第二拇指项仅作布局提示；Snorkle 保持普通空格。设置和 Layout Fluid 持久化，不复制或打包参考布局资产。 | 待验收（完整 Base/Shift、双拇指提示、物理 Space 输入桥、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-56 | Hands Down Promethium、Statica 3×5、Vestnik 与 Diktor 变体 | 分别选择五项布局并打开动态/下一键提示；逐行练习 Base/Shift，Promethium 检查双拇指和 Space→`r`，Statica/Vestnik 检查 Option/Shift+Option 特殊 Cyrillic，Diktor 两项检查数字行；加入 Layout Fluid 后重开。 | 四个 ANSI 主行的提示与输入一致；五项的 AltGr 未重定义键保持布局 Base/Shift，Promethium 显示 `R + 空格`，Statica/Vestnik 的 `ъ/ё/щ` 特殊层正确，Diktor 两项在精简模式仍显示重排数字行；设置和 Layout Fluid 持久化，不复制或打包参考资产。 | 待验收（主行 Base/Shift、AltGr、双拇指输入桥、数字行策略、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-57 | Redaktor、JUIYAF、Zubachev 与 Colemak Qi 变体 | 分别选择五项布局并打开动态/下一键提示；逐行练习 Base/Shift 和 Option/Shift+Option，重点核对 Redaktor 的 `₽`、三种 Cyrillic 布局的非标准大小写配对及两种 Colemak Qi 的标点差异；加入 Layout Fluid 后重开。 | 五项各 47 个 ANSI 位置的提示和输入一致，精简模式显示数字行，AltGr 未重定义键保持布局 Base/Shift；JUIYAF 的小写 `ъ` 与 Zubachev 的大写 `Ь` 反查到实际可输出的实体键，设置和 Layout Fluid 持久化，不复制或打包参考资产。 | 待验收（完整 Base/Shift、AltGr 回退、数字行、大小写精确反查、归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-58 | ColemaQ、Manoonchai、Brasileiro Nativo 与 BEAKL 15 | 分别选择五项布局并打开动态/下一键提示；核对 Base/Shift、Option 回退、精简数字行，并重点检查 Manoonchai 组合符独立键及 Brasileiro Nativo 的 ISO 左下额外键；加入 Layout Fluid 后重开。 | 五项提示和输入与实体位置一致；Brasileiro Nativo 显示 13/12/12/11 键，Manoonchai 组合符不丢键，数字行策略、设置及 Layout Fluid 持久化正确，不复制或打包参考资产。 | 待验收（完整键位、AltGr 回退、ANSI/ISO 行、数字行策略、归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-59 | BEAKL 19、Rolll、Whorfmax Ortho 与 Neo | 分别选择五项布局并打开动态/下一键提示；核对 Base/Shift、Option 回退及精简数字行，重点检查 Neo 的 ISO 左下额外键和两个单层空格键。 | 前四项显示 13/13/11/10 ANSI 行，Neo 显示 13/12/12/11 ISO 行；Neo 两个特殊实体键在四种修饰状态均输出空格，五项设置及 Layout Fluid 持久化正确，不复制或打包参考资产。 | 待验收（完整键位、AltGr 回退、ANSI/ISO 行、单层键与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-60 | Bone、AdNW、Mine、Noted 与 Koy | 分别选择五项布局并打开动态/下一键提示；逐行核对字母、德语变音符、Base/Shift 和 Option 回退，重点检查每项不同的单层空格键位置。 | 五项均显示 13/12/12/11 ISO 行但保持独立排列；单层空格实体键在普通、Shift、Option、Shift+Option 下均为空格，设置及 Layout Fluid 持久化正确，不复制或打包参考资产。 | 待验收（完整 ISO 键位、变体差异、AltGr 回退、单层键与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-61 | 3l、Korean、Ekverto B 与 Sturdy 变体 | 分别选择五项布局并打开动态/下一键提示；核对 Base/Shift、Option 回退和精简数字行，重点检查 3l 空格实体键、Korean Jamo、Ekverto B ISO 额外键及两项 Sturdy 的括号/感叹号差异。 | 3l 与 Korean、Sturdy ANSI 显示 ANSI 行，Ekverto B 与 Sturdy ISO 显示 ISO 行；单层键、选择性 Shift 和非标准符号均落在正确实体位置，设置及 Layout Fluid 持久化正确。 | 待验收（完整键位、AltGr 回退、Jamo、ANSI/ISO 差异与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-62 | Sturdy Ortho、HiYou、Xenia 与 Burmese | 分别选择五项布局并打开动态/下一键提示；核对 Base/Shift、Option 回退与数字行策略，重点检查 HiYou ISO 单层符号键、Xenia 两变体差异及 Burmese 组合符。 | HiYou 显示 ISO 行且 `|`/反斜杠四层稳定；Burmese 每个组合符保留独立实体键并显示精简数字行；其他三项保持独立 ANSI 排列，设置及 Layout Fluid 持久化正确。 | 待验收（完整键位、AltGr 回退、单层键、组合符与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-63 | Gallium v2 Matrix、Gallium NL、Maya 与 Gallaya Angle | 分别选择五项布局并打开动态/下一键提示；核对 Base/Shift、Option 回退、ANSI/ISO 行数及 Layout Fluid 切换，重点检查 Gallium v2 Matrix 的实际 ANSI 类型和 Gallaya ISO 额外键。 | 五项保持独立键位；Gallium v2 Matrix 显示 13/13/11/10 ANSI 行，Gallaya ISO 显示 13/12/12/11 ISO 行及地区 Shift 符号；设置、反查和 Layout Fluid 持久化正确。 | 待验收（完整物理键、AltGr 回退、ANSI/ISO 差异与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-64 | Gallaya Matrix、Minimak 三阶段与 Graphite Angle | 分别选择五项布局并打开动态/下一键提示；逐项核对 Base/Shift、Option 回退、ANSI 行数及 Layout Fluid 切换，重点检查三个 Minimak 阶段差异和数字 3 的 Shift 字符。 | 五项均显示 13/13/11/10 ANSI 行并保持独立键位；Minimak 4/8/12-key 逐步替换不同字母且 Shift+3 显示 `£`；设置、反查和 Layout Fluid 持久化正确。 | 待验收（完整物理键、AltGr 回退、阶段差异与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-65 | Optimot 四层与字母区删除键 | 选择 Optimot，依次核对 Base/Shift/Option/Shift+Option 动态图例；先输入若干字符，再分别以无修饰、Shift、Option、Shift+Option 按下标为 `⌫` 的物理键。 | 48 个 ISO 位置及缺失层显示正确；四次操作均各删除一个允许删除的字符，绝不插入可见 `⌫`，删除位不会被下一字符提示选中；设置和 Layout Fluid 持久化正确。 | 待验收（四层逐键、删除动作、非文本提示集合与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-66 | Graphite 三变体、UGJRMV 与 ORNATE | 分别选择五项布局并打开动态/下一键提示；逐项核对 Base/Shift、Option 回退、ANSI 行数及 Layout Fluid，重点比较三个 Graphite 底行、UGJRMV 区域字符和 ORNATE 的 `q` Shift 位。 | 五项显示 13/13/11/10 ANSI 行且保持独立键位；Graphite VC/KP/Matrix 底行不串位，UGJRMV 字符完整，ORNATE 的 `q` 位 Shift 显示 `A`；设置、反查与 Layout Fluid 持久化正确。 | 待验收（完整物理键、AltGr 回退、变体差异与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-67 | Tarmak 四阶段 | 分别选择 Tarmak 1–4 并打开动态/下一键提示；逐项核对 Base/Shift、Option 回退、ANSI 行数及 Layout Fluid，重点比较每阶段移动的字母位置。 | 四项均显示 13/13/11/10 ANSI 行且保持独立键位；阶段间不会串位，数字与标点层保持一致，设置、反查与 Layout Fluid 持久化正确。 | 待验收（四阶段完整物理键、AltGr 回退、阶段差异与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-68 | Stronk、Seht Drai、Ints、Rollla 与 Wreathy | 分别选择五项布局并打开动态/下一键提示；逐项核对 Base/Shift、Option 回退、ANSI 行数及 Layout Fluid，重点检查引号、斜杠、逗号与句点位置。 | 五项均显示 13/13/11/10 ANSI 行且保持独立键位；标点 Shift 配对不会因自动大小写而错位，设置、反查与 Layout Fluid 持久化正确。 | 待验收（五项完整物理键、AltGr 回退、标点差异与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-69 | Saiga、Saiga E、Krai、Mir 与 Ergol | 分别选择五项布局并打开动态/下一键提示；逐项核对 Base/Shift、Option 回退、ANSI 行数及 Layout Fluid，重点检查 Ergol 数字行和 `’` 键的 Shift 输出。 | 五项均显示 13/13/11/10 ANSI 行且保持独立键位；Ergol 显示欧元、书名号及 `’`→`!` 配对，设置、反查与 Layout Fluid 持久化正确。 | 待验收（五项完整物理键、AltGr 回退、特殊符号与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-70 | Hyperroll、Anishtro、Dusk 与 Panini 两变体 | 分别选择五项布局并打开动态/下一键提示；核对 Base/Shift、Option 回退和 Layout Fluid；重点检查 Anishtro/Dusk 双拇指行与物理 Space，以及 Panini Wide 的方括号和反斜杠位置。 | 五项主键均显示 13/13/11/10 ANSI 行；Anishtro/Dusk 分别显示 `E + 空格`、`R + 空格` 并由物理 Space 输入首项，Panini 两变体不串位；反查和持久化正确。 | 待验收（完整物理键、双拇指、Space 输入桥、变体差异与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-71 | Sword、OPY、Ergo Split 46、Gralmak 与 Vitrimak | 分别选择五项布局并打开动态/下一键提示；逐项核对 Base/Shift、Option 回退、ANSI 行数及 Layout Fluid，重点检查 OPY 的 `ß/§/ö/ä/ü` 与 Ergo Split 46 的欧元、书名号和 `˜`。 | 五项均显示 13/13/11/10 ANSI 行且保持独立键位；特殊字符、重复输出、可用反查、设置和 Layout Fluid 持久化正确。 | 待验收（五项完整物理键、AltGr 回退、特殊字符、反查与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-72 | Persian Colemak 两项、Miligram、Nokwts 与 Vylet v4 | 分别选择五项布局并打开动态/下一键提示；核对 Base/Shift、Option 回退、ANSI/ISO 行数和 Layout Fluid；重点检查 Persian 组合音标/ZWNJ、Miligram ISO 行及数字行策略。 | Persian 字符逐键显示且不粘连，Miligram 显示 13/12/12/11 ISO 行，Miligram 与 Persian Standard 在精简模式显示数字行；其余两项保持 ANSI，反查和持久化正确。 | 待验收（完整物理键、组合字符、ISO、数字行、AltGr 回退与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| VIS-73 | ErgolPT、Rulemak 与 Gralmak S | 分别选择三项布局并打开动态图例/下一键提示，逐键检查 Base、Shift、Option、Shift+Option；ErgolPT 另检查 ISO 行和物理 Space，三项加入 Layout Fluid 后重开。 | ErgolPT 显示 13/12/12/11 ISO 行且 Space 分别输出空格、`-`、空格、`-`；Rulemak/Gralmak S 显示 ANSI 行；显式扩展字符和组合符位置正确，缺失 AltGr 层回退对应 Base/Shift，提示、反查和持久化一致。 | 待验收（全部四层、缺失层回退、Space 与归档由客户端测试覆盖；依用户要求，本轮不启动 GUI） |
| MET-12 | 结果单词历史展开 | 在英语字数模式完成一轮；默认设置下查看结果后手动展开单词历史，再开启“完成后自动展开单词历史”完成一轮 | 默认结果页的单词历史折叠但可手动展开；开启后含单词记录的结果页自动展开，且开关持久化 | 已验收（隔离 release 候选） |
| MET-13 | 近 10 次平均 | 完成同设置的英语 25 词测试，设置“显示近 10 次平均”为“速度和准确率”，重开设置后返回练习区 | 练习区显示本机同设置的最近 1–10 条平均速度与准确率；没有同类成绩时明确显示暂无；开关持久化且不要求登录 | 已验收（隔离 release 候选；暂无分支由自动化覆盖） |
| MET-14 | 本机个人最佳提示 | 完成两次相同设置的英语 25 词测试，开启“显示本机个人最佳”，重开设置后返回练习区；再切换引语或 Zipf 高频词 | 练习区显示同设置、符合 PB 资格的最高速度与准确率；开关持久化；引语与不符合资格的文本流明确不计入 PB | 已验收（隔离 release 候选；资格筛选由自动化覆盖） |
| MET-15 | 结果单词 Burst 热力图 | 先完成一轮英语字数测试；开启“结果页显示单词 Burst 热力图”，关闭并重开设置后再完成一轮，展开单词历史 | 默认不显示热力图；开启后出现速度图例，已尝试词按本轮相对速度着色；没有可测间隔的词为灰色中性，开关持久化 | 已验收（隔离 release 候选；分档与无有效间隔由自动化覆盖） |
| MET-16 | 结果速度轨迹 | 完成一轮持续至少 3 秒、包含一次输错再退格修正的英语字数测试；在结果页依次切换 Raw、Burst、错误按钮并完成下一轮 | 显示本机重建的 WPM、Raw、Burst 与错误轨迹；修正后错误轨迹回落；三个显示选择会保留到下一轮结果 | 已验收（隔离 release 候选；超过 120 秒省略图表由自动化覆盖） |
| MET-17 | 结果 PNG 导出 | 完成一轮练习，在结果页打开“导出”；先复制文字，再复制图片；最后选择保存图片并在系统面板选定临时位置 | 菜单包含这三个明确导出动作；文字和图片复制均显示成功状态；系统保存面板建议以 `typebar-result-*.png` 命名，生成的图片使用当前主题、结果指标与准确度刻度 | 已验收（隔离 release 候选；保存面板的 UTC 名称与 2× PNG 渲染由自动化覆盖） |
| MET-18 | 慢词练习 | 完成一轮英语字数练习，再点“练习慢词”；另完成过短练习或含无计时尾部项的练习 | 排除尾部活动项后，按最慢的四舍五入 20% 选择，最多 20 个位置；最慢目标词权重更高，过短练习不显示入口 | 已验收（QA34B：5 词结果只显示 1 个慢词；尾部排除和上限由自动化覆盖） |
| MET-19 | 重复本轮 | 完成一轮自定义循环计时文本或英语字数测试，在结果页点“重复本轮”；另回到结果页点“再来一次” | “重复本轮”显示与前轮完全相同的提示及配置，开始状态干净且循环文本仍能延长；“再来一次”仍生成新内容 | 已验收（隔离 release 候选；自定义循环源由自动化覆盖） |
| MET-20 | 复制实际输入 | 完成一次包含输错、退格修正的练习，在结果页“导出”选择“复制实际输入”；再查看一个没有回放的旧归档结果 | 剪贴板文本是本机输入回放的最终内容；无回放时显示明确不可用状态，不以提示或统计生成文本 | 已验收（隔离 release 候选；回放重建与缺失分支由自动化覆盖） |
| MET-44 | 输入回放状态、字符定位与声音 | 开启按键音和错误音，完成一轮包含错字、未修正错误词提交、词尾额外字符、退格修正和下一词输入的练习；播放回放、拖动时间轴，再点按目标文本中的已输入与未输入字符；随后关闭错误音再次播放 | 实际输入随时间逐字出现，正确字符使用主文字色，错误字符使用错误色，额外字符另有删除线；退格时对应末字符消失，下一词重新对齐。播放按事件发出当前音色和音量的按键音/错误音；错误字符和仍含错误的单词提交均播放错误音，错误音关闭后回退为按键音。拖动、点选、重置和自动缩进不发声。点按已到达字符会暂停并定位，未到达字符不可点；滑杆仍可用键盘和 VoiceOver 连续定位 | 待验收（状态、删除、强制错误、错误词提交、跨词重新对齐、乱序事件、字符定位、声音区间与设置路由由客户端自动化覆盖；依用户要求不启动 GUI） |
| MET-32 | 复制已练习提示 | 完成一轮至少含两个英文目标的练习，在首词中故意输错；打开结果页“导出”并选择“复制练习提示” | 菜单包含该动作且显示复制成功；自动化验证剪贴板候选为已到达的目标词，不含错误输入或后续目标。禅模式复制本机输入，无空格提示只取已到达的目标范围 | 已验收（QA35；目标选择、无空格和禅模式由自动化覆盖） |
| MET-21 | 错词上下文练习 | 在英语字数练习中故意错两个非首词，完成后打开“错词练习”的“带前词上下文” | 自定义练习依次含“前词 + 错词”短语，仅来自实际尝试词；首词错误只出现首词本身，未达到的提示尾部不会出现 | 已验收（QA21 release 候选：菜单显示两种错词练习；选择“带前词上下文（2）”后打开按原顺序的 `orchard copper window lantern`；边界选择由自动化覆盖） |
| MET-22 | 今日练习汇总 | 当天已有一条本机成绩后，再完成一轮关闭“保存成绩”的练习 | 完成页显示当天本机累计时长和完成次数；已保存与本次运行中未保存成绩各计一次，昨天成绩不参与 | 已验收（QA22 内存候选：首轮已保存显示 1 次；关闭“保存完成成绩”后第二轮显示不保存成绩且今日为 2 次；跨日、去重、格式化由自动化覆盖） |
| MET-23 | 中文错词练习 | 在 1 词简体中文练习中输对首字、错第二字，完成后选“错词练习” | 菜单只显示实际尝试的 Typebar 自有中文词；选中后进入无空格的自定义练习，标点、未知片段和未到达尾部不产生错词 | 已验收（QA25 内存候选：`远山` 输入首字正确、第二字错误后显示“只练错词（1）”；选中后进入自定义模式，文本为 2 字符 `远山`；标点和未到达边界由自动化覆盖） |
| MET-24 | 已修正错误的错词练习 | 在英语及简体中文 1 词练习中故意输错、退格修正并完成；另在输错后删空等待完成 | 即使最终文本正确或为空，错词菜单仍列出实际出错过的目标词；未达到的后续词仍不出现 | 部分验收（QA26 内存候选：英语 `lantern` 中输错、退格修正后结果为 100% 准确率，仍显示两种错词练习各 1 项；英文/中文修正与删空分支由 131 项自动化覆盖。当前自动化无法注入已提交的中文 IME 文本，中文实机输入保持待验收） |
| MET-25 | 错词频次权重 | 在英语单词练习中同一目标位置连续输错、退格后再输错，完成后启动“只练错词”和“带前词上下文” | 结果菜单按去重目标显示；自定义复练会按实际错误次数重复该目标或上下文短语；正确但未完成的词不出现 | 已验收（QA27 内存候选：`canyon` 同一位置两次错误后修正完成，结果仍为 100% 准确率；菜单显示“只练错词（1）”和“带前词上下文（2）”，前者打开 `canyon canyon` 自定义练习；排序、上限和正确未完成分支由自动化覆盖） |
| MET-26 | 上下文错词选择上限 | 在 11 个已尝试英语目标中分别触发错误，再打开“带前词上下文” | 上下文候选按目标错误次数排序；最多 10 个已尝试目标进入练习；每个入选上下文仍按其错误次数重复 | 已验收（QA28 内存候选：`a b c d e f g h i j k` 每词均触发并修正一次，菜单显示“只练错词（11）”和“带前词上下文（10）”；共享目标与频次排序由自动化覆盖） |
| MET-27 | 复制错词列表 | 完成一轮含至少两个错词的练习，在“错词练习”菜单点“复制错词列表” | 结果显示复制成功；剪贴板只含本轮去重目标词并以空格分隔，中文目标也能清晰分开 | 已验收（QA28 内存候选：菜单显示“复制错词列表”，点击后结果页显示“错词列表已复制”；测试覆盖英文与中文列表格式，验收未读取剪贴板内容） |
| MET-28 | 错词练习后的设置恢复 | 从非自定义原测试进入错词练习，完成练习后点“再来一次”；另点“重复本轮”；或手动切到另一非自定义模式后完成一轮 | “再来一次”恢复进入练习前的模式、参数和内容；“重复本轮”仍保留同一份错词练习；手动离开练习后不再跳回过期原设置 | 已验收（QA29 覆盖恢复和重复；QA31 覆盖手动切换后的快照释放） |
| MET-29 | 错词练习的加权段落轮换 | 以至少两个错误目标完成英语测试，且其中一个目标错误次数更高；进入“只练错词”与“带前词上下文” | 两种练习均使用分段完成方式，段数为入选目标数的 5 倍；候选段落顺序经过洗牌，频次较高的目标在候选池中保留更多副本 | 已验收（QA30 目视确认普通错词的 3 个候选生成 15 段洗牌练习；上下文的入选目标计数和同一候选池生成器由自动化覆盖） |
| MET-30 | 错词与慢词组合练习 | 完成一轮同时具有错词及可分类慢词的英语练习；从错词菜单选择“错词 + 慢词” | 菜单仅在两类候选均存在时出现；错词最多取 10 个，合并后按候选总数生成 5 倍的加权分段练习 | 已验收（QA34B：1 错词 + 1 慢词显示 2 个候选并生成 10 段） |
| MET-31 | 上下文错词与慢词组合练习 | 完成一轮同时具有错词及可分类慢词的英语练习；从错词菜单选择“上下文错词 + 慢词” | 菜单保留前词 + 错词短语，并与慢词候选合并；按候选总数生成 5 倍的加权分段练习 | 已验收（QA34B：重复目标在两个已尝试位置产生 2 个上下文候选，加 1 慢词共 3 个；分段构造由 QA33 和自动化覆盖） |
| INP-07 | 已输入字符效果 | 选择“圆点代替已完成词”，正确完成第一个英文词 | 已完成词逐字变为圆点；词间空格、当前词和后续提示位置保持不变 | 已验收（QA23：`marble` 提交后显示为 `••••••`，后续提示与下一键保持正常） |
| MET-07 | 实时仪表样式 | 分别把速度设为迷你、准确率设为关闭、Burst 设为迷你 | 速度与 Burst 缩小、准确率收起；剩余时间和错误仍显示，切回文字后恢复 | 待验收（QA23 已确认三项设置切换和运行态：准确率标签收起、速度/Burst 保留迷你数值；“文字”恢复仍待实机复核） |
| MET-08 | 字数实时进度 | 选择 25 词模式，正确完成第一个词 | “进度”从 0/25 变为 1/25；时间模式仍显示“剩余”秒数 | 已验收（QA23：`0/25` 正确变为 `1/25`；时间模式仍显示“剩余 5s”） |
| MET-09 | 实时进度样式 | 分别在时间和字数模式选择“进度条”；在时间模式选择“闪现文字”与“闪现迷你”，观察剩余 30、29、15、1 秒 | 进度条随倒计时或已提交词增长，文字与比例一致；两种闪现样式仅在限时测试的剩余秒数为 15 的倍数时显示，字数模式保持可见 | 待验收（QA23 字数进度条从 0 变为 `0.04`；闪现时间策略与设置归档由客户端测试覆盖，自动化快照环境不能可靠提供时间分支的中段帧） |
| FUN-04 | Zipf 高频词 | 在英语字数模式启用“Zipf 高频词”，连续重开数次；保持 Zipf 分别切换到 Armenian 与 Albanian | 提示始终取自 Typebar 自有词库；靠前词比后排词更常出现，且不发起网络请求。对参考明确标记为未按频率排序的 Armenian 显示七秒提醒；对未声明排序的 Albanian 显示可能不支持的七秒提醒；二者均保留 Zipf 修饰器 | 待验收（频率、已知不支持、未知提示和修饰器保留由客户端自动化覆盖；依用户要求，本轮不启动 GUI） |
| FUN-05 | 随机大小写 | 在英语字数模式启用“随机大小写”，连续重开数次并再改选“全大写” | 英文字母每次以不同大小写组合出现；数字/标点不变；改选全大写时随机模式自动取消 | 待验收 |
| FUN-06 | 会计数字流 | 在字数模式启用“会计数字流”并输入两个数字块 | 提示为带千分位和小数的数字块；完成按词计数；切换二进制流时前者自动取消 | 待验收 |
| FUN-07 | 方向键流 | 在字数模式启用“方向键流”，按提示依次按上下左右方向键 | 每个方向键写入对应箭头并被计分；非方向键仍按普通输入处理；切换符号流时前者自动取消 | 待验收 |
| TST-07 | 多行自定义文本 | 保存含两行文字的自定义文本并开始练习，在换行位置按 Return | Return 仅在提示含换行时写入换行；可完成整段，多行文本仍可保存与复用 | 已验收（隔离 release 候选） |
| FUN-08 | 即时消息文本 | 在英语字数模式启用“即时消息文本”，观察提示并在每行结尾按 Return | 提示改为小写短行，包裹性标点移除；句末变为换行，Return 可以正确计分；改选全大写时该模式自动取消 | 待验收 |
| FUN-09 | 镜像与倒置练习区 | 分别启用“镜像练习区”“倒置练习区”和二者组合，输入一个正确字符 | 提示区分别水平翻转、旋转 180° 或组合；输入、WPM 与错误计分不改变 | 待验收 |
| FUN-23 | 随机诗歌与百科内容 | 以英语字数模式分别启用“诗性散文”和“知识短文”，每次重开后等待来源状态；在内容替换前与替换后各输入一个字符 | 修饰器启用后才发起随机 PoetryDB/Wikipedia 请求；成功时显示标题或作者并使用清洗后的内容，失败/超时或开始输入后保持原创离线提示，不会中断已输入会话 | 待验收（本地 JSON 解析、适用范围、扩展与会话替换由自动化覆盖） |
| FUN-10 | CRT 练习区 | 启用“CRT 练习区”并开始输入 | 提示区出现扫描线、浅绿色色调和边缘光晕；鼠标点击与输入、WPM、错误计分不受影响 | 待验收 |
| FUN-11 | 地震练习区 | 启用“地震练习区”开始输入，再启用“减少动态效果” | 提示区轻微抖动；输入、WPM、错误计分不改变；减少动态效果启用后立即静止 | 待验收 |
| FUN-12 | 太空练习区 | 启用“太空练习区”开始输入 | 提示区出现星点和主题色光晕；鼠标点击与输入、WPM、错误计分不受影响 | 待验收 |
| FUN-13 | 眩晕练习区 | 启用“眩晕练习区”开始输入，再启用“减少动态效果” | 提示区发生周期性透视倾斜与缩放；输入、WPM、错误计分不改变；减少动态效果启用后立即静止 | 待验收 |
| FUN-14 | 旋转练习区 | 启用“旋转练习区”开始输入，再启用“减少动态效果” | 提示区以匀速循环旋转；输入、WPM、错误计分不改变；减少动态效果启用后立即静止 | 待验收 |
| FUN-15 | 旋转文字 | 启用“旋转文字”开始输入，再启用“减少动态效果” | 每个非空白提示字符独立旋转；当前字符与错误状态仍可辨认；减少动态效果启用后立即静止 | 待验收 |
| FUN-16 | 布局流动 | 在设置配置至少三种布局；在 30 词模式启用“布局流动”，完成每一段词数 | 键盘提示按配置顺序切换；切换前三词提示下一个布局；重启后配置仍保留；输入、成绩不重置 | 待验收 |
| FUN-17 | ASL 指语练习 | 启用“ASL 指语练习”，输入普通英文词并故意输错一个字符 | 字母显示为 Typebar 原创矢量手势提示，J/Z 含运动轨迹；当前和错误状态仍可辨认；键盘输入仍按原英文提示计分 | 已验收（QA37；不宣称可替代正式 ASL 教学） |
| FUN-18 | ASCII / 特殊符号流 | 分别启用“ASCII 字符流”“特殊符号流”，输入至少四个字符块 | ASCII 流只含可打印 ASCII 字符；特殊符号流只含键盘标点、括号和运算符；切换两个模式会互相取消 | 待验收 |
| FUN-19 | 无意义字串 | 启用“无意义字串”并多次重开 | 每个词只含 1–7 位小写 ASCII 字母，不出现词库单词；切换其他字符流时自动取消 | 待验收 |
| FUN-20 | Simon 指令 | 将“键盘提示模式”设为关闭，启用“Simon 指令”后开始输入 | 全部待输入字形隐藏，但键盘提示仍强制显示下一键；输入正确字符后仅已输入字形可见 | 待验收 |
| FUN-21 | 诗性散文 | 在字数模式启用“诗性散文”并连续输入 | 提示使用 Typebar 自有无标点诗性文本，不随语言设置改变；切换其他字符流时自动取消 | 待验收 |
| FUN-22 | 知识短文 | 在字数模式启用“知识短文”并连续输入 | 提示使用 Typebar 自有离线知识段落，无标点和数字；不发起网络请求；切换其他字符流时自动取消 | 待验收 |
| MET-01 | 近期词速 | 在英语字数模式连续完成至少 9 个词 | 指标下展示最近 8 个词速条；重开后清空 | 待验收 |
| MET-02 | 持久化弱项分析与训练 | 完成并保存两轮英语测试：一轮包含已修正错误，另一轮保持全对但在某个字符前明显停顿；再从工具栏或命令面板打开“弱项分析”，检查错误字符、平均间隔与候选词，依次切换 10/25/50 词并开始训练；删除其中一条历史后重新打开 | 只显示本机已保存回放派生的分析轮次、错误次数、尝试次数及高于个人中位线的慢字符；错误和慢字符共同影响排序，候选只来自 Typebar 自写离线词流，训练词数与选择一致；删除历史后报告同步变化，全程无网络请求 | 待验收（计时聚合、零错误慢项、重复/强制错误、自动事件排除、确定性排序、语言隔离、候选来源与训练词数由客户端测试覆盖；依用户要求不启动 GUI） |
| SYS-01 | 本机朗读 | 点击“朗读提示”，随后重开 | macOS 朗读当前提示；重开后立即停止 | 待验收（已验证入口） |
| VIS-01 | 独立光标与样式 | 在设置选择关闭、条形、轮廓、下划线、块状及三种原创图形标记；分别选关闭/慢速/中速/快速平滑档，输入跨行文本；开启节奏引导后也切换样式 | 常规提示的主、节奏标记按目标字符独立定位，跨行后仍正确；关闭时立即更新且不绘制额外标记，三档平滑速度可见差别；各样式和档位重开后保留。卷带、手语和旋转文字的回退呈现另行实机复核 | 待验收（换行定位、档位、样式枚举和归档往返由客户端测试覆盖） |
| VIS-02 | 行宽与键盘布局菜单 | 打开设置中的练习行宽与键盘布局菜单，选择自定义行宽和 ANSI Workman | 行宽菜单含自定义项且选择后出现列数控制；键盘菜单含 ANSI Workman 且可选中；非默认布局按物理 ANSI 键位模拟输入 | 待验收（菜单已验收；物理键位输入由自动化覆盖） |
| CFG-02 | 倒计时提示音设置 | 打开设置中的倒计时提示音菜单 | 菜单独立于错误音，含关闭、结束前 1/3/5/10 秒 | 已验收 |
| TST-02 | 葡萄牙语离线内容 | 在时间模式选择 Português | 菜单可选，练习区显示 Typebar 自有的含葡语重音词流 | 已验收 |

| MET-46 | 按阈值复制慢词 | 完成一轮包含快慢不同、重复目标及一个无法测量 Burst 的练习；在结果页“导出”选择“复制慢词列表…”，先使用默认阈值，再输入较高阈值、0 和一个没有命中项的有效阈值 | 表单默认显示正数单词 Burst 的四舍五入平均 WPM；复制内容严格低于阈值、保持实际输入顺序并保留重复目标，无法计时项不出现。成功状态显示复制数量；0 显示阈值错误，无命中显示明确空结果 | 待验收（默认值、严格边界、顺序、重复、缺失/零 Burst 与空结果策略由客户端自动化覆盖；依用户要求不启动 GUI） |

| MET-47 | 结果页重试发送成绩 | 登录自建服务并开启“将完成成绩发送到自建服务”；完成一轮后让首次请求断网失败，恢复网络并连续点击“重新发送成绩”；同时让旧请求延迟返回，再完成另一轮 | 失败状态明确说明本机成绩已保存并显示重试按钮；重试时显示发送进度且不能重复提交，成功后按钮消失并显示 XP。相同 UUID 不重复累计 XP；旧成绩的迟到响应不覆盖新结果页 | 待验收（状态与重试资格由客户端自动化覆盖；相同 UUID 幂等和 XP 不重复由服务端自动化覆盖；乱序请求所有权经代码审计，依用户要求不启动 GUI） |

| MET-48 | 引语结果来源 | 分别完成一条 Typebar 自有引语和一条带署名的社区审核引语；查看完成页、历史详情、复制结果文字和结果 PNG，再打开一条升级前的引语成绩。对社区结果执行评价与私下举报。 | 四处均显示完成时捕获的来源类型与标题/署名，切换到下一条引语不会改写旧结果；来源最多 80 字符且不含作者账户 ID。旧成绩无来源时保持正常但不显示该行；普通模式永不显示来源。社区结果仍显示评价和举报入口 | 待验收（来源归一化、非引语隔离、结果/SwiftData/旧归档往返、损坏字段降级、文字导出和真实社区 ID 前缀由客户端自动化覆盖；依用户要求不启动 GUI） |

| MET-58 | 复制错词列表 | 完成一轮包含重复误键并已修正、未修正错误和未到达提示尾部的空格语言练习，从结果页“导出”复制错词；再用简体中文无空格练习重复操作，最后完成一轮零错练习 | 仅复制本轮实际输入事件识别出的去重目标，保持首次出现顺序并以空格分隔；修正过的真实误键仍保留，未到达尾部不出现。中文目标之间同样有空格；零错结果不显示该菜单项 | 待验收（错词边界、去重、顺序、中文识别、空列表和复制文本由客户端自动化覆盖；依用户要求不启动 GUI） |

| MET-59 | 完成页测试类型摘要 | 依次完成计时、无限字数、引语、自定义和二进制流练习；计时练习开启标点、数字、盲打、简化重音、一个趣味修饰器、专家难度与词级遇错停下。每轮结束后先改变下一轮设置，再查看完成页和复制结果图片 | 标题区与结果卡仍显示已完成那一轮的模式和档位，而不被新设置改写；引语显示实际长度，计时练习列出所有已启用关键选项，关闭项不出现；自定义和内容替换流不显示无意义的语言。摘要不改变成绩、历史或远端提交 | 待验收（有限/无限参数、实际引语长度、自定义及 `ignoresLanguage` 边界、选项顺序与完成快照隔离由客户端自动化覆盖；依用户要求不启动 GUI） |

| MAC-08 | 原生离线与恢复提示 | 在主界面保持未输入时关闭 Mac 网络，开始一轮练习后再次切换网络，最后在离线状态完成测试并恢复联网 | 首次明确离线时显示“当前离线”，说明练习与本机保存仍可用；测试输入中不出现会重排界面的离线条，完成页再次显示离线说明。恢复联网只短暂提示一次，启动时本来在线及重复系统回调不显示恢复；同步等请求仍以自身响应判断服务是否可用 | 待验收（检查/在线/离线转换、重复回调和仅真实恢复事件由客户端自动化覆盖；依用户要求不启动 GUI） |
| MAC-09 | 离线成绩待发送队列 | 登录自建服务并开启成绩发布，断网完成两轮可保存练习；关闭结果页并重启应用后恢复网络。另切换到不同账户/服务器，再返回原账户；最后让服务分别返回 400 与 503 | 两轮结果都先保存到本机，失败说明明确已排队；恢复网络且仍是原账户时按完成顺序各提交一次，重复 UUID 不增加服务端成绩或 XP。不同作用域绝不收到队列；400 不入队或会停止自动重试，503 保留队列且不会轮询。删除本机成绩或完整重置后不再发送对应项目 | 待验收（作用域规范化、去重、损坏恢复、可恢复错误分类和完整重置清理由客户端自动化覆盖；服务端既有 UUID 幂等测试覆盖重复提交；依用户要求不启动 GUI） |

| MET-60 | 结果图纵轴快捷切换 | 完成一轮速度有明显波动且可显示轨迹的练习，在图例中交替点击“从零”和“自适应”，关闭结果页后再完成一轮，并打开历史图 | “从零”时速度纵轴包含零点，“自适应”时按可见数据缩放；按钮文案和 VoiceOver 值即时反映当前模式。选择保留到下一轮结果及历史速度/准确率图；错误、完成次数和练习分钟图仍从零开始，成绩数值和回放不变 | 待验收（既有偏好往返测试覆盖持久化，目标 macOS 14 构建覆盖结果页入口；依用户要求不启动 GUI） |

| MET-61 | 结果图按秒检查与单词联动 | 完成一轮至少跨三个词、包含一次修正和一秒停顿的练习；在结果速度图中点按或拖动到各秒，并切换 Raw、Burst、错误图层，最后将指针移出图表或点清除 | 选择吸附到最近采样秒，以规则线和信息条显示当前可见指标；该秒真实输入或删除涉及的单词历史自动展开并突出，多词输入可同时突出多项，停顿秒不突出任何词。隐藏图层不出现在信息条；移出或清除后高亮消失，成绩、回放和设置均不改变 | 待验收（最近点、半秒决胜、首段/后续时间边界、多字符输入、分隔符删除、空事件秒与无空格边界由客户端自动化覆盖；macOS 14 Charts API 构建通过，依用户要求不启动 GUI） |
| MET-62 | 结果 Burst 平滑切换 | 完成一轮包含明显快慢变化且至少产生两个结果采样点的练习；在结果图中反复点击“平滑/原始”，选择尖峰附近的一秒，再关闭结果页并完成另一轮 | 平滑模式削弱局部 Burst 尖峰，原始模式恢复逐点值；图中橙线和所选秒信息同步变化，WPM、Raw、错误与关联单词不变。偏好保留到下一轮，关闭 Burst 后切换按钮不可用 | 待验收（三点窗口、原始旁路、其他指标不变与旧设置默认值由客户端自动化覆盖；macOS 14 Charts API 构建通过，依用户要求不启动 GUI） |
| MET-63 | 本机成绩保存失败与重试 | 让应用数据存储在完成成绩时暂时不可写，完成一轮启用了保存和远端发布的练习；检查结果页后恢复写入条件并点击“重新保存到本机” | 首次失败明确显示“尚未保存”和系统错误，仍可查看本轮指标及导出；不显示 PB、标签编辑或历史入口，也不发送远端。恢复后重试只产生一条同 ID 本机成绩，相关入口恢复，并且此时才开始远端发布 | 待验收（成功/失败状态、失败可重试和远端发布门控由客户端自动化覆盖；实际磁盘故障依用户要求不启动 GUI） |
| MET-64 | 完成页命令面板与自选弱项练习 | 先完成一轮有输入的禅模式练习，再完成一轮同时含错词、慢词、上下文和组合候选的普通练习；在完成页点“命令”并另用 ⇧⌘P 打开。执行“自选弱项练习…”，依次选择空组合、仅错词、上下文错词、仅慢词、错词+慢词和上下文+慢词 | 禅模式结果显示下一轮、重复本轮、复制已练习词和复制/保存结果图，但不伪造单词历史或弱项练习；普通结果有真实数据时才增加相应命令。自选页只列可用的错词方式，慢词不存在时禁用开关；空组合不能开始，其余五种组合进入对应加权分节练习。离开结果页的动作只启动一次，保存图片会先关闭命令面板再打开系统面板 | 待验收（命令目录、五路组合策略、不可用与空组合由客户端测试覆盖，目标 macOS 14 构建通过；依用户要求不启动 GUI） |

## 记录格式

完成某项后，将“状态”改为“已验收”，并在本节追加日期、macOS 版本、应用构建、实际操作、观察结果及任何偏差。不得将单元测试或源码阅读替代为人工验收。

## 已观察到的 UI 证据

- 2026-09-02，release 候选包 `Typebar-candidate-20260902-165842.app`：启动后主界面显示五种模式、语言与拼写选择、标点/数字开关、时间步进器、输入框、提示区、实时指标、键盘提示和命令/分享/预设/挑战/历史/数据/同步/好友/通知工具栏。输入 `lantern ` 后，WPM、Raw、Burst、准确率、节奏引导及下一键提示即时更新；选择“放弃本次测试”后弹窗明确提示未保存完成成绩，点击“重新开始”回到初始状态。SMK-01 已验收；这只是主流程冒烟，不代替各专项人工验收。
- 2026-09-02，自动化：原生 `NSTextInputClient` 收到标记文本 `拼` 时仅发出组合显示更新且保持输入列表为空；随后收到确认提交时才将 `拼` 发送至输入引擎，并清空组合显示。组合显示偏好可在本机设置持久化。真实中文输入法候选、取消和多字符组合仍需在设备上验收，因此 INP-02 保持待验收。
- 2026-09-02，自动化：左 Shift + 左侧 A 被识别为错误，左 Shift + 右侧 H 通过；中心的 Y、B 与 6 可由任意侧 Shift 使用。一次手位错误保留实际字符，同时使字形、错误数、准确率、错词复练和 replay 事件一致标记。实体键盘的左右 Shift 状态仍需设备验收，因此 INP-08 保持待验收。
- 2026-09-02，自动化：按键位图档可从所选原创布局反查已重映射的逻辑字符；例如 Dvorak 的 `p` 反查到 ANSI 左手位置 Q，因此左 Shift 判为错误。该档与直接读取 macOS 物理键码的“开启”档分别保存；真实 QMK 设备仍需按 INP-08 验收。
- 2026-09-02，自动化：三种 Typebar 原创代码片段均会产生含换行与 Tab 的提示；正确输入换行会自动记录 Tab 缩进，反缩进会记录自动删除，最终回放重建的文本与完成会话一致。实体键盘 Tab、焦点和可见多行布局仍需按 CNT-04 验收。
- 2026-09-02，release 候选包 `Typebar-candidate-20260902-175007.app`：选择 Code · Swift 后，输入 `let total = values.reduce(0, +)` 与换行，再输入 `for item in items {` 与换行。第三行 `print(item)` 前的目标光标自动移动到一个 Tab 的缩进位；开启“代码：退格反缩进”后按一次退格，光标回到该行行首并撤销自动 Tab。随后恢复英语、时间模式、成绩保存和关闭反缩进。Swift 的实际缩进/反缩进 UI 已验收；JavaScript/Python 与完整完成回放仍待验收。
- 2026-09-02，release 候选包 `Typebar-candidate-20260902-175007.app`：Code · JavaScript 的 `for (const item of items) {` 换行后，`console.log(item);` 前出现自动 Tab 目标位；Code · Python 的 `for item in items:` 换行后，`print(item)` 前也出现自动 Tab 目标位。两种语言在该位置按退格均撤销自动 Tab。JavaScript 轮次自然结束并在本机新增一条 35 WPM 成绩，未删除；Python 轮次通过“放弃本次测试”退出，确认显示“本次没有保存为完成成绩”。随后恢复英语、时间模式、成绩保存和关闭反缩进。CNT-04 的三个语言缩进路径已验收，完整输入与完成回放仍待验收。
- 2026-09-02，自动化：单行卷带的按词锚点取最近完成词后的字符，按字符锚点取当前输入长度；两种模式的水平偏移均受 0–100% 停靠位置限制，关闭档偏移为零，卷带、平滑开关和完整行开关均可恢复。卷带启用会关闭完整行；真实动画、裁剪和多行展开仍需按 VIS-03 验收。
- 2026-09-02，release 候选包 `Typebar-candidate-20260902-175007.app`：设置页显示“单行卷带”的按词菜单、卷带位置 slider 与平滑开关；启用按词后“显示完整提示行”显示为禁用。练习页呈现单行裁剪的卷带文本，输入 `meadow ` 后当前字形转移到下一词，WPM/Raw/Burst/准确率/键盘提示仍同步更新。验收后已将卷带恢复为“关闭”。VIS-03 按词部分已验收；按字符、停靠调节、无动画跳转和完整行展开仍待验收。
- 2026-09-02，release 候选包 `Typebar-candidate-20260902-175007.app`：关闭卷带后，“显示完整提示行”可启用。切换到 Typebar 自有引语时，长引语在练习区完整展开为三行，主窗口随内容高度增长，指标、下一键提示和未开始状态正常。验收后已关闭完整行并切回时间模式。VIS-03 的长引语完整行部分已验收；按字符、停靠调节、无动画跳转和多行代码展开仍待验收。
- 2026-09-02，release 候选包 `Typebar-candidate-20260902-175007.app`：将单行卷带切为“按字符”，停靠滑块从 50% 调至 45%，再关闭“平滑卷带滚动”。输入 `drift moss ` 后当前 `s` 按字符级锚点显示在新停靠位置，实时 WPM/Raw/Burst/准确率/键盘提示仍正常；该轮通过“放弃本次测试”显示为未保存。随后将保存成绩、卷带位置 50%、平滑开关和卷带关闭状态全部恢复。VIS-03 的按字符、停靠调整和关闭平滑路径已验收；多行代码展开仍待验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA5-20260902-182900.app`：开启“显示完整提示行”后，时间模式仍显示固定高度的可滚动练习区；切换至英语字数模式时，25 个词完整显示、AX 树不含提示滚动区，实时指标置于提示区下方且未重叠。此范围与官方源码的 `showAllLines` 语义一致：仅无计时字数、引语和自定义模式展开，卷带模式不支持。多行代码并非该设置的官方适用范围；引语和自定义的隔离候选复验仍待完成。
- 2026-09-02，隔离 release 候选包 `Typebar-QA5-20260902-182900.app`：引语模式下 AX 树不含提示滚动区，完整 Typebar 自有引语与后置指标同时存在；自定义“输入完成”模式输入 304 字符、三段文本后，提示按完整高度展开且 AX 树仍将指标置于其后。长文本会使指标落至当前屏幕可见区外，这是官方 `showAllLines` 说明中已提示的取舍；未发生提示截断或与指标重叠。
- 2026-09-02，隔离 release 候选包 `Typebar-QA5-20260902-182900.app`：单行卷带“按词”启用后，完整行开关自动关闭并禁用；连续输入 6 个正确词，当前目标词按 50% 停靠保持在练习区内，进度与各项实时指标持续更新。切至“按字符”后，将停靠滑块降至 45%、关闭平滑滚动；继续输入 `orchard bright bright `，当前字符即时按 45% 锚点移动，进度从 6/25 更新至 9/25，准确率保持 100%、错误为 0。VIS-03 已验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA5-20260902-182900.app`：将代码练习临时设为 9 词、关闭成绩保存并开启“代码：退格反缩进”。Swift、JavaScript、Python 均完成各自的原创多行片段：输入循环行与 Return 后自动插入 Tab；按退格会同时撤销该自动 Tab 与换行、回到循环行；再输入 Return 会重新自动插入 Tab，随后完成片段。三轮完成页均为 100% 准确率且无错误；播放到回放末尾后，Swift、JavaScript、Python 的回放文本分别与目标片段逐字一致。CNT-04 已验收。
- 2026-09-02，自动化：以官方当前语言目录为验收基线，Typebar 的 69 个代码语言/方言均有独立显示名称和非空的原创离线片段；逐项输入后都能在有限字数代码模式完成，完成 prompt 与结果记录一致。CUDA、VHDL、6502 Assembly 也包含在该回归范围；CNT-05 的菜单可见性与代表项实际输入仍需隔离候选包验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA8-20260902-185950.app`：语言菜单实际列出全部 69 个代码语言/方言。选择 Code · CUDA 后显示原创 CUDA kernel，首个 `_` 的键盘提示为 `-` 键；选择 Code · 6502 Assembly 后显示独立的 LDA/STA/DEX/BNE/RTS 汇编片段。Code · VHDL 已在同一构建链的 `Typebar-QA6-20260902-185200.app` 中显示 entity/architecture 片段。配合全量生成与完成回归，CNT-05 已验收。
- 2026-09-08，自动化：新增 Code · Dockerfile，以 Typebar 自写的虚构 `typebar-runtime` 镜像、练习目录、构建和入口指令生成多行提示；字面输入、有限完成、结果 prompt、禁用简化输入及不提供引语均已验证。全量守卫现逐项生成并完成 70 个代码选择；独立服务也逐项接受 70 个代码成绩和语言榜单筛选，同时拒绝 Dockerfile 社区引语投稿。Dockerfile 的菜单可见性与实际键入尚未启动 GUI 验收，因此 CNT-05 回到部分验收。
- 2026-09-08，自动化：新增 toki pona · ku suli 与 ku lili 两个独立语言选择。两份本地词流非空且互斥，均可生成字数提示、构造四档 Typebar 自有引语、进入 144 项 LTR 混排，并保持禁用简化输入、Zipf 未知、`en`／`en-US` 缺省在线／朗读语义；独立服务已验证两项投稿、成绩与排行榜。真实菜单和 macOS 键入尚未启动 GUI 验收。
- 2026-09-08，自动化：官方引入提交确认 `english_old` 表示 Old English。新增 79 个独立编写、仅含 ASCII 小写字母与 `æ/þ` 的 2–11 字符词，四档原创文本和第 145 项 LTR 混排入口；保持可选简化输入、Zipf 未知及 `en`／`en-US` 缺省在线／朗读语义，独立服务已验证投稿、撤回、成绩与排行榜。真实菜单和 macOS 古英语字符键入尚未启动 GUI 验收。
- 2026-09-08，自动化：官方 PR `#6400` 确认 `french_bitoduc` 的科技词法语趣译概念。新增 72 个独立编写的单 token 词、四档原创文本和第 146 项 LTR 混排入口；保留连字符、`çèéêîô`、`fr`／`fr-fr`、可选简化输入和 Zipf 未知语义，独立服务已验证投稿、撤回、成绩与排行榜。真实菜单和 macOS 法语键入尚未启动 GUI 验收。
- 2026-09-08，自动化：固定 `twitch_emotes` 配置的无空格 token 结构和禁用简化输入语义已重建为 Streaming Emotes · Typebar。新增 80 个完全虚构、大小写敏感并覆盖数字、下划线及 `:()<>` 的 token、四档自有内容和第 147 项 LTR 混排入口；独立服务已验证投稿、撤回、成绩与排行榜。真实菜单与实际键入尚未启动 GUI 验收。
- 2026-09-09，自动化：固定 `typing_of_the_dead` 的多词 section 和原始标点路径已重建为 Arcade Horror Phrases · Typebar。新增 64 段完全原创、20–35 字符且自带大小写和标点的街机恐怖短语、四档原创引语和第 148 项 LTR 混排入口；自动化验证精确词数、关闭标点的小写纯词、开启标点的原样保留、数字替换及投稿、撤回、成绩和排行榜。真实菜单与实际键入尚未启动 GUI 验收。
- 2026-09-09，自动化：固定 `pokemon_1k` 的大词表和特殊条目结构已重建为 Creature Index 1k · Typebar。41×25 构词规则生成 1,025 个原创虚构生物条目，原创替换项保持 28 个多词、17 个符号及 1 个数字条目；新增四档原创引语和第 149 项 LTR 混排入口，并验证最终词数、标点、数字、投稿、撤回、成绩和排行榜。真实菜单与实际键入尚未启动 GUI 验收。
- 2026-09-09，自动化：固定 `league_of_legends` 的题名大小写与多词条目结构已重建为 Arena Strategy Terms · Typebar。17×26 构词规则及原创替换生成 442 个术语，保持 229 个多词、61 个符号、2 个数字和 3–25 字符边界；新增四档原创引语和第 150 项 LTR 混排入口，并验证大小写转换、最终词数、标点、数字及全部服务端数据面。真实菜单与实际键入尚未启动 GUI 验收。
- 2026-09-09，自动化：固定 `russian_contractions` 与 `russian_contractions_1k` 的两个规模已重建为独立 Русский · Краткие формы入口。原创规则分别生成 200 条基础与完整包含基础集的 880 条扩展形式，保持各自长度、多词、符号、数字、大小写及纯西里尔计数；新增第 151、152 项 LTR 混排候选，并验证生成、四档引语、投稿、撤回、成绩和排行榜。真实菜单与实际键入尚未启动 GUI 验收。
- 2026-09-09，自动化：固定 `tamil_old` 已重建为独立 தமிழ் · பழைய தொகுப்பு入口。23×20 的 Typebar 原创音节组合生成 460 条唯一练习项，保持 3–13 Unicode 标量、唯一一条多词项、LTR、joining-script 与 `ta`；新增第 153 项 LTR 混排候选，并验证生成、四档引语、投稿、撤回、成绩和排行榜。与固定词表精确交集为零；真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 English 1k／5k／10k／25k／450k 已提升为五个独立入口。Typebar 原创确定性索引词流保持五组实际规模、唯一性、长度、大小写、符号、`noLazyMode` 和 Zipf 状态；25 词普通出题只读取 25 个索引，专项进程峰值内存约 17.2 MB。450k 弱项分析以单遍有界 Top-K 扫描替代全量中间排序，专项约 1.24 秒、峰值内存约 17.4 MB。五个 ID 已贯通四档引语、投稿、撤回、成绩和排行榜，并从相关映射移入独立映射。真实菜单、筛选大结果集、450k 首次键入感受依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Español 1k／10k／650k 已提升为三个独立入口。原创确定性索引词流保持 998／9,990／646,579 个唯一项及固定长度、大小写、符号、多词和非 ASCII 数量；三项与固定词表的精确交集均为零，并贯通 `es`／`es-ES`、四档引语、配置分享及服务端数据面。真实菜单、650k 筛选大结果集与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Français 1k／2k／10k 已提升为三个独立入口。原创确定性索引词流保持 1,394／2,041／10,251 个唯一项及固定长度、大小写、标点、多词和非 ASCII 数量；三项与固定词表的精确交集均为零，并贯通 `fr`／`fr-FR`、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Deutsch 1k／10k／250k 已提升为三个独立入口。原创确定性索引词流保持 988／9,994／239,243 个唯一项及固定长度、名词大写、标点、多词和非 ASCII 数量；三项与固定词表的精确交集均为零，并贯通 `de`／`de-DE`、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Română 1k／5k／10k／25k／50k／100k／200k 已提升为七个独立入口。原创确定性索引词流保持七档精确规模及固定长度、标点和非 ASCII 数量；七项与固定词表的精确交集均为零，并贯通 `ro`／`ro-RO`、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Polski 2k／5k／10k／20k／40k／200k 已提升为六个独立入口。原创确定性索引词流保持六档精确规模及固定长度、大小写、标点和非 ASCII 数量；六项与固定词表的精确交集均为零，并贯通 `pl`／`pl-PL`、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Беларуская 1k／5k／10k／25k／50k／100k 已提升为六个独立入口。原创确定性西里尔索引词流保持六档精确规模及固定长度、大小写、标点和全量非 ASCII 数量；六项与固定词表的精确交集均为零，并贯通 `be`／`be-BY`、逐档简化输入语义、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Русский 1k／5k／10k／25k／50k／375k 已提升为六个独立入口。原创确定性西里尔索引词流保持六档精确规模及固定长度、大小写、标点、多词和全量非 ASCII 数量；六项与固定词表的精确交集均为零，并贯通 `ru`／`ru-RU`、Zipf、逐档简化输入、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Português 1k／3k／5k／320k／550k 已提升为五个独立入口。原创确定性索引词流保持五档精确规模及固定长度、大小写、标点、多词、数字、符号和非 ASCII 数量；五项与固定词表的精确交集均为零，并贯通 `pt` 百科、逐档朗读与简化输入语义、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Français 600k 已提升为独立入口。原创确定性索引词流保持 633,941 个唯一项、1–33 字符、0 个大写项、34 个标点项、0 个多词或数字项及 282,793 个非 ASCII 项；与固定词表的精确交集为零，并贯通 `fr`／`fr-FR`、简化输入与 Zipf 元数据、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 `arabic_10k` 与 `arabic_egypt_1k` 已提升为两个独立入口。两套原创确定性阿拉伯字母索引词流保持 9,281／1,141 个唯一项及固定长度、大小写、前导／内部／双空格和全量非 ASCII 数量；与固定词表逐档精确交集为零，并贯通 RTL、joining-script、`ar`／`ar-SA`／`ar-EG`、简化输入、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 `korean_1k` 与 `korean_5k` 已提升为两个独立入口。两套原创确定性 Hangul 索引词流保持 975／4,201 个唯一项、1–5／1–6 字符和全量纯 Hangul；与固定词表逐档精确交集为零，并贯通普通空格词界、joining-script、`ko`／`ko-KR`、禁用简化输入与大写锁定提示、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Thai 六档数字规模已提升为独立入口。六套原创确定性索引词流保持逐档规模、Unicode 标量与可见字符长度、组合标记、标点、空格、数字、非字母和 Thai 区段项数量；与固定词表逐档精确交集为零，并贯通普通空格词界、`th`／`th-TH`、禁用简化输入、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Norsk bokmål 1k／5k／10k／150k／600k 已提升为五个独立入口。五套原创确定性索引词流保持逐档规模、长度、大小写、标点、数字、非字母和非 ASCII 数量；与固定词表逐档精确交集为零，并贯通空格词界、`no`／`nb-NO`、可选简化输入、逐档 Zipf 语义、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Norsk nynorsk 1k／5k／10k／100k／400k 已提升为五个独立入口。五套原创确定性索引词流保持逐档规模、长度、大小写、标点、空格、数字、非字母和非 ASCII 数量；与固定词表逐档精确交集为零，并贯通空格词界、`nn`／`nn-NO`、可选简化输入、逐档 Zipf 语义、四档引语、配置分享及服务端数据面。真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Swiss German 1k／2k 与 Afrikaans 1k／10k 已提升为四个独立入口。原创参数化按需索引词流保持 1,000／2,000／1,000／8,164 个唯一项及逐档长度、大小写、标点、空格、数字、非 ASCII 和交叠聚合；四项与固定可见词表的精确交集均为零，并贯通对应百科、朗读、四档引语、配置分享及服务端数据面。Swiss German 两档额外保持 `ß`→`ss` 转换、社区投稿拒绝、成绩接受和旧基础档引语 ID；真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-09，自动化：固定 Italiano 1k／7k／60k／280k 已提升为四个独立入口。原创参数化按需索引词流保持 1,159／7,154／60,442／279,833 个唯一项及逐档长度、大小写、标点、符号和非 ASCII 聚合；与固定词表逐档精确交集为零，并贯通 `it`／`it-IT`、四档引语、配置分享及服务端数据面。280k 普通出题只读取请求索引；真实菜单与实际键入依用户要求未启动 GUI 验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA8-20260902-185950.app`：自定义文本输入 `alpha`、Return、`beta` 后开始练习；提示保持两行，输入相同文本完成页显示 100% 准确率，目标文本逐行一致。随后以 `QA multiline` 保存，已保存文本列表显示标题和两行预览，重新选取后编辑器仍为原两行内容。TST-07 已验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA8-20260902-185950.app`：以 English 自有引语启用“重开时重复当前引语”。Craft 引语输入首字母 `A` 后重开，选择器与全文都保持 Craft；完整输入并完成后点击“再来一次”，选择器变为 Threshold 且提示全文随之切换。TST-05 已验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA9-20260902-191500.app`：曾验证旧实现会把实时仪表显示为一位小数。随后依据官方源码 `frontend/src/ts/config/metadata.tsx` 的结果页语义，发现该实现不兼容并已替换为“结果页固定显示两位小数”；QA9 不再作为 MET-10 验收证据，需由新候选包复验。
- 2026-09-02，隔离 release 候选包 `Typebar-QA11-20260902-192800.app`：开启“结果页固定显示两位小数”后，实时仪表仍保持整数 `0`；完成 25 词英语练习后，完成页显示 `2148.00 WPM`、`2148.00 Raw WPM` 与 `100.00%`，并保存为 QA11 的隔离本机成绩。WPH/CPM/WPS/CPS 的两位小数换算、以及设置持久化由客户端自动化覆盖。MET-10 已验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA9-20260902-191500.app`：选择 Deutsch 并开启“简化重音输入”后，提示实际显示 `brucke`、`grosse`、`muhe`、`strasse` 等 ASCII 形式。输入首词并提交后得到 100% 正确计分；在字数模式输入同一段 25 词简化提示后，完成页显示 25/25 正确、100% 准确率，目标文本仍为简化形式。该完成成绩仅写入 QA9 隔离容器，TST-06 已验收。
- 2026-09-02，当前 `Typebar.app`：设置页显示“听写模式”开关；开启后返回主界面，状态行显示“修饰器：听写模式”，重开练习后出现“听写模式：请听系统语音”。提示文字的隐藏、系统朗读启动和计分正确性仍需在一段完整的人工输入中共同验收，因此 ACC-01 保持待验收。
- 2026-09-02，当前 `Typebar.app`：切换到引语模式后可见“搜索当前引语（仅本机）”。输入 `quiet` 后当前选择保留为内容含该词的原创引语，清空后恢复完整可选列表；搜索字段和界面说明均未触发网络操作，TST-03 已验收。
- 2026-09-02，当前 `Typebar.app`：引语长度菜单提供“超长引语”。选择后显示 Typebar 自有的 English「Long Table」完整长篇提示，可进入输入区；检查后恢复为“全部长度”，TST-04 已验收。
- 2026-09-02，自动化：空格分词文本中“遇错清除当前词”会保留已提交词，清除未提交词，并逐字符记录删除事件以供回放重建；简体中文文本不会触发该行为。仍需在打包应用中验证实际键盘交互，因此 FUN-03 保持待验收。
- 2026-09-02，自动化：信心模式开启时无法退回修改上一错误词；最大档禁用退格；冲突的自由回退、遇错停下/删除会被配置归一化关闭。仍需在打包应用中验证实际键盘交互，因此 INP-06 保持待验收。
- 2026-09-02，当前新版 `Typebar.app`：设置页显示“信心模式”及说明，菜单包含“关闭”“开启”“最大”三档。设置可见性已确认；尚未以真实键盘输入验证回退限制，因此 INP-06 保持待验收。
- 2026-09-02，自动化：快速重开枚举只匹配其对应的原生 Esc/Tab/Enter 字符，关闭档不匹配。仍需在打包应用中逐键触发验证，因此 CFG-03 保持待验收。
- 2026-09-02，当前新版 `Typebar.app`：设置页将快速重开从 Esc 切换至 Tab 和 Enter 后，主界面提示分别同步更新；每次先输入正确首字母再按选定键，均立即换为新提示、移除“放弃本次测试”按钮并将 WPM/Raw/Burst/错误复位。检查后恢复为 Esc。CFG-03 的 Tab/Enter 人工交互已验收；关闭档不匹配和键位映射由自动化覆盖。
- 2026-09-02，自动化：引语重开策略仅在“已开始且未结束”时保留当前引语；关闭策略、未开始或已完成时均不保留。仍需在打包应用中观察实际引语切换，因此 TST-05 保持待验收。
- 2026-09-02，自动化：简化重音输入会将 `árvore`、`Straße`、`cœur`、`Łódź` 分别规范为可直接输入的 `arvore`、`Strasse`、`coeur`、`Lodz`；德语生成词流不再含 ä/ü/ß。该行为已在隔离 release 候选中完成实际提示与输入验收，见后续 TST-06 记录。
- 同一构建的主界面显示“弱项训练”和“朗读提示”按钮，且其辅助说明明确分别限制为本机完成测试的自有词库分析、macOS 本机语音朗读。它们的完整运行效果尚未人工验收，故 MET-02 与 SYS-01 保持待验收。
- 2026-09-02，当前 `Typebar.app`：设置页的“光标样式”菜单提供条形、下划线和块状选项；选择下划线后，练习区首个目标字符下方出现下划线，重启应用后设置仍为下划线。检查后已恢复为原先的块状光标和已开启的听写模式。尚未逐一完成条形、块状的视觉对比，因此 VIS-01 保持待验收。
- 2026-09-02，当前 `Typebar.app`：设置页“练习行宽”菜单显示紧凑、标准、宽、自定义、自适应。选择“自定义”后，界面出现“自定义列数”控制；“键盘布局”菜单显示并可选中 ANSI Workman。检查后恢复为验收前的标准行宽与 ANSI QWERTY。菜单可见性与交互已验收。2026-09-02 自动化新增物理 ANSI 键位模拟检查：Dvorak、Colemak、Workman 与 AZERTY 的代表性非默认键位和 Shift 符号均按独立原生映射转换；仍需在真实实体键盘上逐布局完成输入验收，因此 VIS-02 保持待验收。
- 2026-09-02，当前 `Typebar.app`：设置页显示独立“倒计时提示音”菜单，当前值为“关闭”，说明文本明确其与键击、错误音共用系统提示音和音量；该菜单提供关闭、结束前 1/3/5/10 秒。CFG-02 的菜单可见性已验收；实际音频时点另由自动化策略测试覆盖，未把它替代为人工音频听测。
- 2026-09-02，当前 `Typebar.app`：主界面语言菜单显示 Português。选择后，时间练习区生成 Typebar 自有葡萄牙语词流，观察到 `árvore`、`memória`、`atenção`、`manhã`、`céu` 等字符，不需要网络请求。检查后恢复为英语。引语、社区投稿与服务端排行路径由客户端和服务端自动化测试覆盖，未把它们冒充为本次离线 UI 验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA12-20260902-193500.app`：当时设置页显示历史速度图的零起点开关及趋势图缩放说明，默认开启；关闭后关闭设置窗口并以 ⌘, 重开，开关仍保持关闭。该候选未创建两条不同速度的历史成绩，故尚未将趋势图实际 Y 轴缩放或两个计数柱状图的零起点视为人工验收；该开关现更名为“速度图从零开始”，并同时控制结果轨迹；MET-11 保持待验收。候选进程已退出。
- 2026-09-02，隔离 release 候选包 `Typebar-QA14-20260902-194500.app`：在英语 25 词测试完成页，“本次单词历史”初始为折叠状态，手动点击后显示 25/25 正确的逐词列表。随后开启“完成后自动展开单词历史”，关闭并以 ⌘, 重开设置后开关仍为开启；完成下一轮 25 词测试时，单词历史初始即展开并显示逐词列表。两条成绩只写入 QA14 隔离容器，候选进程已退出；MET-12 已验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA15-20260902-195100.app`：设置页的“显示近 10 次平均”提供关闭、速度、准确率、速度和准确率四档。选中“速度和准确率”后，英语 25 词练习区显示 `近 3 次平均：2108 WPM · 100% 准确率`；关闭并以 ⌘, 重开设置后仍选中“速度和准确率”。同设置最近 10 条截取、不同词数/引语排除与无同类成绩返回“暂无”的策略由客户端自动化覆盖。候选进程已退出；MET-13 已验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA16-20260902-200700.app`：设置页显示“显示本机个人最佳”开关及资格说明；开启、关闭设置并以 ⌘, 重开后仍保持开启。英语时间练习区显示 `本机个人最佳：22 WPM · 98% 准确率`；启用 Zipf 高频词后显示“当前测试不计入 PB”，切换到引语模式仍明确不计入。不同设置、特殊流、遇错停下非满准确率、引语与多语混排的资格筛选由客户端自动化覆盖。候选进程已退出；MET-14 已验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA17-20260902-202100.app`：设置页默认关闭“结果页显示单词 Burst 热力图”；开启后关闭并以 ⌘, 重开设置，开关仍为开启。以英语两词完成 `copper planet` 后展开单词历史，显示“单词 Burst 热力图”、慢/偏慢/均衡/快/最快五档当前 WPM 图例和“无计时”中性说明。当前轮次两词均有可测间隔；无有效间隔的中性路径与分档阈值由客户端自动化覆盖。候选进程已退出；MET-15 已验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA18-20260902-203400.app`：以英语两词 `quiet canyon` 输入中故意键入 `x` 并等待后退格修正，完成页显示本机重建的速度折线和错误柱；AX 图表确认错误轨迹从 0 升至 1，修正后回落至 0。依次关闭 Raw、Burst、错误轨迹后，下一轮 `pocket pocket` 结果页三个按钮仍全部为“隐藏”，仅保留 WPM 线。候选进程已退出；MET-16 已验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA19-20260902-204300.app`：以英语两词 `window orchard` 完成后，结果页“导出”菜单显示“复制结果文字”“复制结果图片”“保存结果图片…”三个动作；执行图片复制后结果页显示“结果图片已复制”。保存面板没有在人工验收中打开，因此未写入任何文件。自动化已验证 UTC 文件名和渲染出的 1520 像素宽主题结果卡；候选进程已退出；MET-17 已验收。
- 2026-09-02，隔离 release 候选包 `Typebar-QA20-20260902-210400.app`：以英语五词 `willow meadow planet harbor amber` 完成后，完成页显示“重复本轮”和“练习慢词（2）”。点击“重复本轮”后，字数模式、5 词配置和完整提示均恢复为同一轮的未开始状态。第二轮完成后，“导出”菜单含“复制实际输入”，执行后显示“实际输入已复制”；点击慢词入口进入自定义输入完成、按原顺序文本 `willow willow willow harbor`，最慢词被加权。候选进程已退出；MET-18、MET-19、MET-20 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA26-20260903-104500.app`：在英语单词 `lantern` 输入 `lax`，退格删除 `x` 后完成 `ntern`；结果页为 100% 准确率，但“错词练习”同时列出“只练错词（1）”与“带前词上下文（1）”。中文字符自动化只能形成标记输入，无法注入已提交的 IME 文本，故中文修正/删空分支仍以自动化覆盖且不标记为实机验收。候选进程已退出；MET-24 部分验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA27-20260903-105600.app`：在英语单词 `canyon` 的同一位置两次输入 `x`、每次均退格修正，完成页仍为 100% 准确率。“错词练习”菜单显示去重的“只练错词（1）”和按错误事件计数的“带前词上下文（2）”；选中前者后进入按原顺序、输入完成的自定义文本 `canyon canyon`。候选进程已退出；MET-25 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA28-20260903-110300.app`：自定义提示 `a b c d e f g h i j k` 的每个目标均触发一次错误并修正后完成。错词菜单显示“只练错词（11）”“带前词上下文（10）”和“复制错词列表”；点击复制后结果页显示“错词列表已复制”。验收没有读取用户剪贴板，英文/中文内容格式由自动化覆盖。候选进程已退出；MET-26、MET-27 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA29-20260903-110900.app`：从 English 字数 1 词的原测试将 `violet` 错输为 `xiolet` 后，选“只练错词（1）”进入自定义、按原顺序的 `violet` 练习。完成后点“重复本轮”仍显示相同的自定义 `violet`；再次完成后点“再来一次”恢复为 English、字数模式和 1 词参数，并生成新的原测试提示。候选进程已退出；MET-28 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA30-20260903-111500.app`：在 English 字数 3 词 `copper thunder summer` 中各输入错误词，结果菜单显示“只练错词（3）”。进入后自定义编辑器显示 15 个由竖线分隔的候选段，完成方式为“分节完成”，实时进度为 `0/15`；段落顺序为 `summer | copper | thunder | …`，并非原提示顺序。错误次数不相同的 `thunder` 在候选池保留了更多副本。上下文目标计数和同一候选池的轮换由自动化覆盖。候选进程已退出；MET-29 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA31-20260903-112000.app`：从 English 字数 1 词 `copper` 的错词练习进入五段 `copper` 临时练习后，手动切换到字数模式并改为 2 词，完成 `voyage harbor`。结果页点“再来一次”后保持 English 字数 2 词，并生成 `marble violet`，没有跳回旧的一词配置。候选进程已退出；MET-28 的手动离开分支已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA32-20260903-113000.app`：English 字数 5 词 `bright summer ripple ripple canyon` 中将首词输为 `xright`，完成页同时显示“错词练习”和“练习慢词（2）”。错词菜单含“错词 + 慢词（3）”；进入后为“分节完成”，编辑器显示 15 个洗牌的 `bright`、`ripple`、`canyon` 候选段，实时进度为 `0/15`。候选进程已退出；MET-30 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA33-20260903-113600.app`：English 字数 5 词 `amber marble harbor thunder planet` 中将第二词输为 `xxxxxx`，完成页同时显示错词与“练习慢词（2）”。错词菜单含“上下文错词 + 慢词（3）”；进入后为“分节完成”，编辑器显示 15 个洗牌候选段，包含 `amber marble` 上下文短语，实时进度为 `0/15`。候选进程已退出；MET-31 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA34B-20260903-114700.app`：按修正后的参考慢词规则完成 English 字数 5 词 `planet planet tangent summer paper`，将第二词输为 `xxxxxx`。结果页只显示“练习慢词（1）”；错词菜单显示“错词 + 慢词（2）”与“上下文错词 + 慢词（3）”。前者进入分节完成的 10 段练习；后者的 3 个候选来自两个已尝试 `planet` 位置的上下文和一个慢词候选。候选进程已退出；MET-18、MET-30、MET-31 已复验。
- 2026-09-03，隔离内存候选包 `Typebar-QA35-20260903-114900.app`：English 字数 2 词 `paper marble` 中将首词输入为 `xaper`，完成后结果页“导出”菜单显示“复制练习提示”。选择后完成页显示“已练习提示已复制”；未读取剪贴板。自动化断言已验证复制候选为已到达目标词 `paper`，不包含错误输入 `xaper` 或后续 `marble`，并覆盖禅模式和无空格范围。候选进程 PID 34454 已退出；MET-32 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA37-20260903-120000.app`：启用“ASL 指语练习”后，以自定义文本 `JZ` 启动。辅助功能树分别标注“J 弧线运动轨迹”和“Z 折线运动轨迹”；输入 `J` 后下一键提示切换为 `Z`，指标为 100% 准确率、0 错误。重开后输入 `x`，首个原创矢量手势显示红色错误状态，指标变为 0% 准确率、1 错误。该功能仅提供原创视觉练习提示，不宣称可替代正式 ASL 教学。候选进程 PID 37086 已退出；FUN-17 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA38-20260903-121200.app`：设置页显示“遇错停下”的关闭/字符/单词三档和“遇错删除”的关闭/字符（退一格）/字符（硬）/单词/单词（硬）五档。选择单词停下后，以自定义 `amber bay` 输入 `amxer `，提示仍停在空格且错误为 1；切换单词（硬）删除后，停下自动回到关闭。重开并输入 `amber ` 后于 `bay` 首字符输入 `x`，下一键返回 `A`，可见光标回到 `amber` 首字符；候选进程 PID 40350 已退出。其他档位、旧布尔偏好迁移、持久化与回放由 140 项自动化覆盖；INP-09 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA39-20260903-122200.app`：设置页的“最低单词速度”菜单显示关闭、固定、弹性三档。选择弹性后关闭并再次打开设置，当前值仍为“弹性”；说明文案明确固定阈值、长词弹性阈值和所有可计时空格提交词的检查范围。自动化以 100 WPM 验证 9 字符词的固定阈值为 100、弹性阈值为 70；同一约 80 WPM 的提交在固定档失败、弹性档继续，错误提交词也会失败，且设置快照会保留弹性档与 WPM。候选进程 PID 42053 已退出；INP-10 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA40-20260903-123300.app`：切换禅模式后，原生界面显示自由输入和 Shift+Enter 完成说明，下一键提示变为无可提示键。输入 `made up`、换行、`line`、Tab、`end` 后，练习区按原样呈现三种输入，指标为 100% 准确率、0 错误且测试保持进行中。AppKit 自动化以带 Shift 的 Return 事件验证只调用禅模式完成回调、不会额外插入换行；会话与 Burst 边界由 142 项客户端自动化覆盖。候选进程 PID 43664 已退出；TST-08 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA41-20260903-123700.app`：从时间切到禅模式后，语言、英文拼写、标点、数字和时长控件均从原生辅助功能树移除；界面保留禅模式自由输入/Shift+Enter 说明，下一键提示为“没有可提示的下一键”。候选进程 PID 44157 已退出；TST-08 的配置收敛已复验。
- 2026-09-03，隔离内存候选包 `Typebar-QA44-20260903-125500.app`：设置页显示更新后的最低单词速度说明，明确空格提交、无空格原始词末字符，以及禅模式的空格/换行提交都会检查。选择弹性档并启用“无空格”后，主界面标出该修饰器；自定义 `amber harbor` 应用后提示实际显示为连续的 `amberharbor`。无空格的逐词边界、循环扩展与固定/弹性阈值由 146 项自动化覆盖；候选进程已退出；INP-10 已复验。
- 2026-09-03，隔离内存候选包 `Typebar-QA45-20260903-125800.app`：启用“无空格”并以自定义 `amber harbor` 开始后，提示显示为连续的 `amberharbor`。输入 `axberharbor` 完成后，结果页显示“本次单词历史 1/2 正确”；展开后逐项显示 `amber → axber` 与 `harbor`。错词菜单只列出“只练错词（1）”及其上下文入口，没有把整段连续提示当成一个错误词。Burst 热力图和慢词选择的逐词路径由 149 项客户端自动化覆盖。候选进程 PID 47866 已退出；FUN-25 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA46-20260903-130300.app`：English 字数 2 词启用“无空格”并重开后，提示显示为连续的 `quietdrift`，实时进度从 `0/2` 开始；输入首词 `quiet` 的末字符后立刻显示 `1/2`，完成 `drift` 后结果页显示“本次单词历史 2/2 正确”。候选进程 PID 48420 已退出；FUN-26 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA47-20260903-130800.app`：选择 expert 和“无空格”，以自定义 `amber harbor` 重开后提示为连续的 `amberharbor`。首词输入 `amxer` 到达词末时立即弹出“本次测试失败 本次没有保存为完成成绩”，没有等待空格或整个提示结束。候选进程 PID 49065 已退出；FUN-27 已验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA48-20260903-131300.app`：选择 normal、遇错停下“单词”与“无空格”，自定义 `amber harbor` 重开后提示为连续的 `amberharbor`。输入普通空格、全角空格和 `amxer` 后，实时指标为 1 个错误、下一键仍为首词末字符 `R`，表明两种空白没有计错且错误词没有越过隐藏边界。受自动化环境的退格虚拟键限制，实际退格修正保留为自动化验收；153 项客户端测试已覆盖删去错误、补全首词并进入下一词。候选进程 PID 49734 已停止；FUN-28 部分验收。
- 2026-09-03，隔离内存候选包 `Typebar-QA49-20260903-132400.app`：以自定义 `amber harbor` 启动普通难度后，普通词首空白的实机 Unicode 注入受自动化输入法限制，保留为客户端测试证据；启用“严格空格”、重开并输入 ASCII 空格后，实时指标显示 `0%` 准确率与 `1` 个错误，下一键为 `M`，确认词首空白被保留为可修正错误而非静默跳过。客户端自动化还验证词首全角空格在普通难度不计错、词间全角空格规范为普通分隔符，以及 expert 词首空格不会立即失败。候选进程 PID 50860 已停止；INP-11 部分验收。
- 2026-09-09，`TST-276`–`TST-284` 自动化验收：Serbian Latin 10k、Serbian 10k、Bulgarian 1k、Bulgarian Latin 1k、Bosnian 4k、Albanian 1k 与 Macedonian 1k／10k／75k 九个入口已覆盖独立 ID、显示名、逐档规模与唯一性、长度、拉丁／西里尔脚本、大小写、标点、内部空格、简化输入、Zipf、在线来源、朗读、四档引语、配置分享和多语排除。服务端投稿、撤回、成绩及排行榜由同一语言 ID 白名单覆盖；本批未启动 Typebar 图形程序，保留菜单选择与真实输入法交互为后续人工验收。
- 2026-09-09，`TST-285`–`TST-298` 自动化验收：最后十四个语言规模入口已覆盖独立 ID、显示名、数量与唯一性、字符／标量长度、标点和空格分布、大小写、Ethiopic／Armenian／Tibetan 等脚本、简化输入、Zipf、在线来源、朗读、四档引语、配置分享、多语排除及服务端投稿／成绩／排行榜。固定机器总账达到 446／446／0／0；本批未启动 Typebar 图形程序，菜单可见性和真实输入法交互仍留作后续人工验收。
- 2026-09-09，`TST-02` 自定义多语自动化验收：固定 schema 的 446 个单语言入口全部进入可搜索选择器；自动化覆盖去重、少于两项回退、LTR、全 RTL、双向、无空格、代码 token、按需大型词流以及旧默认组合。人工步骤为搜索 Python 与 العربية，分别建立 Swift/Python、Arabic/Hebrew、English/Arabic 和简体中文 50k/片假名组合，确认顺序轮转、RTL 对齐和输入完成；依用户要求本批不启动 GUI，因此视觉与真实输入法交互状态仍为待验收。
