# 功能盘点与追踪表

参考仓库固定为 `91bd24bb8513785c7364cbea29296ff7adafac41`。本文件是需求索引，不含参考项目的代码或资产。

官方 48 项 funbox 的逐项来源、映射与未实现缺口见 `OFFICIAL_FUNBOX_AUDIT.md`；该审计是本表 FUN-01/FUN-02 的细化证据。

| ID | 用户能力 | 参考证据（仅用于盘点） | Typebar 目标模块 | 状态 |
| --- | --- | --- | --- | --- |
| TST-01 | 五类测试与模式参数 | `components/pages/test/TestConfig.tsx` | TestDomain | 部分实现 |
| TST-02 | 标点、数字、语言与混合语言 | `config/metadata.tsx` | ContentDomain | 部分实现：计时、字数、引语和禅模式支持原创 English、Español、Deutsch、Français、Italiano、Português 与简体中文离线内容；计时、字数和禅模式可加入数字和相应语言标点；计时、字数和禅模式另有自有的中英混合词流，以及可从七种单语中选择至少两种、按选择顺序轮转的多语混排，均以空格分隔可提交的词。该组合进入测试配置、预设、分享链接、结果和归档；旧配置缺少该字段时会回退到七语组合。Español、Deutsch、Français 与 Português 词表分别覆盖组合重音、变音、ß 或葡语重音输入；“简化重音输入”修饰器会在当前测试提示中把重音、变音、常见连字及 ß 转为普通拉丁输入，不修改原始离线内容或用户文本。自建成绩榜也接受两种混排语言，社区投稿与公开引语接受七种单语。更多语言尚未实现 |
| TST-03 | 引语长度、收藏引语、重复策略 | `TestConfig.tsx`, `quotes-controller.ts` | QuoteDomain | 部分实现：引语模式默认使用自有离线内容，支持短/中/长/超长及全部长度筛选、收藏和“重开时重复当前引语”；启用该策略时，只有正在输入的引语会在重开后保留，完成后重开仍换一条；“随机一条”始终避开当前内容。七种单语均有一则 Typebar 自有超长引语。可按当前来源的标题/文本以空白分词的本机搜索缩小选择，关键词不会离开设备，也不会改写内容；亦可明确切换到社区审核来源，按语言刷新仅服务端批准的投稿。收藏与重复策略会随本机设置和归档保存。跨设备队列尚未实现 |
| TST-04 | 自定义文本、保存文本与长度限制 | `CustomTextModal.tsx`, `SavedTextsModal.tsx` | CustomTextDomain | 部分实现：自定义文本可编辑、保存、列出、复用和删除，并会进入本机备份；编辑器统一限制为 10,000 字符并显示计数，保存、分享和归档导入使用同一有效性规则。可选输入完成、循环计时、循环字数或分节完成；可保持顺序、打乱一次或随机抽取用户文本中的词。自定义提示含换行时，原生输入桥会仅在当前提示需要换行时将 Return 计为换行，避免普通练习误触 Return；用 `|` 分隔段落后可选择前 N 段，练习页显示当前段落进度。循环提示由 Typebar 动态扩展；启用无空格时循环片段保持连续，并保留可安全映射的原始词边界及渲染后词段，以供 Burst、结果词历史、错词与慢词复练使用。配置会进入预设、链接、结果与归档 |
| TST-05 | 禅模式、无限测试、快速重开与放弃 | `event-handlers/test.ts`, `quick-restart.ts` | TestDomain | 部分实现：禅模式不生成或比较提示，只呈现本机自由输入；空格或换行提交输入词，Tab 保留为输入文本，任意内容计为正确且不会自动结束。每个活动词上限为 30 字符以防止意外长按；Shift+Enter 明确结束本轮，⌘R 仍可重开。会话、回放与最低单词 Burst 的空格/换行提交路径已自动化覆盖，真实键盘验收待执行 |

2026-09-03 更正：TST-02 中“禅模式支持原创语言词流、标点与数字”的表述已失效。禅模式的内容完全由用户本机输入决定，不生成或导入词库内容；原生配置界面会隐藏语言、拼写、标点、数字和混合词流设置，它们只用于其他有提示的测试模式。
| INP-01 | 输入、逐字符状态、退格、长词 | `input/handlers/*` | TypingEngine | 部分实现 |
| INP-02 | 组合输入/IME、焦点及 Caps Lock 提示 | `input/*`, `CapsWarning.tsx` | AppKitInputBridge | 部分实现：原生 AppKit 输入桥支持组合输入；标记文本在确认前只更新可选的关闭/下方/替换当前字符组合显示，不会改变提示、输入、计分或回放，确认提交时才进入输入引擎。输入视图会回传实际 responder 焦点，失焦时显示可点击恢复焦点的提示；英文/中英混合练习会读取 macOS Caps Lock 状态显示警告。组合显示和两类提示均可在本机设置持久化；IME 最终人工验收尚未完成 |
| INP-03 | normal/expert/master 难度 | `input/helpers/fail-or-finish.ts` | TypingEngine | 部分实现：普通难度保留错误继续输入；expert 会在每个词的提交点失败，空格分词使用提交空格，无空格修饰器使用保留原始词边界的末字符；master 仍在任一错误字符被接受时立即失败。已修正的错误词只要最终输入与目标一致即可通过 expert 词级检查；强制标记的物理键位错误仍作为真实错误参与对应判定。 |
| INP-04 | 停止/删除错误、严格空格、盲打、自由输入 | `config/metadata.tsx` | TypingEngine | 部分实现：停止/删除错误、严格空格、隐藏额外字符、盲打、自由回退、三档信心模式与反向 Shift 已持久化；遇错停下可选“字符”（拒绝错误按键）或“单词”（允许继续输入，但错误词在完全修正前不能以词提交点提交：普通提示为输入空格，无空格提示为原始词的末字符）。无空格会拒绝直接输入的常见 Unicode 空白字符。遇错删除可选“字符”（错误键会退回一个已接受字符）、“单词”（清空当前词），以及两种在新词首个错误时返回上一词的硬模式；选择会与信心模式和另一类错误规则互斥，旧布尔偏好会迁移到字符档。反向 Shift 通过左右 Shift 状态和 ANSI 键位侧判断要求使用相反手，Y/B/6 可使用任意一侧，手位错误保留真实输入文本但以可回放的输入错误影响字形、统计、错词练习和结果。默认保护已正确提交的词但允许修复错误词，开启自由回退后可修改任意已提交词。信心模式开启时不可退回修复上一个错误词，最大档禁用全部退格；它与自由回退、遇错停下/删除互斥并在配置解码时归一化。“修正后再前进”修饰器会在词提交点拒绝错误词，直至该词完全修正；Typebar 自有的“遇错清除当前词”只对空格分词文本生效，误键会删除当前未提交词、保留已提交词并按逐字符删除事件写入回放。另有 Typebar 自有的最低单词速度规则：只在正确空格提交的词后按真实输入间隔校验；最低整体速度和最低准确率则只在有限测试本会完成时校验最终 WPM/准确率。任一阈值未达都会标记失败，不保存或发布完成成绩。三种阈值均进入预设、分享、结果与归档。其他输入规则仍持续审计。 |

2026-09-03 更正：INP-04 中“最低单词速度只检查正确词”的表述已失效。现行实现提供关闭、固定与弹性三档：固定使用设定 WPM；弹性阈值为 `min(base, floor(base × 1.03^(-2 × (目标词长 − 3))))`。每个具可测 burst 的空格提交词、无空格模式中原始词的末字符提交，以及禅模式的空格/换行提交都会检查，无论该词是否正确。

2026-09-03 补充：INP-04 现依照参考输入规则处理空白：普通难度会忽略每个词首的常见 Unicode 空白，严格空格会保留其为可修正错误，expert/master 与硬删除也会接收该按键；其余常见 Unicode 空白在验证前规范为普通词间空格，而无空格模式会在计分前拒绝它们。普通难度在词内提前输入空格时会保留实际输入、将该词作为一次错误提交并推进到下一目标词；目标词写完后继续输入的额外字符仍属于当前词，显示并计作错误，直至空格提交。每个空格分词输入缓冲限制为目标词（含可见提交分隔符）加 20 个字符，到达上限仍可输入提交空格。提示字形、词进度、结果词历史、错误计数和自由退格都以实际字符到目标位置的映射保持一致，expert 则在同一错误提交点失败。

代码输入现提供官方当前目录的全部 69 个语言/方言选择，并为每项提供 Typebar 原创离线片段；换行后的 Tab 自动缩进、可选退格反缩进和自动事件回放已实现，代码语言按完整字符提示结束有限字数测试。全量自动化会逐项验证提示生成与完成。官方的原始词表规模、代码语料和代码专属统计均未导入或复刻，仍未达到内容层面的功能等价。

INP-04 的“按键位图”档用所选 Typebar 布局的原创映射，将 QMK 等外设已重映射的逻辑字符反查为 ANSI 位置，再判定 Shift 是否来自相反手；它与默认的直接物理键码档分开保存。
| INP-05 | 错误呈现、额外字符和光标行为 | `elements/caret.ts`, `input/*` | TestRenderer | 部分实现：原生逐字符状态模型会区分正确、错误、当前位置、未输入、隐藏与额外字符；练习页以颜色和背景显示错误与当前位置，额外输入会以红色删除线显示，盲打会隐藏已输入字符；“错误字符显示”可关闭、替换为实际输入、在下方提示实际输入，或替换并在下方提示目标字符；它只改变渲染，永不改变提示、输入、计分或回放。已提交词可选择保留、隐藏、淡化或逐字显示圆点，词间空格、当前词和后续提示不受影响。可选原生条形、下划线或块状光标会明确标示当前目标字符，并随设置和归档持久化；“专注当前词”修饰器会在空格分词练习中仅显示当前词，提交空格后显示下一词，不改变完整提示、输入或计分。可选的错误提示音使用 macOS 系统声音，默认关闭且可持久化。平滑光标、逐行滚动和完整 CSS 光标样式尚未实现 |
| MET-01 | 实时 WPM/raw/准确率/burst/计时器 | `states/live-stats.ts`, `test/pace-caret.ts` | MetricsDomain | 部分实现：原生练习页实时显示 WPM、raw WPM、准确率、错误、单词 burst 与计时/进度；时间模式显示剩余倒计时，字数模式无论是否显示分隔符都会显示已提交词/目标词，并不从显示反推计分。进度可独立设为文字、迷你、隐藏或原生条形，条形使用同一 0–1 时间/字数比例；WPM、CPM、WPS、CPS、WPH 可只在实时显示层切换，结果/统计/同步仍使用规范 WPM；“结果页固定显示两位小数”会按当前速度单位格式化完成页的速度、Raw 速度和准确率，而不改变保存的规范数值；速度（含 Raw）、准确率和 burst 可独立设为文字、迷你或隐藏；burst 按单词首键至提交空格计算，无空格模式则以可安全映射的原始词末字符作为提交点，当前词满两键后也会显示。用户可选择在练习区显示近 10 条同设置的本机完成成绩均速、平均准确率或两者；筛选模式、时长/词数（引语按当前内容）、标点、数字、语言、难度与简化输入，且不依赖账户。指标下方以原生条带展示最多 8 个近期已提交词的速度，仅在当前会话内存在，包含实际尝试词以便与错误数对照；可选节奏引导会按自定义目标、同模式同语言的本地个人最佳、平均、当日平均或本次运行中的上一轮速度计算，练习文本中会以可选条形、下划线或块状第二标记显示目标位置；可选“重开后沿用上一轮节奏一次”只作用于下一轮且不落盘。完整节奏统计与原项目完整计时器行为尚未实现 |
| MET-02 | 单词历史、错词练习与输入回放 | `test/replay-ui.ts`, `practise-words.ts`, `weak-spot.ts` | ReplayDomain | 部分实现：空格分词完成结果页（英语、西班牙语和中英混合）会展示实际尝试词的目标/输入对照并标记正误；无空格修饰器在可安全映射提示变换时同样保留每个渲染后词段的目标、实际输入、错误事件与 Burst，不把连续提示误作一个词。单词历史默认折叠，用户可手动展开或在设置中选择完成后自动展开，且该偏好会持久化。可选的结果页单词 Burst 热力图从本轮实际输入事件推导每个已尝试词的速度，再以 Typebar 自有五分位分档着色和显示当前速度单位图例；单事件或没有有效时间间隔的词保持中性，绝不伪造极端速度。可基于实际错误输入事件提供“错词练习”菜单：最多 20 个去重目标按错误次数重复进入有限练习；最多 10 个已尝试上下文目标也按错误次数保留前一个目标词。两种方式都会把按错误次数展开的候选段落池逐轮洗牌，并取入选候选数的 5 倍段落，既保持频次权重又保证每个候选重复出现；同时存在错词与可测慢词时，结果菜单会提供组合练习并将错词候选限制为 10。后来已退格修正或删空的错误仍保留，正确但未完成的词、未达到的后续词不列为错误。简体中文仅从 Typebar 自有词库识别实际输入过的无空格短词生成去重错词练习，跳过标点、未知自定义片段和未到达尾部，复练文本保持无空格。错词练习完成后点“再来一次”会恢复进入练习前的测试配置；“重复本轮”则保留同一份错词提示；若用户先手动切换到另一非自定义模式，则会丢弃临时恢复记录、把该选择作为新的测试来源。慢词练习会排除尾部活动项，按单词 burst 选取最慢的四舍五入 20%、最多 20 个位置，并按慢度加权；无法计时项以 0 参与该本机排序。完成结果会保存 Typebar 自有的插入/删除时间事件和提示快照，结果页可播放、暂停、拖动和重置输入重建。原生“弱项训练”会只从本机已完成回放逐事件统计错误目标字符，再在 Typebar 自有词库中挑选高关联词组成自定义练习，不上传数据、不会复用参考词库；更完整的持久化复练视图尚未实现 |
| MET-03 | 个人最佳、结果页和结果标签 | `collections/results.ts`, `result-screen/*` | ResultsDomain | 部分实现：测试完成后显示关键结果、重开、历史入口与原生导出菜单；可复制仅含本机成绩的文字结果、本机回放重建的实际输入，也可用当前 Typebar 主题渲染全新结果卡，复制 PNG 或通过 macOS 保存面板导出 PNG，不截取或复用参考网页 UI。不存在回放时会明确拒绝复制实际输入，绝不从提示或统计伪造内容。完成页会显示“今日练习”时长和本机完成次数：当天已保存成绩与本次运行中未保存的成绩共同计入，以 ID 去重，跨日成绩不参与。完成页的“重复本轮”会从本轮开始时保存的会话模板重新启动，严格保留测试配置、提示和自定义计时文本的循环源，不携带已输入内容、计时或回放；“再来一次”仍按当前选择生成新内容。结果页会从本机保存的输入回放按秒重建 WPM、Raw、Burst 与当前错误数轨迹，最多显示 120 秒；Raw、Burst、错误层可单独切换并持久化，超过上限或缺少回放时明确不显示图表。历史列表和成绩详情会标记并列最高 WPM 的个人最佳；练习区可选择显示本机当前设置个人最佳，筛选模式、时长/词数、内容选项、语言、难度和简化输入，并排除引语、官方不允许 PB 的 funbox 及 Typebar 自有强制纠错修饰器。成绩可添加/移除受约束的本地标签，并随备份迁移 |
| MET-32 | 复制已练习提示 | `commandline/lists/result-screen.ts` | ResultsDomain | 部分实现：原生结果页“导出”可复制本轮已到达的目标提示；空格分词测试使用实际尝试词的目标文本，绝不复制错误输入或未到达后续词。禅模式仍复制本机输入；简体中文按 Typebar 自有词表把已到达的部分字、括号或词尾标点扩展为完整当前目标词，其他无空格文本按本机回放的最终到达范围截取。没有可用本机回放/目标范围时明确拒绝，不根据统计伪造文本。 |
| MET-04 | 历史、筛选、趋势/柱状/直方图/热力图/连续天数 | `components/pages/account/*` | AnalyticsDomain | 部分实现：本地历史可组合按模式、语言、标签和个人最佳筛选；这些条件可命名保存为仅本机的筛选预设、重新应用或删除。汇总、趋势、WPM 固定区间直方图、近 28 日完成次数/练习分钟柱状图、按日活动聚合、近 12 周热力图和连续天数均随筛选更新；历史速度趋势图可选强制从零开始（默认开启），关闭后按实际数据范围缩放，完成次数和练习分钟的计数轴始终从零开始；原项目的完整账户统计、更多筛选和图表交互尚未实现 |
| CFG-01 | 设置搜索、预设、导入与导出 | `pages/settings/*`, `preset-controller.ts` | PreferencesDomain | 部分实现：原生设置窗口可按中英文标题/关键词过滤测试、显示、主题、账户和恢复默认设置分组；完整测试预设可保存、列出、应用和删除；设置、成绩、预设和自定义文本可用原生文件选择器导入/导出，导入会去重合并，并兼容 v1 归档；原项目细粒度设置搜索等尚未实现 |
| CFG-02 | 全部显示/行为/声音/键盘/外观选项 | `config/metadata.tsx` | PreferencesDomain | 部分实现：难度、输入规则（含最后一词快速结束）、英式/美式英文拼写、字体大小、下一键提示和原创 ANSI QWERTY/Dvorak/Colemak/Workman/French AZERTY 布局选择已持久化并有原生设置 UI；非默认布局还会将 ANSI 物理 keyCode 转为相应字符和 Shift 符号，默认 QWERTY 与 Option/Control/Command 输入保持 AppKit 原生路径。快速重开可选关闭/Esc/Tab/Enter，⌘R 始终可用，选择的键由原生输入视图在进入文本输入前截获并重开。可选择是否保存完成成绩：关闭后仍显示、可复制本次结果，但不写入本机历史/统计/同步，也不发布榜单。可选 macOS 系统错误音、键击音、倒计时提示音（结束前 1/3/5/10 秒任选其一）及 0–100% 共享音量默认关闭且会持久化。键击时仅在输入实际被接受后播放，错误音仅在错误数增加时播放，倒计时提示与错误音独立且不影响计分；控制栏可显式调用 macOS 内置语音朗读当前提示，并会按所选语言选择系统 locale、重开时停止，文本绝不离开设备；其余显示/行为选项尚未实现 |
| CFG-03 | 命令面板、快捷键、配置分享链接 | `commandline/*`, `ShareTestSettings.tsx` | CommandDomain | 部分实现：可用工具栏“命令”或 ⇧⌘K 打开原生命令面板，搜索并执行重开、模式切换、历史、预设、保存文本、数据、同步、好友、通知和设置；可复制或粘贴导入自有 `typebar://test` 配置链接，链接仅含测试配置、引语 ID 或自定义文本；参考项目全部快捷键与网页链接互操作尚未实现 |
| VIS-01 | 内置/自定义/收藏主题、自动与随机切换 | `theme-controller.ts`, `custom-themes.ts` | ThemeDomain | 部分实现：三套原创内置主题可从原生设置切换并持久化；用户可创建、应用和删除仅本地保存的原创 RGB 主题，并随归档迁移；内置和自定义主题均可收藏，收藏栏可一键应用，删除自定义主题会同步取消收藏；可选择跟随 macOS 深浅色在原创纸白/午夜主题间自动切换，或在每次新测试时随机选择原创内置主题 |
| VIS-02 | 背景、字体、行宽、动效、光标、键盘映射 | `settings/custom-setting/*` | AppearanceDomain | 部分实现：练习字体大小、macOS 系统等宽/圆角/衬线/默认四种字体设计、紧凑/标准/宽/自定义（30–140 列）/自适应行宽、单行卷带（按词/按字符、可调停靠位置、可关闭平滑滚动）、可选原创 ANSI QWERTY/Dvorak/Colemak/Workman 和 French AZERTY 下一键提示与物理 ANSI 输入模拟，以及可持久化的节奏引导已实现。卷带只改变单行提示的水平呈现，多行自定义/代码提示仍使用普通布局，绝不改变输入、计分或回放。前四种布局支持 ASCII 字母、数字与标点；AZERTY 另映射法语 `é`、`è`、`ç`、`à` 与对应符号到数字/底排行，未收录字符仍不高亮；“镜像键盘”可将 ASCII 实体键位按原创 ANSI 行镜像后再输入计分，下一键提示同步显示应按的镜像键位，重音、中文与其他非 ASCII 输入不转换；当前目标字符可选择条形、下划线或块状原生光标表现，并随设置和归档迁移；另有纯色、主题色光晕和细网格三种 Typebar 原生矢量背景；光晕可用本机开关减少动态效果，也会遵从 macOS 的“减少动态效果”。字体选择与背景偏好会进入本机设置与归档，不安装、打包或下载第三方字体/图片；更多光标配置和其他非 Latin 布局/映射尚未实现 |
| FUN-01 | 趣味修饰器（视觉、文本、布局、可见性、输入） | `packages/funbox/src/list.ts` | ModifierDomain | 部分实现：48 项官方 funbox 已全部纳入 Typebar 自有的修饰器、文本流、视觉提示或布局策略；逐项映射、互斥关系、差异和唯一的部分覆盖项见 `OFFICIAL_FUNBOX_AUDIT.md`。当前为 47 项功能等价、1 项（ASL 指语提示）部分覆盖、0 项未实现；所有实现均为原创原生代码与内容，不依赖参考项目代码、资产或网络内容。 |
| FUN-02 | 修饰器互斥验证、挑战设置和完成判定 | `funbox-validation.ts`, `challenge-controller.ts` | ModifierDomain, ChallengeDomain | 部分实现：修饰器切换与配置解码会按官方冲突类别归一化；具体类别与逐项覆盖证据见 `OFFICIAL_FUNBOX_AUDIT.md`。原创离线挑战可加载固定测试配置，并按最低 WPM、最低准确率和最高错误数逐项判定，完成页会显示通过状态或具体未满足项；挑战库顶部还提供按本机日历日稳定选择、无需网络的今日挑战。仍需以实际设备操作覆盖每一组冲突组合。 |
| ACC-01 | 注册、登录、密码/邮箱/身份管理 | `components/pages/login/*`, `users.ts` | IdentityService | 部分实现：自建服务和原生账户设置页支持注册、登录、会话恢复、密码更新、邮箱变更与账户删除；删除要求当前密码和原生确认，服务端会清除该账户的会话、成绩、同步、关系、屏蔽与投稿，本机练习历史不会受影响。密码和邮箱更新均验证当前密码；密码更新会使用 bcrypt 重哈希新密码，邮箱更新会验证格式与唯一性，二者都会撤销该账户既有会话并仅保存当前新令牌至 macOS 钥匙串。密码重置和第三方身份尚未实现 |
| ACC-02 | 用户资料、徽章、XP、账户设置 | `components/pages/account*/*` | ProfileDomain | 部分实现：受令牌保护的当前显示名读取/更新与客户端资料刷新已实现；原生账户设置页可编辑公开显示名，并明确说明其公开范围不含邮箱；用户也可明确选择从自建服务全部全局/好友 WPM 与 XP 排行榜隐藏自己，已保存成绩、XP、本机历史与同步不受影响，恢复显示也即时生效。自建服务提供不含邮箱的基础公开资料，原生榜单可打开资料卡。服务端会从已验证的完成成绩即时重算 Typebar 自有总 XP，客户端显示当前总 XP 和公开资料总 XP；历史页另有完全离线、由本机完成成绩即时派生的四项 Typebar 原生成就，不上传、不同步且不替代 XP。公开徽章资料与服务端成就同步尚未实现 |
| SYN-01 | 结果、配置、预设、主题、标签的同步 | `api/routes/{results,configs,presets}.ts` | SyncService | 部分实现：自建服务支持用户作用域的版本/游标同步；原生客户端可上传与拉取版本化设置、结果、预设、主题设置、标签和自定义文本归档并去重合并；细粒度变更、冲突副本、分页和端到端人工验收尚未完成 |
| SOC-01 | 用户搜索、公开资料、好友与屏蔽 | `components/pages/{profile,connections}/*` | SocialService | 部分实现：自建服务支持按公开展示名搜索（不泄露邮箱）、受令牌保护的好友请求、接受、列表、解除与屏蔽；屏蔽会移除双方现有关系、会话记录并阻止后续好友请求。原生客户端可搜索用户、从榜单资料卡请求好友，并在好友面板接受、取消、解除、屏蔽或解除屏蔽；端到端人工验收尚未实现 |
| SOC-02 | 站内信、举报、通知 | `collections/inbox.ts`, `UserReportModal.tsx` | ModerationService | 部分实现：自建服务会为好友请求、接受与新私信生成仅接收者可读取的通知，记录触发者公开资料、创建时间和可选已读时间；私信通知不包含消息正文。原生客户端可刷新、查看并单条标记已读。已接受好友间可发送、读取、刷新与标记已读私有站内信；第三方、非好友和屏蔽关系均不能访问，屏蔽会删除双方会话记录。登录用户还可从公开资料卡提交自定义的资料举报分类及可选说明；服务端将其私有保存到待审核队列、拒绝自举报及重复同类举报，不通知被举报者且不自动处罚。原生设置页的审核员工作台用不落盘的部署密钥读取资料与引语队列；资料队列只含目标公开资料、分类、说明和状态，不显示举报者身份，且只能标为已处理或驳回，不会自动处置目标账户。引语队列仍可批准或拒绝投稿。推送通知、细粒度角色与申诉尚未实现 |
| RANK-01 | 全局/好友/每日/每周 XP 排行榜与排名 | `components/pages/leaderboard/*`, `leaderboards.ts` | RankingService | 部分实现：自建服务可受令牌保护地提交经字段/时长一致性校验后的结果，并提供按模式/语言、服务端当天/ISO 本周筛选的全局或好友 WPM 榜；每位用户在当前筛选下仅保留最佳成绩并获得对应名次，好友榜只包含本人及已接受好友，原生客户端可切换浏览。另有按 ISO 本周聚合、最多 100 人的全局/好友 XP 榜；XP 只由服务端的完成成绩时长、准确率及模式重新计算，禅模式不奖励，重复结果 ID 不重复奖励。请求限速已实现；完整事件回放验真和赛季奖励尚未实现 |
| CONT-01 | 引语查询、评分、提交、审核与举报 | `components/modals/Quote*.tsx`, `quotes.ts` | QuoteService | 部分实现：登录用户可从原生账户设置提交 English、Español/简体中文/Deutsch 引语及可选署名；自建服务验证长度与语言并以待审核状态持久化，部署者配置 `TYPEBAR_MODERATION_TOKEN` 后可读取按状态筛选的审核队列并审核为批准或拒绝；队列只提供引语、举报原因和说明，不暴露举报者身份，公共接口只返回批准内容。原生设置页提供审核工作台，以不落盘的部署密钥读取引语与资料举报队列、显示最小必要上下文并处理待审核项；资料举报只可标为已处理或驳回，绝不自动处置账户。客户端可明确切换并按语言刷新社区审核引语，默认的 Typebar 自有离线引语永不被替换。登录用户可私下举报已批准的社区引语，填写最多 400 字说明；服务端拒绝作者自举报与同原因重复举报，且不会通知作者或自动处置。登录用户可对已审核社区引语一人一票给出“不错”或“不适合”，可改评或撤销；公开列表只显示聚合票数，不公开评分者，删除账户/投稿会同步删除相关评分。离线引语评分仍仅保存在本机、不上传且不替代审核。细粒度审核角色与申诉流程尚未实现 |
| OPS-01 | 服务配置、公告、反作弊、限速、审计和维护任务 | `backend/src/{anticheat,middlewares,jobs,workers}` | OperationsService | 部分实现：Typebar 自有 Vapor 中间件按来源限制注册/登录（10 次/分钟），按访问令牌限制提交成绩（30 次/分钟）和其他写操作（60 次/分钟），读取接口限制为 180 次/分钟；拒绝时返回 `429`、`Retry-After` 与剩余额度头，并仅记录限速策略名而不记录令牌、邮箱或正文。部署者可在启动时设置 `TYPEBAR_MAINTENANCE_MODE=true`；维护期内健康检查和只读请求仍可用，所有写操作统一返回 `503`、300 秒 `Retry-After` 和明确的离线练习提示。成绩提交会验证输入数、错误数、准确率、WPM/raw WPM 与起止时间的数学一致性，计时模式还会核对实际耗时与配置时长。固定窗口计数保存在进程内，重启即清空；分布式限速、持久化审计、公告、不可伪造签名和完整事件回放仍未实现 |
| MAC-01 | 菜单栏、全局热键、离线降级、备份恢复 | macOS 专属增补 | MacIntegration | 部分实现：原生菜单栏项目可激活应用并将当前可见主窗口恢复到前台；用户可在原生设置中明确启用 `⌃⇧Space` 全局唤起，首次开启会请求 macOS 辅助功能授权，未授权时会说明原因并可重新检查；离线运行与备份恢复已覆盖。更完整网络降级尚未实现 |

## 盘点结论

参考实现约有 197 个前端组件，并有独立的前端、后端、共享契约和趣味模式包。因此客户端“像 Monkeytype”并不能构成完整重写；`ACC`、`SYN`、`SOC`、`RANK`、`CONT` 和 `OPS` 都必须拥有我们自己的服务实现与端到端验收。

## 可追溯性规则

每个实现 PR/提交必须至少写出一个本表 ID。完成时还要补充：测试文件、人工验收步骤、数据迁移策略和隐私/离线行为。任何 ID 为“未开始”或“部分实现”时，完整重写仍未完成。
