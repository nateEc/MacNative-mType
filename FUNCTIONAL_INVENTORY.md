# 功能盘点与追踪表

参考仓库固定为 `91bd24bb8513785c7364cbea29296ff7adafac41`。本文件是需求索引，不含参考项目的代码或资产。

官方 48 项 funbox 的逐项来源、映射与未实现缺口见 `OFFICIAL_FUNBOX_AUDIT.md`；该审计是本表 FUN-01/FUN-02 的细化证据。

官方 239 个命名键盘布局的逐项状态见 `OFFICIAL_LAYOUT_AUDIT.md`，机器可读快照与原生映射见 `Compatibility/official-layouts.json`；当前 239 项精确原生、0 项相关替代、0 项系统输入或自定义回退均由自动化测试守恒校验。

| ID | 用户能力 | 参考证据（仅用于盘点） | Typebar 目标模块 | 状态 |
| --- | --- | --- | --- | --- |
| APP-01 | 关于、指标说明、反馈与项目归属 | `AboutPage.tsx`, `components/layout/header/Nav.tsx` | AppInformation | 已实现：macOS 应用菜单与原生命令面板可打开独立“关于 Typebar”窗口；窗口从 bundle 显示版本/构建，说明本机优先、无广告与标注联网边界，独立解释 WPM、Raw、准确率和稳定度，并提供 Typebar 源码、问题反馈及固定 Monkeytype 参考项目链接。兼容性说明明确 Typebar 不包含参考代码、品牌资产、词表、引语、主题或布局文件。参考页面的广告、Monkeytype 支持者与贡献者名单属于其品牌/运营数据，不复制到 Typebar；真实窗口视觉与链接仍待人工验收 |
| APP-02 | 版本历史与发布说明 | `components/modals/VersionHistoryModal.tsx`, `components/layout/footer/VersionButton.tsx` | AppInformation | 已实现：应用菜单、关于窗口和原生命令面板可打开独立版本历史窗口；窗口只在打开或继续分页时请求 Typebar 自己的 GitHub Releases，按发布时间顺序展示正式发布的标题、标签、日期、纯文本说明和受约束的 GitHub 链接，并提供加载、空白、失败重试与“读取更早发布”状态。草稿、预发布、空标签及指向其他站点或仓库的链接不会展示；不复制参考发布说明、HTML 转换器、代码、样式或资产。真实窗口、网络失败和链接仍待人工验收 |
| TST-01 | 五类测试与模式参数 | `components/pages/test/TestConfig.tsx` | TestDomain | 部分实现：time、words、quote、zen、custom 均有独立原生配置与终止规则；计时和字数可选择标准值、任意 JavaScript 安全整数范围内的非负自定义值，或以 0 编码的无限测试。大型有限计时/字数和无限测试都以 Typebar 自有小批次提示按需扩展，不会按完整目标一次性分配文本；有限测试仍只按目标时长/字数结束。无限计时正向显示已用秒数，无限字数显示已提交词数，两者绝不自动完成，只能通过 Bail Out 或双击 Shift+Enter 显示未保存结果；配置可进入预设、归档和 `typebar://test` 分享链接。 |
| TST-02 | 标点、数字、语言与混合语言 | `config/metadata.tsx` | ContentDomain | 部分实现：当前语言目录：376 个可单独练习的语言或书写方式、70 个代码选择和 2 个混合入口。376 个单语入口均使用 Typebar 自有词流与短／中／长／超长引语，70 个代码入口均使用原创离线片段；两个混合入口分别提供中英混合，以及从固定 schema 全部 446 个单语言 ID 中搜索并选择至少两种、按顺序轮转的自选多语。153 项经验证语言仍是默认组合，大型数字规模、RTL、无空格及代码入口不加入默认组合但均可手动选择；大型原创索引词流按需采样，不物化整表。语言元数据决定空格或无空格词界、RTL、连写保护、BCP-47、简化输入和 Zipf 提示；全 RTL 组合使用原生 RTL 段落，双向组合交给 macOS Unicode 双向排版。计时与字数模式可加入数字及对应标点。配置、预设、分享、结果、归档、社区投稿、成绩和排行榜均使用同一枚举，旧配置缺少自选组合时回退原有默认组合。机器总账验证固定版本的 446 个官方 ID 均有独立原生选择，没有兼容代指或未映射配置。状态仍标为“部分”，因为入口与聚合形状覆盖不代表复制官方词表内容，也不代表其余功能域已经完全等价。 |
| TST-03 | 引语长度、收藏引语、重复策略 | `TestConfig.tsx`, `quotes-controller.ts` | QuoteDomain | 部分实现：引语模式以按长度、收藏、搜索三个互斥入口覆盖固定配置的全部六值。`0/1/2/3` 映射为短/中/长/超长的任意长度集合；收藏模式忽略此前长度并从该语言全部本机收藏中轮换；搜索模式忽略长度、以标题/文本的空白分词查询缩小列表并锁定用户所选 ID，重开时不会自动换条。命令面板可直接选择全部或单一长度、进入搜索，并只在当前语言和来源确有本机收藏时显示收藏入口；执行会切换到引语模式并退出此前活动挑战。模式和长度会随预设及配置链接保存，搜索词本身不外发或进入链接；旧单长度配置安全迁移为长度模式，特殊模式候选不存在时安全回到长度模式。普通与收藏候选会构成仅本次运行的随机队列，每轮不重复、耗尽后重洗，并在有其他候选时避免立即重复当前引语；这与参考的队列语义一致但不复制其内容或实现。376 个可单独练习的语言或书写方式均有 Typebar 自有短／中／长／超长引语；亦可明确切换到社区审核来源，按语言刷新仅服务端批准的投稿。收藏与重复策略会随本机设置和归档保存；状态仍为“部分”只表示原创引语内容与官方目录不同。 |
| TST-04 | 自定义文本、保存文本与长度限制 | `CustomTextModal.tsx`, `SavedTextsModal.tsx`, `WordFilterModal.tsx`, `CustomGeneratorModal.tsx` | CustomTextDomain | 部分实现：自定义文本可编辑、保存、列出、复用和删除，并会进入本机备份；编辑器统一限制为 10,000 字符并显示计数，保存、分享和归档导入使用同一有效性规则。保存时可选“长文本”：它会记录在完整、正确匹配的词边界后的原始字符偏移，保留空白与换行格式；重新选择后只呈现剩余部分、固定为完成全文并锁住编辑/循环配置，明确解除跟踪后才可编辑。长文本以双击 Shift+Enter、中止控制或难度失败提前结束时会保存当前位置，完整完成后回到开头；进度和类型会进入本机备份与同步，旧归档缺少字段时仍作为普通文本导入。可选输入完成、循环计时、循环字数或分节完成；循环计时与循环字数均可设为无限，只通过 Bail Out 收尾。可保持顺序、打乱一次或随机抽取用户文本中的词。自定义提示含换行时，原生输入桥会仅在当前提示需要换行时将 Return 计为换行，避免普通练习误触 Return；用 `|` 分隔段落后可选择前 N 段，练习页显示当前段落进度。循环提示由 Typebar 动态扩展；启用无空格时循环片段保持连续，并保留可安全映射的原始词边界及渲染后词段，以供 Burst、结果词历史、错词与慢词复练使用。原生“词表筛选”只处理 Typebar 自创离线词表，支持允许/排除字符、精确字符集、词长与 ICU 正则预览；结果可替换或追加到自定义文本并立即开始，绝不下载或导入参考/第三方词表。原生“生成文本”会按用户输入的片段、每词片段数和词数随机组合本机练习，并在生成前执行统一 10,000 字符上限检查；可替换或追加后立即开始。配置会进入预设、链接、结果与归档 |
| TST-05 | 禅模式、无限测试、快速重开与放弃 | `event-handlers/test.ts`, `quick-restart.ts` | TestDomain | 部分实现：禅模式不生成或比较提示，只呈现本机自由输入；空格或换行提交输入词，Tab 保留为输入文本，任意内容计为正确且不会自动结束。每个活动词上限为 30 字符以防止意外长按；Shift+Enter 明确结束本轮，⌘R 仍可重开。计时、字数及对应自定义循环都支持无限模式：原生进度改为正向累计，提示耗尽前由同一 Typebar 自有批次扩展；快捷重开必须按 Shift，控制栏、命令面板和双击 Shift+Enter 均走 Bail Out 并仅显示未保存结果。固定参考标记为 `noInfiniteDuration` 的布局流动、四档可见范围、记忆、诗性散文和知识短文会阻止新开无限测试；旧配置同时带入冲突项时会在解码时安全移除。自动化覆盖终止、循环、计数、冲突、旧配置迁移、预设和分享；真实键盘验收仍待执行。 |

2026-09-03 更正：TST-02 中“禅模式支持原创语言词流、标点与数字”的表述已失效。禅模式的内容完全由用户本机输入决定，不生成或导入词库内容；原生配置界面会隐藏语言、拼写、标点、数字和混合词流设置，它们只用于其他有提示的测试模式。

2026-09-04 更正：TST-02 与 TST-03 中的“十四种单语”均更新为十九种，新增 `Українська (Latin)`、`Nederlands`、`Dansk`、`Norsk bokmål` 与 `Magyar`。五者均使用 Typebar 自创词流与四档引语，进入配置、预设、分享、结果、归档、多语混排、弱项复练、社区投稿和榜单筛选；Dutch、Danish、Norwegian Bokmål 与 Hungarian 可在用户明确启用时分别使用 `nl`、`da`、`no`、`hu` 百科入口，Latin 为保证提示不被远端西里尔文本替换而保持离线内容。

2026-09-04 补充：当时单语数为二十种，新增 `Svenska`。其词流与四档引语均为 Typebar 自创内容，进入配置、预设、分享、结果、归档、多语轮转、弱项复练、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `sv` 百科入口，系统朗读使用 `sv-SE`。

2026-09-04 补充：当时单语数为二十一种，新增 `Ελληνικά`。其词流与四档引语均为 Typebar 自创内容，进入配置、预设、分享、结果、归档、多语轮转、弱项复练、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `el` 百科入口，系统朗读使用 `el-GR`。

2026-09-04 补充：当时单语数为二十二种，新增 `日本語（カタカナ）`。其词流与四档引语均为 Typebar 自创片假名内容，保留无空格词界，进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；知识短文不会替换片假名提示，避免随机页面混入汉字，系统朗读使用 `ja-JP`。

2026-09-04 补充：当时单语数为二十三种，新增 `日本語（ローマ字）`。其 ASCII 罗马字词流与四档引语均由 Typebar 自创，采用空格分词并进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；知识短文不会以日文原文替换罗马字提示，系统朗读使用 `ja-JP`。

2026-09-04 补充：当时单语数更新为二十四种，新增 `Čeština`。其含捷克语变音字符的词流与四档引语均由 Typebar 自创，采用空格分词并进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `cs` 百科入口，系统朗读使用 `cs-CZ`。

2026-09-04 补充：当时单语数更新为二十五种，新增 `Български`。其西里尔词流与四档引语均由 Typebar 自创，采用空格分词并进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `bg` 百科入口，系统朗读使用 `bg-BG`。

2026-09-04 补充：当时单语数更新为二十六种，新增 `Română`。其含罗马尼亚语组合变音字符的词流与四档引语均由 Typebar 自创，采用空格分词并进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `ro` 百科入口，系统朗读使用 `ro-RO`。

2026-09-04 补充：当时单语数更新为二十七种，新增 `Suomi`。其含芬兰语变音字符的词流与四档引语均由 Typebar 自创，采用空格分词并进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `fi` 百科入口，系统朗读使用 `fi-FI`。

2026-09-04 补充：当时单语数更新为二十八种，新增 `Eesti`。其含爱沙尼亚语变音字符的词流与四档引语均由 Typebar 自创，采用空格分词并进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `et` 百科入口，系统朗读使用 `et-EE`。

2026-09-04 补充：当时单语数更新为二十九种，新增 `Íslenska`。其含冰岛语特殊字母的词流与四档引语均由 Typebar 自创，采用空格分词并进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `is` 百科入口，系统朗读使用 `is-IS`。

2026-09-04 补充：当时单语数更新为三十种，新增 `Slovenčina`。其含斯洛伐克语变音字符的词流与四档引语均由 Typebar 自创，采用空格分词并进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `sk` 百科入口，系统朗读使用 `sk-SK`。

2026-09-04 补充：当时单语数更新为三十一种，新增 `Slovenščina`。其含斯洛文尼亚语特殊字母的词流与四档引语均由 Typebar 自创，采用空格分词并进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `sl` 百科入口，系统朗读使用 `sl-SI`。

2026-09-04 补充：当时单语数更新为三十二种，新增 `Hrvatski`。其含克罗地亚语特殊字母的词流与四档引语均由 Typebar 自创，采用空格分词并进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；用户明确选择“知识短文”时才请求 `hr` 百科入口，系统朗读使用 `hr-HR`。

2026-09-04 补充：当前单语数更新为三十四种，新增 `Српски` 与 `Srpski (Latin)`。两者均使用 Typebar 自创词流和四档引语，进入配置、预设、分享、结果、归档、多语轮转、社区投稿和榜单筛选；西里尔版在用户明确选择“知识短文”时请求 `sr` 百科入口，Latin 版始终保留原创离线拉丁文本，避免随机西里尔内容覆盖当前书写形式；两者均使用系统 `sr-RS` 朗读。新增的 `Serbian Cyrillic · Typebar` 自写键位图可用于键盘提示、显式输入模拟、归档和 Layout Fluid。

2026-09-04 补充：当前单语数更新为三十五种，新增 `Greeklish`。词流和四档引语均由 Typebar 原创 ASCII 文本构成，以空格分词并进入配置、预设、分享、结果、归档、多语轮转、弱项复练、社区投稿和榜单筛选；虽然语言关联为 `el`，知识短文不会请求或应用希腊字母原文，以保持用户所选 Latin 书写形式。系统朗读选择 `el-GR`。

2026-09-04 补充：当前单语数更新为三十六种，新增 `Norsk nynorsk`。词流和短/中/长/超长引语均由 Typebar 原创文本构成，以空格分词并进入配置、预设、分享、结果、归档、多语轮转、弱项复练、社区投稿和榜单筛选；仅当用户明确启用“知识短文”时请求 `nn` 百科入口，系统朗读选择 `nn-NO`。此前 TST-02、TST-03 与 CONT-01 对当前单语数量的表述以本条为准。

2026-09-04 补充：当前单语数更新为三十七种，新增 `Afrikaans`。词流和短/中/长/超长引语均由 Typebar 原创文本构成，以空格分词并进入配置、预设、分享、结果、归档、多语轮转、弱项复练、社区投稿和榜单筛选；仅当用户明确启用“知识短文”时请求 `af` 百科入口，系统朗读选择 `af-ZA`。此前对当前单语数量的表述以本条为准。

2026-09-04 补充：当前单语数更新为三十八种，新增 `Filipino`。词流和短/中/长/超长引语均由 Typebar 原创文本构成，以空格分词并进入配置、预设、分享、结果、归档、多语轮转、弱项复练、社区投稿和榜单筛选；仅当用户明确启用“知识短文”时请求 `tl` 百科入口，系统朗读选择 `fil-PH`。此前对当前单语数量的表述以本条为准。

2026-09-04 补充：当前单语数更新为三十九种，新增 `Català`。词流和短/中/长/超长引语均由 Typebar 原创文本构成，以空格分词并进入配置、预设、分享、结果、归档、多语轮转、弱项复练、社区投稿和榜单筛选；仅当用户明确启用“知识短文”时请求 `ca` 百科入口，系统朗读选择 `ca-ES`。此前对当前单语数量的表述以本条为准。

2026-09-04 补充：当前单语数更新为四十种，新增 `Bahasa Indonesia`。词流和短/中/长/超长引语均由 Typebar 原创文本构成，以空格分词并进入配置、预设、分享、结果、归档、多语轮转、弱项复练、社区投稿和榜单筛选；仅当用户明确启用“知识短文”时请求 `id` 百科入口，系统朗读选择 `id-ID`。此前对当前单语数量的表述以本条为准。

2026-09-04 补充：当前单语数更新为四十一种，新增 `Bahasa Melayu`。词流和短/中/长/超长引语均由 Typebar 原创文本构成，以空格分词并进入配置、预设、分享、结果、归档、多语轮转、弱项复练、社区投稿和榜单筛选；仅当用户明确启用“知识短文”时请求 `ms` 百科入口，系统朗读选择 `ms-MY`。此前对当前单语数量的表述以本条为准。

2026-09-07 审计更正：任何概览行中的较早语言数量均由当前 133 种单语和 123 语 LTR 默认混排取代；完整清单见 `OFFICIAL_LANGUAGE_AUDIT.md`。单语成绩筛选使用 133 种单语契约，社区投稿与公开引语使用相同集合但按固定源码排除 Swiss German；十种 RTL 单语不进入默认或自选多语混排。

2026-09-05 补充：当前单语数更新为八十七种，新增 `Moroccan Arabic`（`arabicMorocco`）。固定参考的 `arabic_morocco` 定义 `rightToLeft: true`、`joiningScript: true`、`orderedByFrequency: false` 与 `bcp47: ar-MA`，没有 `noLazyMode`。Typebar 以独立的自写摩洛哥阿拉伯语词流和四档引语通过 macOS 原生 RTL/连写排版呈现，绝不导入参考字典或引语；知识短文按 BCP 首段使用 `ar`，系统朗读精确使用 `ar-MA`。它保留用户显式选择的简化输入，却不会触发标准 Arabic 专用的默认快捷开关；Zipf 显示明确的“词表未按频率排序”提示但不移除修饰器。Moroccan Arabic 已进入配置、预设、分享、结果、归档、社区投稿和排行榜，但不进入未验收的双向多语混排。

2026-09-05 补充：当前单语数更新为八十八种，新增 `پښتو`（Pashto）。固定参考的 `pashto` 定义 `rightToLeft: true`、`joiningScript: true`、`noLazyMode: true` 与 `bcp47: ps`，没有词频排序标记。Typebar 以独立的自写 Pashto 词流和四档引语通过 macOS 原生 RTL/连写排版呈现，绝不导入参考字典或引语；知识短文和系统朗读均精确使用 `ps`。非自定义练习禁用简化输入，自定义文本仍可显式使用；Zipf 显示可能不支持的提示但不移除修饰器。Pashto 已进入配置、预设、分享、结果、归档、社区投稿和排行榜，但不进入未验收的双向多语混排。

2026-09-05 补充：当前单语数更新为八十九种，新增 `سنڌي`（Sindhi）。固定参考的 `sindhi` 定义 `rightToLeft: true`、`joiningScript: true`、`orderedByFrequency: false` 与 `bcp47: sd`，没有 `noLazyMode`。Typebar 以独立的自写 Sindhi 词流和四档引语通过 macOS 原生 RTL/连写排版呈现，绝不导入参考字典或引语；知识短文和系统朗读均精确使用 `sd`。保留用户显式选择的简化输入，但不触发标准 Arabic 专用的默认快捷开关；Zipf 显示明确的“词表未按频率排序”提示但不移除修饰器。Sindhi 已进入配置、预设、分享、结果、归档、社区投稿和排行榜，但不进入未验收的双向多语混排。

2026-09-05 补充：当前单语数更新为九十种，新增 `Occitan`。固定参考的 `occitan` 定义 `bcp47: oc-FR`，没有 RTL、连写、`noLazyMode` 或词频排序标记。Typebar 以独立的自写 Occitan 词流和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文按 BCP 首段使用 `oc`，系统朗读精确使用 `oc-FR`。保留用户显式选择的简化输入；Zipf 显示可能不支持的提示但不移除修饰器。Occitan 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为九十一种，新增 `Oromo`。固定参考的 `oromo` 定义 `bcp47: om` 与 `orderedByFrequency: true`，没有 RTL、连写或 `noLazyMode` 标记。Typebar 以独立的自写 Oromo 词流和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读均精确使用 `om`。保留用户显式选择的简化输入与 Zipf 高频词。Oromo 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为九十二种，新增 `Македонски`（Macedonian）。固定参考的 `macedonian` 仅定义 `noLazyMode: true`，没有 BCP-47、RTL、连写或词频排序标记。Typebar 以独立的自写 Macedonian 西里尔词流和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读严格使用缺省 `en` 与 `en-US`。普通练习禁用简化输入，而自定义文本保留该例外；Zipf 显示可能不支持提示而保留修饰器。Macedonian 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为九十三种，新增 `Қазақша`（Kazakh）。固定参考的 `kazakh` 仅定义 `noLazyMode: true`，没有 BCP-47、RTL、连写或词频排序标记。Typebar 以独立的自写 Kazakh 西里尔词流和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读严格使用缺省 `en` 与 `en-US`。普通练习禁用简化输入，而自定义文本保留该例外；Zipf 显示可能不支持提示而保留修饰器。Kazakh 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为九十四种，新增 `Tiếng Việt`（Vietnamese）。固定参考的 `vietnamese` 没有 BCP-47、RTL、连写、`noLazyMode` 或词频排序标记。Typebar 以独立的自写 Vietnamese 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读严格使用缺省 `en` 与 `en-US`。保留用户显式选择的简化输入；Zipf 显示可能不支持提示而保留修饰器。Vietnamese 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为九十五种，新增 `Jyutping`。固定参考的 `jyutping` 定义 `bcp47: zh-Hant`，没有 RTL、连写、`noLazyMode` 或词频排序标记。Typebar 以独立的自写 ASCII 加声调数字词流和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文按 BCP 首段使用 `zh`，系统朗读精确使用 `zh-Hant`。百科汉字内容在该路径按中文词界分词，本地 Jyutping 词流仍以空格提交。保留用户显式选择的简化输入；Zipf 显示可能不支持提示而保留修饰器。Jyutping 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为九十六种，新增 `Pinyin`。固定参考的 `pinyin`、`pinyin_1k` 与 `pinyin_10k` 只定义名称，不定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序标记。Typebar 以独立的自写 ASCII 拼音词流和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读严格使用缺省 `en` 与 `en-US`。保留用户显式选择的简化输入；Zipf 显示可能不支持提示而保留修饰器。Pinyin 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为九十七种，新增 `Հայերէն (Արեւմտեան)`（Western Armenian）。固定参考的 `armenian_western` 与 `armenian_western_1k` 定义 `bcp47: hyw`，不定义 RTL、连写、`noLazyMode` 或词频排序标记。Typebar 以独立的自写西部亚美尼亚语正字法词流和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读均精确使用 `hyw`。保留用户显式选择的简化输入；Zipf 显示可能不支持提示而保留修饰器。Western Armenian 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为九十八种，新增 `Башҡортса`（Bashkir）。固定参考的 `bashkir` 定义 `bcp47: ba` 与 `orderedByFrequency: true`，不定义 RTL、连写或 `noLazyMode`。Typebar 以独立的自写 Bashkir 西里尔词流和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读均精确使用 `ba`。保留用户显式选择的简化输入和 Zipf 高频词。Bashkir 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为九十九种，新增 `Euskara`（Euskera）。固定参考的 `euskera` 定义 `rightToLeft: false` 与 `bcp47: eu`，不定义连写、`noLazyMode` 或词频排序标记。Typebar 以独立的自写 Basque 词流和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读均精确使用 `eu`。保留用户显式选择的简化输入；Zipf 显示可能不支持提示而保留修饰器。Euskera 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百种，新增 `Frysk`（Frisian）。固定参考的 `frisian` 与 `frisian_1k` 定义 `bcp47: fy-FY`，不定义 RTL、连写、`noLazyMode` 或词频排序标记。Typebar 以独立的自写 Frisian 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文按 BCP 首段使用 `fy`，系统朗读精确使用 `fy-FY`。保留用户显式选择的简化输入；Zipf 显示可能不支持提示而保留修饰器。Frisian 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百零一种，新增 `isiZulu`（Zulu）。固定参考的 `zulu` 不定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序标记。Typebar 以独立的自写 isiZulu 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读严格采用缺省 `en` 与 `en-US`。保留用户显式选择的简化输入；Zipf 显示可能不支持提示而保留修饰器。Zulu 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百零二种，新增 `ʻŌlelo Hawaiʻi`（Hawaiian）。固定参考的 `hawaiian` 与 `hawaiian_1k` 定义 `rightToLeft: false`、`bcp47: haw` 与 `orderedByFrequency: true`，不定义连写或 `noLazyMode`。Typebar 以独立的自写 Hawaiian 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读均精确使用 `haw`。保留用户显式选择的简化输入和 Zipf 高频词。Hawaiian 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百零三种，新增 `Taqbaylit`（Kabyle）。固定参考的基础、1k、2k、5k 与 10k 组都定义 `bcp47: kab` 与 `orderedByFrequency: false`，不定义 RTL、连写或 `noLazyMode`。Typebar 以独立的自写 Taqbaylit 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读均精确使用 `kab`。保留用户显式选择的简化输入；Zipf 显示不支持提示而保留修饰器。Kabyle 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百零四种，新增 `Malti`（Maltese）。固定参考的基础与 1k 组都定义 `bcp47: mt`，不定义 RTL、连写、`noLazyMode` 或词频排序。Typebar 以独立的自写 Maltese 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读均精确使用 `mt`。保留用户显式选择的简化输入；Zipf 显示可能不支持提示而保留修饰器。Maltese 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百零五种，新增 `toki pona`。固定参考的基础、ku suli 与 ku lili 组均定义 `noLazyMode: true`，不定义 BCP-47、RTL、连写或词频排序。Typebar 以独立的自写 toki pona 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考字典或引语；知识短文和系统朗读严格使用缺省 `en` 与 `en-US`。普通练习禁用简化输入，而自定义文本保留例外；Zipf 显示可能不支持提示而保留修饰器。toki pona 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百零六种，新增 `isiXhosa`（Xhosa）。主参考的 `xhosa` 定义 `rightToLeft: false` 与 `bcp47: xh`，而 `xhosa_3k` 未重申这些可选元数据；参考代码按当前词组读取，因而主词组使用 `xh`、3k 会走缺省。Typebar 以独立的自写 Xhosa 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入任一参考词表或引语；用户可见的 isiXhosa 选择按主词组精确使用 `xh` 知识短文与系统朗读。保留显式简化输入；Zipf 显示可能不支持提示而保留修饰器。isiXhosa 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百零七种，新增 `བོད་སྐད་`（Tibetan）。固定参考的基础与 1k 组都定义 `rightToLeft: false`、`joiningScript: true`、`noLazyMode: true` 与 `bcp47: bo-TI`，不定义词频排序。Typebar 以独立的自写 Tibetan 内容和四档引语通过 macOS 原生 LTR 连写排版呈现，绝不导入参考字典或引语；提示层保留系统塑形、使用较紧行距，并禁止圆点逐字替换破坏连写簇。知识短文与系统朗读分别精确使用 `bo` 与 `bo-TI`；普通练习禁用简化输入，而自定义文本保留例外。Zipf 显示可能不支持提示而保留修饰器。Tibetan 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百零八种，新增 `Кыргызча`（Kyrgyz）。固定参考的基础与 1k 组都定义 `bcp47: ky-KY`，不定义 RTL、连写、`noLazyMode` 或词频排序。Typebar 以独立的自写 Kyrgyz 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考词表或引语；知识短文按 BCP 首段使用 `ky`，系统朗读精确使用 `ky-KY`。保留用户显式选择的简化输入；Zipf 显示可能不支持提示而保留修饰器。Kyrgyz 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百零九种，新增 `ייִדיש`（Yiddish）。固定参考配置定义 `rightToLeft: true`、`joiningScript: true` 与 `bcp47: yi`，不定义 `noLazyMode` 或词频排序。Typebar 以独立的自写 Yiddish 内容和四档引语通过 macOS 原生 RTL/连写排版呈现，绝不导入参考词表或引语；提示层保留系统塑形、使用较紧行距，并禁止圆点逐字替换破坏字形。知识短文和系统朗读均精确使用 `yi`；保留显式简化输入，Zipf 显示可能不支持提示而保留修饰器。Yiddish 已进入配置、预设、分享、结果、归档、社区投稿和排行榜；因双向混排未完成专门交互验收，不进入默认或自选多语混排。

2026-09-05 补充：当前单语数更新为一百一十种，新增 `Удмурт кыл`（Udmurt）。固定参考配置只定义名称，不定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序。Typebar 以独立的自写 Udmurt 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考词表或引语；知识短文和系统朗读严格走 `en`／`en-US` 缺省路径。保留显式简化输入；Zipf 显示可能不支持提示而保留修饰器。Udmurt 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百一十一种，新增 `Yorùbá`（Yoruba）。固定参考配置只定义名称，不定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序。Typebar 以独立的自写 Yoruba 声调内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考词表或引语；知识短文和系统朗读严格走 `en`／`en-US` 缺省路径。保留显式简化输入；Zipf 显示可能不支持提示而保留修饰器。Yoruba 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百一十二种，新增 `Kiswahili`（Swahili）。固定参考配置定义 `noLazyMode: true`，不定义 BCP-47、RTL、连写或词频排序。Typebar 以独立的自写 Swahili 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考词表或引语；知识短文和系统朗读严格走 `en`／`en-US` 缺省路径。普通练习禁用简化输入，而自定义文本保留例外；Zipf 显示可能不支持提示而保留修饰器。Swahili 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百一十三种，新增 `Ikinyarwanda`（Kinyarwanda）。固定参考配置定义 `noLazyMode: true`、`orderedByFrequency: true` 与 `bcp47: rw-RW`，不定义 RTL 或连写。Typebar 以独立的自写 Kinyarwanda 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考词表或引语；知识短文按 BCP 首段使用 `rw`，系统朗读精确使用 `rw-RW`。普通练习禁用简化输入，而自定义文本保留例外；Zipf 使用自有高频词。Kinyarwanda 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百一十四种，新增 `chiShona`（Shona）。固定参考配置只定义名称，不定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序。Typebar 以独立的自写 Shona 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考词表或引语；知识短文和系统朗读严格走 `en`／`en-US` 缺省路径。保留显式简化输入；Zipf 显示可能不支持提示而保留修饰器。Shona 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百一十五种，新增 `Biełaruskaja łacinka`（Belarusian Łacinka）。固定参考配置定义 `noLazyMode: false`，不定义 BCP-47、RTL、连写或词频排序。Typebar 以独立的自写 Łacinka 内容和四档引语通过 macOS 原生 LTR 排版呈现，绝不导入参考词表或引语；知识短文和系统朗读严格走 `en`／`en-US` 缺省路径。保留显式简化输入；Zipf 显示可能不支持提示而保留修饰器。它已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百一十七种，新增 `Qırımtatarca`（Crimean Tatar Latin，`tatarCrimean`）与 `Къырымтатарджа`（Crimean Tatar Cyrillic，`tatarCrimeanCyrillic`）。固定参考的拉丁及西里尔十个词表档位均定义 `noLazyMode: true` 与 `bcp47: crh-CRH`，不定义 RTL、连写或词频排序。Typebar 分别以独立自写词流和四档引语通过 macOS 原生 LTR 空格分词呈现，绝不导入参考词表或引语；知识短文使用 `crh`，系统朗读精确使用 `crh-CRH`。普通练习禁用简化输入，而自定义文本保留例外；Zipf 显示可能不支持提示且保留修饰器。两个书写选择均已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百一十八种，新增 `tlhIngan Hol`（Klingon，`klingon`）。固定参考基础与 1k 配置均定义 `bcp47: tlh`，不定义 RTL、连写、`noLazyMode` 或词频排序。Typebar 以独立自写的大小写敏感、词内撇号词流和四档引语通过 macOS 原生 LTR 空格分词呈现，绝不导入参考词表或引语；知识短文与系统朗读均使用 `tlh`。保留显式简化输入；词内 `'` 不会被标点规范化误移除；Zipf 显示可能不支持提示且保留修饰器。Klingon 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百一十九种，新增 `Quenya`（`quenya`）。固定参考配置未定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序。Typebar 以独立自写的 Quenya 词流和四档引语通过 macOS 原生 LTR 空格分词呈现，绝不导入参考词表或引语；知识短文与系统朗读严格使用 `en`／`en-US` 缺省路径。保留显式简化输入；Zipf 显示可能不支持提示且保留修饰器。Quenya 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百二十一种，新增 `Viossa`（`viossa`）与 `Viossa · Njutro`（`viossaNjutro`）。固定参考中两者均未定义 BCP-47、RTL 或连写，并设 `orderedByFrequency: false`；Typebar 以彼此独立、明示为练习 idiolect 的原创词流和四档引语通过 macOS 原生 LTR 空格分词呈现，绝不导入参考词表或引语。知识短文与系统朗读严格使用 `en`／`en-US` 缺省路径，Zipf 显示不支持提示且保留修饰器。Viossa 保留显式简化输入，Njutro 的 `noLazyMode: true` 则在普通练习禁用它并为自定义文本保留例外。两个选择均已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百二十二种，新增 `Te reo Māori`（`maori`）。固定参考只将 `maori_1k` 暴露为 Māori，且未定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序；Typebar 以保留长元音 macron 的原创词流和四档引语通过 macOS 原生 LTR 空格分词呈现，绝不导入参考词表或引语。知识短文与系统朗读严格使用 `en`／`en-US` 缺省路径，保留显式简化输入，Zipf 显示可能不支持提示且保留修饰器。该选择已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-05 补充：当前单语数更新为一百二十四种，新增 `Lojban · gismu`（`lojbanGismu`）与 `Lojban · cmavo`（`lojbanCmavo`）。固定参考把 `lojban_gismu` 与 `lojban_cmavo` 作为两个独立选择；Typebar 对应保留根词与结构词边界，后者不把 `.` 和 `'` 当作可剥离装饰标点。两个配置均未定义 BCP-47、RTL、连写或词频排序，均定义 `noLazyMode: true`；故本机原创词流和四档引语以 LTR 空格分词、`en`／`en-US` 缺省在线/朗读、普通练习禁用简化输入而自定义文本保留例外、Zipf 未知提示呈现，绝不导入参考词表或引语。两个选择均已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜。

2026-09-04 补充：当前单语数更新为四十二种，新增 `العربية`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；词流特意含有短元音等组合符号，用户通过 macOS Arabic 输入源完成实际输入。普通提示区使用原生 RTL 文本方向与右对齐，保守关闭只按 LTR 几何测量的卷带和独立 TextKit 光标覆盖；当前字符状态仍由富文本显示。用户明确选择“知识短文”时才请求 `ar` 页面，系统朗读使用 `ar-SA`。Arabic 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选；为避免未验证的双向字符顺序和光标行为，它暂不进入默认或自选多语混排。切到 Arabic 时，一个默认开启、可持久化关闭的“快速输入”偏好会把短元音、tanwin、shadda、sukun 和常见 alef 变体归一为更易输入的提示；该会话仍显式记录“简化重音输入”，不影响其他语言。

2026-09-04 补充：当前单语数更新为四十三种，新增 `עברית`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区使用与 Arabic 相同的 macOS RTL 文本方向、右对齐和原生双向排版，保守关闭卷带与独立 TextKit 光标覆盖。用户通过 macOS Hebrew 输入源完成实际输入；明确选择“知识短文”时请求 `he` 页面，系统朗读使用 `he-IL`。Hebrew 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选；在双向混排有专门交互验收前，它不进入默认或自选多语混排。参考将它标为不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为四十四种，新增 `فارسی`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区使用 macOS RTL 文本方向、右对齐和原生双向排版，保守关闭卷带与独立 TextKit 光标覆盖。用户通过 macOS Persian 输入源完成实际输入；明确选择“知识短文”时请求 `fa` 页面，系统朗读使用 `fa-IR`。Persian 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选；在双向混排有专门交互验收前，它不进入默认或自选多语混排。参考将它标为不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为四十五种，新增 `اردو`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区使用 macOS RTL 文本方向、右对齐和原生双向排版，保守关闭卷带与独立 TextKit 光标覆盖。用户通过 macOS Urdu 输入源完成实际输入；明确选择“知识短文”时请求 `ur` 页面，系统朗读使用 `ur-PK`。Urdu 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选；在双向混排有专门交互验收前，它不进入默认或自选多语混排。参考将它标为不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为四十六种，新增 `தமிழ்`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Tamil 输入源完成实际输入；明确选择“知识短文”时请求 `ta` 页面，系统朗读使用 `ta-IN`。Tamil 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为四十七种，新增 `हिन्दी`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Hindi 输入源完成实际输入；明确选择“知识短文”时请求 `hi` 页面，系统朗读使用 `hi-IN`。Hindi 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为四十八种，新增 `ગુજરાતી`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Gujarati 输入源完成实际输入；明确选择“知识短文”时请求 `gu` 页面，系统朗读使用 `gu-IN`。Gujarati 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为四十九种，新增 `বাংলা`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Bangla 输入源完成实际输入；明确选择“知识短文”时请求 `bn` 页面，系统朗读使用 `bn-BD`。Bangla 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为五十种，新增 `ไทย`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容；参考的实际生成器会在非 `nospace` 修饰器下为词元追加空格提交符，因此 Typebar 也以空格提交，而非从自然书写习惯推断无空格模式。普通提示区使用 LTR 文本排版，用户通过 macOS Thai 输入源完成实际输入；明确选择“知识短文”时请求 `th` 页面，系统朗读使用 `th-TH`。Thai 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排；不应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为五十一种，新增 `नेपाली`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Nepali 输入源完成实际输入；明确选择“知识短文”时请求 `ne` 页面，系统朗读使用 `ne-NP`。Nepali 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为五十二种，新增 `ಕನ್ನಡ`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Kannada 输入源完成实际输入；明确选择“知识短文”时请求 `kn` 页面，系统朗读使用 `kn-IN`。Kannada 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为 `joiningScript` 且不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为五十三种，新增 `తెలుగు`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Telugu 输入源完成实际输入；明确选择“知识短文”时请求 `te` 页面，系统朗读使用 `te-IN`。Telugu 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为 `joiningScript` 且不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为五十四种，新增 `മലയാളം`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Malayalam 输入源完成实际输入；明确选择“知识短文”时请求 `ml` 页面，系统朗读使用 `ml-IN`。Malayalam 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为 `joiningScript` 且不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为五十五种，新增 `संस्कृतम्`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Sanskrit 输入源完成实际输入；明确选择“知识短文”时请求 `sa` 页面，系统朗读也使用参考给出的精确 BCP-47 标识 `sa`。Sanskrit 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为 `joiningScript` 且不适用 lazy-mode，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为五十六种，新增 `සිංහල`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Sinhala 输入源完成实际输入；明确选择“知识短文”时请求 `si` 页面，系统朗读也精确使用参考给出的 BCP-47 标识 `si`，不补造地区后缀。Sinhala 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为 `joiningScript`，但没有 `noLazyMode`，因此 Typebar 不会强制关闭用户选择的“简化重音输入”。

2026-09-04 补充：当前单语数更新为五十七种，新增 `ខ្មែរ`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Khmer 输入源完成实际输入；明确选择“知识短文”时请求 `km` 页面，系统朗读使用参考给出的 BCP-47 标识 `km-KH`。Khmer 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为 `joiningScript` 与 `noLazyMode`，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为五十八种，新增 `မြန်မာ`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生组合输入和 LTR 文本排版。用户通过 macOS Burmese 输入源完成实际输入；明确选择“知识短文”时请求 `my` 页面，系统朗读使用参考给出的 BCP-47 标识 `my-MM`。缅甸语已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。参考将它标为 `joiningScript` 与 `noLazyMode`，Typebar 也不对其应用“简化重音输入”。

2026-09-04 补充：当前单语数更新为五十九种，新增 `ລາວ`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Lao 输入源和 LTR 文本排版。明确选择“知识短文”时请求 `lo` 页面，系统朗读也精确使用参考给出的 BCP-47 标识 `lo`，不补造地区后缀。老挝语已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排；参考未设 `noLazyMode`，故保留用户显式选择的简化输入。

2026-09-05 补充：当前单语数更新为六十种，新增 `አማርኛ`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Amharic 输入源和 LTR 文本排版。明确选择“知识短文”时按参考的 `am-ET` 首段请求 `am` 页面，系统朗读精确使用完整 BCP-47 标识 `am-ET`。Amharic 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排；参考未设 `noLazyMode`，故保留用户显式选择的简化输入。

2026-09-05 补充：当前单语数更新为六十一种，新增 `Հայերեն`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Armenian 输入源和 LTR 文本排版。参考仅标记 `noLazyMode: true`，但没有 BCP-47，因此 Typebar 严格采用参考的缺省路径：明确选择“知识短文”时请求 `en` 页面，系统朗读使用 `en-US`，且不应用简化重音输入。Armenian 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。

2026-09-05 补充：当前单语数更新为六十二种，新增 `ქართული`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Georgian 输入源和 LTR 文本排版。参考仅标记 `noLazyMode: true`，但没有 BCP-47，因此 Typebar 严格采用参考的缺省路径：明确选择“知识短文”时请求 `en` 页面，系统朗读使用 `en-US`，且不应用简化重音输入。Georgian 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，也安全加入默认和自选多语混排。

2026-09-05 补充：当前单语数更新为六十三种，新增 `Azərbaycanca`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Azerbaijani 输入源和 LTR 文本排版。参考只定义 `bcp47: az-AZ`，因此明确选择“知识短文”时按首段请求 `az` 页面，系统朗读精确使用 `az-AZ`；未标记 RTL、连写或 `noLazyMode`，故可安全加入默认和自选多语混排，并保留用户显式选择的简化输入。Azerbaijani 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为六十四种，新增 `Беларуская`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Belarusian 输入源和 LTR 文本排版。参考定义 `noLazyMode: true` 与 `bcp47: be-BY`，因此明确选择“知识短文”时按首段请求 `be` 页面，系统朗读精确使用 `be-BY`，并禁用简化输入；未标记 RTL 或连写，故可安全加入默认和自选多语混排。Belarusian 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选，独立拉丁变体未在本项中冒充实现。

2026-09-05 补充：当前单语数更新为六十五种，新增 `کوردی ناوەندی`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区使用 macOS Central Kurdish 输入源、RTL 文本排版和原生连写字形。参考定义 `rightToLeft: true`、`joiningScript: true` 与 `bcp47: ckb`，因此明确选择“知识短文”时请求 `ckb` 页面，系统朗读也精确使用 `ckb`；未定义 `noLazyMode`，故保留用户显式选择的简化输入。Central Kurdish 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选；因双向混排尚未完成专门交互验收，它不会进入默认或自选多语混排。

2026-09-05 补充：当前单语数更新为六十六种，新增 `Lietuvių`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Lithuanian 输入源和 LTR 文本排版。参考没有 BCP-47、RTL、连写或 `noLazyMode`，因此 Typebar 严格采用官方缺省路径：明确选择“知识短文”时请求 `en` 页面，系统朗读使用 `en-US`，并保留用户显式选择的简化输入。Lithuanian 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为六十七种，新增 `Latviešu`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Latvian 输入源和 LTR 文本排版。参考只定义 `bcp47: lv`，因此明确选择“知识短文”时请求 `lv` 页面，系统朗读精确使用 `lv`；未标记 RTL、连写或 `noLazyMode`，故可安全加入默认和自选多语混排，并保留用户显式选择的简化输入。Latvian 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为六十八种，新增 `Монгол`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Mongolian 输入源和 LTR 文本排版。参考只定义 `noLazyMode: true`，没有 BCP-47、RTL 或连写，因此 Typebar 严格采用官方缺省路径：明确选择“知识短文”时请求 `en` 页面，系统朗读使用 `en-US`，并禁用简化输入。Mongolian 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为六十九种，新增 `Gaeilge`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Irish 输入源和 LTR 文本排版。参考定义 `bcp47: ga-IE`，因此明确选择“知识短文”时按首段请求 `ga` 页面，系统朗读精确使用 `ga-IE`；未标记 RTL、连写或 `noLazyMode`，故可安全加入默认和自选多语混排，并保留用户显式选择的简化输入。Irish 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为七十种，新增 `Galego`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Galician 输入源和 LTR 文本排版。参考定义 `bcp47: gl-ES` 和 `orderedByFrequency: true`，因此明确选择“知识短文”时按首段请求 `gl` 页面，系统朗读精确使用 `gl-ES`，并保留 Zipf 高频词选项；未标记 RTL、连写或 `noLazyMode`，故可安全加入默认和自选多语混排，并保留用户显式选择的简化输入。Galician 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为七十一种，新增 `मराठी`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Marathi 输入源和 LTR 文本排版。参考定义 `noLazyMode: true` 与 `orderedByFrequency: true`，没有 BCP-47、RTL 或连写，因此 Typebar 严格采用官方缺省路径：明确选择“知识短文”时请求 `en` 页面，系统朗读使用 `en-US`，并保留 Zipf 高频词、禁用简化输入。Marathi 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为七十二种，新增 `Shqip`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Albanian 输入源和 LTR 文本排版。参考仅定义 `name: albanian`，没有 BCP-47、RTL、连写、`noLazyMode` 或词频排序标记，因此 Typebar 严格采用官方缺省路径：明确选择“知识短文”时请求 `en` 页面，系统朗读使用 `en-US`，并保留用户显式选择的简化输入。Albanian 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为七十三种，新增 `Ichibemba`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Bemba 输入源和 LTR 文本排版。参考定义 `bcp47: bem`、`rightToLeft: false` 与 `orderedByFrequency: false`，没有 `noLazyMode` 或连写，因此知识短文和系统朗读均精确使用 `bem`，可加入默认和自选多语混排并保留用户显式选择的简化输入。启用 Zipf 时会显示词表未按频率排序的七秒提示，但不会关闭修饰器。Bemba 已进入配置、预设、分享、结果、归档、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为七十四种，新增 `Bosanski`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS Bosnian 输入源和 LTR 文本排版。参考仅定义 `name: bosnian` 与 `orderedByFrequency: true`，没有 BCP-47、RTL、连写或 `noLazyMode`，因此 Typebar 严格采用官方缺省路径：明确选择“知识短文”时请求 `en` 页面，系统朗读使用 `en-US`，同时保留 Zipf 高频词和用户显式选择的简化输入。Bosnian 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为七十七种，新增 `Esperanto`、`Esperanto · X-sistemo` 和 `Esperanto · H-sistemo` 三种独立书写模式。三者都使用 Typebar 原创词流和短/中/长/超长引语，LTR 空格提交，并进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。固定参考中，标准 Esperanto 仅定义 `orderedByFrequency: true`，X-sistemo 定义 `noLazyMode: true` 且未声明词频排序，H-sistemo 同时定义 `noLazyMode: true` 与 `orderedByFrequency: true`；三者均未定义 BCP-47、RTL 或连写。因此知识短文和朗读一律保留官方 `en`／`en-US` 缺省分支；标准书写保留简化输入，X/H 保持 ASCII 文字并禁用简化输入；X 的 Zipf 启用时显示七秒“可能不支持”提示但不关闭修饰器。

2026-09-05 补充：当前单语数更新为七十八种，新增 `Latina`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生 LTR 文本排版。参考仅定义 `name: latin`，没有 BCP-47、RTL、连写、`noLazyMode` 或词频排序标记，因此 Typebar 严格采用官方缺省路径：明确选择“知识短文”时请求 `en` 页面，系统朗读使用 `en-US`，并保留用户显式选择的简化输入。启用 Zipf 时会显示可能不支持的七秒提示但不会关闭修饰器。Latin 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为七十九种，新增 `Friulian`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生 LTR 文本排版。参考定义 `bcp47: fur`，没有 RTL、连写、`noLazyMode` 或词频排序标记，因此知识短文与系统朗读均精确使用 `fur`，并保留用户显式选择的简化输入。启用 Zipf 时会显示可能不支持的七秒提示但不会关闭修饰器。Friulian 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为八十种，新增 `Malagasy`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生 LTR 文本排版。参考只定义 `noLazyMode: true`，没有 BCP-47、RTL、连写或词频排序标记，因此 Typebar 严格采用官方缺省路径：明确选择“知识短文”时请求 `en` 页面，系统朗读使用 `en-US`，并禁用简化输入。启用 Zipf 时会显示可能不支持的七秒提示但不会关闭修饰器。Malagasy 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为八十一种，新增 `Cymraeg`（Welsh）。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生 LTR 文本排版。参考仅定义 `name: welsh`，没有 BCP-47、RTL、连写、`noLazyMode` 或词频排序标记，因此 Typebar 严格采用官方缺省路径：明确选择“知识短文”时请求 `en` 页面，系统朗读使用 `en-US`，并保留用户显式选择的简化输入。启用 Zipf 时会显示可能不支持的七秒提示但不会关闭修饰器。Welsh 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为八十二种，新增 `Hausa`。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区沿用 macOS 原生 LTR 文本排版。参考定义 `bcp47: ha`，没有 RTL、连写、`noLazyMode` 或词频排序标记，因此知识短文与系统朗读均精确使用 `ha`，并保留用户显式选择的简化输入。启用 Zipf 时会显示可能不支持的七秒提示但不会关闭修饰器。Hausa 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为八十三种，新增 `Татарча`（Tatar）。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 西里尔内容，以空格提交；普通提示区沿用 macOS 原生 LTR 文本排版。参考定义 `bcp47: tt` 与 `orderedByFrequency: true`，没有 RTL、连写或 `noLazyMode` 标记，因此知识短文与系统朗读均精确使用 `tt`，保留用户显式选择的简化输入和 Zipf 高频词。Tatar 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为八十四种，新增 `Oʻzbekcha`（Uzbek）。词流和短/中/长/超长引语均为 Typebar 原创 Unicode 内容，以空格提交；普通提示区遵循参考显式 `rightToLeft: false` 的 macOS 原生 LTR 文本排版。参考定义 `bcp47: uz-UZ`，没有连写、`noLazyMode` 或词频排序标记，因此知识短文按首段精确使用 `uz`，系统朗读精确使用 `uz-UZ`，并保留用户显式选择的简化输入。启用 Zipf 时会显示可能不支持的七秒提示但不会关闭修饰器。Uzbek 已进入配置、预设、分享、结果、归档、默认/自选多语混排、社区投稿和排行榜筛选。

2026-09-05 补充：当前单语数更新为八十五种，新增 `Swiss German`。固定参考将 `swiss_german` 作为独立基础词表组，定义 `bcp47: de-CH`；它没有 RTL、连写、`noLazyMode` 或词频排序标记。源码的词流及引语读取均回退 German，并在可见词中把 `ß` 替换为 `ss`。Typebar 严格复用自有 German 词流与四档引语后做相同转换，绝不导入参考字典或引语；自定义文本也遵循该转换。知识短文按 BCP 首段使用 `de`，系统朗读精确使用 `de-CH`，保留简化输入，Zipf 显示可能不支持提示但不移除修饰器。Swiss German 已进入配置、预设、分享、结果、归档、默认／自选多语混排、成绩与排行榜；按源码不允许社区投稿，也不会使用社区引语来源。

2026-09-04 审计更正：好友 WPM 与 XP 榜已可在“同步”页切换全局/好友范围、查看本人名次并打开公开资料；待完成的社交范围仅包括系统推送和真实设备端到端验收。社区引语举报现包含语法错误、重复引语、低质量内容，以及既有的版权、内容、署名与其他类别；既有服务端记录不迁移或丢弃。

2026-09-04 补充：当前内置键盘图更新为二十一种，新增 `Hungarian QWERTZ · Typebar`。该 Typebar 自写映射覆盖现有原创 Magyar 词流中的 `á/é/í/ó/ö/ő/ú/ü/ű`、QWERTZ 的 Y/Z 位置与练习所需标点，可在键盘提示、显式输入模拟、归档和 Layout Fluid 中使用；它不导入官方或系统布局资产，未收录的死键和 Option 层仍交给 macOS 当前输入源。

2026-09-04 补充：当前内置键盘图更新为二十二种，新增 `Greek Alphabetic · Typebar`。该 Typebar 自写的字母顺序映射覆盖现有原创 Greek 词流的二十四个基本字母、七个重音元音、词末 `ς` 与练习标点，可在键盘提示、显式输入模拟、归档和 Layout Fluid 中使用；它不是 macOS 或官方布局资产的导入，未收录字符、死键和 Option 层仍交给 macOS 当前输入源。此条同时取代 `VIS-02` / `VIS-05` 中将 Greek 列入未覆盖非 Latin 映射的较早表述。

2026-09-05 补充：当前内置键盘图更新为二十三种，新增 `Polish (Programmers)`。该 Typebar 独立实现以 ANSI QWERTY 为基础，并为公开标准的 `ą/ć/ę/ł/ń/ó/ś/ź/ż` 和 `€` 增加 Option/Shift+Option 输入层；字符高亮、动态图例、显式物理输入模拟、反查、归档和 Layout Fluid 都使用同一份本机映射。Control+Option 不被截获，未定义的 Option 组合、死键及其他布局的 Option 层仍交给 macOS；不读取或导入 Monkeytype 布局 JSON。

2026-09-05 补充：当前内置键盘图更新为二十四种，新增 `Swedish QWERTY`。它以公开的瑞典普通/Shift 键位独立定义 `å/ö/ä/½`、ISO `< >` 与对应标点的原生提示、显式物理输入模拟、反查、归档和 Layout Fluid；死键和 Option 层仍交给 macOS。此条取代本文较早的二十三种计数，不读取或导入 Monkeytype 布局 JSON。

2026-09-05 补充：当前内置键盘图更新为二十五种，新增 `Danish QWERTY`。它以公开的丹麦普通/Shift 键位独立定义 `å/æ/ø/§`、ISO `< >` 与对应标点的原生提示、显式物理输入模拟、反查、归档和 Layout Fluid；死键与 Option/AltGr 层仍交给 macOS。此条取代本文较早的二十四种计数，不读取或导入 Monkeytype 布局 JSON。

2026-09-05 补充：当前内置键盘图更新为二十六种，新增 `Norwegian QWERTY`。它把现有 Nordic 基础键位以固定源码的挪威命名公开，覆盖 `å/ø/æ` 的提示、显式物理输入模拟、反查、归档和 Layout Fluid；旧 `Nordic QWERTY` 仍作为已有设置的兼容选项。死键和 Option/AltGr 层仍交给 macOS，不读取或导入 Monkeytype 布局 JSON。

2026-09-05 补充：当前内置键盘图更新为二十七种，新增 `ANSI Colemak-DH`。它按公开 ANSI 基础变体独立定义相对标准 Colemak 的 `B/G`、`D/V`、`H/M` 键位，用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid；ISO、wide 与其他 DH 变体仍由系统输入或自定义键盘图处理，不读取或导入 Monkeytype 布局 JSON。

2026-09-05 补充：当前内置键盘图更新为二十八种，新增 `Turkish F`。它按公开基础行独立定义 `ğ/ı/i/İ/ü/ö/ç/ş`、数字行和 ISO `< >`，用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid；AltGr 与组合式死键仍由 macOS 处理，不读取或导入 Monkeytype 布局 JSON。

2026-09-05 补充：当前内置键盘图更新为二十九种，新增 `Bulgarian Phonetic Traditional`。它按公开普通/Shift 键位独立定义 `ч/я/ъ/ш/щ/ю/ь/ѝ`、`№`、`€` 与 ISO 物理键上的 `ю`，用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid；AltGr 仍由 macOS 处理，不读取或导入 Monkeytype 布局 JSON。

2026-09-05 补充：当前内置键盘图更新为三十种，新增 `Belarusian`。它按公开普通/Shift 键位独立定义 `Ў/ў`、`І/і`、`Ё/ё`、`№` 和 ISO `< >`，用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid；Option/AltGr 仍由 macOS 处理，不读取或导入 Monkeytype 布局 JSON。

2026-09-05 补充：当前内置键盘图更新为三十一种，新增 `Macedonian`。它按公开普通/Shift 键位独立定义 `Љ/Њ/Ѕ/Ѓ/Ж/Ќ/Џ`、ISO `ѐ/Ѐ`、排版引号和区域标点，用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid；Option/AltGr 仍由 macOS 处理，不读取或导入 Monkeytype 布局 JSON。

2026-09-05 补充：当前内置键盘图更新为三十二种，新增 `Pashto`。它以公开普通/Shift 键位独立定义波斯-阿拉伯字母、东阿拉伯数字、组合音标、ZWJ/ZWNJ 与区域标点，用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid；提示层按 Unicode scalar 匹配组合字符，避免将基础字母和音标误视为一个不可输入的书写簇。不输出字符的 ISO 键、AltGr、方向控制与其他系统层继续由 macOS 处理，不读取或导入 Monkeytype 布局 JSON。此条取代本文较早“三十一种”及“除 Russian、Ukrainian 与 Serbian Cyrillic 外非拉丁布局未实现”的描述。

2026-09-05 补充：当前内置键盘图更新为三十三种，新增 `Estonian`。它以公开普通/Shift 键位独立定义 `õ/ä/ö/ü/š/ž`、ISO `< >` 与区域标点，用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid；AltGr 和组合式死键继续交给 macOS，不读取或导入 Monkeytype 布局 JSON。此条取代本文较早“三十二种”的计数。

2026-09-05 补充：当前内置键盘图更新为三十四种，新增 `Persian (Standard)`。它以公开普通/Shift 键位独立定义波斯字母、东阿拉伯数字、`﷼`、组合音标、ZWJ/ZWNJ、ISO 与区域标点，用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid；提示层与映射解析按 Unicode scalar 保留组合字符。AltGr 与方向控制继续交给 macOS，不读取或导入 Monkeytype 布局 JSON。此条取代本文较早“三十三种”的计数。

2026-09-06 补充：当前内置键盘图更新为三十五种，新增 `Arabic (101)`。它以公开普通/Shift 键位独立定义阿拉伯字母、组合音标、`لإ/لأ/لا/لآ` 完整文本输出、ISO 与区域标点，用于原生提示、显式物理输入模拟、完整输出反查、归档和 Layout Fluid；模拟器的布局层现可保留单键的多字符输出，既有单字符 API 仍保持原语义。AltGr、ZWJ/ZWNJ 与方向控制继续交给 macOS，不读取或导入 Monkeytype 布局 JSON。此条取代本文较早“三十四种”的计数。

2026-09-06 补充：当前内置键盘图更新为三十六种，新增 `Hebrew`。它以公开普通/Shift 键位独立定义希伯来字母、Shift 拉丁/标点层和 ISO 键，用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid。AltGr、Caps Lock 元音点和方向控制继续交给 macOS，不读取或导入 Monkeytype 布局 JSON。此条取代本文较早“三十五种”的计数。

2026-09-06 补充：当前内置键盘图更新为三十七种，新增 `Arabic (102)`。它以公开普通/Shift 键位独立定义 `ذ`、ISO tatweel、组合音标、连字与区域标点，用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid。AltGr、ZWJ/ZWNJ 与方向控制继续交给 macOS，不读取或导入 Monkeytype 布局 JSON。此条取代本文较早“三十六种”的计数。

2026-09-07 补充：当前内置键盘图更新为三十八种，新增 `Arabic (macOS)`。固定参考源码只用于确认 `arabic_mac` 功能名；键位依据 macOS TIS/`UCKeyTranslate` 返回的系统输入行为独立实现，覆盖阿拉伯数字、组合音标、区域标点、Option/Shift+Option 层及无输出 Shift 键，并用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid。此条取代本文较早“三十七种”的计数，不读取或导入 Monkeytype 布局 JSON、文本或资产。

2026-09-07 补充：当前内置键盘图更新为三十九种，新增 `Persian (Farsi)`。固定参考源码只用于确认 `persian_farsi` 功能名；键位依据 Microsoft Persian KLID `00000429` 的公开键位表独立实现，覆盖 Farsi Yeh、Keheh、Gaf、ISO `پ`、组合音标和 `ریال` 多字符快捷键，并用于原生提示、显式物理输入模拟、完整输出反查、归档和 Layout Fluid。下一键提示优先匹配精确单字符输出，避免多字符快捷键抢占其中字符；Control 层继续交给 macOS。此条取代本文较早“三十八种”的计数，不读取或导入 Monkeytype 布局 JSON、文本或资产。

2026-09-07 补充：当前内置键盘图更新为四十种，新增 `Urdu Phonetic (CRULP)`。固定参考源码只用于确认 `urdu_phonetic` 功能名；键位依据 CLE/CRULP v1.1 规范与 SIL Keyman 开放映射交叉独立实现，覆盖 Urdu 字母、数字、组合音标、宗教符号、Option/Right Alt 层及明确无输出的 Shift+F，并用于原生提示、显式物理输入模拟、反查、归档和 Layout Fluid。此条取代本文较早“三十九种”的计数，不读取或导入 Monkeytype 布局 JSON、第三方代码、字体或键盘资产。

2026-09-07 补充：当前内置键盘图更新为四十一种，新增 `Thai Kedmanee`。固定参考源码只用于确认 `thai_kedmanee` 功能名；键位依据 Microsoft Thai Kedmanee KLID `0000041E` 的公开键位表与 NECTEC 的 TIS 820 资料独立实现，并以 macOS `com.apple.keylayout.Thai` 的系统翻译结果交叉核对，覆盖泰文字母、元音、声调、数字和标点的普通/Shift 层，用于原生提示、显式物理输入模拟、反查、归档与 Layout Fluid。Option 与组合输入继续由 macOS 处理；此条取代本文较早“四十种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为四十二种，新增 `Thai Pattachote`。固定参考源码只用于确认 `thai_pattachote` 功能名；键位依据 Microsoft Thai Pattachote KLID `0001041E` 及公开驱动表独立实现，并以 NECTEC 布局资料和 macOS `com.apple.keylayout.Thai-PattaChote` 系统翻译结果交叉核对，覆盖完整标准数字行及泰文字母、元音、声调和标点普通/Shift 层，用于原生提示、显式物理输入模拟、反查、归档与 Layout Fluid。Option 与组合输入继续由 macOS 处理；此条取代本文较早“四十一种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为四十三种，新增 `Hindi – InScript (macOS)`。固定参考源码只用于确认 `hindi_inscript` 功能名；键位依据 macOS `com.apple.keylayout.Devanagari` 的系统翻译行为独立实现，覆盖天城文数字、元音、辅音、组合符、`ज्ञ/त्र/क्ष/श्र` 多码点输出和明确无输出的普通/Shift 键，并用于原生提示、显式物理输入模拟、完整输出反查、归档与 Layout Fluid。BIS 与 Microsoft 资料只用于标准家族交叉核对；Option 与复杂组合输入继续由 macOS 处理。此条取代本文较早“四十二种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为四十四种，新增 `Armenian – HM QWERTY`。固定参考源码只用于确认 `armenian_hm_qwerty` 功能名；依据 macOS 同名系统输入源独立实现 Base、Shift、Option 与 Shift+Option 四层，并用于提示、模拟、反查、归档与 Layout Fluid。此条取代本文较早“四十三种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为四十五种，新增 `Mongolian Cyrillic`。固定参考源码只用于确认 `mongolian` 功能名；依据 macOS 同名系统输入源独立实现 Base、Shift、Option 与 Shift+Option 四层，并用于提示、模拟、反查、归档与 Layout Fluid。主体字母层与 Unicode CLDR 的标准布局交叉核对；此条取代本文较早“四十四种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为四十七种，新增 `Dvorak – Left-Handed` 与 `Dvorak – Right-Handed`。固定参考源码只用于确认 `dvorak_L` / `dvorak_R` 功能名；依据 macOS 同名系统输入源独立实现 Base、Shift、Option 与 Shift+Option 四层，并用于提示、模拟、反查、归档与 Layout Fluid。Apple 与 Unicode CLDR 资料用于交叉确认左右手语义；此条取代本文较早“四十五种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为四十八种，新增 `Brazilian – ABNT2`。固定参考源码只用于确认 `ABNT2` 功能名；依据 macOS 同名系统输入源独立实现 Base、Shift、Option 与 Shift+Option 四层，并以 12 键底行支持 ISO keyCode 10 和 ABNT2 专用 keyCode 94，用于提示、模拟、反查、归档与 Layout Fluid。Unicode CLDR 用于交叉确认 103 键几何；此条取代本文较早“四十七种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为四十九种，新增 `Tamil99 (macOS)`。固定参考源码只用于确认 `tamil99` 功能名；依据 macOS 同名系统输入源独立实现 Base、Shift、Option 与 Shift+Option 四层，并保留 Tamil 组合符、`ஸ்ரீ` 多码点输出和明确空输出的修饰层，用于提示、模拟、完整输出反查、归档与 Layout Fluid。Microsoft 与 Unicode CLDR 资料用于标准家族交叉核对；此条取代本文较早“四十八种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为五十种，新增 `Colemak-DH ISO`。固定参考源码只用于确认 `colemak_dh_iso` 功能名；依据 Colemak-DH 官方公开说明与 CC0 macOS 基础层独立实现 ISO Angle Mod，把 `Z` 放在左 Shift 右侧的额外实体键，并保留 `X C D V`、中央反引号和 `K H , . /` 的普通/Shift 映射，用于提示、模拟、反查、归档与 Layout Fluid。此条取代本文较早“四十九种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为五十一种，新增 `Colemak-DH Matrix`。固定参考源码只用于确认 `colemak_dh_matrix` 功能名；依据 Colemak-DH 官方 CC0 macOS 基础层独立实现正交矩阵底行 `Z X C D V K H , . /`，与 ANSI 的中央 `Z` 重定位和 ISO 的额外实体键保持为三个不同布局，并用于提示、模拟、反查、归档与 Layout Fluid。此条取代本文较早“五十种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为五十三种，新增 `Colemak-DHk ANSI` 与 `Colemak-DHk ISO`。固定参考源码只用于确认 `colemak_dhk` / `colemak_dhk_iso` 功能名；依据 ColemakMods 官方 CC0 macOS 基础层独立实现主行 `K`、底行 `M/H` 语义，并分别保留 ANSI 的中央 `Z` 与 ISO 的额外 `Z`/中央反引号实体键。两者均进入提示、模拟、反查、归档与 Layout Fluid；此条取代本文较早“五十一种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为五十五种，新增 `Colemak-DH Wide ANSI` 与 `Colemak-DH Wide ISO`。固定参考源码只用于确认 `colemak_dh_wide` / `colemak_dh_iso_wide` 功能名；依据 ColemakMods 官方 CC0 定义独立实现 Wide Mod 的数字行和右手字母/标点位移，并保留 ANSI 与 ISO 不同的底行实体键。两者均进入提示、模拟、反查、归档与 Layout Fluid；此条取代本文较早“五十三种”的计数，不读取或导入 Monkeytype 布局 JSON、代码或资产。

2026-09-07 补充：当前内置键盘图更新为五十六种，新增 `Japanese Hiragana`。Typebar 以独立 Swift 映射重新实现固定参考可观察的 47 个 ANSI 位置及普通/Shift 行为，覆盖普通假名、小假名、`を`、日文括号与 `、。・`；已进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 51 项精确原生、3 项相关替代、185 项系统输入或自定义回退；不复制、打包或运行参考布局 JSON、代码、字体或其他资产。

2026-09-07 补充：当前内置键盘图更新为五十九种，新增 `ANSI Colemak Angle`、`ANSI Colemak Wide` 与 `ANSI Norman`。三项均以独立 Swift 映射覆盖 47 个 ANSI Base/Shift 位置，分别保留 Angle 底行、Wide 数字/标点位移和 Norman 字母排列，并进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 54 项精确原生、3 项相关替代、182 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为六十二种，新增 `ANSI Colemak-DHv`、`Programmer Workman` 与 `Turkish E`。前两项完整覆盖 47 个 ANSI Base/Shift 位置，Turkish E 覆盖含额外 ISO 键的 48 个位置；三项均进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 57 项精确原生、3 项相关替代、179 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为六十七种，新增 `MTGAP ASRT`、`Halmak`、`QGMLWB`、`QGMLWY` 与 `QWPR`。五项均以独立 Swift 映射完整覆盖 47 个 ANSI Base/Shift 位置，其中 Halmak 保留非标准数字及标点 Shift 层，并全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 62 项精确原生、3 项相关替代、174 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为七十二种，新增 `Programmer Dvorak`、`Programmer Dvorak Prime`、`German Dvorak`、`German Dvorak Improved` 与 `Spanish Dvorak`。两项 Programmer 变体完整覆盖 47 个 ANSI Base/Shift 位置并保留各自符号数字层，三项区域变体完整覆盖含额外 ISO 键的 48 个位置；全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 67 项精确原生、3 项相关替代、169 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为七十七种，新增 `Swedish Colemak`、`Swedish Dvorak`、`French Dvorak`、`French AZERTY (AFNOR)` 与 `French Bépo`。五项均完整覆盖 48 个 ISO 物理位置；前三项覆盖 Base/Shift，AFNOR 与 Bépo 另完整覆盖 Option/Shift+Option，AFNOR 还实现空格键的 NBSP/窄 NBSP 层。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 72 项精确原生、3 项相关替代、164 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为七十九种，新增 `French Bépo (AFNOR)` 与 `ANSI Alpha`。Bépo AFNOR 完整覆盖 48 个 ISO 位置的 Base/Shift/Option/Shift+Option 与普通空格，保留未分配层为空；Alpha 完整覆盖 47 个 ANSI Base/Shift 位置。两项均进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 74 项精确原生、3 项相关替代、162 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为八十三种，新增 `ANSI Hands Down`、`ANSI Hands Down Alt`、`ANSI Hands Down Neu` 与 `ANSI Hands Down Neu Inverted`。四项均完整覆盖 47 个 ANSI Base/Shift 位置，Neu 两项保留非标准数字与标点 Shift 层，并全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 78 项精确原生、3 项相关替代、158 项系统输入或自定义回退。`handsdown_promethium` 因另有独立 `R` 拇指键而继续回退，不伪装为标准 ANSI；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为八十八种，新增 `MTGAP`、`MTGAP Full`、`Ina`、`Soul` 与 `Niro`。五项均完整覆盖 47 个 ANSI Base/Shift 位置；MTGAP、Full 与 Ina 保留各自非标准符号层，MTGAP Full 还在精简提示中强制显示其有效数字行。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 83 项精确原生、3 项相关替代、153 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为九十三种，新增 `TypeHack`、`ISRT`、`ISRT Angle`、`Engram` 与 `Engrammer`。五项均完整覆盖 47 个 ANSI Base/Shift 位置；TypeHack 与 Engram 保留各自非标准符号层，Engram 两项还在精简提示中强制显示其有效数字行，ISRT Angle 保留独立底行角度变体。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 88 项精确原生、3 项相关替代、148 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为九十八种，新增 `Semimak`、`Semimak JQ`、`Semimak JQC`、`Canary` 与 `Canary Matrix`。五项均完整覆盖 47 个 ANSI Base/Shift 位置；三种 Semimak 保留各自 J/Q/C 交换，Canary Matrix 保留与普通 Canary 不同的字母与标点位置。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 93 项精确原生、3 项相关替代、143 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百零三种，新增 `Boo`、`Boo Mangle`、`APT`、`APT Angle` 与 `Middlemak`。五项均完整覆盖 47 个 ANSI Base/Shift 位置；Boo 的反向 `?/` 与 Boo Mangle 的数字符号层、`@/^` 配对均被显式保留，Boo Mangle 在精简提示中强制显示数字行，APT Angle 保留独立底行。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 98 项精确原生、3 项相关替代、138 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百零八种，新增 `Middlemak-NH`、`Foalmak`、`Quartz`、`Arensito` 与 `ARTS`。五项均完整覆盖 47 个 ANSI Base/Shift 位置；Middlemak-NH 保留与普通 Middlemak 不同的 N/H/A 主行，Foalmak 保留两个独立句点实体键并允许反查落在任一精确键，Quartz 保留重排数字行并在精简提示中强制显示。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 103 项精确原生、3 项相关替代、133 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百一十三种，新增 `Capewell Dvorak`、`Colman`、`Heart`、`Klauser` 与 `Oneproduct`。五项均完整覆盖 47 个 ANSI Base/Shift 位置；Capewell Dvorak 保留重排数字行并在精简提示中强制显示，Klauser 保留分散标点，Oneproduct 显式保留问号键普通层 `?`、Shift 层 `/`。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 108 项精确原生、3 项相关替代、128 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百一十八种，新增 `Pine`、`Real`、`Sertain`、`CTGAP` 与 `Graphite`。五项均完整覆盖 47 个 ANSI Base/Shift 位置；Real 保留重排数字行并在精简提示中强制显示，Graphite 显式保留 `'→_`、`,→?`、`-→"` 与 `/→<` 的非标准 Shift 层。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 113 项精确原生、3 项相关替代、123 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百二十三种，新增 `Focal`、`Zenith`、`Dhorf`、`Gust` 与 `Recurva`。五项均完整覆盖 47 个 ANSI Base/Shift 位置；Gust 保留重排的方括号数字行，但依固定公开元数据在精简模式始终隐藏该行，即使下一键是方括号也需选择“精简（数字行）”或“完整”才显示。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 118 项精确原生、3 项相关替代、118 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百二十八种，新增 `Pine v4`、`Three`、`Asset`、`Dwarf` 与 `Flaw`。五项均完整覆盖 47 个 ANSI Base/Shift 位置；Dwarf 显式保留逗号到 `>`、句号到 `<` 的非标准 Shift 层。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 123 项精确原生、3 项相关替代、113 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百三十三种，新增 `STNDC`、`UCIEA`、`Whorf`、`Whorf 6` 与 `Whorfmax`。五项均完整覆盖 47 个 ANSI Base/Shift 位置；STNDC 与 UCIEA 显式保留重排数字行和多组非标准 Shift 配对，并按固定公开元数据在精简模式显示数字行。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 128 项精确原生、3 项相关替代、108 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百三十八种，新增 `Octa8`、`Nerps`、`Gallium`、`Gallium Angle` 与 `Gallium v2`。五项均完整覆盖 47 个 ANSI Base/Shift 位置，三种 Gallium 保持独立的 V/J/G/Z 实体位置。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 133 项精确原生、3 项相关替代、103 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百四十三种，新增 `Nila`、`Noctum`、`Cascade`、`Vylet` 与 `Romak`。五项均完整覆盖 47 个 ANSI Base/Shift 位置，Cascade 与 Vylet 的逗号/句点非标准 Shift 配对由逐键映射显式保留。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 138 项精确原生、3 项相关替代、98 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百四十八种，新增 `Scythe`、`Inqwerted`、`Rain`、`Night` 与 `Night STIC`。五项均完整覆盖 47 个 ANSI Base/Shift 位置，Night 的 `=` 与反斜杠数字行实体位置由独立映射保留。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 143 项精确原生、3 项相关替代、93 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百五十三种，新增 `Whix2`、`Haruka`、`Kuntum`、`Kuntem` 与 `Kuntem-JQ`。Haruka 与三个 Kuntum/Kuntem 变体覆盖 47 个 ANSI Base/Shift 位置；Whix2 的 40 个赋值位置与 7 个明确空位分别作为可输入键和不输出、不反查的视觉空键实现。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 148 项精确原生、3 项相关替代、88 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百五十八种，新增 `BEAKL Zi`、`Snorkle`、`MALTRON`、`PRSTEN` 与 `RSTHD`。五项覆盖 47 个 ANSI Base/Shift 位置；BEAKL Zi、MALTRON、PRSTEN 与 RSTHD 额外显示其双拇指行，普通 Mac 的唯一物理 Space 按固定参考可观察语义映射到第五行首项，第二项仅作提示。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 153 项精确原生、3 项相关替代、83 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百六十三种，新增 `Hands Down Promethium`、`Statica 3×5`、`Vestnik`、`Diktor` 与 `Diktor Voronov Mod`。五项覆盖四个 ANSI 主行，AltGr 未重定义键保持各自 Base/Shift；Promethium 使用双拇指提示并由物理 Space 输入首项 `r`，Statica/Vestnik 另保留三组特殊 AltGr Cyrillic 输出，Diktor 两项保留重排数字符号层并在精简提示中显示数字行。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 158 项精确原生、3 项相关替代、78 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百六十八种，新增 `Redaktor`、`JUIYAF`、`Zubachev`、`Colemak Qi;x` 与 `Colemak Qi`。五项覆盖 47 个 ANSI Base/Shift 位置，保留重排数字符号层、AltGr 基础层回退和精简提示数字行；反查新增精确大小写输出优先级，以正确区分 JUIYAF/Zubachev 的非标准 Cyrillic Shift 配对及重复字符。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 163 项精确原生、3 项相关替代、73 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百七十三种，新增 `ColemaQ`、`ColemaQ F`、`Thai Manoonchai`、`Brasileiro Nativo` 与 `BEAKL 15`。Brasileiro Nativo 按 13/12/12/11 的 ISO 物理行实现，其余四项按 ANSI 实现；泰文组合符以独立键层字符串保存，避免 Unicode 字素聚合改变实体键数量。五项均覆盖 Base/Shift、AltGr 基础层回退、提示、模拟、反查、设置归档与 Layout Fluid，并保持各自精简数字行策略。官方矩阵同步更新为 168 项精确原生、3 项相关替代、68 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百七十八种，新增 `BEAKL 19`、`BEAKL 19 Bis`、`Rolll`、`Whorfmax Ortho` 与 `Neo`。前四项完整落在 ANSI 47 个位置，Neo 保留 13/12/12/11 ISO 行与两个单层空格实体键；单层键在 Shift/Option/Shift+Option 下按可观察规则回退首层。五项均进入提示、模拟、反查、设置归档与 Layout Fluid，并在精简提示中隐藏数字行。官方矩阵同步更新为 173 项精确原生、3 项相关替代、63 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百八十三种，新增 `Bone`、`AdNW`、`Mine`、`Noted` 与 `Koy`。五项均保留 Neo 系 13/12/12/11 ISO 行、独立字母及德语变音符排列、单层空格实体键和精简数字行隐藏策略；缺失 Shift/AltGr 层按可观察规则回退首层。全部进入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 178 项精确原生、3 项相关替代、58 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百八十八种，新增 `3l`、`Korean`、`Ekverto B`、`Sturdy Angle ANSI` 与 `Sturdy Angle ISO`。3l 保留单层数字、标点及八个空格实体键，Korean 保留兼容 Jamo 与选择性 Shift，Ekverto B 和两项 Sturdy 保留各自 ISO/ANSI 物理结构及非标准符号配对；缺失 AltGr 层回退 Base/Shift。全部进入提示、模拟、反查、设置归档与 Layout Fluid，并隐藏精简数字行。官方矩阵同步更新为 183 项精确原生、3 项相关替代、53 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百九十三种，新增 `Sturdy Ortho`、`HiYou`、`Xenia`、`Xenia Alt` 与 `Burmese`。HiYou 保留 ISO 行及单层 `|`/反斜杠键，Burmese 以逐键字符串保留组合符并显示精简数字行，其余三项保持各自 ANSI 排列；五项均实现 Base/Shift、AltGr 回退、提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 188 项精确原生、3 项相关替代、48 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为一百九十八种，新增 `Gallium v2 Matrix`、`Gallium NL`、`Maya`、`Gallaya Angle ANSI` 与 `Gallaya Angle ISO`。Gallium v2 Matrix 依固定参考元数据保留 ANSI 物理类型，Gallaya ISO 保留额外 ISO 键与地区 Shift 符号；五项均实现完整 Base/Shift、AltGr 基础层回退、提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 193 项精确原生、3 项相关替代、43 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为二百零三种，新增 `Gallaya Matrix`、`Minimak 4-key`、`Minimak 8-key`、`Minimak 12-key` 与 `Graphite Angle`。五项依固定参考元数据保留 ANSI 物理类型，完整实现 Base/Shift 与 AltGr 基础层回退；三个 Minimak 阶段保留逐步变化的键位和英式 `£` Shift 符号。全部接入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 198 项精确原生、3 项相关替代、38 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为二百零四种，新增 `Optimot`。它以独立 Swift 数据结构保留 48 个 ISO 物理位置及 Base/Shift/Option/Shift+Option 四层；字母区 keycode 11 的 `⌫` 位调用原生向后删除，不作为文本进入提示匹配、输入、计分或回放。布局接入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 199 项精确原生、3 项相关替代、37 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为二百零九种，新增 `Graphite Angle VC`、`Graphite Angle KP`、`Graphite Matrix`、`UGJRMV` 与 `ORNATE`。五项均实现完整 ANSI Base/Shift 与 AltGr 基础层回退；三个 Graphite 变体保留不同底行，UGJRMV 保留带长音符/软音符字母和特殊数字符号，ORNATE 保留固定参考中的非对称 Shift 配对。全部接入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 204 项精确原生、3 项相关替代、32 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为二百一十三种，新增 `Tarmak 1`、`Tarmak 2`、`Tarmak 3` 与 `Tarmak 4`。四项均以独立 ANSI Base/Shift 键表保留从 QWERTY 向 Colemak 迁移的阶段差异，并实现 AltGr 基础层回退、提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 208 项精确原生、3 项相关替代、28 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为二百一十八种，新增 `Stronk`、`Seht Drai`、`Ints`、`Rollla` 与 `Wreathy`。五项均以独立 ANSI Base/Shift 键表保留各自字母与标点位置，并实现 AltGr 基础层回退、提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 213 项精确原生、3 项相关替代、23 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为二百二十三种，新增 `Saiga`、`Saiga E`、`Krai`、`Mir` 与 `Ergol`。五项均以独立 ANSI Base/Shift 键表保留各自字母与标点位置；Ergol 额外保留欧元、书名号、弯引号与非对称 Shift 配对。全部接入 AltGr 基础层回退、提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 218 项精确原生、3 项相关替代、18 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为二百二十八种，新增 `Hyperroll`、`Anishtro`、`Dusk`、`Panini` 与 `Panini Wide`。五项均保留完整 ANSI Base/Shift 与 AltGr 基础层回退；Anishtro 与 Dusk 显示双拇指行，普通 Mac 的物理 Space 分别输入 `e` 与 `r`，Panini Wide 保留方括号和反斜杠的宽位重排。全部接入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步更新为 223 项精确原生、3 项相关替代、13 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为二百三十三种，新增 `Sword`、`OPY`、`Ergo Split 46`、`Gralmak` 与 `Vitrimak`。五项均保留完整 ANSI Base/Shift 与 AltGr 基础层回退；OPY 保留德语字母和重排数字符号，Ergo Split 46 保留欧元、书名号与 `˜`，其余布局保持各自标点位置。全部接入提示、模拟、可用反查、设置归档与 Layout Fluid。官方矩阵同步更新为 228 项精确原生、3 项相关替代、8 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-07 补充：当前内置键盘图更新为二百三十八种，新增 `Persian Farsi Colemak`、`Persian Standard Colemak`、`Miligram`、`Nokwts` 与 `Vylet v4`。Persian 两项以逐物理键字符串保留组合音标、波斯数字与 ZWNJ，Miligram 保留 13/12/12/11 ISO 行并与 Persian Standard 强制显示精简数字行，Nokwts/Vylet v4 保持独立 ANSI 标点。五项均接入 Base/Shift、AltGr 基础层回退、提示、模拟、可用反查、设置归档与 Layout Fluid。官方矩阵同步更新为 233 项精确原生、3 项相关替代、3 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-08 补充：当前内置键盘图更新为二百四十一种，新增 `ErgolPT`、`Rulemak` 与 `Gralmak S`。三项以 Typebar 自有 Swift 数据结构保留完整物理位置、显式四层和缺失层回退；ErgolPT 另保留 13/12/12/11 ISO 行及物理 Space 的 Shift `-` 输出。它们均接入提示、模拟、可用反查、设置归档与 Layout Fluid。官方矩阵同步更新为 236 项精确原生、3 项相关替代、0 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-08 补充：当前内置键盘图更新为二百四十四种，新增独立的 `Hungarian (ISO)`、`JCUKEN (ANSI)` 与 `Bulgarian (BDS)`。三项分别保留匈牙利 ISO 符号层、无额外 ISO 键的 ANSI JCUKEN，以及 BDS ISO 排列与精简提示隐藏数字行；均接入提示、模拟、反查、设置归档与 Layout Fluid。官方矩阵同步达到 239 项精确原生、0 项相关替代、0 项系统输入或自定义回退；不复制、打包或运行参考布局资产。

2026-09-04 补充：当时 `CFG-02`、`VIS-02` 与 `VIS-05` 的非拉丁布局覆盖新增 `Bulgarian Cyrillic · Typebar`。它是 Typebar 自写的第 19 个内置物理键位映射，覆盖 `я/ъ/щ/ч/ь` 与 ISO `< >`，可在键盘提示、显式输入模拟、归档和 Layout Fluid 中使用；这不是官方或系统 BDS 布局的复制，需精确系统布局时用户仍可选择 macOS 当前输入源。
| INP-01 | 输入、逐字符状态、退格、长词 | `input/handlers/*` | TypingEngine | 部分实现 |
| INP-02 | 组合输入/IME、焦点及 Caps Lock 提示 | `input/*`, `CapsWarning.tsx` | AppKitInputBridge | 部分实现：原生 AppKit 输入桥支持组合输入；标记文本在确认前只更新可选的关闭/下方/替换当前字符组合显示，不会改变提示、输入、计分或回放，确认提交时才进入输入引擎。输入视图会回传实际 responder 焦点，连续失焦满 1 秒时才显示可点击恢复焦点的提示，重新获焦会取消等待；有大小写语义的拉丁文与西里尔文练习会读取 macOS Caps Lock 状态显示警告，中文、日语假名与韩语不会显示无意义的提示。组合显示和两类提示均可在本机设置持久化；IME 最终人工验收尚未完成 |
| INP-03 | normal/expert/master 难度 | `input/helpers/fail-or-finish.ts` | TypingEngine | 部分实现：普通难度保留错误继续输入；expert 会在每个词的提交点失败，空格分词使用提交空格，无空格修饰器使用保留原始词边界的末字符；master 仍在任一错误字符被接受时立即失败。已修正的错误词只要最终输入与目标一致即可通过 expert 词级检查；强制标记的物理键位错误仍作为真实错误参与对应判定。 |
| INP-04 | 停止/删除错误、严格空格、盲打、自由输入 | `config/metadata.tsx` | TypingEngine | 部分实现：停止/删除错误、严格空格、隐藏额外字符、盲打、自由回退、三档信心模式与反向 Shift 已持久化；遇错停下可选“字符”（拒绝错误按键）或“单词”（允许继续输入，但错误词在完全修正前不能以词提交点提交：普通提示为输入空格，无空格提示为原始词的末字符）。无空格会拒绝直接输入的常见 Unicode 空白字符。遇错删除可选“字符”（错误键会退回一个已接受字符）、“单词”（清空当前词），以及两种在新词首个错误时返回上一词的硬模式；已保留原始词界的无空格提示会使用相同的词级删除语义，无法可靠还原词界则保守地保留字符级回退。选择会与信心模式和另一类错误规则互斥，旧布尔偏好会迁移到字符档。反向 Shift 通过左右 Shift 状态和 ANSI 键位侧判断要求使用相反手，Y/B/6 可使用任意一侧，手位错误保留真实输入文本但以可回放的输入错误影响字形、统计、错词练习和结果。默认保护已正确提交的词但允许修复错误词，开启自由回退后可修改任意已提交词。信心模式开启时不可退回修复上一个错误词，最大档禁用全部退格；它与自由回退、遇错停下/删除互斥并在配置解码时归一化。“修正后再前进”修饰器会在词提交点拒绝错误词，直至该词完全修正；Typebar 自有的“遇错清除当前词”只对空格分词文本生效，误键会删除当前未提交词、保留已提交词并按逐字符删除事件写入回放。另有 Typebar 自有的最低单词速度规则：只在正确空格提交的词后按真实输入间隔校验；最低整体速度和最低准确率则只在有限测试本会完成时校验最终 WPM/准确率。任一阈值未达都会标记失败，不保存或发布完成成绩。三种阈值均进入预设、分享、结果与归档。其他输入规则仍持续审计。 |

2026-09-03 更正：INP-04 中“最低单词速度只检查正确词”的表述已失效。现行实现提供关闭、固定与弹性三档：固定使用设定 WPM；弹性阈值为 `min(base, floor(base × 1.03^(-2 × (目标词长 − 3))))`。每个具可测 burst 的空格提交词、无空格模式中原始词的末字符提交，以及禅模式的空格/换行提交都会检查，无论该词是否正确。

2026-09-03 补充：INP-04 现依照参考输入规则处理空白：普通难度会忽略每个词首的常见 Unicode 空白，严格空格会保留其为可修正错误，expert/master 与硬删除也会接收该按键；其余常见 Unicode 空白在验证前规范为普通词间空格，而无空格模式会在计分前拒绝它们。直/弯单引号、直/弯双引号、常见短横线与低位逗号会规范为提示使用的字形并计为正确。普通难度在词内提前输入空格时会保留实际输入、将该词作为一次错误提交并推进到下一目标词；自定义或引语提示含换行时，Return 同样是词提交符，提前按下也会作为错误提交并进入下一行，词首 Return 则会提交一个空词。严格空格、停止/删除错误与“修正后再前进”不会允许该词首跳转。目标词写完后继续输入的额外字符仍属于当前词，显示并计作错误，直至空格或换行提交。“隐藏额外字符”只隐藏这类额外字形，输入仍会被接受并进入错误统计与回放。每个空格分词输入缓冲限制为目标词（含可见提交分隔符）加 20 个字符，到达上限仍可输入提交空格。提示字形、词进度、结果词历史、错误计数和自由退格都以实际字符到目标位置的映射保持一致，expert 则在同一错误提交点失败。

有限的引语和自定义文本测试会保留等长但错误的最后一个词供用户修正；只有最后一词正确、输入词提交分隔符，或在未启用停止/删除错误时明确开启快速结束，才会完成。

原生输入桥将一次粘贴或已确认的多字符输入法提交作为单个输入事件逐字符写入、逐字符保留回放；expert/master 难度和最低单词 burst 则只在该事件的最后一个字符判定。逐键输入仍在每个字符即时判定。

当提示期待普通三连句点时，单个 Unicode 省略号会在本机输入层展开为三个句点；提示自身期待省略号时则保留原字符。展开后的字符会独立进入计分和回放。

原生输入桥支持 macOS 的“删除上一个词”命令：会清空当前未提交词；若当前词为空，则仅在自由回退或上一个已提交词错误时回退并清除该词。信心模式与最大信心模式均不会回退到已提交词，已提交正确词在默认模式同样受保护。

快速重开与提示键冲突时会保留输入：提示含 Tab 且快速重开设为 Tab 时，普通 Tab 输入、Shift+Tab 重开；提示含换行且快速重开设为 Enter 时，普通 Enter 输入、Shift+Enter 重开。字数至少 1,000 词或时长至少 15 分钟的长测试会保护重开：Esc/Tab 必须配合 Shift；Enter 不再重开；无论重开键设为何值，双击 Shift+Enter 都会中止并显示仅当前窗口可见的结果，绝不保存、本机统计、同步或发布，⌘R 仍可用。禅模式的 Shift+Enter 仍只用于明确结束本轮。保存的长文本也会触发同一保护，并在双击中止时写入仅本机的恢复位置。

代码输入现提供官方当前目录的全部 70 个语言/方言选择，并为每项提供 Typebar 原创离线片段；最新增加 Dockerfile，以虚构镜像和本地练习路径覆盖常见指令、换行与字面输入。换行后的 Tab 自动缩进、可选退格反缩进和自动事件回放已实现，代码语言按完整字符提示结束有限字数测试。全量自动化会逐项验证提示生成与完成；服务端成绩与排行榜使用同一份显式代码语言白名单，社区引语仍只允许自然语言。官方的原始词表规模、代码语料和代码专属统计均未导入或复刻，仍未达到内容层面的功能等价。

INP-04 的“按键位图”档用所选 Typebar 布局的原创映射，将 QMK 等外设已重映射的逻辑字符反查为 ANSI 位置，再判定 Shift 是否来自相反手；它与默认的直接物理键码档分开保存。
| INP-05 | 错误呈现、额外字符和光标行为 | `elements/caret.ts`, `input/*` | TestRenderer | 部分实现：原生逐字符状态模型会区分正确、错误、当前位置、未输入、隐藏与额外字符；练习页以颜色和背景显示错误与当前位置，额外输入会以红色删除线显示，盲打会隐藏已输入字符；“错误字符显示”可关闭、替换为实际输入、在下方提示实际输入，或替换并在下方提示目标字符；它只改变渲染，永不改变提示、输入、计分或回放。已提交词可选择保留、隐藏、淡化或逐字显示圆点，词间空格、当前词和后续提示不受影响。当前目标与节奏目标可独立设为关闭、条形、轮廓、下划线、块状或三种原创矢量标记，并随设置和归档持久化；常规提示以独立 TextKit 测量层按实际富文本、字体和换行宽度定位，可关闭或按 150 / 100 / 85 ms 三档原生动画移动。卷带、手语和旋转文字等专用提示保持保守的静态光标回退，待统一实机检查；“专注当前词”修饰器会在空格分词练习中仅显示当前词，提交空格后显示下一词，不改变完整提示、输入或计分。可选的错误提示音使用 macOS 系统声音，默认关闭且可持久化。 |
| MET-01 | 实时 WPM/raw/准确率/burst/计时器 | `states/live-stats.ts`, `test/pace-caret.ts` | MetricsDomain | 部分实现：原生练习页实时显示 WPM、raw WPM、准确率、错误、单词 burst 与计时/进度；时间模式显示剩余倒计时，字数模式无论是否显示分隔符都会显示已提交词/目标词，并不从显示反推计分。进度可独立设为文字、迷你、隐藏或原生条形，条形使用同一 0–1 时间/字数比例；WPM、CPM、WPS、CPS、WPH 可只在实时显示层切换，结果/统计/同步仍使用规范 WPM；“结果页固定显示两位小数”会按当前速度单位格式化完成页的速度、Raw 速度和准确率，而不改变保存的规范数值；速度（含 Raw）、准确率和 burst 可独立设为文字、迷你或隐藏；burst 按单词首键至提交空格计算，无空格模式则以可安全映射的原始词末字符作为提交点，当前词满两键后也会显示。用户可选择在练习区显示近 10 条同设置的本机完成成绩均速、平均准确率或两者；筛选模式、时长/词数（引语按当前内容）、标点、数字、语言、难度与简化输入，且不依赖账户。指标下方以原生条带展示最多 8 个近期已提交词的速度，仅在当前会话内存在，包含实际尝试词以便与错误数对照；可选节奏引导会按自定义目标、同模式同语言的本地个人最佳、活动标签个人最佳、全部平均、自然日平均、本次运行中的上一轮速度，以及固定参考语义的最近 10 次同设置平均或滚动 24 小时同设置最佳计算，练习文本中会以可选条形、下划线或块状第二标记显示目标位置。兼容平均与 24 小时最佳会匹配模式参数、引语内容、标点、数字、语言、难度、简化输入及任一活动标签；可选“重开后沿用上一轮节奏一次”只作用于下一轮且不落盘。完整节奏统计与原项目完整计时器行为尚未实现 |
| MET-02 | 单词历史、错词练习与输入回放 | `test/replay-ui.ts`, `practise-words.ts`, `weak-spot.ts` | ReplayDomain | 部分实现：空格分词完成结果页（英语、西班牙语和中英混合）会展示实际尝试词的目标/输入对照并标记正误；无空格修饰器在可安全映射提示变换时同样保留每个渲染后词段的目标、实际输入、错误事件与 Burst，不把连续提示误作一个词。单词历史默认折叠，用户可手动展开或在设置中选择完成后自动展开，且该偏好会持久化。可选的结果页单词 Burst 热力图从本轮实际输入事件推导每个已尝试词的速度，再以 Typebar 自有五分位分档着色和显示当前速度单位图例；单事件或没有有效时间间隔的词保持中性，绝不伪造极端速度。可基于实际错误输入事件提供“错词练习”菜单：最多 20 个去重目标按错误次数重复进入有限练习；最多 10 个已尝试上下文目标也按错误次数保留前一个目标词。两种方式都会把按错误次数展开的候选段落池逐轮洗牌，并取入选候选数的 5 倍段落，既保持频次权重又保证每个候选重复出现；同时存在错词与可测慢词时，结果菜单会提供组合练习并将错词候选限制为 10。后来已退格修正或删空的错误仍保留，正确但未完成的词、未达到的后续词不列为错误。简体中文仅从 Typebar 自有词库识别实际输入过的无空格短词生成去重错词练习，跳过标点、未知自定义片段和未到达尾部，复练文本保持无空格。错词练习完成后点“再来一次”会恢复进入练习前的测试配置；“重复本轮”则保留同一份错词提示；若用户先手动切换到另一非自定义模式，则会丢弃临时恢复记录、把该选择作为新的测试来源。慢词练习会排除尾部活动项，按单词 burst 选取最慢的四舍五入 20%、最多 20 个位置，并按慢度加权；无法计时项以 0 参与该本机排序。完成结果会保存 Typebar 自有的插入/删除时间事件和提示快照，结果页可播放、暂停、拖动和重置输入重建；播放、暂停与拖动时还会用结果轨迹的同一插入/删除/强制错误语义显示当前速度，并遵循 WPM/CPM/WPS/CPS/WPH 设置。实际输入轨迹会逐字显示正确、错误和带删除线的额外字符，退格按时间轴撤回末字符；点按目标文本中已到达的字符会立即暂停并定位，未到达字符不可点，前一词的额外输入不会错开下一词。播放只为新跨过的人工插入/删除事件调用当前按键音与错误音设置；错误音关闭时按官方语义回退到已开启的按键音，拖动、字符定位、重置与自动缩进均不发声。该单点分析不受结果图 120 秒密度上限约束，正常顺序事件不重复排序，异常乱序导入则稳定排序后重建。原生“弱项训练”只从本机已完成回放逐事件统计错误目标字符，再在 Typebar 自有词库中挑选高关联词组成自定义练习，不上传数据、不会复用参考词库；工具栏和命令面板可打开持久化弱项分析，按有可用错误回放的语言显示分析轮次、错误总数、排序后的弱项字符和候选词，并可选择 10/25/50 词直接训练。报告每次从现有 SwiftData 成绩派生，因此历史导入、同步或删除后自然更新，不维护可能失真的第二份统计；其他更完整的回放分析仍持续审计。 |
2026-09-09 补充：完成结果的“导出”菜单在确有错词时提供“复制错词列表”，使用本轮回放已经派生的去重目标顺序，以空格分隔输出；无空格语言也保留显式分隔，便于阅读和复用。没有错词时不显示入口，不从提示尾部、最终统计或远端数据猜测内容，也不新增持久化字段。
2026-09-07 更正：MET-02 中较早的“只统计错误目标字符”和“有可用错误回放”表述已由当前实现取代。弱项分析会从非自动的本机回放同时聚合真实错误、强制物理键位错误和正间隔按键时间，以每位用户当前历史的中位间隔建立个人基线；只显示发生错误或平均间隔高于该基线的字符，并按 Typebar 自有的归一化优先级选择自写离线候选词。零错误但明显偏慢的字符也可生成训练，自动缩进不参与，删除键只更新真实输入间隔边界。该模型不复用参考的移动平均、采样数、错误惩罚参数、代码、词表或 UI。

2026-09-07 补充：`MET-02` 新增原生物理按键时序摘要。AppKit 输入桥把 keyDown、自动重复标记和 keyUp 交给会话；Typebar 只接受实体键的首个非重复 keyDown，并统计同键闭合前的按住时长、相邻有效 keyDown 的间隔，以及同时按住至少两键的闭合重叠总时长。完成页、历史详情和 CSV 显示/导出时长与间隔的平均值、总体标准差和样本数，并显示重叠毫秒数及其占本轮用时的比例；SwiftData 与归档保存匿名时序样本和累计重叠。未匹配释放、负间隔、重复 keyDown、测试结束后的释放、结束时尚未释放的末键及未闭合重叠均不计入，不会伪造 0 ms。
- 自动化：`testPhysicalKeyTimingsUseOnlyClosedNonRepeatEvents` 验证按住时长 100/200 ms、间隔 100/200 ms、两类 150 ms 均值与 50 ms 总体标准差、50 ms 多键重叠及所有忽略边界；`testNativeInputBridgeReportsPhysicalKeyDownRepeatAndKeyUp` 验证 AppKit 事件通路；SwiftData 往返、旧归档缺省和 CSV 七项时序字段另有覆盖。
- 人工验收：完成一次普通练习并释放至少两个有短暂重叠的字符键，确认完成页出现按键时长、按键间隔和重叠时长摘要；打开该历史详情及 CSV，确认三处数据一致。按住最后一键至结果出现时，该未闭合键及仍打开的重叠区间不得增加统计。
- 迁移与隐私：新增结果与 SwiftData 字段均为可选或安全缺省，旧成绩隐藏缺失摘要而不推断数据；只保存持续时间和累计重叠，不保存物理 keyCode、字符、提示之外的新内容，也不上传到 Typebar 服务。统计完全离线且不参与 WPM、准确率、错误或排行榜计算。

| MET-03 | 个人最佳、结果页和结果标签 | `collections/results.ts`, `result-screen/*` | ResultsDomain | 部分实现：测试完成后会先显式确认本机保存；成功后显示关键结果、重开、历史入口与原生导出菜单，失败则保留当前结果、显示错误并允许原地重试，保存成功前不宣称本机 PB、不开放标签/历史，也不发布远端。可复制仅含本机成绩的文字结果、本机回放重建的实际输入，也可用当前 Typebar 主题渲染全新结果卡，复制 PNG 或通过 macOS 保存面板导出 PNG，不截取或复用参考网页 UI。不存在回放时会明确拒绝复制实际输入，绝不从提示或统计伪造内容。完成页会显示“今日练习”时长和本机完成次数：当天已保存成绩与本次运行中未保存的成绩共同计入，以 ID 去重，跨日成绩不参与。完成页的“重复本轮”会从本轮开始时保存的会话模板重新启动，严格保留测试配置、提示和自定义计时文本的循环源，不携带已输入内容、计时或回放；“再来一次”仍按当前选择生成新内容。结果页会从本机保存的输入回放按秒重建 WPM、Raw、Burst 与当前错误数轨迹，最多显示 120 秒；Raw、Burst、错误层可单独切换并持久化，Burst 另可在本机三点平滑与原始尖峰间即时切换，超过上限或缺少回放时明确不显示图表。历史列表和成绩详情会标记并列最高 WPM 的个人最佳；练习区可选择显示本机当前设置个人最佳，筛选模式、时长/词数、内容选项、语言、难度和简化输入，并排除引语、官方不允许 PB 的 funbox 及 Typebar 自有强制纠错修饰器。成绩可添加/移除受约束的本地标签，并随备份迁移 |
2026-09-10 补充：完成页现在拥有独立原生命令面板，通过页面“命令”或当前窗口 ⇧⌘P 打开。目录复用完成页已有的下一轮、重复本轮、单词历史、错词/慢词/组合练习、复制已练习词和 Typebar 结果图导出动作；回放、单词历史或练习候选不存在时不会展示相应命令。参考结果命令中的 `custom...` 由原生“自选弱项练习”覆盖：错词可关闭、直接练习或带前词上下文，并可独立加入慢词；五种有效组合精确路由到 Typebar 已有练习计划，空组合与缺少相应候选的组合禁止开始。离开完成页的动作先关闭内层面板再进入下一轮，避免原生 sheet 争用；选择与展开状态只在当前完成页存在，不增加存储、同步或迁移字段。

2026-09-10 标签命令补充：主命令面板以本机活动标签和历史成绩中的规范标签构造动态目录，提供清除、逐项启用/关闭和新建入口。活动标签保持原顺序，历史标签按大小写与重音无关去重后排序；每个 Unicode 标签使用稳定的 UTF-8 十六进制内部 ID，路由时仍须匹配当前已知集合，过期或伪造 ID 不生效。新建与切换统一复用最多五个、每项 24 字符的既有标签策略；达到上限时仍可关闭标签但不能新增。设置只影响之后开始的练习，进行中会话继续使用启动快照。目录、编辑器和会话语义不增加存储、同步或网络字段。

2026-09-10 声音命令补充：目录在既有键击音与错误音之外补齐固定 `playTimeWarning` 的关闭/1/3/5/10 秒五项。执行具体键击音、错误音或非关闭倒计时命令后会通过 Typebar 现有本机声音控制器试听一次；关闭与音量命令保持静默。试听不重开测试、不退出挑战、不引入参考音频资产，也不增加设置或归档字段。

2026-09-10 光标命令补充：命令面板按固定 schema 增加 4 档 `smoothCaret`、主光标与节奏光标各 8 种样式，以及 `repeatedPace` 开/关，共 22 项。`default` 严格映射到 Typebar 的条形光标，三种图形样式由原生 SwiftUI 独立绘制；全部命令直接更新已有持久设置，不重开、不退出挑战，也不新增迁移字段。随后补齐固定 `paceCaret` 的 6 个枚举入口与自定义输入入口；为避免改写既有已保存偏好，新增“最近 10 次同设置平均”和“过去 24 小时同设置最佳”两个模式，保留 Typebar 原有全部平均与自然日平均。自定义编辑器按当前 WPM/CPM/WPS/CPS/WPH 输入并换算到规范 WPM；取消不改变挑战，确认或选择枚举模式会退出挑战并重开。新增枚举仅扩展既有 Codable 值，不改写旧值或归档结构。

2026-09-10 `CFG-03` 显示命令补充：命令面板增加固定元数据中无需输入框的 48 项，覆盖实时进度、速度/准确率/Burst、颜色、透明度、高亮、已输入效果、卷带、平滑滚动、完整行、速度单位、结果小数位和图表零基线。全部复用既有持久字段与原生渲染，不重开、不增加存储或迁移字段；仅 `highlightMode` 与 `showAllLines` 按固定挑战控制器退出活动挑战。固定元数据隐藏的 WPH 不列为命令，但仍保留在设置页。机器总账新增 10 组完整枚举与 4 个布尔键作为固定证据；不复制参考样式、资产或实现。
2026-09-10 补充：结果速度轨迹的图例现可直接切换“从零”与“自适应”纵轴，无需离开完成页进入设置；操作写回既有 `startGraphsAtZero` 偏好，因此后续结果图与历史速度/准确率图保持一致。完成次数、练习分钟和错误图仍强制包含零点。该入口只改变图表尺度，不重算或修改成绩、回放、PB、同步及导出数据，也不新增存储字段。
2026-09-10 补充：结果速度轨迹可用 macOS 14 原生图表选择检查最近采样秒，以规则线和信息条显示该秒的 WPM、Raw、Burst 与当前错误数；隐藏的可选轨迹不会在信息条中冒充可见数据。选择还会展开单词历史，并仅突出该采样时间窗内真实插入或删除事件涉及的目标词；首段包含零时刻，后续采用前开后闭边界，多字符输入、删除词间分隔符及 Typebar 已保留边界的无空格结果均由本机回放确定。没有事件的秒不伪造单词，离开图表或明确清除会恢复普通历史。交互状态不落盘，不改变成绩、回放、统计、PB、同步或导出，也不复制参考图表实现和样式。

2026-09-10 补充：结果图的 Burst 可在默认平滑和原始轨迹间切换。Typebar 使用独立实现的三点三角窗口，仅替换图表和按秒检查中的 Burst 展示值；WPM、Raw、错误、时间点和单词联动逐点保持不变。选择会随设置和归档持久化，旧设置缺少字段时默认平滑；成绩、回放、同步和导出数据不被重写。

2026-09-10 补充：完成成绩不再只依赖 SwiftData 自动保存。Typebar 插入记录后会显式提交上下文，只有成功才显示“已保存”、本机/标签 PB、标签编辑和历史入口，并继续可选的远端发布；失败时撤销该次未提交插入、保留当前结果页和错误说明，用户可创建同 ID 的新记录原地重试。这样失败记录不会以仅在内存中可见的幽灵项进入历史，也不会造成远端先于本机接受成绩；不增加存储字段或网络协议。
2026-09-09 补充：完成页标题区与 Typebar 原创结果卡会直接从本轮不可变成绩快照派生测试类型摘要，而不是读取下一轮的当前设置。计时/字数显示有限档或无限，引语以本轮提示和语言识别实际短/中/长/超长；常规非自定义模式显示语言，固定参考标记为 `ignoresLanguage` 的内容替换修饰器则只显示其真实内容身份。摘要仅列出当时启用的标点、数字、盲打、简化重音输入、趣味修饰器、非普通难度及遇错停下/删除规则。该摘要不新增存储、网络或迁移字段，也不参与计分和排行榜。
2026-09-09 补充：引语成绩会在会话开始时捕获最小来源元数据，并在完成页、历史详情、复制文字及 Typebar 原创结果卡中显示“Typebar 自有/社区审核 + 标题”。标题折叠空白并限制为 80 字符；不保存作者账户 ID、额外网络状态或新增正文，也不进入远端成绩提交。来源以结果和 SwiftData 的可选字段加入，旧归档、旧本机记录或损坏的可选来源均安全回退为不显示；非引语结果强制忽略该字段。社区结果反馈同时兼容运行时 `community-<UUID>` 标识，完成后评价与私下举报入口不再因前缀解析失败而消失。
2026-09-07 补充：完成结果现从 Typebar 原生的最终输入到目标位置映射派生“匹配/错位/额外/跳过”四项字符分类，并显示于完成页和历史详情、保存到 SwiftData/归档及导出 CSV。“匹配 + 错位 + 额外”严格等于最终保留的输入字符数；“跳过”单独表示提前提交跨过的目标位置，不把计时结束时尚未到达的提示尾部算作遗漏。禅模式把用户自由输入全部记为匹配。旧成绩没有映射时仅以既有输入/正确字符数守恒回退，额外与跳过保持为零，绝不伪造明细。该分类不参与 WPM、Raw、准确率或既有错误数计算，也不复制参考实现代码、资产或数据。
2026-09-10 `CFG-03` 行为命令补充：命令面板严格增加固定 quickRestart 的 off/esc/tab/enter 四值，以及 blindMode、alwaysShowWordsHistory 和 singleListCommandLine 各两值，共十项。它们只写入已有的快速重开、盲打、结果单词历史默认展开和面板浏览偏好，立即生效且不重开、不退出挑战，不新增存储或迁移字段；`manual` 映射到原生分组导航并保留 `>` 全局搜索，`on` 映射到打开即展示全部命令。

2026-09-10 `CFG-03` 练习阈值命令补充：命令面板严格增加最低整体速度 `off/custom`、最低准确率 `off/custom` 和最低单词 Burst `off/fixed/flex` 七项。自定义页按当前 WPM/CPM/WPS/CPS/WPH 显示单位换算；整体速度与 Burst 使用固定 schema 的有限非负 number 且没有 Typebar 旧实现的 300 WPM 上限，准确率限定为 0–100，Burst 输入保持整数语义。确认后退出挑战并按新规则重开，取消不修改当前练习或挑战。三项持久字段从整数安全扩展为小数，字段名与 Codable 结构不变，旧整数设置、预设、归档和分享配置可直接解码。

2026-09-10 `CFG-03` Funbox 命令补充：命令面板按固定 `changeFunbox<name>` 名称增加 48 项切换与 `changeFunboxNone` 清除入口，并保留 `numbers`、`network` 别名。46 项直接写入已有 Typebar 修饰器并重开，互斥组继续走同一归一化策略；`polyglot` 切换已有自选多语模式，`weakspot` 打开已有本机弱项分析，由用户选择训练规模后开始。Memory 从不支持的时间或禅模式启用时切到字数模式；有限专用项不能在无限测试中启用，活动的锁定重开会拒绝所有切换。清除只移除固定映射项，不误删 Typebar 自有符号流、纠错修饰器或独立简化输入，也不复制参考代码、文案或资产。

2026-09-10 `CFG-03` 自定义背景命令补充：命令面板按固定 ID 增加远程 URL、本地图片、三种适配与模糊／亮度／饱和度／不透明度四项输入；存在 URL 或本地图片时才显示统一移除命令。URL 继续接受留空移除，并严格限制为 HTTP(S) 的 PNG、JPG、GIF 或 WebP；本地导入复用安全作用域文件选择器，图片只存于这台 Mac 且不进入归档。适配和滤镜只更新所选既有持久字段，不清空输入、不退出挑战或重开；原生输入范围沿用设置页的模糊 0–20、亮度 0–2、饱和度 0–3、不透明度 0–1，以免命令面板削弱已保存的 Typebar 能力。移除先安全删除本地文件，再清空 URL，删除失败时保留 URL 以避免部分提交；没有新增存储、同步或迁移字段，也不复制参考实现、样式或资产。

2026-09-10 `CFG-03` 当前主题收藏命令补充：命令面板严格使用固定 `addThemeToFavorite`／`removeThemeFromFavorite` ID，并按当前可见内置主题的收藏状态只显示其中一个动作。常规模式作用于手动主题，随机模式作用于本轮实际随机结果，跟随 macOS 时按当前颜色方案作用于对应浅色／深色主题；自定义主题不伪装成固定命令，仍使用 Typebar 设置中的现有收藏控件。执行复用既有收藏持久字段且不重开、不退出挑战；过期或伪造动作不会反向切换状态，也不增加归档或迁移字段。

2026-09-10 `CFG-03` 主题选择预览补充：内置主题命令改为固定 `changeTheme<名称>` ID，自定义主题命令改为固定 `setCustomThemeId<ID>` ID，同时继续接受此前 Typebar 的 `theme.builtin.*`／`theme.custom.*` 内部 ID 作为无损兼容入口。单列表或分组搜索的首个可见主题，以及鼠标当前指向的任一主题，会通过 ContentView 的瞬时覆盖层预览 Typebar 自有颜色；非主题、空结果、查询／分组变化和关闭面板会清除覆盖，并精确恢复此前手动、自定义、随机或跟随系统的显示。只有执行命令才调用已有主题选择和持久化，不会因浏览写入设置、归档、同步或退出挑战；自定义 ID 必须仍解析到当前主题，已删除或伪造项不能预览。实现不复制参考主题颜色、样式、代码或资产。

2026-09-10 `CFG-03` 字体命令补充：命令面板增加四种 Typebar 原生系统设计、固定 `FontNameSchema` 中当前 Mac 确实已安装字体的 `setFontFamily<name>` 命令、固定 `customFontName`／`customLocalFont`／`removeLocalFont`，以及原生“浏览本机字体”入口。每次构造目录只枚举一次 macOS 字体家族并做大小写／重音无关精确匹配，避免逐候选触发 AppKit 解析，也不会在导入或移除后保留失效缓存；完整字体浏览仍在打开时刷新。自定义名称接受 1–50 个无控制字符的 Unicode 家族或 PostScript 名称，以覆盖真实 macOS 字体名；未安装名称会保留并安全回退。系统设计会清除名称覆盖，本地文件继续优先于两者；导入严格限制 TTF／OTF／WOFF／WOFF2，文件存在时只显示移除固定命令。所有动作即时生效且不重开、不退出挑战；名称沿用既有归档字段，本地文件仍只存当前 Mac。参考捆绑的网页字体文件不复制、不下载、不打包，因此未安装字体保留为明确内容资产差异。

2026-09-10 `CFG-03` 导航命令补充：命令面板按固定 ID 提供 `viewTypingPage`、`viewLeaderboards`、`viewAbout`、`viewSettings`、`viewAccount`、`searchProfile` 与 `toggleFullscreen` 七项。练习页入口关闭面板后恢复原生输入焦点；榜单复用已有自建服务 WPM／XP 页；设置与账户进入 macOS 设置窗口；关于使用 Typebar 自有窗口；全屏解析命令 sheet 的父窗口，只切换当前 Typebar 主窗口。公开资料搜索始终可见，按服务端匿名 GET 契约接受 2–40 个无控制字符的 Unicode 展示名，结果可打开现有公开资料卡；好友请求和举报继续由资料卡要求登录。搜索只有用户提交后联网，其余导航均不增加请求；没有新增存储、同步或迁移字段，也不复制参考路由代码、文案或样式。

2026-09-10 `CFG-03` 命令面板键盘导航补充：搜索框保持输入焦点时，方向键、Tab／⇧Tab 与 Ctrl-J/N/K/P 可在当前分类或命令结果中循环移动活动行，Return 进入分类或执行命令；Esc 依次清除 `>` 全局搜索、返回分类根级，根级继续交给原生 sheet 关闭。活动行使用 Typebar 自有的细光标与轻量底色并自动滚动到可见范围，键盘活动主题与鼠标悬停主题走同一瞬时预览路径。空结果不执行命令，筛选变化会将活动项稳定归一到首行；实现只使用视图内事件，不安装全局键盘监听，不新增持久化、同步、迁移或网络状态，也不复制参考界面代码或样式。

2026-09-10 `CFG-03` 顶层工具命令补充：命令面板增加固定 `changeCustomModeText`、`shareTestSettings`、`randomizeTheme` 与 `signOut`。前两项始终分别进入 Typebar 现有自定义文本编辑区和配置链接页；随机主题仅在现有随机池启用且未被系统跟随接管时显示，调用同一无重复瞬时主题袋而不改手动选择；登出仅在已认证时显示，清除当前规范化服务地址对应的钥匙串会话和内存账户状态，不删除本机练习数据。旧内部 `share` 继续作为兼容路由但不出现在目录。固定网页端的广告、Service Worker 缓存、隐藏诊断、品牌社群链接，以及语义不同的临时通知清理均明确不映射；没有新增存储、迁移、服务端接口或参考代码／资产。

| MET-32 | 复制已练习提示 | `commandline/lists/result-screen.ts` | ResultsDomain | 部分实现：原生结果页“导出”可复制本轮已到达的目标提示；空格分词测试使用实际尝试词的目标文本，绝不复制错误输入或未到达后续词。禅模式仍复制本机输入；简体中文按 Typebar 自有词表把已到达的部分字、括号或词尾标点扩展为完整当前目标词，其他无空格文本按本机回放的最终到达范围截取。没有可用本机回放/目标范围时明确拒绝，不根据统计伪造文本。 |
| MET-43 | 本机个人最佳表 | `modals/PbTablesModal.tsx`, `pages/profile/UserProfile.tsx` | ResultsDomain | 已实现：练习历史提供“个人最佳表…”，分别展示本机计时和字数模式中每一组同类设置的最佳已完成成绩。分组与现有本机 PB 指示器共享模式参数、语言、难度、标点、数字和简化重音比较语义；不合资格的修饰器、混排语言、放弃和非完成成绩均排除。行中显示速度、Raw、准确率、稳定度、选项和日期，历史中的“个人最佳”筛选及奖杯也复用同一组 ID；不读取账户或网络数据。 |
| MET-04 | 历史、筛选、趋势/柱状/直方图/热力图/连续天数 | `components/pages/account/*` | AnalyticsDomain | 部分实现：本地历史可组合按模式、语言、标签、难度、标点、数字、实际完成引语长度、时长、字数、修饰器、个人最佳和参考项目同语义的最近 24 小时/7 天/30 天/90 天/全部时间筛选；时长和字数各支持 4 个参考标准档及“自定义”档，任意档位可并选、取消或全部取消；标点和数字均可限制为包含、不包含或全部；修饰器可多选，并可独立选“无修饰器”，命中任意选中修饰器或空修饰器集合。原生引语会用 Typebar 自有元数据识别实际长度，社区引语使用本机既有字符阈值分类，避免把“全部长度”的练习选择误作实际结果；无法解码配置的旧记录只在不限制引语长度或修饰器时保留。全部条件可命名保存为仅本机的筛选预设、重新应用或删除，旧预设会安全回退为全部时间、全部难度以及不限制标点、数字、引语长度、时长、字数和修饰器。汇总、趋势、WPM 固定区间直方图、近 28 日完成次数/练习分钟柱状图、按日活动聚合、近 12 周热力图和连续天数均随筛选更新；历史趋势可分别显示速度、准确率、10 次均值和 100 次均值，速度与准确率至少保留一个，关闭最后一条主线时自动恢复另一条，10/100 次均值开关保持独立；速度图另绘制本机历史个人最佳包络，并按实际键入时长估算每小时速度变化；四项可见性随设置归档迁移。历史速度趋势图可选强制从零开始（默认开启），关闭后按实际数据范围缩放，完成次数和练习分钟的计数轴始终从零开始；原项目的完整账户统计、更多筛选和图表交互尚未实现 |
| MET-50 | 筛选联动的账户统计摘要 | `components/pages/account/TestStats.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：练习历史会对当前筛选集即时显示开始次数、完成数及完成率、每次完成重开比、有效键入时长、按每条成绩 `WPM / 60 × 完整测试秒数` 四舍五入后求和的估算词数，以及 WPM、Raw、准确率和稳定度各自的最高值、全体平均与最近 10 次平均。已开始但未完成前触发的每次重开会写入最终完成结果的非负 `restartCount`；开始次数为完成数加重开数，完成率向下取整，每次完成重开比保留一位小数。估算词数与 WPM 使用同一完整测试时长，存在 AFK 时不会重复扣减；最近 10 次严格取完成时间倒序的前十条，速度与 Raw 按当前 WPM/CPM/WPS/CPS/WPH 显示单位格式化，空集合安全归零。该值进入 SwiftData、v3 本机归档、CSV、同步与既有远端成绩提交；v1/v2 归档和旧本机记录缺失时保守回退为 0，不猜测历史重开。SwiftData 采用可空加法字段并已在含 19 条旧记录的真实 store 副本上验证迁移、追加第 20 条及重开读取；升级后不建议与旧版本交替写入，因为旧版本新建成绩无法记录重开数。必须回退时可恢复升级前备份，或接受旧版本期间产生的成绩在再次升级后重开数为 0。所有摘要只读取当前本机筛选集，不额外访问账户或网络。 |
| MET-51 | 筛选联动的每日活动指标 | `components/pages/account/DailyActivityChart.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：练习历史的近 28 日图表除完成次数、有效练习分钟和平均稳定度外，还可切换查看每日平均速度、最高速度、平均准确率与每次完成重开比。每日平均值保留小数，不先逐条舍入；速度图值与无障碍说明遵循当前 WPM/CPM/WPS/CPS/WPH 单位。所有指标按本机可配置统计日界从当前筛选集即时聚合，重开比使用当日重开总数除以完成数；没有成绩的日期仍保留在连续 28 日时间轴，计数/分钟/重开比为 0，平均与最高类不绘制伪造的零值，因此关闭“从零开始”时速度与百分比能按真实范围缩放。点按或拖动图表可选择最近日期，以规则线标记并同时显示练习时长、完成次数、重开比、最高/平均速度、平均准确率和平均稳定度；空白日的平均类明确显示为不可用，不伪造零值，同距日期稳定选择较早一天。参考网页把练习分钟柱与平均速度线叠在同一双轴图并把其余值放入提示；Typebar 采用更适合原生小窗口和 VoiceOver 的单指标菜单加原生日期详情，不复制网页图表代码或样式。选择状态只存在于当前视图，所有指标只派生已有结果，不新增持久化、同步或网络字段。 |
| MET-52 | 历史排序与渐进显示 | `components/pages/account/AccountPage.tsx`, `Table.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：本机历史列表可按日期、速度、Raw、准确率或稳定度选择升序/降序；主值相同时固定以较新完成时间优先，再以结果 UUID 升序决胜，避免 SwiftUI 刷新后同值行跳动。默认日期降序，首屏只呈现 10 条，每次再显示至多 10 条并在末页精确收口；任一筛选、排序字段或方向变化都会回到首屏。删除使用当前可见顺序定位，CSV 按当前排序导出全部匹配结果，不受屏幕分页截断。图表、活动、摘要和成就仍从完整筛选集及既有日期顺序派生，不会因列表排序或只显示前十条而改变。实现不持久化临时列表状态，也不访问网络。 |
| MET-53 | 单位感知的速度直方图 | `components/pages/account/HistogramChart.tsx`, `utils/typing-speed-units.ts` | AnalyticsDomain | 已实现：历史速度分布随当前 WPM、CPM、WPS、CPS、WPH 显示单位即时重新分桶，分别采用 10、50、0.5、5、250 的原生档宽，并在标题和每柱 VoiceOver 说明中显示当前单位。分桶从零开始保留中间空档，以半开区间标签准确表达整数与小数边界；任一成绩在换算后恰好计入一档。为防止手工导入的异常超大速度生成无界 Swift Charts 数据，最多生成 512 档，超出范围的成绩汇入最后一个明确标为“≥”的溢出档而不丢失计数。图表继续随完整历史筛选集更新，只换算呈现值，不改规范 WPM 存储、CSV、统计、同步或网络请求。 |
| MET-54 | 历史趋势图成绩选择 | `components/pages/account/HistoryChart.tsx`, `AccountPage.tsx` | AnalyticsDomain | 已实现：速度或准确率历史图可通过 macOS 14 Swift Charts 原生单值选择交互定位离指针最近的本机成绩；两图共享选择并以规则线标记同一结果。若选择值恰好位于两条成绩中间，稳定选择较新成绩，再以 UUID 决胜。图下摘要显示当前速度单位下的速度与 Raw、准确率、稳定度、完成时间、模式参数、语言、难度、标点/数字和个人最佳状态，并可明确打开既有成绩详情或清除选择。筛选变化及删除所选成绩时会清空选择，不会暗中跳到另一条；列表排序与分页不影响图表选择。交互状态仅在当前窗口内存在，不改结果、设置、归档、同步或网络。 |
| MET-55 | 图表当前筛选摘要 | `components/pages/account/Charts.tsx` | AnalyticsDomain | 已实现：历史统计与图表上方持续显示当前筛选的原生横向摘要。默认条件只显示“筛选：全部成绩”，避免用十二个“全部”标签制造噪声；存在限制时按时间、模式、时长、字数、难度、标点、数字、语言、引语长度、修饰器、标签、个人最佳的固定顺序，仅显示真正收窄结果的类别。全部取消产生的无匹配状态会原样显示，旧单模式/语言/难度/标签/PB 预设通过既有 effective 兼容视图生成相同摘要。每项可由 VoiceOver 独立读取，超出 520 点窗口时横向滚动；摘要只派生当前本机状态，不参与匹配、不持久化且不联网。 |
| MET-56 | 每日练习分钟趋势线 | `components/pages/account/DailyActivityChart.tsx` | AnalyticsDomain | 已实现：近 28 日活动切换到练习分钟时，会以次要色原生虚线显示筛选结果中每日练习时长的最小二乘趋势。拟合只使用实际存在完成成绩的日期，按真实日历间隔计算，不把为保持连续横轴而补出的空白日误作零分钟；线段仅覆盖首个到最后一个有效练习日。少于两个有效日期、同日数据或非有限导入值会安全隐藏趋势。切换到其他活动指标时不显示该线；计算只派生本机筛选结果，不新增设置、存储、同步或网络。 |
| MET-57 | 历史行模式、字符与标签摘要 | `components/pages/account/Table.tsx` | ResultsDomain, AnalyticsDomain | 已实现：本机历史每行在大号速度主值旁直接显示模式及计时/字数参数、准确率、完成时间、匹配/错位/额外/跳过四元字符统计和现有结果标签，同时保留 Raw、稳定度及个人最佳标记。无限计时/字数明确显示“无限”，旧记录缺少配置时显示“未知”而不从完成时长猜测；标签沿用已有规范化规则，大小写与重音无关去重但保留首次输入的显示形式。长元数据在 520 点窗口单行截断并提供完整悬停文本，VoiceOver 会展开朗读四个字符类别及标签；整行仍可打开现有详情与标签编辑器。该摘要只读取已有本机成绩，不改排序、分页、删除、存储、同步或网络。 |
| MET-36 | 历史“全部/当前设置”快捷筛选 | `components/pages/account/Filters.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：历史筛选顶部可一键恢复为全部，或按当前 Typebar 测试配置带入模式、语言、难度、标点、数字、时长/字数档、实际引语长度及修饰器。多修饰器继续按参考的任一命中语义匹配；Typebar 标签只附于已完成成绩、没有独立的全局活动标签状态，因此快捷筛选不凭空限制标签。 |
| MET-37 | 历史多选语言筛选 | `components/pages/account/Filters.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：历史“语言”筛选可任意多选、全选或全部取消，命中任一所选语言；全选保持不限制语言的旧行为并保留无法解码语言的既有成绩。旧筛选预设仅有单个语言字段时仍按该一项解释，新预设保存完整选择。 |
| MET-38 | 历史多选标签筛选 | `components/pages/account/Filters.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：存在本机标签时可任意多选、全选或全部取消标签，并可独立选择“无标签”；结果命中任一选中标签或没有标签时保留。新预设持久化动态标签集合；旧预设仅有单个标签字段时继续按该标签筛选。 |
| MET-39 | 历史个人最佳四态筛选 | `components/pages/account/Filters.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：历史个人最佳条件支持全部、仅个人最佳、排除个人最佳和无匹配项（参考实现的两项同时取消）。新预设存储四态；旧预设的 `personalBestOnly: true` 保持“仅个人最佳”，缺失或 `false` 保持全部。 |
| MET-40 | 历史多选模式筛选 | `components/pages/account/Filters.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：模式条件可任选计时、字数、引语、禅和自定义模式，按任一命中；全选保留无法解码模式的历史记录，全部取消明确显示无匹配项。新预设持久化完整集合，旧单模式预设保持原语义。 |
| MET-41 | 历史多选难度筛选 | `components/pages/account/Filters.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：难度条件可任选普通、专家和大师，按任一命中；全选保留无法解码难度的历史记录，全部取消明确显示无匹配项。新预设持久化完整集合，旧单难度预设保持原语义。 |
| MET-42 | 历史标点与数字四态筛选 | `components/pages/account/Filters.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：标点和数字条件均支持全部、包含、不包含及无匹配项，完整表达参考实现两个布尔开关的四种组合。新状态会进入本机预设；既有三态预设保持原语义。 |
| MET-49 | 历史多选引语长度筛选 | `components/pages/account/Filters.tsx`, `collections/results.ts` | AnalyticsDomain | 已实现：短、中、长、超长引语长度可任意组合；选择会按任一引语长度匹配，且沿用参考实现的非引语回退桶。全部取消仅保留非引语成绩。新预设持久化集合，旧单长度预设保持原本精确匹配语义。 |
| CFG-01 | 设置搜索、预设、导入与导出 | `pages/settings/*`, `preset-controller.ts` | PreferencesDomain | 已实现：原生设置窗口为每项可配置能力维护独立的中英文标题/关键词索引；多词查询按每项命中的词数全局计分，只列出最高匹配的具体设置及所属区域，并隐藏没有结果的区域。恢复默认会经原生确认重置设置并移除本地背景图片，但按参考边界保留本地字体文件和练习历史。完整测试预设可保存、列出、应用和删除；设置、成绩、预设和自定义文本可用原生文件选择器导入/导出，导入会去重合并，并兼容 v1 归档 |
| MET-34 | 结束前闲置保护与有效键入时长 | `test/events/stats.ts`, `test/test-logic.ts`, `test/result.ts` | TypingEngine, ResultsDomain, AnalyticsDomain | 部分实现：原生会话只在本机按一秒区间记录键盘活动，计时器保持墙钟截止而不暂停；有限测试结束前连续 5 个输入区间没有文本插入时，结果标记为“闲置无效”，仅在当前窗口显示，绝不写入历史、统计、同步或发布。结果会显示总用时、闲置秒数及其占比、有效键入时长；仅实际键入时长进入本机“今日练习”和历史分析。归档和本机存储包含闲置时长，旧归档缺失该字段时回退为 0。 |
| MET-35 | 结果稳定度与按键稳定度 | `test/events/stats.ts`, `test/test-logic.ts`, `test/result.ts`, `components/pages/account/{Table,TestStats,DailyActivityChart}.tsx`, `packages/util/src/numbers.ts` | ResultsDomain, AnalyticsDomain | 部分实现：完成页、已保存成绩详情和历史列表会从 Typebar 自有的本机插入/删除回放重建每秒速度与按键间隔，以同一离散采样和 Kogasa 型映射显示稳定度百分比；按键样本会排除最后一个未闭合间隔。历史摘要还显示最高、平均和近 10 次平均稳定度；近 28 日活动图可切换为每日平均稳定度。没有回放或有效样本时明确显示 `0%`，绝不根据最终 WPM、提示或统计伪造数值；本机回放结构与参考网页事件不同，未复制其实现、代码或资产。 |
| CFG-02 | 全部显示/行为/声音/键盘/外观选项 | `config/metadata.tsx` | PreferencesDomain | 部分实现：难度、输入规则（含最后一词快速结束）、英式/美式英文拼写、字体大小、下一键提示和原创 ANSI QWERTY/Dvorak/Colemak/Workman/French AZERTY/Turkish Q/Polish (Programmers)/Russian JCUKEN/Ukrainian JCUKEN 布局选择已持久化并有原生设置 UI；非默认布局还会将 ANSI 物理 keyCode 转为相应字符和 Shift 符号，Polish (Programmers) 额外模拟公开标准的 Option/Shift+Option 字符层；默认 QWERTY、Control/Command 和其余 Option 输入保持 AppKit 原生路径。简化输入现在作为独立输入开关呈现并持久保存，不再混入趣味修饰器；不支持的语言仅在当前练习临时过滤，切回后恢复，自定义文本保留例外。快速重开可选关闭/Esc/Tab/Enter，⌘R 始终可用；长测试中的 Esc/Tab 需配合 Shift，Enter 不再重开，而双击 Shift+Enter 始终中止并显示未保存结果。可选择显示页面底部的实际重开和命令面板快捷键；输入框获焦时提示淡出，偏好会随归档保存。开启原创节奏伙伴时，文字式实时速度与准确率会自动收为迷你显示，关闭或既有非文字样式不变。可选择是否保存完成成绩：关闭后仍显示、可复制本次结果，但不写入本机历史/统计/同步，也不发布榜单。键击音提供 26 种可持久化的 Typebar 原创原生音型，其中 22 种由本机内存即时合成且不包含网页音频资产；错误音与倒计时提示音（结束前 1/3/5/10 秒任选其一）各有四种 macOS 系统音型。三类均可试听并共用 0–100% 音量，默认关闭且会随归档持久化。键击时仅在输入实际被接受后播放，错误音仅在错误数增加时播放，倒计时提示独立且每轮只在所选秒数触发一次；任一声音无法载入时都不影响输入或计分。控制栏可显式调用 macOS 内置语音朗读当前提示，并会按所选语言选择系统 locale、重开时停止，文本绝不离开设备；其余显示/行为选项尚未实现 |
| CFG-03 | 命令面板、快捷键、配置分享链接 | `commandline/*`, `ShareTestSettings.tsx` | CommandDomain | 部分实现：可用工具栏“命令”、Typebar 的 ⇧⌘K、与固定参考一致的 ⇧⌘P，或参考动态入口打开原生命令面板。快速重开不是 Esc 时动态入口为 Esc；快速重开是 Esc 时改为 Tab；若提示本身接受 Tab，则再保护为 ⇧Tab，禅模式也采用同一保护以保留 Typebar 的自由 Tab 输入。⇧⌘P 另由当前窗口的 macOS 菜单命令提供；所有输入桥入口均在进入输入、计分、回放或对应按键活动前消费，长按不重复打开。默认单列表打开即显示全部命令并可输入关键词筛选、执行；也可切换为分组导航，按练习、文本/预设/挑战、主题、成绩、数据、同步/社交和设置浏览，输入 `>` 可从任意分组搜索全部命令。练习命令可直接选择参考标准的 15/30/60/120 秒与 10/25/50/100 词，也可从两个原生输入页设置 0 至 JavaScript 最大安全整数的自定义时长/字数；0 保持无限语义，大型有限值用按需提示块避免预分配巨型文本。命令还可明确开启/关闭标点和数字，选择全部/单一引语长度、当前来源本机收藏和本地引语搜索，或按显示名及固定 ID 搜索并切换全部 448 个 Typebar 本机语言、聚合与代码入口；引语模式只列出有自有引语的语言，避免生成空练习。另有普通/专家/大师难度、美式/英式拼写、引语重开时换新/重复当前，以及保存完成成绩开/关九项原生命令。输入分组另按固定 schema 提供 33 项严格选项，覆盖自由回退、严格空格、反向 Shift、遇错停下、遇错删除、信心模式、最后一词快速结束、错字提示、组合输入显示、隐藏额外字符、简化重音输入和代码退格反缩进；枚举值 `max`、`letter_hard`、`word_hard` 会映射到 Typebar 自有类型而不改变存储格式。布局分组提供固定 `LayoutSchema` 的 `default + 239` 个命令：`default` 恢复 macOS 当前输入源，其余逐项映射到已审计的原生物理键模拟；五个仅属于 Typebar 的内置布局继续留在设置中，不伪装成参考命令。布局命令不联动视觉键盘选择。声音分组提供固定命令元数据的 3 档音量、`off + 26` 个键击音和 `off + 4` 个错误音；编号逐一映射到 Typebar 自有音型或 macOS 系统音，不复制参考音包，关闭时保留最近音型，全部即时更新且不重开或退出挑战。时长、字数、引语、语言、难度、拼写，以及固定元数据标为需重开的严格空格、遇错停下、简化输入、布局和代码反缩进会退出活动挑战并立即重开；其他输入选项、引语重开策略与成绩保存只更新设置，不中断当前输入或挑战。互斥输入规则继续由 `AppSettings` 的同一路径归一化。未知、非标准或当前模式不可用的 ID 被拒绝且不改变状态。命令目录只引用 Typebar 枚举和自有显示名，不导入参考词表、代码或资产。完成页提供独立的“命令”和 ⇧⌘P 入口，复用已有下一轮、重复本轮、单词历史、错词、慢词、组合练习、复制已练习词及结果图导出动作；只有本轮确有相应回放、词历史或候选时才显示依赖数据的命令。还可通过原生“自选弱项练习”组合错词、前词上下文和慢词，空或不可用组合不会启动。可直接应用本机已保存且可读取的预设，并保留预设库的创建时间顺序；原创离线挑战也可直接加载。内置/自定义主题可作为命令直接选择，收藏项优先显示，选择后会退出“跟随 macOS”以立即应用。引语模式会显示仅作用于本机的收藏/取消收藏当前引语命令。保存长文本、时长至少 1 小时、字数至少 5,000 或禅模式进行中时，面板还会显示“中止长测试”，须经原生确认后才显示未保存结果。可复制或粘贴导入自有 `typebar://test` 配置链接，链接仅含测试配置、引语 ID 或自定义文本；参考项目其余网页链接互操作尚未实现 |
| CFG-35 | 命令面板设置 JSON 导入导出 | `commandline/lists.ts`, `config/lifecycle.ts` | CommandDomain, PreferencesDomain | 已实现：固定 `importSettingsJSON`／`exportSettingsJSON` 打开原生等宽 JSON 编辑页。v2 文档包含完整 `AppSettingsSnapshot`、当前模式／限制／语言／标点／数字等测试配置、计时／字数／自定义模式的五项独立限制记忆与单独持久化的 Layout Fluid 布局序列；导入先检查版本、完成 JSON 解码，再规范重复／空布局并同步应用，失败不改变现有设置。v1 文档继续可读，缺少的限制记忆会从活动配置推导并用既有默认值补齐。快照应用补齐遇错停下／删除的具体枚举档、代码反缩进与四项卷带显示设置，旧完整归档也因此不再静默漏项。文档迁移偏好、自定义主题和自定义键盘，但不包含成绩、提示、自定义练习正文、回放、保存文本、凭据或本地背景／字体文件；未复制参考实现、代码或资产。 |
| VIS-01 | 内置/自定义/收藏主题、自动与随机切换 | `theme-controller.ts`, `custom-themes.ts` | ThemeDomain | 部分实现：三套原创内置主题可从原生设置切换并持久化；用户可创建、应用和删除仅本地保存的原创 RGB 主题，并随归档迁移；内置和自定义主题均可收藏，收藏栏可一键应用，删除自定义主题会同步取消收藏；可选择跟随 macOS 深浅色，并为浅色/深色分别选择任一内置主题作为自动切换目标，手动主题与自定义主题选择会保留、关闭自动切换后恢复。参考项目的自动目标也是预定义主题列表，不包含自定义主题。完成测试后可从全部、收藏、浅色、深色、自定义或按当前系统深浅色筛选的原生主题池中无重复轮换；随机结果不写回手动主题，关闭应用后恢复，旧“重开随机”归档会迁移为“全部主题” |
| VIS-03 | 翻转与彩色测试文字 | `test-ui.ts`, `test.scss` | AppearanceDomain | 已实现：可独立翻转已输入与后续提示的明暗关系，或将已输入提示改用当前原创主题强调色；两者组合时后续提示使用强调色。错误、额外输入、输入规则、计分、结果与回放不随颜色设置改变，偏好会随本机归档持久化 |
| VIS-02 | 背景、字体、行宽、动效、光标、键盘映射 | `settings/custom-setting/*` | AppearanceDomain | 部分实现：练习字体大小、macOS 系统等宽/圆角/衬线/默认四种字体设计，以及可浏览和搜索的当前 Mac 已安装字体家族目录；也可手填家族名或 PostScript 名。目录在打开选择页时由 AppKit 读取，不写入归档；名称会随本机设置和归档保存，缺失时安全回退到所选系统设计。也可选择用户拥有的 TTF、OTF、WOFF、WOFF2 本地字体文件：原生 Core Text 会先读取描述符并在当前进程实际注册，只有可实例化的字体才优先覆盖名称选择；文件私有保存在本机应用支持目录，替换失败会恢复旧字体，且文件不随账户或设置归档迁移。Typebar 不包含 Web 字体解码器，也不复制官方字体目录或字体资产。紧凑/标准/宽/自定义（30–140 列）/自适应行宽、单行卷带（按词/按字符、可调停靠位置、可关闭平滑滚动）、可选原创 ANSI QWERTY/Dvorak/Colemak/Workman、French AZERTY、Turkish Q、Polish (Programmers)、Hungarian QWERTZ · Typebar、Russian JCUKEN、Ukrainian JCUKEN、Serbian Cyrillic · Typebar、Nordic QWERTY 及 VIS-05 所列区域布局的下一键提示与物理输入模拟，以及可持久化的节奏引导已实现。卷带只改变单行提示的水平呈现，多行自定义/代码提示仍使用普通布局，绝不改变输入、计分或回放。前四种布局支持 ASCII 字母、数字与标点；AZERTY 另映射法语 `é`、`è`、`ç`、`à` 与对应符号到数字/底排行，Turkish Q 映射 `ğ`、`ü`、`ı`、`İ`、`ş`、`ö`、`ç` 与 ISO 标点，Polish (Programmers) 原生模拟 `ą`、`ć`、`ę`、`ł`、`ń`、`ó`、`ś`、`ź`、`ż` 与 `€` 的 Option/Shift+Option 层，Hungarian QWERTZ · Typebar 原生模拟 `á`、`é`、`í`、`ó`、`ö`、`ő`、`ú`、`ü`、`ű` 与 QWERTZ 的 Y/Z 位置，Nordic QWERTY 则原生模拟 Å、Ø、Æ 及 ISO 键位；Russian JCUKEN 原生模拟 Cyrillic 的 `ё`、`й`、`ж`、`э`、`я`、`ь`、`ю` 与 `№`；Ukrainian JCUKEN 原生模拟 `ґ`、`ї`、`і` 与 `є`；Serbian Cyrillic · Typebar 原生模拟 `љ`、`њ`、`ђ`、`ћ`、`ж` 与 `џ`；未收录字符仍不高亮；“镜像键盘”可将 ASCII 实体键位按原创 ANSI 行镜像后再输入计分，下一键提示同步显示应按的镜像键位，重音、中文与其他非 ASCII 输入不转换；当前目标字符与节奏目标均可选择关闭、条形、轮廓、下划线、块状或三种原创矢量光标表现，并随设置和归档迁移；另有纯色、主题色光晕和细网格三种 Typebar 原生矢量背景；光晕可用本机开关减少动态效果，也会遵从 macOS 的“减少动态效果”。Typebar 不安装、上传、打包或下载第三方字体；只有用户明确提供自定义背景 URL 时才会请求该图片，详情见 VIS-04；除 Russian、Ukrainian 与 Serbian Cyrillic 外的其他非 Latin 布局/映射尚未实现 |
| VIS-05 | 区域键盘布局 | `layouts.ts`, `config/metadata.tsx` | KeyboardGuide, AppKitInputBridge | 已实现：German QWERTZ、Swiss German、Swiss French、Nordic QWERTY、UK QWERTY、Spanish QWERTY、Italian QWERTY、Portuguese QWERTY (ISO/ANSI)、Latin American QWERTY、Turkish Q、Polish (Programmers)、Hungarian QWERTZ · Typebar 共十三种区域 Latin 布局，以及 Russian JCUKEN、Ukrainian JCUKEN 与 Serbian Cyrillic · Typebar。前者覆盖 Y/Z、对应的变音/重音字母、`#/'` 或 `ç`/`£`；Nordic QWERTY 覆盖 Å、Ø、Æ、§、¤ 与 ISO `< >`；UK 覆盖 `£`、`#`、`~` 与 `@`；西班牙语和意大利语映射 ñ/¿ 或 è/ò/à/ù/ç；葡萄牙语覆盖 `ç`、`º`、`ª`、`«`/`»` 与 ANSI/ISO 标点差异；拉丁美洲布局覆盖 `|`、`°`、ñ、¿/¡ 与 `[`；Turkish Q 覆盖 `ğ`、`ü`、`ı`、`İ`、`ş`、`ö`、`ç` 与 ISO `,;`；Polish (Programmers) 覆盖 `ą`、`ć`、`ę`、`ł`、`ń`、`ó`、`ś`、`ź`、`ż` 与 `€` 的 Option/Shift+Option 层；Hungarian QWERTZ · Typebar 覆盖 `á`、`é`、`í`、`ó`、`ö`、`ő`、`ú`、`ü`、`ű` 与练习标点；Russian JCUKEN 覆盖 `ё`、`й`、`ж`、`э`、`я`、`ь`、`ю`、`№`、`\\`/`/` 及 ISO `< >`；Ukrainian JCUKEN 覆盖 `ґ`、`ї`、`і`、`є`、`я`、`ь`、`ю` 及 ISO `< >`；Serbian Cyrillic · Typebar 覆盖 `љ`、`њ`、`ђ`、`ћ`、`ж`、`џ` 及 ISO `< >`。这些布局中无基础字符的死键不会被模拟，继续交给 macOS。物理 keycode 模拟支持常规 ANSI 键与 macOS ISO section 键。布局会进入设置、归档及 layoutfluid；每轮序列至多选择十五种，除 Polish (Programmers) 外的 Option 层、其他死键和除 Russian、Ukrainian 与 Serbian Cyrillic 外的非拉丁布局仍走系统输入或保持未实现。 |
| VIS-04 | 本地/远程自定义背景、适配与滤镜 | `CustomBackground.tsx`, `theme-controller.ts` | AppearanceDomain | 已实现：可填写 HTTP(S) 的 PNG/JPG/GIF/WebP URL，或经原生文件选择器导入本地图片；本地图片优先显示，只保存在当前 Mac 的应用支持目录，不上传、不随归档导出。远程 URL、覆盖/完整显示/填满适配与模糊、亮度、饱和度、不透明度滤镜会随归档持久化；URL 只在用户明确填写后才会请求，加载失败时保留 Typebar 原生背景 |
| FUN-01 | 趣味修饰器（视觉、文本、布局、可见性、输入） | `packages/funbox/src/list.ts` | ModifierDomain | 部分实现：48 项官方 funbox 已全部纳入 Typebar 自有的修饰器、文本流、视觉提示或布局策略；逐项映射、互斥关系与差异见 `OFFICIAL_FUNBOX_AUDIT.md`。48 项均达到用户可见功能等价、0 项未实现；其中 ASL 指语提示以原创原生矢量呈现，功能流不逐像素复刻官方专用字体。所有实现均为原创原生代码与内容，不依赖参考项目代码、资产或网络内容。 |
| FUN-02 | 修饰器互斥验证、挑战设置和完成判定 | `funbox-validation.ts`, `challenge-controller.ts` | ModifierDomain, ChallengeDomain | 部分实现：修饰器切换与配置解码会按官方冲突类别归一化；具体类别与逐项覆盖证据见 `OFFICIAL_FUNBOX_AUDIT.md`。原创离线挑战可加载固定测试配置，并按最低 WPM、最低准确率和最高错误数逐项判定，完成页会显示通过状态或具体未满足项；挑战库顶部还提供按本机日历日稳定选择、无需网络的今日挑战。仍需以实际设备操作覆盖每一组冲突组合。 |
| ACC-01 | 注册、登录、密码/邮箱/身份管理 | `components/pages/login/*`, `users.ts` | IdentityService | 部分实现：自建服务和原生账户设置页支持注册、登录、会话恢复、密码更新、密码重置、邮箱验证、邮箱变更与账户删除；删除要求当前密码和原生确认，服务端会清除该账户的会话、成绩、同步、关系、屏蔽与投稿，本机练习历史不会受影响。密码和邮箱更新均验证当前密码；密码更新会使用 bcrypt 重哈希新密码，邮箱更新会验证格式与唯一性、复位验证状态，二者都会撤销该账户既有会话并仅保存当前新令牌至 macOS 钥匙串。密码重置只在部署者配置 HTTPS 投递 webhook 后开放：请求接口对已注册、未注册和无效邮箱返回相同成功响应，令牌只保存 SHA-256 哈希、20 分钟过期且一次性；投递失败会撤销新令牌，完成重置会注销所有会话、不自动创建新会话。相同 webhook 还会在注册或邮箱变更后自动投递 24 小时的一次性验证码，原生界面可重发并粘贴验证码；重发或改邮箱会撤销旧码，验证成功不会轮换当前会话。未配置投递会明确提示。GitHub/Google/Discord 已通过 macOS 系统授权窗口支持授权码登录、PKCE、哈希化且一次性的 state、新用户展示名补充、身份关联/移除，以及匹配既有关联提供商的重新验证；Discord 仅请求 `identify email` 并拒绝缺失或未验证邮箱，提供商令牌不落盘。真实提供商账户端到端人工验收尚未完成 |
| ACC-02 | 用户资料、徽章、XP、账户设置 | `components/pages/account*/*` | ProfileDomain | 部分实现：受令牌保护的当前显示名读取/更新与客户端资料刷新已实现；原生账户设置页可编辑公开显示名，并明确说明其公开范围不含邮箱；用户也可明确选择从自建服务全部全局/好友 WPM 与 XP 排行榜隐藏自己，已保存成绩、XP、本机历史与同步不受影响，恢复显示也即时生效。自建服务提供不含邮箱的基础公开资料，原生榜单可打开资料卡。服务端会从已验证的完成成绩即时重算 Typebar 自有总 XP，客户端显示当前总 XP 和公开资料总 XP；历史页另有完全离线、由本机完成成绩即时派生的四项 Typebar 原生成就，不上传、不同步且不替代 XP。服务端现以已接受成绩派生十枚原创公开徽章，选择、返回与奖励通知验收见 ACC-04；人工授予的开发者、捐赠者或社区角色徽章不在本地重写中伪造 |
| ACC-03 | Discord 公开头像 | `DiscordAvatar.tsx`, `utils/discord-avatar.ts` | IdentityService, ProfileDomain | 已实现：已关联 Discord 的用户可在原生资料设置主动开启公开头像；默认关闭，关闭或移除关联后公开资料端点立即不返回头像。服务端仅接受 Discord 身份中格式有效的 snowflake 与头像哈希，客户端再次验证后才请求 CDN，并在网络失败时回退原生人物图标；不保存提供商访问令牌，也不向搜索/好友嵌套资料暴露 Discord 关联 |
| ACC-04 | 公开资料徽章选择与奖励投递 | `User.tsx`, `UserDetails.tsx`, `leaderboards.ts`, `collections/inbox.ts` | IdentityService, ProfileDomain, RankingService | 已实现：服务端以至少含一个实际输入事件的已接受成绩派生十枚 Typebar 原创徽章，覆盖首次／十次完成、98% 准确率、连续一分钟零误差、80／100 WPM、累计 15／60 分钟、五种语言和四种模式；合法但零输入的结果保留在完成统计中，却不会推进任何徽章。账户设置只列出当前已解锁的项目，用户可选择一枚或明确不显示。选择后的最小徽章标识只进入公开资料、WPM 榜与 XP 榜，未解锁 ID 被拒绝；未选择、删除所有相应服务端成绩或旧服务不返回字段时均安全不显示。每枚徽章首次由一条新成绩解锁时，会向成绩所有者投递一次带最小徽章展示信息的私有通知；成绩 ID 重试与未产生新解锁的后续成绩不会重复投递，其他账户不可见。徽章标题和 SF Symbol 为原创定义，不复用参考编号、名称、条件、图像或奖励资产；参考中的开发者、捐赠和社区角色徽章依赖人工授予，Typebar 不伪造这些身份 |
| ACC-05 | 排行榜 Discord 头像 | `leaderboards.ts`, `DiscordAvatar.tsx` | IdentityService, RankingService | 已实现：已关联且主动公开 Discord 头像的用户会在 WPM 榜、XP 榜和资料卡显示同一已校验 CDN 标识；未开启、取消关联或非法哈希时三处均不返回。原生榜单异步加载失败会回退人物图标，搜索、好友和通知等轻量嵌套资料仍不携带 Discord 关联 |
| ACC-11 | 重置服务端公开个人最佳 | `AccountTab.tsx`, `ReauthConfirmModals.tsx`, `users.ts` | IdentityService, ProfileDomain | 已实现：原生账户设置可在再次确认身份后开启新的服务端公开 PB 纪元；密码账户输入当前密码，纯第三方账户消费五分钟一次性重新验证凭据，凭据使用后不可重放。服务端不删除或改写任何成绩，完成/开始次数、练习时长、XP、徽章、活动、排行榜和其他账户均保持不变；公开最佳 WPM、最高稳定度及八个标准档 PB 只从重置后由服务端新接收的成绩派生，因此随后较低成绩也可成为新 PB。重置时间和成绩接收时间均为可选持久化字段，旧状态缺失时维持原行为；旧客户端缺少响应字段时不受影响，原生客户端读取旧服务响应时安全显示为从未重置。本机历史与本机 PB 始终离线独立，不随服务端操作改变。升级发布前应备份服务状态；已写入 PB 纪元后若降级到不认识新字段的旧服务并再次持久化，纪元与接收时间可能丢失，因此应优先前滚，必须回退时恢复升级前备份并接受恢复旧 PB 语义 |
| ACC-12 | 导出全部服务端成绩 CSV | `AccountPage.tsx`, `utils/misc.ts` | IdentityService, ResultsDomain | 已实现：登录用户可从原生账户设置导出当前账户的全部服务端成绩元数据，而不是只导出界面显示的最近二十条。客户端按现有私有成绩接口每页最多 1,000 条读取，验证分页总数固定、ID 无重复且最终数量精确后才打开 macOS 保存面板；期间有新增、删除或重复页时明确拒绝残缺快照并提示重试。CSV 使用 UTC 文件名和 RFC 4180 转义，只含模式、参数、语言、WPM、Raw、准确率、稳定度、错误、事件数、标签与起止时间，不含提示、实际输入、回放、邮箱、令牌、本机历史或其他账户数据。导出是显式联网读取和用户选择位置的本地原子写入，不改变服务端或本机状态，也不复用参考导出代码 |
| ACC-13 | 完整重置账户数据 | `DangerZoneTab.tsx`, `ReauthConfirmModals.tsx`, `users.ts` | IdentityService, SyncService, LocalPersistence | 已实现：原生账户设置在明确的不可撤销确认与重新验证后，先幂等重置当前自建服务账户，再清理当前 Mac。服务端清除成绩、XP、公开 PB、资料、榜单退出、徽章选择、开发者密钥、同步记录和收件通知，但保留 UUID、邮箱、显示名、验证状态、密码/第三方登录、当前会话、好友、屏蔽、投稿及已固定的公开统计日边界；其他账户不受影响。本机清除成绩、练习预设、筛选预设、保存文本、设置、本地背景和字体，并清零该服务器与账户的同步游标/版本，防止旧归档回灌。操作无新增持久化字段或数据迁移；新客户端连接旧服务会在本机删除前明确失败，旧客户端会忽略新增路由。若服务端成功而本机清理失败，界面明确报告部分完成，可安全重复整项操作。发布前应备份/导出需要保留的数据；降级不恢复已删除内容，只能从备份恢复 |
| SOC-03 | 公开连续练习摘要与账户日界 | `packages/schemas/src/users.ts`（`streak`、`maxStreak`、`testActivity`）, `StreakHourOffsetModal.tsx` | SocialService, ProfileDomain | 已实现：资料卡显示由服务端已接受成绩派生的当前连续与最长连续天数；默认按 UTC 00:00 分日，登录用户可在原生账户设置从 −11 至 +12 小时、每 30 分钟一档固定一次公开日界。服务端持久化该账户选择，并以同一逻辑练习日重算最近 365 天活动与连续摘要；第二次设置返回冲突，非法范围或非半小时档被拒绝。该账户日界与只管设备历史的本机日界彼此独立，不修改成绩时间。活动与连续摘要共用隐私开关；搜索、好友和通知中的轻量资料不带摘要、单条时间、提示、回放、邮箱或令牌。旧服务响应回退 UTC/未设置，旧持久化状态默认 UTC。真实账户界面与跨日结果仍待人工验收 |
| SOC-04 | 公开练习统计 | `packages/schemas/src/users.ts`（`typingStats.timeTyping`、`typingStats.completedTests`、`typingStats.startedTests`） | SocialService, ProfileDomain | 已实现：资料卡会显示所有服务端已接受完成成绩的累计实际时长、完成次数与开始次数。本机在已开始但未结束时重开会计入本轮 `restartCount`；服务端仅在首次接受该成绩 ID 时原子累加 `restartCount + 1`，重复提交不会重复计数。与官方一样，未最终提交完成成绩的孤立练习不会进入服务端统计；它不读取或上传本机历史、提示或输入回放。旧客户端省略字段默认一轮一次，旧服务响应安全回退为零。 |

2026-09-08 更正：SOC-01 与较早段落中“公开活动固定按 UTC 日聚合”的描述由 SOC-03 当前状态取代。UTC 仍是默认值；账户一旦明确固定公开日界，资料卡会标出实际偏移并按该边界解释 365 天活动。轻量资料与隐私边界不变。
| SYN-01 | 结果、配置、预设、主题、标签的同步 | `api/routes/{results,configs,presets}.ts` | SyncService | 部分实现：自建服务支持用户作用域的版本/游标同步；原生客户端可上传与拉取版本化设置、结果、预设、主题设置、标签和自定义文本归档并去重合并。拉取会按每页最多 100 条变更顺序读完，只有本机归档成功解析并合并后才提交游标与版本，服务端旧响应缺少分页标记时安全当作完整页。上传遇到并发版本冲突时，客户端从起始游标重新读取权威归档，保留本机标量设置和双方成绩；同名但内容不同的远端预设/文本，以及同 UUID 但内容不同的远端主题/自定义键盘布局，会换名或换 ID 另存为“同步冲突”副本，再按服务器最新版本重试。合并器会为实际另存的副本返回类型和最终显示名；同步页按规范化服务地址和用户 UUID 显示这台 Mac 最近 50 条冲突记录及时间，并可只清空当前作用域。记录不含文本正文、成绩、归档载荷或凭据，损坏时安全视为空。客户端同步游标与版本按同一服务器和账户维度分区，不同账户或服务器不会继承彼此检查点；旧全局检查点保留但不复制，新作用域从零幂等重放。Bearer 令牌同样按服务器地址分区，旧全局令牌只在启动记录的同一服务器迁移后删除，服务切换不会跨域发送凭据。细粒度变更、服务端内容级三方合并和真实多设备端到端人工验收尚未完成 |
| SOC-01 | 用户搜索、公开资料、好友与屏蔽 | `components/pages/{profile,connections}/*` | SocialService | 部分实现：自建服务支持按公开展示名搜索（不泄露邮箱）、受令牌保护的好友请求、接受、列表、解除与屏蔽；屏蔽会移除双方现有关系、会话记录并阻止后续好友请求。公开资料和好友条目显示该账户已提交成绩的最佳 WPM、最高稳定度、完成/开始次数与累计练习时长；资料卡另显示 15/30/60/120 秒和 10/25/50/100 词八个标准档的最佳成绩，含语言、速度、准确率、稳定度和完成日期，以及按 UTC 日聚合的最近 12 个月完成活动；绝不返回提示、输入回放、邮箱或令牌。活动明细只在打开资料卡时传输，搜索、好友和通知等嵌套资料保持轻量。稳定度来自客户端本机回放，服务端只保留并范围校验；旧服务响应会在原生客户端安全显示为 `0%`、空个人最佳和无活动。原生客户端可搜索用户、从榜单资料卡请求好友，并在好友面板接受、取消、解除、屏蔽或解除屏蔽；端到端人工验收尚未实现 |
| SOC-02 | 站内信、举报、通知 | `collections/inbox.ts`, `UserReportModal.tsx` | ModerationService | 部分实现：自建服务会为好友请求、接受、新私信与首次徽章解锁生成仅接收者可读取的通知，记录触发者公开资料、可选最小徽章展示信息、创建时间、可选已读时间、当前账户未读总数和 100 条固定容量；私信通知不包含消息正文，徽章通知不包含成绩、提示或回放。每个账户只保留最新写入的 100 条通知，超限只淘汰该收件人最旧记录；载入旧持久化状态时也按相同规则规范化，不影响其他账户。原生通知中心显示当前数量/上限；主工具栏会在登录和关闭通知窗口时刷新未读徽标，窗口内刷新、标记已读、逐条删除或经确认清空后即时更新。旧服务缺少总数、容量或徽章载荷时安全回退，未来未知通知类型以通用标题和图标显示而不会使整个收件箱解码失败；网络失败保持未知而不伪造为零。服务端按访问令牌约束收件人，其他账户无法读取或删除，清空操作也不会删除好友关系、私信或已解锁徽章。已接受好友间可发送、读取、刷新与标记已读私有站内信；第三方、非好友和屏蔽关系均不能访问，屏蔽会删除双方会话记录。登录用户还可从公开资料卡提交自定义的资料举报分类及可选说明；服务端将其私有保存到待审核队列、拒绝自举报及重复同类举报，不通知被举报者且不自动处罚。原生设置页的审核员工作台用不落盘的部署密钥读取资料与引语队列；资料队列只含目标公开资料、分类、说明和状态，不显示举报者身份，且只能标为已处理或驳回，不会自动处置目标账户。引语队列仍可批准或拒绝投稿。系统推送、细粒度角色与申诉尚未实现 |
| RANK-01 | 全局/好友/每日/每周 XP 排行榜与排名 | `components/pages/leaderboard/*`, `leaderboards.ts` | RankingService | 部分实现：自建服务可受令牌保护地提交经字段/时长一致性校验后的结果，并提供按模式/语言、服务端当天/ISO 本周筛选的全局或好友 WPM 榜；每位用户在当前筛选下仅保留最佳成绩并获得对应名次，好友榜只包含本人及已接受好友，原生客户端可切换浏览。榜单还显示由原生客户端本机回放重建的稳定度；服务端只验证其为 0–100 的有限值，不能把它当作完整事件回放验真。旧客户端请求、既有服务端结果和旧服务响应都会安全回退为 `0%`。另有按 ISO 本周聚合、最多 100 人的全局/好友 XP 榜；XP 只由服务端的完成成绩时长、准确率及模式重新计算，禅模式不奖励，重复结果 ID 不重复奖励。请求限速已实现；完整事件回放验真和赛季奖励尚未实现 |
2026-09-09 补充：当天完成且未退出榜单的成绩提交响应会返回该账户在同模式、同语言今日全局 WPM 榜中的当前名次；结果页以结构化回执显示并可直接打开预设为相同筛选的现有排行榜。旧服务缺少字段时不显示入口，旧客户端忽略新增可选字段。重复 UUID 的响应从首次保存记录重新派生，篡改重试正文不会改变 XP 回执或榜单筛选；历史日成绩与隐身账户不返回今日名次。
| CONT-01 | 引语查询、评分、提交、审核与举报 | `components/modals/Quote*.tsx`, `quotes.ts` | QuoteService | 部分实现：登录用户可从原生账户设置提交三十四种受支持单语的引语及可选署名；自建服务验证长度与语言并以待审核状态持久化，部署者配置 `TYPEBAR_MODERATION_TOKEN` 后可读取按状态筛选的审核队列并审核为批准或拒绝；队列只提供引语、举报原因和说明，不暴露举报者身份，公共接口只返回批准内容。原生设置页提供审核工作台，以不落盘的部署密钥读取引语与资料举报队列、显示最小必要上下文并处理待审核项；资料举报只可标为已处理或驳回，绝不自动处置账户。客户端可明确切换并按语言刷新社区审核引语，默认的 Typebar 自有离线引语永不被替换。登录用户可私下举报已批准的社区引语，填写最多 400 字说明；服务端拒绝作者自举报与同原因重复举报，且不会通知作者或自动处置。登录用户可对已审核社区引语一人一票给出“不错”或“不适合”，可改评或撤销；公开列表只显示聚合票数，不公开评分者，删除账户/投稿会同步删除相关评分。离线引语评分仍仅保存在本机、不上传且不替代审核。细粒度审核角色与申诉流程尚未实现 |
| OPS-01 | 服务配置、公告、反作弊、限速、审计和维护任务 | `backend/src/{anticheat,middlewares,jobs,workers}` | OperationsService | 部分实现：Typebar 自有 Vapor 中间件按来源限制注册/登录（10 次/分钟），按访问令牌限制提交成绩（30 次/分钟）和其他写操作（60 次/分钟），读取接口限制为 180 次/分钟；拒绝时返回 `429`、`Retry-After` 与剩余额度头，并仅记录限速策略名而不记录令牌、邮箱或正文。部署者可在启动时设置 `TYPEBAR_MAINTENANCE_MODE=true`；维护期内健康检查和只读请求仍可用，所有写操作统一返回 `503`、300 秒 `Retry-After` 和明确的离线练习提示。部署审核密钥可发布或删除最多 500 字的公开纯文本公告，并可附加计划日期；客户端启动及账户状态变更时读取，普通公告只在当前 Mac 本机关闭，置顶公告持续显示到服务端删除。公告正文的 `{date}`、`{dateNoTime}` 和 `{dateDifference}` 会按 macOS 当前地区替换为计划日期时间、日期或相对时间，且公告不携带账户、提示、输入、成绩、邮箱或令牌。成绩提交会验证输入数、错误数、准确率、WPM/raw WPM 与起止时间的数学一致性，计时模式还会核对实际耗时与配置时长。固定窗口计数保存在进程内，重启即清空；分布式限速、持久化审计、数据保留策略、不可伪造签名和完整事件回放仍未实现 |
| MAC-01 | 菜单栏、全局热键、离线降级、备份恢复 | macOS 专属增补 | MacIntegration | 部分实现：原生菜单栏项目可激活应用并将当前可见主窗口恢复到前台；用户可在原生设置中明确启用 `⌃⇧Space` 全局唤起，首次开启会请求 macOS 辅助功能授权，未授权时会说明原因并可重新检查。应用只创建一个被动 `NWPathMonitor`：首次明确离线后在尚未输入的练习页和完成页显示低干扰提示，说明练习、计分和本机保存仍可用，而同步、账户、社区与榜单可能暂时不可用；测试进行中断网不会重排输入界面，真实的离线到在线转换只提示一次恢复，首次探测在线或重复状态不会伪造恢复。路径可用性不被当作自建服务健康检查，不轮询、不自动发送、不新增存储或迁移字段。SwiftData 持久容器无法打开时不再终止进程、删除文件或静默创建空库，而是进入原生恢复页，显示原存储位置与可复制错误并允许在 Finder 定位；修复磁盘空间、权限或迁移问题后重启会重新尝试同一文件。联网操作的统一排队与自动重试仍未实现。 |

2026-09-10 补充：用户已开启“将完成成绩发送到自建服务”时，网络错误、未授权会话、超时、限速或 5xx 会把已保存成绩加入持久待发送索引；索引只含规范化服务器作用域、用户 UUID 和成绩 UUID，不含令牌、提示、输入或回放。首次上线、真实网络恢复、同账户登录或重新开启发布时会按原顺序逐条重试，首个暂时失败即停止，不轮询；服务端以既有成绩 UUID 幂等去重。普通永久性 4xx 与无效地址不入队，已排队项目若后来被永久拒绝或本机成绩已删除则移除；账户/服务器变化会在请求前阻止跨作用域发送，完整账户与本机重置会清空索引。其他同步、社区和账户写入仍没有统一队列，故 `MAC-01` 保持部分实现。

### 2026-09-04 ACC-04 验收补充

- 自动化：`server/Tests/TypebarServerCoreTests/HealthRouteTests.swift` 覆盖未解锁拒绝、成绩派生、显式选择/清除、资料与两种榜单显示、旧客户端省略字段时保留选择，以及删除服务端成绩后立即隐藏；`Tests/TypebarTests/TypingEngineTests.swift` 覆盖新旧账户/榜单响应解码。
- 手动验收：登录自建服务并上传一条满足条件的完成成绩，刷新资料后在“公开资料徽章”选择一项；从 WPM 榜、XP 榜和资料卡确认其显示。选择“不显示”后刷新资料卡确认隐藏；清除该账户服务端成绩后再次刷新，确认徽章不再出现。
- 迁移：`selectedBadgeID` 是服务端归档中的可选字段，旧归档安全解码为空；旧客户端省略该字段时保留现有选择，新客户端以空字符串明确清除。
- 隐私与离线：仅服务端已接受结果的计数、速度、准确率和时间长度用于派生，公开响应只含用户选中的标题/SF Symbol；本机练习历史、提示、回放、邮箱和令牌均不会随徽章公开或上传。

### 2026-09-04 ACC-05 验收补充

- 自动化：Discord 头像隐私测试覆盖资料卡与 WPM/XP 榜的联动显示，关闭公开开关后两种榜单立即不再返回头像；客户端榜单响应解码会复用同一严格 CDN URL 校验。
- 手动验收：关联 Discord 并开启公开头像后刷新 WPM/XP 榜，确认头像与资料卡一致；关闭开关并刷新两处榜单，确认头像消失。图片加载失败时应只回退该行的系统人物图标，不影响名次或成绩。
- 迁移与隐私：榜单头像字段均为可选，旧服务响应安全解码为空；服务端只将已开启公开且通过哈希校验的 Discord 身份投影到榜单，绝不返回邮箱、提供商令牌或本机练习数据。

### 2026-09-04 SOC-03 验收补充

- 自动化：服务端测试以连续两天、间隔一天的三条已接受成绩验证当前/最长连续均为两天，并验证关闭公开活动后日历和连续摘要同时不返回；客户端测试覆盖旧资料响应缺失字段的安全回退与新摘要的解码。
- 手动验收：在自建服务连续两个 UTC 日各上传至少一条成绩后，从资料卡确认“当前连续”和“最长连续”均为两天；关闭“公开活动”并刷新资料卡，确认日历和两项摘要同时隐藏。
- 迁移与隐私：摘要每次读取从服务端已接受成绩临时派生，不引入持久化字段；客户端将旧服务缺失字段视为无摘要。公开值不使用或泄露用户本机的统计日分界，也不返回单条时间戳、提示、回放、邮箱或令牌。

### 2026-09-04 SOC-04 验收补充

- 自动化：服务端资料端点测试以五条不同配置的已接受成绩验证累计实际时长为 135 秒与开始次数为 5；独立测试验证两次中途重开会使首条成绩贡献 3 次开始、重复提交同一 ID 不重复累加、删除服务端成绩后次数清零。客户端兼容性测试覆盖旧资料响应回退为零及新字段解码。
- 手动验收：开始一轮练习后中途重开两次再完成，从资料卡确认“完成成绩”为 1、“开始”为 3；删除该账户服务端成绩后刷新资料卡，确认开始次数与时长归零。本机历史不应受影响。
- 迁移与隐私：`startedTestCount` 是服务端用户归档的可选累计字段，旧归档安全解码为零；旧客户端不传 `restartCount` 时按一次开始处理。公开响应不含单条时间戳、提示、实际输入、回放、邮箱、令牌或本机未完成练习。

### 自定义键盘输入边界

`VIS-02` 的自定义键盘图可单独作为视觉提示，也可明确选择为基础物理键位模拟：四行 Unicode 标签按对应的 ANSI 物理位置绑定；要模拟时四行最多为 13/13/11/10 个字符。Shift 行留空时，可单字符大小写转换的字母用普通/Shift 映射；填写与基础行等长的 Shift 行时可精确映射符号和其他字符。超出物理容量的图保留为视觉提示；缺少或删除选中的图时立即回退到 macOS 输入源，旧归档缺少 Shift 行时按留空处理。Option、Control、Command 和 Layout Fluid 保持系统或内置布局路径。这是让用户自行覆盖未内置布局的原创入口，不打包或复用参考项目的命名键盘资产。

### 实时指标呈现

`MET-01` 的进度、速度、Raw、Burst 与准确率可以独立选择主题强调色、辅助文字、主文字或黑色，以及 25%、50%、75% 或 100% 透明度。它们仅为本机 SwiftUI 呈现偏好，会随设置归档；错误数与分节进度保持默认可读性，输入、计分、结果、统计及回放均不会被改变。

### 无空格原生词界

`TST-02` 的简体中文、繁体中文与日语假名 time/words 提示会仅在整段能由 Typebar 自有词库完整重建时保留隐藏词界。每个自有词及其生成的数字、标点后缀都在末字符提交；字数进度、Burst、最低单词速度、结果词历史，以及“遇错停下/修正后再前进/遇错删除”的逐词规则因此可用。自定义文本、引语和远端文本不能完整匹配时保守回退到字符级，不猜测词界、不改写提示。

- 自动化：客户端测试覆盖三种无空格语言的数字/标点词界、进度、完成结果词历史和 Burst；另覆盖反写后词界顺序/后缀、单词级遇错停下，以及四档遇错删除在已保留词界时的当前词/上一词状态转换。
- 手动验收：选择任一无空格语言的 4 词练习并开启数字和标点；输入首个可见词及其后缀后，确认显示 `1/4`。完成一轮后展开词历史，确认四项与提示一致；输入首词末字符的错误后，确认单词级遇错停下拒绝前进直到修正。分别开启“单词”和两种硬删除，确认词内错误只清当前词、下一词词首错误分别退回上一词的末字符或清空上一词。
- 迁移、隐私与离线：词界仅存活于当前会话和其本机结果，不增加设置或归档字段；完全由 Typebar 自有词库推导，不请求、不上传或复制外部内容。

进度还可选择“闪现文字”或“闪现迷你”：仅限时测试会在剩余秒数为 15 的倍数时显示倒计时，其余秒数保持各自的文字留位或迷你留位；字数、引语和自定义输入完成等非限时测试始终显示进度。这是 Typebar 自有的呈现策略，不影响会话计时、计分或结果。

主光标与节奏光标都可设为“关闭”，此时仅保留正常的当前/待输入字符颜色，不会绘制任何额外标记。常规提示使用独立的 TextKit 测量层按实际富文本、字体和换行宽度定位光标；位置可关闭或按 150 / 100 / 85 ms 三档原生动画移动。除条形、下划线和块状外，轮廓与三种原创矢量标记也可选；全部选项进入本机设置和归档。卷带、手语与旋转文字等专用提示保持各自的保守静态回退，待实机统一检查。

### 本机已安装字体

练习字体可从当前 Mac 已安装字体家族的可搜索目录中选择，也可手填家族名或 PostScript 名。目录由 AppKit 在用户打开选择页时读取，经空白清洗、大小写与重音无关去重和稳定排序后展示；搜索同样忽略大小写、重音和全半角，目录内容不持久化。也可主动选择用户拥有的 TTF、OTF、WOFF、WOFF2 文件；Typebar 不自行解码格式，而是交给原生 Core Text 读取描述符并在当前进程注册，只有随后能以 PostScript 名实例化时才让新副本覆盖名称选择。候选无效或当前 macOS 无法加载时不会移除既有字体。常规提示、卷带、旋转文字、组合输入显示和 TextKit 光标测量共享同一解析结果，因而不会因字体不同而产生独立光标偏移。输入不存在的名称时，界面明确提示并回退到所选的系统设计，名称仍会随本机设置和归档保留。本地字体文件不随设置归档或账户迁移，移除后立即回退；它不会安装到 macOS 字体册、上传或嵌入应用，也不复制参考项目字体目录或资产。

### 节奏伙伴

原创的“节奏伙伴”只在已开始、未结束的练习中显示。它根据 ANSI 物理键位的左右手状态改变姿态；6/Y/B/Space 等中间键会按次交替归属一只手，130–180 WPM 间渐进增强速度反馈。它不使用参考项目图片，失焦和重开会清空手部状态，且开关随本机设置和归档持久化；该呈现不改变输入、计分、结果或回放。

### 键入能量效果

参考项目的隐藏 power mode 与节奏伙伴是独立设置。Typebar 提供关闭、柔和、高能、超载和极限五档的原创原生映射：每次输入引擎实际接受文本时，在练习区绘制短暂粒子轨迹；柔和/超载按正确或错误使用主题强调色/错误色，高能/极限使用随机光谱色，后两档中的超载/极限会短暂抖动练习面板。粒子不会改变提示、输入、计分、回放或结果；重开、失焦、关闭该效果以及 macOS“减少动态效果”都会清除或抑制动画。设置仅本机持久化并随归档迁移，不使用参考项目的 Canvas 代码或任何素材。

### 提示高亮范围

`VIS-05` 提供关闭、当前字符、当前词、当前词加后续 1/2/3 个词六种原生提示高亮范围。词范围只适用于常规空格分词提示；卷带、无空格、听写、记忆与预读遮挡会安全降级为当前字符。该偏好仅改变 SwiftUI 呈现，进入本机设置归档，不改变输入、计分、结果、统计或回放。

### 键盘提示模式

`VIS-06` 提供关闭、静态、按键反馈与下一键四种原创原生键盘提示模式：静态只显示所选布局，按键反馈以 180ms 的强调色/红色短暂标记最近按下的正确/错误键，下一键显示当前目标键。键盘大小可在 0.5–3.5 间以 0.1 步长调节，越界归一化；它用原生尺寸重排，不复用网页 zoom/CSS。图例可选小写、大写、空白或动态；动态档随实际 Shift 与 Caps Lock 切换，数字和标点只在按住 Shift 时显示其 Shift 符号。按键集可选精简、精简（数字行）和完整：精简档按官方 QWERTY/Dvorak/Colemak 的顶行策略隐藏数字行，但 AZERTY 保留数字行，下一键为数字时也会显示；完整档追加不可输入的修饰键、Tab、Return、删除和 Escape 图示。键盘样式覆盖官方的 `staggered`、`alice`、`matrix`、`split`、`split_matrix`、`steno`、`steno_matrix` 七个选择值：它们分别原生表达错列、人体工学内弧、平齐矩阵、分体错列、分体矩阵、分体速录和分体速录矩阵。速录样式是原创的标准速录键位图示，不模拟速录和弦输入；所有样式仅改变呈现，保持 macOS 原生输入和计分路径不变。命令面板严格公开 18 个离散键盘值、一个带固定范围/步长校验的大小输入页，以及 `overrideSync + 239` 个视觉布局值；同步态持续跟随输入模拟，而显式视觉布局从不改动输入映射。Polish (Programmers) 的动态图例会在 Option/Shift+Option 时显示其公开标准字符层并用于明确选中的内置模拟；其他内置布局没有 Option 字符层，因此这些 Option 输入继续由 macOS 原生处理。听写时隐藏键盘提示，Simon 指令始终使用下一键。模式、大小、图例、按键集、样式和布局进入本机设置归档；它们只读取当前输入与目标，不改变输入、计分、结果、统计或回放。

### 闲置保护与有效键入时长

`MET-34` 以原创的本机一秒活动时间线审计测试结束前的输入节奏。计时器从第一次输入起按墙钟计时，绝不因闲置暂停；有限测试最后连续 5 个输入区间没有文本插入时，会产生“闲置无效”结果。该结果保留总用时、闲置秒数、闲置占总时长百分比与有效键入秒数供当前窗口查看，但不会保存、本机统计、同步或发布。删除等键盘活动可减少总闲置秒数；只有文本插入可解除结束前闲置判定。已保存的正常结果以总用时减去闲置时长计算“今日练习”和历史分析时长；旧归档中没有闲置字段时安全按 0 秒处理。所有活动时间线和结果字段均在设备本机生成，不读取、上传或复用参考实现代码或资产。

## 盘点结论

以下逐批“当前单语数”保留为实现时间线，不能覆盖本文件开头机器守护的当前目录摘要；最终有效值为 279 个单语、153 个 LTR 多语候选、70 个代码选择和 2 个混合入口。

2026-09-08 `TST-133`：新增 toki pona · ku suli 与 toki pona · ku lili 两个独立选择。前者在现有 Typebar 自有基础词流上增加 15 个独立整理的核心词，后者使用 20 个与前者互斥的扩展词；四档文本从 Typebar 自有 toki pona 文本派生独立 ID 与语言身份。两项均保持固定 `noLazyMode: true`、缺省 `en`／`en-US`、LTR 空格词界与 Zipf 未知语义，并贯通混排、投稿、成绩和排行榜。未读取或复制官方 ku 词值。

2026-09-08 `TST-134`：新增 Old English 独立选择。官方引入提交 `7b6ed784813b4ad64941f8033df6a5dc5b070d52` 解除 `english_old` 名称歧义；Typebar 以 79 个独立编写、仅含 ASCII 小写字母与 `æ/þ` 的词和四档原创文本实现，保持固定缺省元数据对应的 LTR 空格词界、可选简化输入、`en`／`en-US` 与 Zipf 未知语义，并贯通混排、投稿、撤回、成绩和排行榜。未读取或复制参考词值、引语、代码或资产。

2026-09-08 `TST-135`：新增 Français · Bitoduc 独立选择。官方 PR `#6400` 明确其科技词法语趣译概念；Typebar 以 72 个独立编写的科技法语与趣味复合词和四档原创文本实现，保持单 token、5–14 字符、连字符与 `çèéêîô` 练习边界，并按固定 `bcp47: fr-fr` 接入 LTR 空格词界、`fr`／`fr-fr`、可选简化输入、Zipf 未知、混排、投稿、撤回、成绩和排行榜。未读取 bitoduc.fr 或参考词值、引语、代码、资产。

2026-09-08 `TST-136`：新增 Streaming Emotes · Typebar 独立选择。固定 `twitch_emotes` 配置只定义 `noLazyMode: true`；Typebar 使用 80 个完全虚构、大小写敏感的单 token 及四档自有 token 流，覆盖数字、下划线和 `:()<>`，并按缺省 `en`／`en-US`、禁用简化输入、Zipf 未知语义接入混排、投稿、撤回、成绩和排行榜。未读取或复制 Twitch 表情名称、聊天数据、图像、参考词值、代码或资产。

2026-09-09 `TST-137`：新增 Arcade Horror Phrases · Typebar 独立选择。固定 `typing_of_the_dead` 的多词 section、`originalPunctuation: true`、禁用简化输入和 Zipf 不支持语义由专用生成器重建；64 段完全原创短语会拆成单词并严格按请求词数截断，关闭标点时输出小写纯词，开启时保留短语自身大小写和标点，数字仍按词位替换。四档原创引语、混排、投稿、撤回、成绩和排行榜均已接入；未读取或复制游戏台词、参考短语、代码或资产。

2026-09-09 `TST-138`：新增 Creature Index 1k · Typebar 独立选择。固定 `pokemon_1k` 的 1,025 个唯一条目、3–12 字符、28 个多词、17 个符号、1 个数字及 `orderedByFrequency: false` 结构由 41×25 的原创构词规则和原创特殊项重建；专用生成器保持最终词数、合成标点和数字替换，四档原创引语、混排、投稿、撤回、成绩和排行榜均已接入。未读取或复制宝可梦名称、设定、代码或资产。

2026-09-09 `TST-139`：新增 Arena Strategy Terms · Typebar 独立选择。固定 `league_of_legends` 的 442 个唯一条目、题名大小写、3–25 字符、229 个多词、61 个符号和 2 个数字结构由 17×26 的原创构词规则与结构替换重建；关闭标点时过滤源符号并转小写，开启时保留题名大小写并添加合成标点。四档原创引语、混排、投稿、撤回、成绩和排行榜均已接入；未读取或复制英雄、装备、技能名称、游戏代码或资产。

2026-09-09 `TST-140`：新增两个 Русский · Краткие формы独立选择。基础入口使用 200 条原创短形式，1k 入口使用完整包含基础集的 880 条原创扩展形式；两个集合分别保持固定版本的长度、多词、符号、数字、大小写和纯西里尔结构。二者均按 `ru-RU`、禁用简化输入、Zipf 不支持及关闭标点时转小写语义接入四档引语、混排、投稿、撤回、成绩和排行榜；未读取或复制参考词值、代码或资产。

2026-09-09 `TST-141`：新增独立 தமிழ் · பழைய தொகுப்பு选择，以 23×20 的 Typebar 自写音节组合生成 460 条唯一 joining-script 项，保持 3–13 个 Unicode 标量、仅一条多词项及 Tamil 区段字符边界。入口使用 `ta`／`ta-IN`，接入四档自有引语、混排、投稿、撤回、成绩和排行榜；固定 446 个 ID 现已全部映射，仍不复制参考词值、代码或资产。

2026-09-09 `TST-142`：新增 English 1k／5k／10k／25k／450k 五个独立规模入口。原创确定性索引词流让普通出题只生成请求的词，词表筛选与弱项分析才扫描所选规模；它保持固定实际数量、唯一性、长度、大小写、符号、`noLazyMode` 与词频排序能力。五个 ID 均贯通四档自有引语、配置、预设、分享、归档、投稿、撤回、成绩和排行榜，且为避免重复权重不加入默认多语混排。机器总账更新为 446／238／208／0，不复制参考词值、代码或资产。

2026-09-09 `TST-143`：新增 Español 1k／10k／650k 三个独立规模入口。Typebar 原创确定性索引词流保持 998／9,990／646,579 个唯一项及固定长度、大小写、符号、多词和非 ASCII 数量；三项均使用 `es`／`es-ES`、可选简化输入和 Zipf 未知语义，贯通四档自有引语及全部数据面，且不加入默认多语混排。普通出题只生成请求项，与固定词表精确交集为零；机器总账更新为 446／241／205／0。

2026-09-09 `TST-144`：新增 Français 1k／2k／10k 三个独立规模入口。Typebar 原创确定性索引词流保持 1,394／2,041／10,251 个唯一项及固定长度、大小写、标点、多词和非 ASCII 数量；三项使用 `fr`／`fr-FR`、可选简化输入和 Zipf 未知语义，贯通四档自有引语及全部数据面，且不加入默认多语混排。普通出题只生成请求项，与固定词表精确交集为零；机器总账更新为 446／244／202／0。

2026-09-09 `TST-145`：新增 Deutsch 1k／10k／250k 三个独立规模入口。Typebar 原创确定性索引词流保持 988／9,994／239,243 个唯一项及固定长度、名词大写、标点、多词和非 ASCII 数量；三项使用 `de`／`de-DE`、可选简化输入和 Zipf 未知语义，贯通四档自有引语及全部数据面，且不加入默认多语混排。普通出题只生成请求项，与固定词表精确交集为零；机器总账更新为 446／247／199／0。

2026-09-09 `TST-146`：新增 Română 1k／5k／10k／25k／50k／100k／200k 七个独立规模入口。Typebar 原创确定性索引词流保持 1,000／5,000／10,000／25,000／50,000／100,000／200,000 个唯一项及各档固定长度、标点和非 ASCII 数量；七项使用 `ro`／`ro-RO`、可选简化输入和 Zipf 未知语义，贯通四档自有引语及全部数据面，且不加入默认多语混排。普通出题只生成请求项，与固定词表精确交集为零；机器总账更新为 446／254／192／0。

2026-09-09 `TST-147`：新增 Polski 2k／5k／10k／20k／40k／200k 六个独立规模入口。Typebar 原创确定性索引词流保持 2,338／5,000／10,000／20,000／40,000／199,979 个唯一项及各档固定长度、大小写、标点和非 ASCII 数量；六项使用 `pl`／`pl-PL`、可选简化输入和 Zipf 未知语义，贯通四档自有引语及全部数据面，且不加入默认多语混排。普通出题只生成请求项，与固定词表精确交集为零；机器总账更新为 446／260／186／0。

2026-09-09 `TST-148`：新增 Беларуская 1k／5k／10k／25k／50k／100k 六个独立规模入口。Typebar 原创确定性西里尔索引词流保持 997／5,044／10,725／24,133／52,817／106,381 个唯一项及各档固定长度、大小写、标点和非 ASCII 数量；六项使用 `be`／`be-BY`、逐档简化输入语义和 Zipf 未知状态，贯通四档自有引语及全部数据面，且不加入默认多语混排。普通出题只生成请求项，与固定词表精确交集为零；机器总账更新为 446／266／180／0。

2026-09-09 `TST-149`：新增 Русский 1k／5k／10k／25k／50k／375k 六个独立规模入口。Typebar 原创确定性西里尔索引词流保持 996／4,971／9,996／26,037／51,682／376,092 个唯一项及各档固定长度、大小写、标点、多词和非 ASCII 数量；六项使用 `ru`／`ru-RU`，1k／5k 支持 Zipf，只有 5k 禁用简化输入。它们贯通四档自有引语及全部数据面且不加入默认多语混排；普通出题只生成请求项，与固定词表精确交集为零，机器总账更新为 446／272／174／0。

2026-09-09 `TST-150`：新增 Português 1k／3k／5k／320k／550k 五个独立规模入口。Typebar 原创确定性索引词流保持 1,000／3,043／5,665／318,601／558,207 个唯一项及各档固定长度、大小写、标点、多词、数字、符号和非 ASCII 数量；五项使用 `pt` 百科路径，并分别保持 `pt-BR`／`pt-PT`／`pt` 朗读标识，5k 与两档大型入口禁用简化输入。它们贯通四档自有引语及全部数据面且不加入默认多语混排；普通出题只生成请求项，与固定词表精确交集为零，机器总账更新为 446／277／169／0。

2026-09-09 `TST-151`：新增 Français 600k 独立规模入口。Typebar 原创确定性索引词流保持 633,941 个唯一项、1–33 字符、0 个大写项、34 个标点项、0 个多词或数字项及 282,793 个非 ASCII 项；使用 `fr`／`fr-FR`、可选简化输入和 Zipf 未知语义，贯通四档自有引语与全部数据面且不加入默认多语混排。普通出题只生成请求项，与固定词表精确交集为零，机器总账更新为 446／278／168／0。

2026-09-09 `TST-152`：新增 العربية 10k 与 العربية المصرية 1k 两个独立规模入口。两套原创确定性阿拉伯字母索引词流保持 9,281／1,141 个唯一项及各自长度、大小写、前导／内部／双空格和全量非 ASCII 结构；两项使用 RTL 空格词界、joining-script、`ar` 百科、`ar-SA`／`ar-EG` 朗读与 Zipf 未知语义，均支持简化输入，仅 10k 沿用基础 Arabic 的自动简化偏好。它们贯通四档自有引语及全部数据面且不加入 LTR 多语混排；与固定词表逐档精确交集为零，机器总账更新为 446／280／166／0。

2026-09-09 `TST-153`：新增 한국어 1k 与 5k 两个独立规模入口。两套原创确定性 Hangul 索引词流保持 975／4,201 个唯一项、1–5／1–6 字符和全量纯 Hangul 结构；两项使用普通空格词界、joining-script、`ko` 百科、`ko-KR` 朗读并禁用简化输入与大写锁定提示。它们贯通四档自有引语及全部数据面且不重复加入默认多语混排；与固定词表逐档精确交集为零，机器总账更新为 446／282／164／0。

2026-09-09 `TST-154`：新增 ไทย 1k／5k／10k／20k／50k／60k 六个独立规模入口。原创确定性 Thai 索引词流保持 1,000／5,000／10,000／18,737／50,000／60,000 个唯一项，并逐档保持 Unicode 标量与可见字符长度、组合标记、标点、空格、数字、非字母和 Thai 区段项数量。六项使用普通空格词界、`th` 百科、`th-TH` 朗读，禁用简化输入且不标记 joining-script；它们贯通四档自有引语及全部数据面，不重复加入默认多语混排，与固定词表逐档精确交集为零。机器总账更新为 446／288／158／0。

2026-09-09 `TST-155`：新增 Norsk bokmål 1k／5k／10k／150k／600k 五个独立规模入口。原创确定性索引词流保持 1,000／5,000／10,000／142,938／614,970 个唯一项，以及逐档长度、大小写、标点、数字、非字母和非 ASCII 数量；前三档保留词频排序能力，后两档保持未知。五项使用空格词界、`no` 百科、`nb-NO` 朗读和可选简化输入，贯通四档自有引语及全部数据面，不重复加入默认多语混排，与固定词表逐档精确交集为零。机器总账更新为 446／293／153／0。

2026-09-09 `TST-156`：新增 Norsk nynorsk 1k／5k／10k／100k／400k 五个独立规模入口。原创确定性索引词流保持 1,000／5,000／9,939／104,745／410,719 个唯一项，以及逐档长度、大小写、标点、空格、数字、非字母和非 ASCII 数量；前三档保留词频排序能力，后两档保持未知。五项使用空格词界、`nn` 百科、`nn-NO` 朗读和可选简化输入，贯通四档自有引语及全部数据面，不重复加入默认多语混排，与固定词表逐档精确交集为零。机器总账更新为 446／298／148／0。

2026-09-09 `TST-157`：新增简体中文 1k／5k／10k／50k 四个独立规模入口。原创确定性按需 CJK 索引词流保持 1,000／5,000／10,000／50,000 个唯一项、逐档 2–5／2–6／2–7／2–9 字符，以及固定 Unicode 数字、标点、大写、非 ASCII 与纯 CJK 数量。四项使用无空格词界、`zh` 百科、`zh-CN` 朗读并禁用简化输入与大写锁定提示，贯通四档自有引语及全部数据面，不加入默认多语混排；与固定词表逐档精确交集为零。机器总账更新为 446／302／144／0。

2026-09-09 `TST-158`：新增繁體中文 1k／5k／10k／50k 四个独立规模入口。原创确定性按需繁体 CJK 索引词流保持 1,000／4,991／9,974／49,925 个唯一项、逐档 2–5／2–6／2–7／1–9 字符，以及固定 Unicode 数字、标点、大写、非 ASCII、纯 CJK与 50k 数字标点交叠数量。四项使用无空格词界、`zh` 百科、`zh-TW` 朗读、禁用简化输入与大写锁定提示，并保留词频排序能力；它们贯通四档自有引语及全部数据面，不加入默认多语混排，与固定词表逐档精确交集为零。机器总账更新为 446／306／140／0。

2026-09-09 `TST-159`：新增 नेपाली 1k 独立规模入口。原创按需天城文索引词流保持 1,000 个唯一项、1–7 字素、1–11 Unicode 标量与 927 个含组合标记条目；全部标量位于天城文区块，不含大写、数字、标点、空格或符号。入口使用空格词界、连写保护、`ne` 百科、`ne-NP` 朗读，禁用简化输入与大写锁定提示，贯通四档自有引语和全部服务端数据面，不加入多语混排；与固定词表精确交集为零。机器总账更新为 446／307／139／0。

2026-09-09 `TST-160`：新增 Azərbaycanca 1k 独立规模入口。原创按需拉丁索引词流保持 989 个唯一项、2–7 字符与 668 个非 ASCII 条目，且无大写、数字、标点、空格或符号。入口使用 LTR 空格词界、`az` 百科、`az-AZ` 朗读，保留简化输入与大写锁定提示，贯通四档自有引语和全部服务端数据面，不加入多语混排；与固定词表精确交集为零。机器总账更新为 446／308／138／0。

2026-09-09 `TST-161`：新增 Malagasy 1k 独立规模入口。原创按需索引词流保持 975 个唯一项、3–23 字符、5 个大写项、148 个标点项与 32 个非 ASCII 项，其中 25 项同时含标点和非 ASCII；无空格、数字或符号。入口禁用简化输入，使用默认 `en` 百科与 `en-US` 朗读，贯通四档自有引语和全部服务端数据面，不加入多语混排；与固定词表精确交集为零。

2026-09-09 `TST-162`：新增 Bahasa Melayu 1k 独立规模入口。原创按需索引词流保持 1,000 个唯一项、2–16 字符、1 个大写项、12 个标点项与 1 个内部空格项，三类互不重叠且全部为 ASCII。入口使用 `ms` 百科与 `ms-MY` 朗读，贯通四档自有引语和全部服务端数据面，不加入多语混排；与固定词表精确交集为零。机器总账更新为 446／310／136／0。

2026-09-09 `TST-163`：新增 Монгол 10k 独立规模入口。原创按需西里尔索引词流保持 9,219 个唯一项、1–18 字符、897 个大写项，全部为非 ASCII 字母且无标点、空格、数字、符号或组合标记。入口禁用简化输入，使用默认 `en` 百科与 `en-US` 朗读，贯通四档自有引语和全部服务端数据面，不加入多语混排；与固定词表精确交集为零。机器总账更新为 446／311／135／0。

2026-09-09 `TST-164`–`TST-166`：新增 Українська 1k／10k／50k 三个独立规模入口。原创按需西里尔索引词流分别保持 1,000／9,998／49,991 个唯一项、1–18／1–30／1–31 字符，以及各档大写、标点、数字与交叠聚合；全部条目含非 ASCII 且无空格、符号或组合标记。

2026-09-09 `TST-167`–`TST-169`：新增 Українська (Latin) 1k／10k／50k 三个独立规模入口。原创按需拉丁索引词流保持同档实际规模和长度，并分别保留 ASCII／非 ASCII、大写、标点、数字及交叠聚合。六个入口均禁用简化输入，使用 `uk` 百科标识与 `uk-UA` 朗读，贯通四档自有引语和全部服务端数据面，不加入多语混排；与对应固定词表精确交集为零。机器总账更新为 446／317／129／0。

2026-09-09 `TST-170`–`TST-171`：新增 Bahasa Indonesia 1k／10k 两个独立规模入口。原创按需 ASCII 索引词流分别保持 1,020／13,769 个唯一项、2–14／2–13 字符及各自大写与标点聚合；固定 1k 配置的异常 `bcp47: hu-HU` 原样映射为 `hu` 百科和 `hu-HU` 朗读，10k 使用 `id`／`id-ID`，两档均禁用简化输入。

2026-09-09 `TST-172`–`TST-173`：新增 کوردی ناوەندی 2k／4k 两个独立规模入口。原创按需 Arabic-script 索引词流分别保持 1,486／4,256 个唯一项、1–11 字符及 4／3 个内部空格项，全部含非 ASCII、无大写、标点、数字、符号或组合标记；两档保持 RTL、连写、`ckb` 百科／朗读和可选简化输入。四个入口均贯通四档自有引语和全部服务端数据面，不加入多语混排；与对应固定词表精确交集为零。机器总账更新为 446／321／125／0。

2026-09-09 `TST-174`–`TST-175`：新增 Swiss German 1k／2k 两个独立规模入口。原创按需索引词流保持 1,000／2,000 个唯一可见项、2–16／2–29 字符，以及逐档大小写、标点、空格、数字、非 ASCII 与交叠结构；两档沿用 German 四档自有引语并对全部可见／自定义内容执行 `ß` 到 `ss` 转换，接入 `de`／`de-CH`、配置分享、成绩和排行榜，不加入多语混排且按固定专用分支拒绝社区引语投稿。与对应固定可见词表精确交集为零。

2026-09-09 `TST-176`–`TST-177`：新增 Afrikaans 1k／10k 两个独立规模入口。原创按需索引词流保持 1,000／8,164 个唯一项、2–19／2–22 字符，以及逐档大小写、标点、非 ASCII 与交叠结构；仅 1k 按固定元数据禁用简化输入，两档均接入 `af`／`af-ZA`、四档自有引语和全部服务端数据面且不加入多语混排。与对应固定词表精确交集为零；机器总账更新为 446／325／121／0。

2026-09-09 `TST-178`–`TST-181`：新增 Italiano 1k／7k／60k／280k 四个独立规模入口。原创参数化按需索引词流保持 1,159／7,154／60,442／279,833 个唯一项、1–14／1–18／1–18／1–25 字符，以及逐档大小写、标点、符号和非 ASCII 聚合；四档均使用 LTR 空格词界、`it` 百科、`it-IT` 朗读、可选简化输入和 Zipf 未知语义，继承四档自有 Italian 引语并贯通全部服务端数据面，不加入多语混排。普通出题不物化大型词流，与对应固定词表逐档精确交集为零；机器总账更新为 446／329／117／0。

2026-09-09 `TST-182`–`TST-193`：新增 Esperanto 标准、X-sistemo 与 H-sistemo 各 1k／10k／25k／36k 共十二个独立规模入口。原创参数化按需索引词流保持逐档实际数量、1 字符下限、15／17／18／40 字符上限及非 ASCII 聚合；标准书写保留简化输入，X/H 禁用简化输入，Zipf 精确跟随固定元数据。十二档使用 LTR 空格词界、`en` 百科、`en-US` 朗读并按书写族继承四档自有引语，贯通服务端数据面且不加入多语混排。普通出题不物化大型词流，与对应固定词表逐档精确交集为零；机器总账更新为 446／341／105／0。

2026-09-09 `TST-194`–`TST-201`：新增 Ελληνικά 与 Greeklish 各 1k／5k／10k／25k 共八个独立规模入口。两套原创按需索引词流保持逐档实际数量、1 字符下限、16／20／21 字符上限及脚本/非 ASCII 聚合；Greek 保留简化输入与 Zipf 未知语义，Greeklish 禁用简化输入并明确不支持 Zipf，同时阻止百科希腊原文替换 Latin 练习。八档使用 LTR 空格词界、`el` 百科代码、`el-GR` 朗读并按书写族继承四档自有引语，贯通服务端数据面且不加入多语混排。普通出题不物化大型词流，与对应固定词表逐档精确交集为零；机器总账更新为 446／349／97／0。

2026-09-09 `TST-202`–`TST-209`：新增 Qırımtatarca 与 Къырымтатарджа 各 1k／5k／10k／15k 共八个独立规模入口。两套原创按需索引词流保持 1,000／5,000／10,000／15,082 项、逐档 17–20 字符上限，以及脚本、大小写、标点、非 ASCII 和标点/非 ASCII 交叠聚合。八档禁用简化输入，使用 LTR 空格词界、`crh` 百科代码、`crh-CRH` 朗读并按书写族继承四档自有引语，贯通服务端数据面且不加入多语混排。普通出题不物化整表，与对应固定词表逐档精确交集为零；机器总账更新为 446／357／89／0。

2026-09-09 `TST-210`–`TST-217`：新增 Occitan 与 Taqbaylit 各 1k／2k／5k／10k 共八个独立规模入口。两套原创按需索引词流保持逐档 1,000／2,000／5,000／10,000 项、长度和非 ASCII 聚合；Kabyle 另保持 139／299／736／1,420 个标点项及其中 35／62／179／345 个非 ASCII 交叠项。Occitan 使用 `oc`／`oc-FR` 与 Zipf 未知语义，Kabyle 使用 `kab` 并明确不支持 Zipf；八档继承各族四档自有引语，贯通服务端数据面且不加入多语混排。普通出题不物化整表，与对应固定词表逐档精确交集为零；机器总账更新为 446／365／81／0。

2026-09-09 `TST-218`–`TST-225`：新增 עברית 1k／5k／10k、فارسی 1k／5k／20k 与 اردو 1k／5k 共八个独立规模入口。三套原创按需索引词流保持 1,000／5,000／10,000、1,000／5,000／21,715 及 934／4,981 项的实际规模，并保持逐档长度、全非 ASCII、Persian 20k 的标点／空格／组合标记／格式字符及 Urdu 多词结构。八档使用 RTL 空格词界与连写保护；Hebrew 规模档按固定元数据保留简化输入，Persian 与 Urdu 禁用；三族分别继承四档自有引语，贯通服务端数据面且不加入多语混排。普通出题不物化整表，与对应固定词表逐档精确交集为零；机器总账更新为 446／373／73／0。

2026-09-09 `TST-226`–`TST-230`：新增 Tamil 1k、Telugu 1k、Bangla 10k、Hindi 1k 与 Gujarati 1k 五个独立规模入口。五套原创按需索引词流保持 951／901／9,734／999／1,004 项、标量与字素长度、组合标记，以及 Bangla 标点和 Telugu／Gujarati 内部空格结构。五档均使用 LTR 空格词界、连写保护并禁用简化输入；Tamil 1k 保持 Zipf 支持，其余为未知，分别继承各族四档自有引语并贯通服务端数据面，不加入多语混排。普通出题不物化整表，与对应固定词表逐档精确交集为零；机器总账更新为 446／378／68／0。

2026-09-09 `TST-231`–`TST-239`：新增 Turkish 1k／5k、Kazakh 1k、Kyrgyz 1k、Tatar 1k／5k／9k 与 Uzbek 1k／70k 九个独立规模入口。原创按需索引词流保持逐档实际规模、长度、大小写、标点、内部空格、非 ASCII 与交叠结构；Kazakh 1k 禁用简化输入，Tatar 三档保持 Zipf 支持，其余档保留简化输入并使用 Zipf 未知语义。九档继承各族四档自有引语和固定在线／朗读路径，贯通服务端数据面且不加入多语混排；Uzbek 70k 普通出题不物化整表，与对应固定词表逐档精确交集为零。机器总账更新为 446／387／59／0。

2026-09-09 `TST-240`–`TST-248`：新增 Czech 1k／10k、Slovak 1k／10k、Slovenian 1k／5k、Croatian 1k 与 Dutch 1k／10k 九个独立规模入口。原创按需索引词流保持逐档实际规模、长度、大小写、标点、内部空格层级、非 ASCII、数字与交叠结构；Dutch 两档禁用简化输入，Slovenian 两档保持 Zipf 支持，其余档保留简化输入与 Zipf 未知语义。九档继承各族四档自有引语和固定在线／朗读路径，贯通服务端数据面且不加入多语混排；普通出题不物化整表，与对应固定词表逐档精确交集为零。机器总账更新为 446／396／50／0。

2026-09-09 `TST-249`–`TST-257`：新增 Danish 1k／10k、Swedish 1k、Finnish 1k／10k、Estonian 1k／5k／10k 与 Icelandic 1k 九个独立规模入口。原创按需索引词流保持逐档实际规模、长度、大小写、标点、非 ASCII、数字与交叠结构；九档保留简化输入与 Zipf 未知语义，前八档使用固定本族在线／朗读路径，Icelandic 1k 按缺省元数据使用 `en`／`en-US`。九档继承各族四档自有引语、贯通服务端数据面且不加入多语混排；普通出题不物化整表，与对应固定词表逐档精确交集为零，Danish 受限授权词值未被复制。机器总账更新为 446／405／41／0。

2026-09-09 `TST-258`–`TST-266`：新增 Irish 1k、Filipino 1k、Hungarian 1k／2k、Welsh 1k、Lithuanian 1k／3k、Latvian 1k 与 Maltese 1k 九个独立规模入口。原创按需索引词流保持逐档实际规模、长度、组合标记、大小写、标点、内部空格、非 ASCII 与交叠结构；Filipino 1k 禁用简化输入，Hungarian 1k 明确不支持 Zipf，其余档保留简化输入与 Zipf 未知语义。Irish、Hungarian、Latvian 和 Maltese 数字档使用各自固定在线／朗读路径，Filipino、Welsh 与 Lithuanian 数字档按缺省元数据使用 `en`／`en-US`；九档继承各族四档自有引语、贯通服务端数据面且不加入多语混排。普通出题不物化整表，与固定词表逐档精确交集为零；机器总账更新为 446／414／32／0。

2026-09-09 `TST-267`–`TST-275`：新增 Vietnamese 1k／5k、Pinyin 1k／10k、Hausa 1k、Bemba 1k／10k、Catalan 1k 与 Frisian 1k 九个独立规模入口。原创按需索引词流保持逐档实际规模、长度、大小写、标点、多级内部空格、非 ASCII 与交叠结构；九档保留简化输入，Bemba 两档明确不支持 Zipf，其余为未知。Hausa、Bemba 与 Frisian 数字档使用固定 BCP-47 在线／朗读路径，Vietnamese、Pinyin 与 Catalan 数字档按缺省元数据使用 `en`／`en-US`；九档继承各族四档自有引语、贯通服务端数据面且不加入多语混排。普通出题不物化整表，与固定词表逐档精确交集为零；机器总账更新为 446／423／23／0。

2026-09-09 `TST-276`–`TST-284`：新增 Serbian Latin 10k、Serbian 10k、Bulgarian 1k、Bulgarian Latin 1k、Bosnian 4k、Albanian 1k 与 Macedonian 1k／10k／75k 九个独立规模入口。原创按需索引词流保持逐档实际规模、长度、拉丁或西里尔脚本、大小写、标点、多级内部空格与交叠结构；Serbian、Bulgarian 和 Macedonian 指定规模禁用简化输入，Bulgarian 两档明确不支持 Zipf，其余保持固定元数据的未知状态。仅具 BCP-47 的 Serbian 与 Bulgarian 规模档接入对应在线／朗读路径，其余按缺省 `en`／`en-US`；九档继承各族四档自有引语、贯通服务端数据面且不加入多语混排。普通出题不物化整表，与固定词表逐档精确交集为零；机器总账更新为 446／432／14／0。

2026-09-09 `TST-285`–`TST-298`：新增 Amharic 1k／5k、Armenian 1k、Western Armenian 1k、Belarusian Lacinka 1k、Hawaiian 1k、Japanese Romaji 1k、Klingon 1k、Oromo 1k／5k、Shona 1k、Tibetan 1k、English Five-Letter 1k 与 Xhosa 3k 十四个独立入口。Typebar 自写确定性词流保持固定配置的数量、唯一性、长度、字符／标量边界、标点、空格、大小写及 Ethiopic／Armenian／Tibetan 等脚本形状；元数据、四档引语、配置分享、成绩、排行榜和社区投稿贯通，规模入口不重复加入多语混排。机器总账达到 446／446／0／0；该数字表示官方配置 ID 均有独立入口，不表示复制官方词值、引语、资产或实现。

2026-09-08 `TST-02` 更正：当前单语数为一百三十六种，默认／自选 LTR 多语候选为一百二十六种。新增 Ἑλληνιστικὴ Κοινή、Pig Latin 与 Lorem Ipsum · Typebar；三者均有 Typebar 自有词流、短／中／长／超长文本，并贯通配置、预设、分享、结果、归档、混排、社区投稿、撤回、成绩和排行榜。Koine Greek 使用 `el`／`el-GR` 在线与朗读路径并保留简化输入；Pig Latin 只确定性转换 Typebar 自有英语内容，Lorem Ipsum 只使用 Typebar 自写伪拉丁内容，二者按固定 `noLazyMode: true` 禁用普通练习简化输入。未读取或导入参考内容、转换实现或资产。
2026-09-08 `TST-02` 更正：当前单语数为一百三十七种，默认／自选 LTR 多语候选为一百二十七种。新增 English · Five Letter，以 Typebar 自写的五字母词流和四档全五字母文本重建固定 `wordle` 目录的长度语义；不读取参考词值。它使用缺省 `en`／`en-US` 在线和朗读路径，保留简化输入、Zipf 未知提示，并贯通配置、预设、分享、结果、归档、混排、社区投稿、撤回、成绩和排行榜。
2026-09-08 `TST-02` 更正：当前单语数为一百三十八种，默认／自选 LTR 多语候选为一百二十八种。新增 Kokanu，以其官方语法和词典确认 LTR 拉丁转写、基础词汇与结构，四档练习文本均由 Typebar 独立编写；不读取参考词值或引语。固定 `xxs-Lat`、`orderedByFrequency: false` 与未设置 `noLazyMode` 分别映射到 `xxs`／`xxs-Lat` 在线和朗读路径、Zipf 不支持提示及可选简化输入，并贯通配置、预设、分享、结果、归档、混排、社区投稿、撤回、成绩与排行榜。
2026-09-08 `TST-02` 更正：当前单语数为一百三十九种，默认／自选 LTR 多语候选为一百二十九种。新增 Likanu；按固定元数据使用 LTR、连写提示、`xxs-Uixs`、Zipf 不支持和可选简化输入。字符与音节边界取自 Kokanu 官方书写说明，Typebar 以自写解析器确定性转换自有 Kokanu 词流和四档文本，不读取参考词值、引语或转换实现；配置、预设、分享、结果、归档、混排、社区投稿、撤回、成绩与排行榜均接受 `likanu`。
2026-09-08 `TST-02` 更正：当前单语数为一百四十二种，默认／自选 LTR 多语候选为一百三十二种。新增 English · Commonly Misspelled、English · Contractions 与 English · Double Letter；三者分别提供 Typebar 自写的正确拼写、全撇号缩写和相邻双字母词流及四档原创文本。固定 `noLazyMode: true` 与 `orderedByFrequency: false` 映射为禁用简化输入及 Zipf 不支持提示，并使用 `en`／`en-US` 在线和朗读路径；不读取参考词值，完整贯通客户端与服务端数据面。
2026-09-08 `TST-02` 更正：当前单语数为一百四十四种，默认／自选 LTR 多语候选为一百三十四种。新增 English · Legal 与 English · Medical，均提供 Typebar 自写领域词流和四档文本。Legal 使用缺省可选简化输入与 Zipf 未知提示；Medical 按固定配置禁用简化输入并显示 Zipf 不支持；两者均走 `en`／`en-US`，不读取参考词值，并完整贯通客户端与服务端数据面。
2026-09-08 `TST-02` 更正：当时单语数为一百四十五种，默认／自选 LTR 多语候选为一百三十五种。新增 English · Shakespearean，以 Typebar 自写古体词流和四档仿古文本实现，不复制莎士比亚作品或参考词值；按固定元数据禁用简化输入、显示 Zipf 未知提示并使用 `en`／`en-US`，完整贯通客户端与服务端数据面。当时浅克隆尚不能解释 `english_old`，随后已由官方引入提交确认其为 Old English 并在 `TST-134` 实现。
2026-09-08 `TST-02` 更正：当前单语数为一百四十六种，默认／自选 LTR 多语候选为一百三十六种。新增 Svenska · Å Ä Ö；固定 `swedish_diacritics` 配置只定义 `bcp47: sv-SE`，且其 261 个参考词均为 4–6 字符并包含 `å/ä/ö`。Typebar 不读取词值，以 58 个独立自写、满足同一可见约束的词及四档原创瑞典语文本实现；保留简化输入、显示 Zipf 未知提示，使用 `sv`／`sv-SE` 并完整贯通客户端与服务端数据面。
2026-09-08 `TST-02` 更正：当前单语数为一百四十七种，默认／自选 LTR 多语候选为一百三十七种。新增 Português · Acentos e cedilha；固定 `portuguese_acentos_e_cedilha` 配置只定义 `bcp47: pt-PT`，结构审计确认每个参考词均含葡萄牙语重音字母或 `ç`，但不读取词值。Typebar 以独立自写的专项词流和四档原创葡萄牙语文本实现，保留简化输入、显示 Zipf 未知提示，使用 `pt`／`pt-PT` 并完整贯通客户端与服务端数据面。
2026-09-08 `TST-02` 更正：当前单语数为一百四十八种，默认／自选 LTR 多语候选为一百三十八种。新增 Русский · Аббревиатуры；固定 `russian_abbreviations` 配置定义 `bcp47: ru-RU`、`noLazyMode: true` 与 `orderedByFrequency: false`。结构审计只确认 245 个参考 token 均为无空格、无数字的西里尔形式，223 个全大写且 240 个不超过 6 字符，不读取词值。Typebar 以 64 个独立自写短缩略词和四档原创俄语文本实现，禁用简化输入、显示 Zipf 不支持，并贯通客户端与服务端数据面。

2026-09-08 `TST-130` 更正：当前单语数为一百五十种，默认／自选 LTR 多语候选为一百四十种。新增原生与 Latynka 两种 Ukrainian endings，以独立自写词流分别保持 1–4 字符乌克兰西里尔和 1–5 字符 `a-zïğš` 边界，各自提供四档原创文本。两项按固定配置禁用简化输入，以 `en`／`en-US` 走缺省在线／朗读路径，Zipf 状态未知，并贯通客户端与服务端数据面；不读取参考词值。完整客户端 385 项与服务端 73 项测试通过，测试前后均无 Typebar 图形进程。

2026-09-08 `TST-131` 更正：当前单语数为一百五十一种，默认／自选 LTR 多语候选为一百四十一种。新增 বাংলা · অক্ষর，以 Unicode Bengali 标准区块独立编排 62 项字符练习和四档原创文本，保持固定配置的字符长度结构、`bn`／`bn-BD`、禁用简化输入、Zipf 未知与 LTR 空格提交，并贯通客户端与服务端数据面。同时把原生连写呈现守卫从 3 项修正为固定元数据要求的全部 26 项；独立组合附加符不会跨空格吞并词界，Bengali danda 作为内在 token 不会被装饰标点清理。完整客户端 386 项与服务端 73 项测试通过，测试前后均无 Typebar 图形进程；不读取参考词值或复制实现。

2026-09-08 `TST-132` 更正：当前单语数为一百五十二种，默认／自选 LTR 多语候选为一百四十二种。新增 Git 专项，以本机 Git 2.50.1 的公开命令界面和通用版本控制概念独立选取 52 项小写 ASCII 词流，并提供四档原创英文文本。词流保持固定长度分布，符号仅使用 `-`／`@`，其中 `@` 是独立的一字符 Git 引用 token；按固定 `noLazyMode` 禁用简化输入，以 `en`／`en-US` 走缺省在线／朗读路径，Zipf 状态未知，并贯通客户端与服务端数据面。完整客户端 387 项与服务端 73 项测试通过，测试前后均无 Typebar 图形进程；不读取参考词值或复制实现。

参考实现约有 197 个前端组件，并有独立的前端、后端、共享契约和趣味模式包。因此客户端“像 Monkeytype”并不能构成完整重写；`ACC`、`SYN`、`SOC`、`RANK`、`CONT` 和 `OPS` 都必须拥有我们自己的服务实现与端到端验收。

2026-09-09 `TST-02` 当前更正：自定义多语组合现提供固定 schema 全部 446 个单语言 ID，而 153 项仅作为旧配置与新建配置的默认组合。候选可按显示名或原始 ID 搜索，至少保留两项、自动去重并按选择顺序轮转；同向 RTL 组合使用原生 RTL 段落，LTR/RTL 双向组合由 macOS Unicode 双向排版，无空格和代码入口在组合中转为带空格的独立 token。大型原创索引词流按需采样，不物化整表。此前各语言条目中的“不进入多语混排”统一更正为“不加入默认组合，但可进入自选多语”；不改变既有语言 ID、默认组合或旧配置回退，也不增加网络请求或复制参考词值、代码与资产。

2026-09-09 `MET-02` 补充：结果页新增“复制慢词列表…”原生导出动作。阈值表单默认使用本轮全部正数单词 Burst 的四舍五入平均 WPM（没有有效样本时为 80），并按实际输入顺序复制严格低于阈值的目标词；重复目标保留，缺失计时排除，零 Burst 按参考可见语义纳入。非法阈值和空结果均给出明确状态，不读取参考词值、代码或资产，不改变成绩、归档或同步格式。

2026-09-09 `MET-03` 补充：开启服务端成绩发布后，结果页以独立状态显示发送中、成功或失败；失败时保留本机成绩并提供“重新发送成绩”。重试复用原成绩 UUID，服务端既有幂等提交保证不会重复累计 XP；客户端以当前结果 UUID 拒绝旧异步响应覆盖新结果，并阻止同一成绩发送中的重复点击。状态仅存在于当前结果页，不新增持久化字段；离线练习、归档与同步格式不变。

## 可追溯性规则

每个实现 PR/提交必须至少写出一个本表 ID。完成时还要补充：测试文件、人工验收步骤、数据迁移策略和隐私/离线行为。任何 ID 为“未开始”或“部分实现”时，完整重写仍未完成。
