# Typebar — Monkeytype 功能兼容重写规范

## 目标与边界

Typebar 是一个独立的原生 macOS 应用及其配套服务，目标是覆盖 Monkeytype 的所有用户可见功能，同时提供无广告桌面体验。

- 参考基线：`monkeytypegame/monkeytype` 提交 `91bd24bb8513785c7364cbea29296ff7adafac41`。
- 实现规则：不复制、不翻译、不移植原项目的源代码、测试代码、文案、图像、字体、音频、主题、词库、布局或线上数据。
- 验收规则：下表每项必须有独立实现、自动化测试和人工验收记录；任何未实现项都不能标记为完成。
- 兼容含义：功能与关键交互可用，而非像素级复制其品牌或界面。

## 架构

| 层级 | 方案 | 责任 |
| --- | --- | --- |
| macOS 客户端 | SwiftUI、SwiftData、AppKit 输入桥接 | 打字引擎、设置、历史、图表、原生菜单栏和快捷键 |
| 本地数据 | SwiftData + 加密钥匙串 | 离线结果、设置、预设、主题、账户凭据 |
| 同步服务 | 独立 API 与数据库 | 登录、跨设备同步、公开资料、好友、排行榜、挑战、内容审核 |
| 内容管线 | 自建/明确授权数据 | 词库、引语、键盘布局、主题、挑战与声音 |

## 功能兼容性矩阵

状态：`盘点完成` 仅代表已从参考项目定位功能；不代表已实现。

| 域 | 必须覆盖的能力 | 状态 |
| --- | --- | --- |
| 核心测试 | time、words、quote、zen、custom 五种测试；自定义时长/字数/文本；开始、重启、结束和放弃 | 部分实现：五种离线模式可在 UI 配置；计时可设为 5–3600 秒、字数可设为 1–1000 词；开始后可从原生控制栏明确放弃，放弃与失败不会写入完成成绩；计时、字数、引语和禅模式支持原创 English/Español/Deutsch/Français/Italiano/Português/简体中文内容；模式终止、放弃与内容工厂已测试 |
| 输入引擎 | 实时逐字符校验、退格、空格规则、长词、IME/组合输入、焦点/大小写警告、输入回放 | 部分实现：原生 AppKit 输入、退格、重启和组合输入协议已实现；输入法标记文本只更新本机组合显示，确认前绝不进入输入引擎或计分，确认时才作为一次提交插入；用户可选择关闭、在提示下方显示或临时替换当前字符，且设置会随归档持久化。练习页有独立的逐字符状态模型，显示正确、错误、当前位置、待输入和额外输入，盲打会隐藏已输入字符；四档可见范围修饰器可在空格分词练习中显示当前词，或再预读 1/2/3 个未来词，完整提示仍用于输入和计分；“遇错清除当前词”会在空格分词文本中移除当前未提交词并为每个移除字符记录回放删除事件；真实 responder 失焦时会提示点击练习区恢复输入，英文/中英混合且 Caps Lock 开启时会警告，两项可持久化开关；可选错误提示音直接使用 macOS 系统声音、默认关闭；IME 最终人工验收与回放尚未完成 |
| 难度与输入规则 | normal、expert、master；停止/删除错误、严格空格、盲打、自由输入、快速结束 | 部分实现：normal/expert/master、停止/删除错误、严格空格、隐藏额外字符、盲打、自由回退、三档信心模式与字数模式的最后一词快速结束已建模；“反向 Shift”读取 macOS 左右 Shift 物理状态并按 ANSI 键位侧判断，要求使用相反手；Y、B、6 可使用任意侧。错误手位保留实际字符，但会作为真实输入错误同步影响字形、错误数、准确率、错词复练与回放事件。错误字形可选择保持目标、替换为实际输入、在下方显示实际输入或替换并在下方显示目标，纯为本地呈现且不会改变计分或回放；信心模式开启时不可退回修复上一个错误词，最大档禁用全部退格，且与自由回退、停止/删除错误互斥；默认不可退回修改已正确提交词，但可修复错误词，开启自由回退后可修改任意已提交词；“修正后再前进”会拒绝存在错误的当前词所输入的空格，直至该词完全修正；快速结束仅在没有停止/删除错误规则时对等长度的错误最后一词生效。最低单词速度是 Typebar 自有规则，只评估正确空格提交词的实际输入速度，低于阈值会失败且不保存完成成绩；其余输入规则尚未实现 |

“反向 Shift”的“按键位图”档会用所选 Typebar 布局的原创键位表，将外设已重映射后的逻辑字符反查为 ANSI 物理位置，再执行同一左右手判定；这是对 QMK 等外部重映射的本机等价实现，仍须用真实设备验收左右 Shift 状态。

代码语言选择集现覆盖官方当前目录的 69 项（含版本/方言项，例如 Python 1k、GDScript 2、CUDA、VHDL 与 6502 Assembly）。每项都有 Typebar 原创的离线输入片段；代码练习支持换行、Tab、正确换行后的自动缩进、可选的退格反缩进、有限字数模式的完整提示结束，以及自动插入/删除也能重放的事件记录。自动化会逐一生成并完成 69 个选择。官方各语言的完整词表规模、原始代码语料与代码专属统计没有导入或复刻，因此仍不能标记为内容层面的功能完全等价。

练习显示还增加了原创的单行卷带：按词或按字符把输入位置维持在用户选择的水平位置，可关闭平滑滚动；它只改变单行提示的呈现，多行提示保持普通布局，绝不改变输入、计分或回放。关闭卷带后可启用“显示完整提示行”；它仅在无计时的字数、引语和自定义测试中展开提示完整高度，计时与禅模式继续维持有限、可滚动练习区。两项在 UI 中互斥。
| 题材与语言 | 多语言词库、标点、数字、英式拼写、混合语言、引语、个人文本及保存文本 | 部分实现：自有 English/Español/Deutsch/Français/Italiano/Português/简体中文词库、引语和个人文本；Español、Français 与 Português 原创词库含重音词，Deutsch 原创词库含变音和 ß，均支持 macOS Unicode 输入。自定义文本编辑器限制为 10,000 字符，保存、分享与归档导入会使用统一验证，可选输入完成、循环计时、循环字数或分节完成，并可保持顺序、打乱一次或随机抽取用户文本中的词。分节用 `|` 定义，用户可选择前 N 段并在练习页查看段落进度；循环提示由 Typebar 自有引擎动态扩展。七种单语各有 Typebar 自有短/中/长/超长引语；引语可按这些长度筛选、收藏、标题/文本的本机多词搜索，并可控制随机选择是否允许重复；默认用自有离线内容，用户也可明确切换到仅服务端已审核通过的社区引语。计时、字数和禅模式支持数字与相应语言标点；英文可选择自有的美式或英式拼写词表；中英混合模式交替使用 Typebar 自有两套词库，并以空格分隔词；另可从七种单语中选取至少两种、按选择顺序轮转的自定义多语组合，配置会进入预设、分享、结果和归档。自建服务的成绩榜、社区投稿和公开引语同样接受七种单语。官方完整语言规模、词表与内容更新机制尚未实现 |
| 趣味模式 | funbox 修饰器及其互斥/组合校验、挑战预设与完成条件 | 部分实现：官方 48 项 funbox 已在专项审计中逐项映射，其中 47 项达到功能等价、ASL 指语练习采用原创原生矢量提示而为部分覆盖；完整清单、差异和证据见 `OFFICIAL_FUNBOX_AUDIT.md`。修饰器互斥、持久化和挑战完成条件均由本机引擎验证；仍需持续补充真实设备人工验收。 |
| 实时反馈 | WPM、raw WPM、accuracy、burst、计时器、平滑/节奏光标、单词历史、热力图、键盘映射 | 部分实现：实时 WPM、raw WPM、准确率、错误、单词 burst、剩余倒计时与原创 ANSI QWERTY/Dvorak 下一键提示已实现，覆盖 ASCII 字母、数字与标点；选择 ANSI Dvorak/Colemak/Workman 或 French AZERTY 时，原生输入桥会按物理 ANSI keyCode 独立模拟对应字符与 Shift 符号，默认 QWERTY 和含 Option/Control/Command 的输入仍交给 AppKit，以保留系统快捷键与 IME/死键组合路径。字数模式显示已提交词/目标词，而时间模式仍显示剩余秒数；进度可用文字、迷你、隐藏或原生条形呈现，条形严格取同一时间/字数 0–1 比例；实时速度三项可选择 WPM、CPM、WPS、CPS 或 WPH 显示。用户可选择显示近 10 条同设置的本机完成成绩平均速度、准确率或两者；筛选模式、时长/词数（引语按当前内容）、标点、数字、语言、难度与简化输入，且不要求登录。完成结果页可选择固定显示两位小数：按当前速度单位格式化速度与 Raw 速度，同时将准确率显示为两位小数；本机保存、统计与同步仍保持规范 WPM；速度（含 Raw）、准确率与 burst 可分别使用文字、迷你或关闭呈现；完成词可按本机偏好保留、隐藏、淡化或以圆点显示，不改变词间空格、当前词、后续提示、输入或计分；当前目标字符可按本机设置显示为条形、下划线或块状光标，且会随归档迁移；burst 按单词首键到提交空格计算，当前词满两键后可预览，指标下方会展示最多 8 个近期已提交词的本机会话速度条带；结果页可选择按本轮实际输入事件形成的 Typebar 自有五分位为已尝试词着色并显示速度图例，没有可测间隔的词保持中性；同一份本机回放还会按秒重建最多 120 秒的 WPM、Raw、Burst 与错误轨迹，三项可单独开关并持久化；可持久化节奏引导可选择自定义速度、同模式同语言的本地个人最佳、平均、当日平均或本次运行中的上一轮速度，在文本中以可选条形、下划线或块状第二标记显示目标进度；“重开后沿用上一轮节奏一次”只影响下一次重开，不会写入成绩或云端。更完整的平滑光标、非 ANSI 和非 ASCII 映射尚未实现 |
| 结果 | 结果页、统计、个人最佳、重复测试、错词练习、标签、结果筛选、导出/删除 | 部分实现：完成后原生结果页展示关键指标、逐词目标/输入历史、重开、历史入口和“导出”菜单；单词历史默认折叠，仍可手动展开，用户也可持久化选择在完成后自动展开。用户可复制仅含本机成绩的文字结果、本机回放重建的实际输入，或将 Typebar 原创、随当前主题配色的结果卡复制为 PNG、通过 macOS 保存面板导出 PNG；结果卡的准确度刻度由本机准确率派生，不截取或复刻参考网页的 UI、资产或水印。回放缺失时不允许复制实际输入，避免以提示或统计推断用户内容。结果页的“重复本轮”从初始会话模板新开完全相同的一轮：保留配置、提示和自定义循环文本源，清除已输入内容、计时与回放；“再来一次”仍生成当前选择下的新内容。以空格分词的结果可从错词菜单启动去重的错词练习，或按每次实际尝试以“前词 + 错词”形式启动上下文练习；未达到的后续词从不参与。若本轮至少有五个可测词速，也可依本机最慢四分位目标词启动有限的加权慢词练习，未测得速度的词不会被纳入。用户可关闭“保存完成成绩”，此时仍可查看/导出本次结果，却不会写入本机历史、统计、同步或排行榜。已保存完成结果会保存自有插入/删除事件和提示快照，结果页可播放、暂停、拖动和重置输入回放；控制栏的“弱项训练”会只分析本机已完成回放中的真实逐事件偏差，并从 Typebar 自有词库生成关联词练习，绝不上传成绩或使用参考词库；可编码结果、本地持久化和可删除的本地历史列表已实现；历史列表和详情页会标记个人最佳，结果可添加/移除受约束的本地标签；设置、成绩、预设与自定义文本可在原生界面导入/导出并去重合并；更完整的回放分析等其余尚未实现 |
| 统计 | 测试历史、趋势/柱状/直方图、每日活动、连续天数、汇总指标 | 部分实现：本地历史可组合按模式、语言、标签和个人最佳筛选，并可将条件以名称保存为仅本机的 SwiftData 筛选预设，重新应用或删除；汇总指标、WPM 趋势图、WPM 固定区间直方图、近 28 日完成次数/练习分钟柱状图、按日活动聚合、近 12 周热力图和连续天数均跟随筛选；“速度图从零开始”默认开启，关闭后历史速度趋势图与结果轨迹均按实际数据范围缩放，避免把计数和错误图错设为非零起点；更多图表交互尚未实现 |
| 外观 | 内置与自定义主题、收藏、随机/按系统切换主题、背景、字体、行宽、颜色和动画设置 | 部分实现：纸白、午夜、林地三套原创内置主题可持久化切换；用户可创建、应用和删除原创的本地 RGB 自定义主题，并随归档迁移；内置和自定义主题可收藏并从设置中的收藏栏一键应用，删除自定义主题会同步取消收藏；可选择按 macOS 深浅色自动切换原创纸白/午夜主题，或每次新测试随机选择原创内置主题；练习页可选择等宽、圆角、衬线或默认 macOS 系统字体设计，以及紧凑/标准/宽/自适应行宽并按字体缩放。背景可选 Typebar 自有的纯色、主题色光晕或细网格；光晕可以减少动态效果，且遵从 macOS 辅助功能。字体、背景选择与节奏引导设置会随归档迁移；更多光标配置尚未实现 |
| 声音与辅助 | 键击/错误/警告声音、音量、键盘布局与按键提示、可读性设置 | 部分实现：可选 macOS 系统键击音和错误提示音共用 0–100% 音量，默认关闭；键击音仅在输入实际被接受后触发，错误音只在错误数增加时触发，缺少某个系统声音时不会影响输入或计分。控制栏可显式用 macOS 内置语音朗读当前提示，按选择语言使用系统 locale，重开时立即停止且文本不离开设备。键盘布局与下一键提示已实现。警告声音和更多可读性设置尚未实现 |
| 预设与操作 | 配置预设、导入/导出、命令面板、快捷键、可分享的测试配置 | 部分实现：⌘R 始终可重开，另可在本机设置选择关闭/Esc/Tab/Enter 快速重开；完整测试预设及自定义文本可本地保存、列出、应用和删除；设置、成绩、预设和文本已可导入/导出；原生命令面板可经工具栏或 ⇧⌘K 搜索操作；可复制或导入自有 `typebar://test` 配置链接，链接仅含测试配置、引语 ID 或自定义文本；参考项目全部快捷键和网页链接互操作尚未实现 |
| 身份与账户 | 注册、登录、密码重置/更新、邮箱更新、资料、令牌/第三方身份管理 | 部分实现：自建服务与原生设置页支持注册、登录、会话恢复、当前资料、退出、密码更新、邮箱更新和账户删除；删除要求当前密码和原生确认，会清除自建服务内该账户的会话、成绩、同步、关系、屏蔽与投稿，同时保留本机练习历史。密码更新验证当前密码并重哈希新密码；邮箱更新验证格式和唯一性；两者均撤销该账户既有会话并回传仅当前设备保存的新令牌。用户可选择从所有自建服务全局/好友 WPM 和 XP 榜隐藏；服务端即时排除既有与新成绩，而成绩、XP、同步和本机历史仍被保留。服务端会从已验证成绩重算总 XP，并在当前账户和不含邮箱的公开资料中返回。练习历史页会从本机完成成绩即时派生四项原创成就，不上传或替代 XP；密码重置、第三方身份管理、公开成就资料和真实账户端到端人工验收尚未完成 |
| 云端同步 | 配置、结果、标签、个人主题与预设的跨设备同步和冲突策略 | 部分实现：登录自建服务后，原生同步面板可上传本机归档，或按去重规则拉取并合并远端归档；分页、细粒度变更、冲突副本和端到端人工验收尚未完成 |
| 社交 | 公开资料、好友请求、好友结果/榜单、屏蔽、通知、举报与站内信 | 部分实现：自建服务与原生客户端支持公开展示名搜索、公开资料、好友请求、接受、取消、解除、屏蔽与解除屏蔽；好友请求和接受会产生仅接收者可读取、可标记已读的站内通知。已接受好友可从原生好友面板打开一对一会话，发送、刷新和读取最多 1,000 字的私有消息；非好友、第三方与被屏蔽关系会被服务端拒绝，屏蔽还会删除双方会话记录。登录用户可在公开资料卡提交私有资料举报，也可对已批准的社区引语选择原因并填写说明；服务端拒绝自举报与重复同类举报，且不会通知目标或自动处罚。原生设置页提供两类审核工作台：部署密钥仅停留在当前内存会话，资料队列只显示目标公开资料、举报上下文和状态，引语队列只显示引语与举报上下文，均不显示举报者身份；资料举报只能标为已处理或驳回，不会自动处置账户。好友结果/榜单、推送与端到端人工验收尚未完成 |
| 排行榜 | 全局、每日、每周 XP、好友榜、筛选、排名与反作弊结果验证 | 部分实现：独立服务可接收带基本范围/时间校验的成绩，并提供模式/语言及服务端当天/ISO 本周筛选的全局或好友 WPM 榜；每位用户在当前筛选下仅保留最佳成绩并获得对应名次，好友榜只包含本人和已接受好友，原生客户端可切换浏览。全局/好友 XP 榜按服务端 ISO 周聚合，最多返回 100 人；XP 从已验证成绩的练习时长、准确率和模式重算，禅模式为零，重复结果 ID 不重复计算。限速已实现；赛季奖励与更完整反作弊尚未实现 |
| 内容社区 | 引语浏览/收藏/评分/提交/审核/举报 | 部分实现：原创离线引语可按长度浏览、收藏，并作本机“不错”/“不适合”评分或清除评分；登录用户可提交引语等待自建服务审核，部署者可凭显式配置的审核密钥读取按状态筛选的队列并批准或拒绝，客户端可选择浏览和练习已批准内容。原生设置页已提供审核工作台，审核密钥不落盘，队列按状态展示引语、署名和举报上下文而不显示举报者身份，待审核投稿可批准或拒绝。资料举报也有独立队列，显示目标公开资料与举报上下文，可标为已处理或驳回，但不会自动处置账户。登录用户还可对他人已审核投稿给出单一“不错”或“不适合”社区评价、改评或撤销；列表只显示聚合票数，评分者身份不公开，账户或投稿删除会清除关联评分。已批准投稿支持填写说明的私有内容举报。细粒度审核角色和申诉流程尚未完成 |
| 系统能力 | 菜单栏、全局唤起、离线优先、网络失败降级、通知、备份恢复 | 部分实现：菜单栏可激活应用并将可见主窗口恢复到前台；用户可明确启用 `⌃⇧Space` 全局唤起，应用只在取得 macOS 辅助功能权限后监听，未授权会显示系统设置路径。离线运行与本地 `.app` 打包已验证；已实现自建服务内好友关系通知及原生通知中心；系统推送和更完整的网络降级尚未实现 |
| 运营/管理 | 服务配置、公告、审核、速率限制、审计/指标、数据保留与删除 | 部分实现：自建服务已有审核队列、请求限速和启动时维护模式；设置 `TYPEBAR_MAINTENANCE_MODE=true` 后，状态和只读查询可继续，任何写入会得到可重试的 `503`，原生客户端会明确提示本机离线练习不受影响。公告、持久化审计/指标、数据保留策略、分布式限速和完整反作弊仍未实现 |

## 交付门槛

1. 核心、输入、结果、设置、内容与统计必须在断网状态下可用。
2. 每个远程模块必须具备等价的自建 API 和客户端流程；不能依赖 Monkeytype 的生产接口、账号、排行榜或内容。
3. 每条矩阵能力要关联：需求编号、独立测试、人工验收、实现位置与完成状态。
4. 上述条目全数验收前，项目保持进行中，不以“核心功能可用”作为完整重写的结论。

## 现有验证证据

| 模块 | 证据 | 覆盖 |
| --- | --- | --- |
| 核心会话状态机 | `Tests/TypebarTests/TypingEngineTests.swift` | 五种模式的结束条件、难度失败、输入规则、统计计算、结果值对象与超界输入 |
| 离线内容工厂 | `Tests/TypebarTests/TypingEngineTests.swift` | 五类模式按各自长度/文本语义生成 prompt |
| 结果存储 | `Tests/TypebarTests/TypingEngineTests.swift` | 内存 SwiftData 容器中的结果与配置往返 |
| 偏好设置 | `Tests/TypebarTests/TypingEngineTests.swift`、打包应用的设置窗口 | 难度、输入规则、字体、主题、练习背景和减少动态效果可持久化；设置 UI 可实际切换三种原生背景，且光晕模式显示减少动态效果开关 |
| 测试预设 | `Tests/TypebarTests/TypingEngineTests.swift`、打包应用的预设库 | 配置、规则、引语/自定义文本能作为一个完整预设往返存储 |
| 数据迁移 | `Tests/TypebarTests/TypingEngineTests.swift`、打包应用的“数据迁移”界面 | 版本化设置、成绩、预设和自定义文本归档往返；导入去重合并；v1 归档兼容；导入/导出文件选择器已人工验收（已取消，未写入真实文件） |
| 本地统计 | `Tests/TypebarTests/TypingEngineTests.swift` | 完成次数、均速、最佳、准确率、累计时长，以及 WPM 固定区间分布（包括空档区间）聚合 |
| 挑战验收 | `Tests/TypebarTests/TypingEngineTests.swift` | 原创离线挑战的通过与全部未满足条件都可被独立验证；挑战标记可随测试配置编码，并与历史配置兼容 |
| 构建 | `swift test`、`swift build` | macOS Swift 包能编译且 118 项客户端测试通过；独立 Vapor 服务的 40 项自动化测试通过 |
| 运行时 UI | 打包的 `Typebar.app` 辅助功能树与截图 | 焦点输入、时间/字数模式、实时指标、可滚动长文本区，以及正确输入/错误/退格/Esc 重开；2026-09-02 已人工切换“光晕”背景，确认减少动态效果开关状态和练习页主题色光晕绘制，检查后恢复为纯色 |
