# Typebar — Monkeytype 功能兼容重写规范

## 目标与边界

Typebar 是一个独立的原生 macOS 应用及其配套服务，目标是覆盖 Monkeytype 的所有用户可见功能，同时提供无广告桌面体验。

- 参考基线：`monkeytypegame/monkeytype` 提交 `91bd24bb8513785c7364cbea29296ff7adafac41`。
- 实现规则：不复制、不翻译、不移植原项目的源代码、测试代码、文案、图像、字体、音频、主题、词库、布局或线上数据。
- 验收规则：下表每项必须有独立实现、自动化测试和人工验收记录；任何未实现项都不能标记为完成。
- 兼容含义：功能与关键交互可用，而非像素级复制其品牌或界面。

## 架构

| 层级 | 方案 | 责任 |
| --- | --- | --- |
| macOS 客户端 | SwiftUI、SwiftData、AppKit 输入桥接 | 打字引擎、设置、历史、图表、原生菜单栏和快捷键 |
| 本地数据 | SwiftData + 加密钥匙串 | 离线结果、设置、预设、主题、账户凭据 |
| 同步服务 | 独立 API 与数据库 | 登录、跨设备同步、公开资料、好友、排行榜、挑战、内容审核 |
| 内容管线 | 自建/明确授权数据 | 词库、引语、键盘布局、主题、挑战与声音 |

## 功能兼容性矩阵

状态：`盘点完成` 仅代表已从参考项目定位功能；不代表已实现。

| 域 | 必须覆盖的能力 | 状态 |
| --- | --- | --- |
| 核心测试 | time、words、quote、zen、custom 五种测试；自定义时长/字数/文本；开始、重启、结束和放弃 | 部分实现：五种离线模式可在 UI 配置；计时可设为 5–3600 秒、字数可设为 1–1000 词；开始后可从原生控制栏明确放弃，放弃与失败不会写入完成成绩；自定义保存文本可选长文本恢复：以完整词边界的原始字符偏移保留进度，选择时固定为完成全文，中止时保存剩余位置、完成时复位，旧归档兼容为普通文本；保存长文本、时长至少 1 小时或禅模式进行中时，命令面板也会提供需确认的中止入口；计时、字数和引语模式支持原创 English/Español/Deutsch/Français/Italiano/Português/简体中文内容；禅模式不生成提示，呈现用户本机自由输入，空格/换行提交词、Tab 保留为文本，Shift+Enter 明确结束；模式终止、放弃与内容工厂已测试 |
| 输入引擎 | 实时逐字符校验、退格、空格规则、长词、IME/组合输入、焦点/大小写警告、输入回放 | 部分实现：原生 AppKit 输入、退格、重启和组合输入协议已实现；输入法标记文本只更新本机组合显示，确认前绝不进入输入引擎或计分，确认时才作为一次提交插入；用户可选择关闭、在提示下方显示或临时替换当前字符，且设置会随归档持久化。练习页有独立的逐字符状态模型，显示正确、错误、当前位置、待输入和额外输入，盲打会隐藏已输入字符；四档可见范围修饰器可在空格分词练习中显示当前词，或再预读 1/2/3 个未来词，完整提示仍用于输入和计分；“遇错清除当前词”会在空格分词文本中移除当前未提交词并为每个移除字符记录回放删除事件；真实 responder 连续失焦满 1 秒时才提示点击练习区恢复输入，重新获焦会取消等待；英文/中英混合且 Caps Lock 开启时会警告，两项可持久化开关；可选四种 macOS 系统错误音型，默认关闭，错误数实际增加才会触发；IME 最终人工验收与回放尚未完成 |
| 难度与输入规则 | normal、expert、master；停止/删除错误、严格空格、盲打、自由输入、快速结束 | 部分实现：normal/expert/master、停止/删除错误、严格空格、隐藏额外字符、盲打、自由回退、三档信心模式与字数模式的最后一词快速结束已建模；遇错停下可选字符档（拒绝错误按键）和单词档（允许继续输入、阻止错误词的空格提交）。遇错删除可选字符档（退回一个已接受字符）、单词档（清空当前词）及两个硬档（在新词首个错误时返回上一词）；旧布尔偏好会无损迁移为字符档，所有档位会进入设置、预设、分享、结果与归档，且与信心模式和另一类错误规则互斥。“反向 Shift”读取 macOS 左右 Shift 物理状态并按 ANSI 键位侧判断，要求使用相反手；Y、B、6 可使用任意侧。错误手位保留实际字符，但会作为真实输入错误同步影响字形、错误数、准确率、错词复练与回放事件。错误字形可选择保持目标、替换为实际输入、在下方显示实际输入或替换并在下方显示目标，纯为本地呈现且不会改变计分或回放；信心模式开启时不可退回修复上一个错误词，最大档禁用全部退格，且与自由回退、停止/删除错误互斥；默认不可退回修改已正确提交词，但可修复错误词，开启自由回退后可修改任意已提交词；“修正后再前进”会拒绝存在错误的当前词所输入的空格，直至该词完全修正；快速结束仅在没有停止/删除错误规则时对等长度的错误最后一词生效。最低单词速度可关闭，或按固定阈值、按目标词长弹性降低的阈值校验每个具可测 burst 的普通空格提交词，以及禅模式中由空格或换行提交的本机输入词；低于阈值会失败且不保存完成成绩；其他输入规则仍持续审计。 |

有限引语和自定义文本的完成条件会保留等长但错误的最后一个词，供用户修正；仅当最后一词正确、输入词提交分隔符，或在未启用停止/删除错误时明确开启快速结束，测试才会结束。

原生 AppKit 输入桥将一次粘贴或已确认的多字符输入法提交逐字符计分和写入回放，但只在该次提交的最后一个字符评估 expert/master 难度和最低单词 burst；逐键输入继续逐键评估。

输入单个 Unicode 省略号而提示期待三个普通句点时，输入桥会展开并分别记录三个句点；提示本身期待省略号时保持原字符。

输入桥还支持 macOS 的删除上一个词命令：它会清空当前未提交词，或在自由回退/前一词错误时清除前一提交词；信心模式、最大信心模式和默认模式下正确的已提交词均不可回退。

快速重开键与提示输入冲突时会要求 Shift：包含 Tab 的提示中普通 Tab 输入、Shift+Tab 重开；包含换行的提示中普通 Enter 输入、Shift+Enter 重开。字数至少 1,000 词或时长至少 15 分钟时，Esc/Tab 重开还必须配合 Shift；Enter 不再重开，而无论快速重开选项如何，双击 Shift+Enter 都会中止并显示仅当前窗口可见的未保存结果，⌘R 不受影响。禅模式保留 Shift+Enter 作为明确结束命令。中止是独立结果状态，不会进入本机历史、统计、同步、发布、挑战或错词训练。

“反向 Shift”的“按键位图”档会用所选 Typebar 布局的原创键位表，将外设已重映射后的逻辑字符反查为 ANSI 物理位置，再执行同一左右手判定；这是对 QMK 等外部重映射的本机等价实现，仍须用真实设备验收左右 Shift 状态。

代码语言选择集现覆盖官方当前目录的 69 项（含版本/方言项，例如 Python 1k、GDScript 2、CUDA、VHDL 与 6502 Assembly）。每项都有 Typebar 原创的离线输入片段；代码练习支持换行、Tab、正确换行后的自动缩进、可选的退格反缩进、有限字数模式的完整提示结束，以及自动插入/删除也能重放的事件记录。自动化会逐一生成并完成 69 个选择。官方各语言的完整词表规模、原始代码语料与代码专属统计没有导入或复刻，因此仍不能标记为内容层面的功能完全等价。

练习显示还增加了原创的单行卷带：按词或按字符把输入位置维持在用户选择的水平位置，可关闭平滑滚动；它只改变单行提示的呈现，多行提示保持普通布局，绝不改变输入、计分或回放。关闭卷带后可启用“显示完整提示行”；它仅在无计时的字数、引语和自定义测试中展开提示完整高度，计时与禅模式继续维持有限、可滚动练习区。两项在 UI 中互斥。无空格修饰器会移除可见分隔符但保留每个可安全映射的原始词结束位置及渲染后词段：字数进度、实时 Burst、固定/弹性最低单词速度、循环自定义提示、结果词历史、热力图、错词与慢词复练均在这些边界工作，不会把整段扁平文本误作一个词；无法安全一一映射的文本变换会保守退回字符级结果。
| 题材与语言 | 多语言词库、标点、数字、英式拼写、混合语言、引语、个人文本及保存文本 | 部分实现：自有 English/Español/Deutsch/Français/Italiano/Português/简体中文词库、引语和个人文本；Español、Français 与 Português 原创词库含重音词，Deutsch 原创词库含变音和 ß，均支持 macOS Unicode 输入。自定义文本编辑器限制为 10,000 字符，保存、分享与归档导入会使用统一验证，可选输入完成、循环计时、循环字数或分节完成，并可保持顺序、打乱一次或随机抽取用户文本中的词。分节用 `|` 定义，用户可选择前 N 段并在练习页查看段落进度；循环提示由 Typebar 自有引擎动态扩展。七种单语各有 Typebar 自有短/中/长/超长引语；引语可按这些长度筛选、收藏、标题/文本的本机多词搜索，并可控制随机选择是否允许重复；默认用自有离线内容，用户也可明确切换到仅服务端已审核通过的社区引语。启用“诗性散文”或“知识短文”时，空格分词的 time/words 练习会原生获取随机 PoetryDB 或 Wikipedia 内容并显示来源；超时、失败和输入已经开始时保留原创离线流。计时和字数模式支持数字与相应语言标点；禅模式只使用用户本机输入，不生成任何词库或提示内容；英文可选择自有的美式或英式拼写词表；中英混合模式交替使用 Typebar 自有两套词库，并以空格分隔词；另可从七种单语中选取至少两种、按选择顺序轮转的自定义多语组合，配置会进入预设、分享、结果和归档。自建服务的成绩榜、社区投稿和公开引语同样接受七种单语。官方完整语言规模、词表与内容更新机制尚未实现 |
| 趣味模式 | funbox 修饰器及其互斥/组合校验、挑战预设与完成条件 | 部分实现：官方 48 项 funbox 已在专项审计中逐项映射，均达到用户可见功能等价；ASL 指语练习采用原创原生矢量提示，覆盖手势、J/Z 轨迹、状态和键盘计分，但不逐像素复刻官方专用字体。完整清单、差异和证据见 `OFFICIAL_FUNBOX_AUDIT.md`。修饰器互斥、持久化和挑战完成条件均由本机引擎验证；仍需持续补充真实设备人工验收。 |
| 实时反馈 | WPM、raw WPM、accuracy、burst、计时器、平滑/节奏光标、单词历史、热力图、键盘映射 | 部分实现：实时 WPM、raw WPM、准确率、错误、单词 burst、剩余倒计时与原创 ANSI/ISO 键盘提示已实现，覆盖当前十五种原生布局的字母、数字、区域符号与重音字符。键盘图与官方 `layout` 输入模拟已拆分：默认所有字符、IME、死键和 Option 图层均交给 macOS 当前输入法；用户明确选择模拟时，原生输入桥才按物理 keyCode 生成目标布局字符。视觉键盘图可选内置布局、直接由 macOS 当前输入源的 Unicode 键盘数据生成（普通、Shift、Option、Shift+Option 图层，输入源切换通知会立即刷新），或最多二十份用户创建、应用、删除并归档的原创 Unicode 四行图；它们不影响输入，Layout Fluid 仍只切换可模拟的内置图。系统没有提供 Unicode 布局数据时安全回退内置图。Layout Fluid 会随阶段同时切换键盘图和模拟输入，且显式 QWERTY 模拟也会生效。Option/Control/Command 始终保留给 AppKit。字数模式显示已提交词/目标词，而时间模式仍显示剩余秒数；进度可用文字、迷你、隐藏或原生条形呈现，条形严格取同一时间/字数 0–1 比例；实时速度三项可选择 WPM、CPM、WPS、CPS 或 WPH 显示。用户可选择显示近 10 条同设置的本机完成成绩平均速度、准确率或两者；筛选模式、时长/词数（引语按当前内容）、标点、数字、语言、难度、简化输入和活动标签，且不要求登录。完成结果页可选择固定显示两位小数：按当前速度单位格式化速度与 Raw 速度，同时将准确率显示为两位小数；本机保存、统计与同步仍保持规范 WPM；速度（含 Raw）、准确率与 burst 可分别使用文字、迷你或关闭呈现；完成词可按本机偏好保留、隐藏、淡化或以圆点显示，不改变词间空格、当前词、后续提示、输入或计分；当前目标与节奏标记可独立设为关闭、条形、轮廓、下划线、块状或三种原创矢量标记，常规提示以 TextKit 测量层按 150/100/85 ms 三档动画定位，并随归档迁移；burst 按单词首键到提交空格计算，当前词满两键后可预览，指标下方会展示最多 8 个近期已提交词的本机会话速度条带；结果页可选择按本轮实际输入事件形成的 Typebar 自有五分位为已尝试词着色并显示速度图例，没有可测间隔的词保持中性；同一份本机回放还会按秒重建最多 120 秒的 WPM、Raw、Burst 与错误轨迹，三项可单独开关并持久化；可持久化节奏引导可选择自定义速度、同模式同语言的本地个人最佳、活动标签个人最佳、平均、当日平均或本次运行中的上一轮速度；“重开后沿用上一轮节奏一次”只影响下一次重开，不会写入成绩或云端。官方当前 239 份命名键盘图资产没有被打包或导入；以 macOS 当前输入源和用户自写原创视觉图作为原生替代路径。完整逐项审计见 `OFFICIAL_CONFIG_AUDIT.md`。 |
| 闪现进度 | 限时倒计时的文字/迷你闪现呈现 | 已实现：在 Typebar 原生实时指标中可选择“闪现文字”或“闪现迷你”。仅配置了时长的测试会在剩余秒数为 15 的倍数时展示倒计时；字数、引语和输入完成等无时长测试始终显示进度。它们只影响呈现，沿用同一会话倒计时、计分和结果数据；真实设备节奏观察仍待人工验收。 |
| 关闭光标标记 | 主光标与节奏光标的无标记呈现 | 已实现：两处样式选择均提供“关闭”。关闭后保留当前/待输入字符的普通呈现，不再绘制主光标或第二节奏标记；偏好会持久化并随归档迁移。常规提示使用独立 TextKit 光标测量层，支持关闭、150/100/85 ms 平滑移动、轮廓和三种原创矢量标记；卷带、手语和旋转文字保持保守静态回退，待实机统一检查。 |
| 结果 | 结果页、统计、个人最佳、重复测试、错词练习、标签、结果筛选、导出/删除 | 部分实现：完成后原生结果页展示关键指标、逐词目标/输入历史、重开、历史入口和“导出”菜单；单词历史默认折叠，仍可手动展开，用户也可持久化选择在完成后自动展开。用户可复制仅含本机成绩的文字结果、本机回放重建的实际输入，或将 Typebar 原创、随当前主题配色的结果卡复制为 PNG、通过 macOS 保存面板导出 PNG；结果卡的准确度刻度由本机准确率派生，不截取或复刻参考网页的 UI、资产或水印。历史页可将当前筛选后的本机成绩以 Typebar 原生 CSV 导出，使用 UTC 文件名和 RFC 4180 转义，包含成绩、配置、标签及本机派生稳定度，但绝不包含提示、实际输入或回放。回放缺失时不允许复制实际输入，避免以提示或统计推断用户内容。完成页还会显示本机“今日练习”时长和次数，合并当天已保存成绩与本次运行中未保存的成绩、按 ID 去重，跨日成绩不计入。已保存完成结果会立即提供与历史详情相同的本地标签编辑器；最多五个、每个 1–24 字符，按大小写与重音无关去重，修改直接写入同一条本机历史，未保存结果不会显示会误导持久化的标签入口。结果页的“重复本轮”从初始会话模板新开完全相同的一轮：保留配置、提示和自定义循环文本源，清除已输入内容、计时与回放；“再来一次”仍生成当前选择下的新内容。错词菜单只依据实际错误输入事件生成：最多 20 个去重目标按错误次数重复练习，或选取最多 10 个已尝试目标按错误次数以“前词 + 错词”启动上下文练习；按错误次数展开的候选段落会逐轮洗牌，并以入选候选数的 5 倍段数构成有限练习，因此频次既是权重、每个候选也会反复出现。已修正或删空的错误仍参与，正确但未完成的词和未达到的后续词从不参与。简体中文会从 Typebar 自有词库中识别实际输入过的无空格短词并去重复练，标点、未知自定义片段和未到达尾部不记为错词，复练文本不插入空格。错词练习完成后，“再来一次”会恢复进入练习前的配置，而“重复本轮”仍使用同一份错词提示；用户若手动改到另一非自定义模式，则取消临时恢复、将新选择作为下一次“再来一次”的来源。若本轮至少有五个可测词速，也可依本机最慢四分位目标词启动有限的加权慢词练习，未测得速度的词不会被纳入。用户可关闭“保存完成成绩”，此时仍可查看/导出本次结果，却不会写入本机历史、统计、同步或排行榜。已保存完成结果会保存自有插入/删除事件和提示快照，结果页可播放、暂停、拖动和重置输入回放；控制栏的“弱项训练”会只分析本机已完成回放中的真实逐事件偏差，并从 Typebar 自有词库生成关联词练习，绝不上传成绩或使用参考词库；可编码结果、本地持久化和可删除的本地历史列表已实现；历史列表和详情页会标记个人最佳，设置、成绩、预设与自定义文本可在原生界面导入/导出并去重合并；更完整的回放分析等其余尚未实现 |
| 统计 | 测试历史、趋势/柱状/直方图、每日活动、连续天数、汇总指标 | 部分实现：本地历史可组合按模式、语言、标签、难度、标点、数字、实际完成引语长度、时长、字数、修饰器、个人最佳以及最近 24 小时/7 天/30 天/90 天/全部时间筛选；时长和字数分别支持 4 个参考标准档和“自定义”档，允许任意组合或全部取消；标点和数字可分别限制为包含、不包含或全部；修饰器允许任意多选，并可单独选择“无修饰器”，以命中任一选中修饰器或空集合。原生引语按 Typebar 自有元数据识别长度，社区引语按本机既有字符阈值分类，不把“全部长度”的练习设置错当作历史事实；无法解码配置的旧记录只在不限制引语长度或修饰器时保留。所有条件均可按名称保存为仅本机的筛选预设，重新应用或删除；旧预设缺少时间范围、难度、标点、数字、引语长度、时长、字数或修饰器时安全按全部时间、全部难度且不限制内容选项和档位读取。汇总指标、速度/准确率趋势、速度 10/100 次均值、速度个人最佳包络、WPM 固定区间直方图、近 28 日完成次数/练习分钟柱状图、按日活动聚合、近 12 周热力图和连续天数均跟随筛选。历史页的速度、准确率、10 次均值和 100 次均值可独立开关并持久化；每小时变化仅使用本机完成成绩的实际键入时长估算。“速度图从零开始”默认开启，关闭后历史速度趋势图与结果轨迹均按实际数据范围缩放，避免把计数和错误图错设为非零起点；更多图表交互尚未实现 |
| 本机统计日分界 | 连续天数与活动图的日期边界 | 已实现：首次选择 −11 至 +12 小时、30 分钟步进的本机分界后会锁定，并随设置/归档迁移；旧归档回退至本机零点。连续天数、近 28 日活动柱状图和 12 周热力图共用此分界，原始成绩时间不被改写。 |
| 活动标签 | 持续启用、成绩注入、预设、活动标签 PB 与当前设置筛选 | 已实现：用户可启用最多五个本机标签，未开始的练习立即捕获新集合；已开始的练习固定保留开局集合，界面会明确提示重开后才生效。完成成绩、重复本轮、设置归档与完整预设均保留标签；当前平均/PB在有活动标签时按任一标签命中、为空时不额外限制，活动标签个人最佳节奏引导则取同配置和语言下任一活动标签的最高完成速度。已保存且符合本机 PB 资格的完成结果，会为本轮每个标签按本机可比历史显示首次、新增或既有 PB；未保存、引语及不符合资格的成绩不显示该反馈。结果图还会为本轮未超过的已有标签 PB 绘制可持久化开关的水平线。 |
| 结果图 PB 标线 | 普通本机 PB、新 PB 皇冠与独立标线开关 | 已实现：已保存且符合本机 PB 资格的完成结果，会按所有同配置的本机完成成绩显示首次或增量皇冠反馈；已有 PB 未被本轮超过时，结果图会绘制带数值的普通 PB 水平线。普通 PB 与活动标签 PB 各有独立、随设置和归档持久化的开关；旧设置缺失新字段时默认显示。未保存、引语与不符合资格的成绩不声称或绘制本机 PB。 |
| 历史快捷筛选 | 恢复全部与按当前测试配置过滤 | 已实现：历史页提供“全部”和“当前测试设置”。后者精确选择模式、语言、难度、内容选项、标准/自定义时长或字数档、实际引语长度及修饰器；修饰器延续参考的任一命中规则。活动标签为空时仅显示无标签成绩；有活动标签时按任一活动标签命中。 |
| 历史多选语言 | 按任意语言集合过滤成绩 | 已实现：语言筛选从单选升级为多选；选择任意一个或多个本机语言时按任一命中，空选择明确不显示成绩。全选等同于不限制语言，包含无法解码配置的历史记录；旧单语言预设照旧解码，新预设持久化完整集合。 |
| 历史多选标签 | 按任意标签或无标签过滤成绩 | 已实现：标签筛选有明确的“全部”状态，并可转成包含“无标签”的动态显式集合；任一标签命中或空标签集合都会保留。未来新增标签不会扰动“全部”，但已保存的显式预设会保持创建时的选择；旧单标签预设继续按原语义解码。 |
| 历史个人最佳四态 | 全部、仅 PB、排除 PB、无匹配项 | 已实现：筛选 UI 和本机预设可表达参考实现 PB 两项的全部组合，包括两项都取消的无匹配状态；新字段存在时优先使用，遗留 Boolean 只在新字段缺失时按原“仅 PB / 全部”语义回退。 |
| 历史多选模式 | 按任意模式集合筛选成绩 | 已实现：模式条件升级为可组合的计时、字数、引语、禅和自定义模式；任意选择按或关系命中，全部取消时明确无匹配。新预设保存完整集合；遗留单模式字段只在新集合缺失时按原语义读取。 |
| 历史多选难度 | 按任意难度集合筛选成绩 | 已实现：难度条件升级为可组合的普通、专家和大师；任意选择按或关系命中，全部取消时明确无匹配。新预设保存完整集合；遗留单难度字段只在新集合缺失时按原语义读取。 |
| 历史标点与数字四态 | 全部、包含、不包含、无匹配项 | 已实现：标点和数字条件均可表达参考实现两个布尔开关的全部四种组合；两项都取消时明确无匹配。已保存的三态预设仍按相同含义读取，新预设可保存第四态。 |
| 历史多选引语长度 | 按任意引语长度集合筛选成绩 | 已实现：短、中、长、超长可组合选择，任一长度命中；新集合遵循参考结果查询的非引语回退桶，因此空选择仅保留非引语成绩。新预设保存完整集合，旧单长度字段继续采用 Typebar 原有的精确匹配语义。 |
| 区域 QWERTY/QWERTZ ISO 布局 | 键盘提示与输入布局模拟 | 已实现：原生键盘提示与显式 AppKit 物理 keycode 模拟支持 German QWERTZ、Swiss German、Swiss French、Nordic QWERTY、UK QWERTY、Spanish QWERTY、Italian QWERTY、Portuguese QWERTY (ISO/ANSI)、Latin American QWERTY。用户还可保存最多二十个原创的 Unicode 四行键盘图，独立选择显示或明确用于基础物理键位模拟；后者要求四行最多为 13/13/11/10 个字符，将可单字符大小写转换的字母映射到普通/Shift，符号不猜测 Shift 配对。超过物理容量的图保留视觉提示，没有已选图时安全回退 macOS 输入源。默认系统输入和显式模拟是独立持久化设置，旧归档中的非 QWERTY 选择会保守迁移为同一模拟布局。它们覆盖对应的 Y/Z、重音、`£`、`#`、`@`、Å、Ø、Æ、ñ、¿、¡、ç、`º`、`ª`、`«`/`»` 和 ISO `< >` 键。Layout Fluid 会按阶段同步图和模拟布局，最多使用当前十五种原生布局（官方配置上限为 15）；死键与 Option 层在系统输入模式下保留 macOS 输入路径。 |
| 外观 | 内置与自定义主题、收藏、随机/按系统切换主题、背景、字体、行宽、颜色和动画设置 | 部分实现：纸白、午夜、林地三套原创内置主题可持久化切换；用户可创建、编辑、应用和删除原创的本地 RGB 自定义主题，并随归档迁移；编辑原位保留主题 ID，因此收藏、当前选择和随机主题引用不失效。内置和自定义主题可收藏并从设置中的收藏栏一键应用，删除自定义主题会同步取消收藏；可选择按 macOS 深浅色自动切换，并分别指定浅色和深色所用的内置主题，或在每次完成测试后从全部、收藏、浅色、深色、自定义、按当前系统深浅色筛选的原生主题池中轮换；轮换结果只存在当前运行，不会改写手动主题，旧“重开随机”偏好会迁移为全部主题。练习页可选择等宽、圆角、衬线或默认 macOS 系统字体设计，也可填写已安装字体的名称，或导入用户拥有的 TTF/OTF 文件；导入字体仅在当前 Mac 的私有应用支持目录加载，覆盖名称选择且不随归档迁移。紧凑/标准/宽/自适应行宽会按字体缩放。背景可选 Typebar 自有的纯色、主题色光晕或细网格；光晕可以减少动态效果，且遵从 macOS 辅助功能。独立的五档“键入能量效果”会在接受输入时绘制原创粒子，高档位短暂抖动练习面板；它遵从“减少动态效果”，不改变输入、计分或结果。参考实现可读 WOFF/WOFF2，原生版本尚未加入该 Web 字体解码；其他兼容项继续盘点 |
| 测试文本颜色 | 翻转已输入/后续提示颜色、彩色提示 | 已实现：可独立翻转已输入与后续提示的明暗关系，或让已输入提示使用当前主题强调色；组合开启时后续提示使用强调色。错误、额外输入与计分保持独立，偏好会进入本机归档；真实设备对四种组合的视觉观察仍待人工验收。 |
| 自定义背景 | 远程/本地背景图片、适配、模糊/亮度/饱和度/透明度 | 已实现：用户可明确填写 HTTP(S) 的 PNG/JPG/GIF/WebP URL，或通过 macOS 文件选择器导入本地图片；本地图片优先、只留在当前 Mac 的应用支持目录，绝不上传或导出。远程 URL、覆盖/完整显示/填满适配及四项滤镜进入设置归档；只有填写 URL 时才发起图片请求，失败时安全回退到 Typebar 原生背景。真实图片加载、四种滤镜与适配仍待人工验收。 |
| 声音与辅助 | 键击/错误/警告声音、音量、键盘布局与按键提示、可读性设置 | 部分实现：键击、错误与倒计时警告均可独立启用，并各有四种 Typebar 原生的 macOS 系统音型、试听入口和共用 0–100% 音量，默认关闭且会随归档持久化；键击音仅在输入实际被接受后触发，错误音只在错误数增加时触发，倒计时音每轮仅在所选的结束前 1/3/5/10 秒触发一次。缺少某个系统声音时会回退或静默，绝不影响输入或计分。控制栏可显式用 macOS 内置语音朗读当前提示，按选择语言使用系统 locale，重开时立即停止且文本不离开设备。键盘布局与下一键提示已实现。更多可读性设置尚未实现 |
| 预设与操作 | 配置预设、导入/导出、命令面板、快捷键、可分享的测试配置 | 部分实现：⌘R 始终可重开，另可在本机设置选择关闭/Esc/Tab/Enter 快速重开；长测试需用 Shift+Esc/Tab，Enter 不再重开，而双击 Shift+Enter 中止并显示未保存结果。可选择显示练习页底部的实际重开和 ⇧⌘K 命令面板快捷键，输入获焦时提示淡出，偏好会随归档保存；命令面板可在默认单列表搜索与分组导航间切换，前者需输入关键词才显示全部命令，后者按本机功能分组浏览并支持 `>` 随时搜索全部命令。完整测试预设及自定义文本可本地保存、列出、应用和删除；设置、成绩、预设和文本已可导入/导出；原生命令面板可经工具栏或 ⇧⌘K 搜索操作；可复制或导入自有 `typebar://test` 配置链接，链接仅含测试配置、引语 ID 或自定义文本；参考项目全部快捷键和网页链接互操作尚未实现 |
| 身份与账户 | 注册、登录、密码重置/更新、邮箱验证/更新、资料、令牌/第三方身份管理 | 部分实现：自建服务与原生设置页支持注册、登录、会话恢复、当前资料、退出、密码更新、密码重置、邮箱验证、邮箱更新、撤销所有设备会话和账户删除；删除、会话撤销、移除第三方方式和添加密码均要求一次性重新验证与原生确认：密码账户验证当前密码，纯第三方账户通过已关联提供商的 macOS 系统授权窗口确认身份。服务端只保存五分钟重新验证凭据的哈希且使用即作废；删除与会话撤销会清除相应自建服务会话，删除还会清除账户的成绩、同步、关系、屏蔽与投稿，同时保留本机练习历史。密码更新验证当前密码并重哈希新密码；邮箱更新验证格式和唯一性、复位验证状态；两者均撤销该账户既有会话并回传仅当前设备保存的新令牌。密码重置由部署者配置的 HTTPS webhook 投递邮件，请求端对任何邮箱给出同一响应；服务端只保存 20 分钟的一次性令牌哈希，投递失败会撤销令牌，重置成功会注销所有会话且要求新密码重新登录。相同 webhook 也会在注册或改邮箱后投递 24 小时的一次性验证码，原生设置页可重发并完成验证；重发或改邮箱会撤销旧码，验证成功不轮换会话。未配置投递时，接口和原生设置页都会如实提示不可用。服务端已具备 GitHub/Google 授权码登录、PKCE、哈希化一次性 state、已验证邮箱解析、新用户补充展示名、登录方式关联、纯第三方账户的密码添加/移除及“最后一种方式不可移除”保护；第三方重新验证必须与该账户已关联的提供商身份精确匹配，提供商令牌不落盘。原生账户设置会通过 macOS 系统授权窗口完成回调、新用户显示名、关联、重新验证、移除和密码管理，并兼容尚不返回登录方式的旧服务响应；真实提供商账户端到端人工验收尚未完成。用户可选择从所有自建服务全局/好友 WPM 和 XP 榜隐藏；服务端即时排除既有与新成绩，而成绩、XP、同步和本机历史仍被保留。服务端会从已验证成绩重算总 XP，并在当前账户和不含邮箱的公开资料中返回。练习历史页会从本机完成成绩即时派生四项原创成就，不上传或替代 XP；公开成就资料尚未完成 |
| 开发者密钥 | 创建、一次展示、列出、改名、禁用、删除与自动化私有成绩读取/上传 | 已实现：原生设置和自建服务提供账户拥有的 Typebar 开发者密钥，名称受限、每账户最多五个。明文只在创建响应显示一次，持久化只含 SHA-256 哈希及元数据；常规会话才能管理。自动化客户端可用 `X-Typebar-Access-Key` 读取或上传自己的最小成绩元数据，不能读取资料或同步数据、修改账户或管理密钥；禁用/删除即时拒绝后续读取和上传。原生设置可查看近期服务端成绩。实现不复制参考项目的命名、代码或资产；真实双端部署流程仍待人工验收。 |
| 远端成绩管理 | 读取近期服务端成绩、编辑单条成绩标签、清除所有服务端成绩 | 已实现：原生账户设置可读取当前账户已提交成绩的最小元数据，可为单条成绩添加或移除最多五个原创标签，并可在重新确认身份后永久清除全部服务端成绩及相应 XP。标签会去除首尾空白、限制为每个 1–24 个字符，且按大小写与重音无关的比较去重；服务端仅允许常规会话编辑所属成绩，旧数据缺少标签时安全显示为空。密码账户清除时需当前密码，纯第三方账户走 macOS 系统授权并消费五分钟一次性凭据；服务端只删除当前账户的成绩，绝不影响本机历史、同步、设置或其他账户。开发者密钥只能读取/上传成绩，不能编辑标签或清除。 |
| 云端同步 | 配置、结果、标签、个人主题与预设的跨设备同步和冲突策略 | 部分实现：登录自建服务后，原生同步面板可上传本机归档，或按去重规则拉取并合并远端归档。自建服务可将变更分为每页最多 100 条；客户端会读取至最后一页，且只有归档实际合并成功才保存游标与版本，因此解析或合并失败可安全重试。旧服务省略分页字段时仍按单个完整页兼容；细粒度变更、冲突副本和端到端人工验收尚未完成 |
| 社交 | 公开资料、好友请求、好友结果/榜单、屏蔽、通知、举报与站内信 | 部分实现：自建服务与原生客户端支持公开展示名搜索、公开资料、好友请求、接受、取消、解除、屏蔽与解除屏蔽；账户可编辑公开简介、键盘说明、GitHub、X / Twitter 用户名与 HTTPS 个人网站，并独立选择是否在详情资料公开活动日历。公开资料与好友行显示账户的最佳 WPM 和最高稳定度，后者是客户端从本机回放派生、服务端只范围校验和保留的展示值；资料卡还显示 15/30/60/120 秒和 10/25/50/100 词标准档的最佳成绩、语言、速度、准确率、稳定度和完成日期，以及按 UTC 日聚合的最近 12 个月完成活动，但绝不包含提示、输入回放、邮箱或令牌。关闭活动隐私后，详情端点不返回日历；搜索、好友和通知中的嵌套资料始终保持轻量且不含活动。旧服务响应会安全回退为 0%、空个人最佳、空公开资料和无活动。好友请求和接受会产生仅接收者可读取、可标记已读的站内通知。已接受好友可从原生好友面板打开一对一会话，发送、刷新和读取最多 1,000 字的私有消息；非好友、第三方与被屏蔽关系会被服务端拒绝，屏蔽还会删除双方会话记录。登录用户可在公开资料卡提交私有资料举报，也可对已批准的社区引语选择原因并填写说明；服务端拒绝自举报与重复同类举报，且不会通知目标或自动处罚。资料举报可细分为不当显示名、简介、链接、误导资料、异常成绩或其他问题。原生设置页提供两类审核工作台：部署密钥仅停留在当前内存会话，资料队列只显示目标公开资料、举报上下文和状态，引语队列只显示引语与举报上下文，均不显示举报者身份；资料举报只能标为已处理或驳回，不会自动处置账户。好友结果/榜单、推送与端到端人工验收尚未完成 |
| 排行榜 | 全局、每日、每周 XP、好友榜、筛选、排名与反作弊结果验证 | 部分实现：独立服务可接收带基本范围/时间校验的成绩，并提供模式/语言及服务端当天、昨天完整日或 ISO 本周筛选的全局或好友 WPM 榜；每位用户在当前筛选下仅保留最佳成绩并获得对应名次，好友榜只包含本人和已接受好友，原生客户端可切换浏览。常规登录会话还可读取自己在当前全局或好友 WPM 榜、及 ISO 本周或前一完整周 XP 榜的实际条目，即使其名次落在公开列表前 100 名之外；隐身或没有合资格成绩时明确为空，开发者密钥没有该权限。XP 响应会回显范围，旧自建服务若无法确认上周范围，原生客户端会明确提示而不显示可能错误的本周数据。榜单会显示原生客户端从本机回放派生的稳定度；服务端仅验证该展示值处于 0–100，尚未有完整事件回放验真。旧请求、既有服务端持久化结果和旧响应均以 0% 兼容。全局/好友 XP 榜按服务端 ISO 周聚合，最多返回 100 人；XP 从已验证成绩的练习时长、准确率和模式重算，禅模式为零，重复结果 ID 不重复计算。限速已实现；赛季奖励与更完整反作弊尚未实现 |
| 内容社区 | 引语浏览/收藏/评分/提交/审核/举报 | 部分实现：原创离线引语可按长度浏览、收藏，并作本机“不错”/“不适合”评分或清除评分；完成结果会捕获本轮实际引语，内置内容可直接收藏或本机评分，已审核社区内容会保留聚合评价并允许登录用户改评、撤销或填写说明私下举报，绝不因下一轮选择变化而操作其他引语。登录用户可提交引语等待自建服务审核，部署者可凭显式配置的审核密钥读取按状态筛选的队列并批准或拒绝，客户端可选择浏览和练习已批准内容。原生设置页已提供审核工作台，审核密钥不落盘，队列按状态展示引语、署名和举报上下文而不显示举报者身份，待审核投稿可批准或拒绝。资料举报也有独立队列，显示目标公开资料与举报上下文，可标为已处理或驳回，但不会自动处置账户。登录用户还可对他人已审核投稿给出单一“不错”或“不适合”社区评价、改评或撤销；列表只显示聚合票数，评分者身份不公开，账户或投稿删除会清除关联评分。已批准投稿支持填写说明的私有内容举报。细粒度审核角色和申诉流程尚未完成 |
| 系统能力 | 菜单栏、全局唤起、离线优先、网络失败降级、通知、备份恢复 | 部分实现：菜单栏可激活应用并将可见主窗口恢复到前台；用户可明确启用 `⌃⇧Space` 全局唤起，应用只在取得 macOS 辅助功能权限后监听，未授权会显示系统设置路径。离线运行与本地 `.app` 打包已验证；已实现自建服务内好友关系通知及原生通知中心；系统推送和更完整的网络降级尚未实现 |
| 运营/管理 | 服务配置、公告、审核、速率限制、审计/指标、数据保留与删除 | 部分实现：自建服务已有审核队列、请求限速和启动时维护模式；设置 `TYPEBAR_MAINTENANCE_MODE=true` 后，状态和只读查询可继续，任何写入会得到可重试的 `503`，原生客户端会明确提示本机离线练习不受影响。公告、持久化审计/指标、数据保留策略、分布式限速和完整反作弊仍未实现 |

实时进度、速度、Raw、Burst 与准确率另有四种原生颜色和 25%、50%、75%、100% 四档透明度。该偏好只改 SwiftUI 指标呈现，不改错误数、段落进度、输入、计分、结果、统计或回放，并会进入本机设置归档。

提示高亮范围提供关闭、当前字符、当前词和当前词加后续 1/2/3 个词；词范围只用于常规空格分词提示，卷带、无空格、听写、记忆与预读遮挡均降级为当前字符。它只改变原生呈现并会进入本机设置归档，不改变输入、计分、结果、统计或回放。

键盘提示提供关闭、静态、按键反馈和下一键四种原创原生模式；按键反馈仅以 180ms 的视觉短暂标记最近的正确/错误物理输入，下一键仅标记当前目标。键盘大小可在 0.5–3.5 间以 0.1 步长调节，并用 SwiftUI 原生尺寸重排而非网页 zoom/CSS。图例可设为小写、大写、空白或动态；动态档随实际 Shift 与 Caps Lock 切换，数字和标点仅在 Shift 按下时显示 Shift 符号。按键集可选精简、精简（数字行）和完整：精简档遵循官方 QWERTY/Dvorak/Colemak 的顶行隐藏和 AZERTY 的顶行保留策略，下一键为数字时显示数字行；完整档仅追加不可输入的原生功能键图示。键盘样式完整接受官方的 `staggered`、`alice`、`matrix`、`split`、`split_matrix`、`steno`、`steno_matrix` 枚举，并以 Typebar 原生错列、人体工学内弧、矩阵、分体和速录几何实现；速录样式只展示原创标准速录键位，不替代速录和弦输入。除十五种内置图外，用户可创建最多二十份每行 1–16 个 Unicode 可见字符的原创四行键盘图，并选择作为纯视觉提示，或明确用于基础物理键位模拟：可单字符大小写转换的字母按普通/Shift 映射，符号保持原样而不猜测配对。缺少或删除选中的图时会回退 macOS 输入源，Layout Fluid 继续只同步内置图与模拟布局。Option/Control/Command 图层始终保留给 macOS。听写隐藏提示，Simon 指令强制下一键；模式、大小、图例、按键集、样式、键盘图和输入模拟设置都会随本机归档保存，只有显式模拟和 Layout Fluid 会影响实际输入。

自定义主题现在可独立保存并编辑提示文字、辅助文字、光标、淡化已输入、错误、额外输入，以及彩色模式下的错误与额外输入颜色；它们只用于练习区的文本与反馈，避免覆盖原生设置界面的系统可访问性文字颜色。旧主题归档缺少字段时以强调、辅助或常规反馈颜色派生稳定默认色。

## 交付门槛

1. 核心、输入、结果、设置、内容与统计必须在断网状态下可用。
2. 每个远程模块必须具备等价的自建 API 和客户端流程；不能依赖 Monkeytype 的生产接口、账号、排行榜或内容。
3. 每条矩阵能力要关联：需求编号、独立测试、人工验收、实现位置与完成状态。
4. 上述条目全数验收前，项目保持进行中，不以“核心功能可用”作为完整重写的结论。

## 现有验证证据

| 模块 | 证据 | 覆盖 |
| --- | --- | --- |
| 核心会话状态机 | `Tests/TypebarTests/TypingEngineTests.swift` | 五种模式的结束条件、难度失败、输入规则、统计计算、结果值对象与超界输入 |
| 离线内容工厂 | `Tests/TypebarTests/TypingEngineTests.swift` | 五类模式按各自长度/文本语义生成 prompt |
| 结果存储 | `Tests/TypebarTests/TypingEngineTests.swift` | 内存 SwiftData 容器中的结果与配置往返 |
| 闲置保护 | `Tests/TypebarTests/TypingEngineTests.swift` | 一秒活动区间、结束前五秒闲置无效、有效键入时长、旧归档默认值与 SwiftData 往返 |
| 结果稳定度 | `Tests/TypebarTests/TypingEngineTests.swift` | 根据 Typebar 自有回放重建均匀节奏的速度/按键稳定度，并在没有有效回放时安全回退为零 |
| 偏好设置 | `Tests/TypebarTests/TypingEngineTests.swift`、打包应用的设置窗口 | 难度、输入规则、字体、主题、练习背景、键盘图和输入布局模拟可持久化；设置 UI 可实际切换三种原生背景，且光晕模式显示减少动态效果开关。恢复默认会要求确认、完整复位设置并移除本地背景图片；本地字体文件与练习历史保留在当前 Mac |
| 活动标签 | `Tests/TypebarTests/TypingEngineTests.swift` | 本机持久化、归档、预设往返、完成成绩注入、重复测试保留、当前统计/历史筛选、活动标签个人最佳节奏引导、完成页标签 PB 反馈及可开关图表水平线 |
| 结果图 PB 标线 | `Tests/TypebarTests/TypingEngineTests.swift` | 同配置本机 PB 比较、首次/新增/已有边界、引语排除与新旧图表开关设置兼容 |
| 测试预设 | `Tests/TypebarTests/TypingEngineTests.swift`、打包应用的预设库 | 配置、规则、引语/自定义文本能作为一个完整预设往返存储 |
| 数据迁移 | `Tests/TypebarTests/TypingEngineTests.swift`、打包应用的“数据迁移”界面 | 版本化设置、成绩、预设和自定义文本归档往返；导入去重合并；v1 归档兼容；导入/导出文件选择器已人工验收（已取消，未写入真实文件） |
| 本地统计 | `Tests/TypebarTests/TypingEngineTests.swift` | 完成次数、均速、最佳、准确率、累计时长，以及 WPM 固定区间分布（包括空档区间）聚合 |
| 挑战验收 | `Tests/TypebarTests/TypingEngineTests.swift` | 原创离线挑战的通过与全部未满足条件都可被独立验证；挑战标记可随测试配置编码，并与历史配置兼容 |
| 构建 | `swift test`、`swift build` | macOS Swift 包能编译且 244 项客户端测试通过；独立 Vapor 服务的 60 项自动化测试通过 |
| 运行时 UI | 打包的 `Typebar.app` 辅助功能树与截图 | 焦点输入、时间/字数模式、实时指标、可滚动长文本区，以及正确输入/错误/退格/Esc 重开；2026-09-02 已人工切换“光晕”背景，确认减少动态效果开关状态和练习页主题色光晕绘制，检查后恢复为纯色 |

## 2026-09-03 审计更正

- “结果”域中先前关于慢词练习“至少五个可测词速、最慢四分位且排除未计时项”的表述已失效。现行实现以本机已尝试目标为序，排除尾部活动项后按 burst 升序选取四舍五入的最慢 20%、最多 20 个位置；缺失 burst 以 0 排序，并按所选位置的慢度构建有限加权练习。该更正以 `MET-18` 的自动化和 QA34B 证据为准。
- 结果页“导出”新增 `MET-32`：普通空格分词测试复制已到达的目标词而非实际错误输入或未到达提示；禅模式复制本机输入，无空格提示按本机已到达的目标范围截取。该功能不读取、上传或复用参考项目内容。
- “难度与输入规则”域中关于最低单词速度“只评估正确词”的表述已失效。现行实现提供关闭、固定与弹性三档；弹性阈值使用 `min(base, floor(base × 1.03^(-2 × (目标词长 − 3))))`，每个具可测 burst 的空格提交词都会检查，无论该词是否正确。
- expert 难度的词级校验会在实际词提交点运行：普通提示使用空格，无空格提示使用保留原始词边界的末字符；历史错误已修正后不触发失败，强制标记的物理键位错误仍视为错误。
- 无空格提示会拒绝直接插入的常见 Unicode 空白字符；遇错停下的单词档与“修正后再前进”都将原始词的末字符视为提交点，只有整词正确时才前进。
- 输入空白遵循参考的显式 Unicode 空白集合：普通难度忽略每个源词开头的空白，严格空格则保留它作为可修正错误；expert/master 和硬删除规则也会接收词首空白。词间的同类空白会规范为普通空格，Return 仍只在提示要求换行时进入普通测试，并像空格一样提交当前词。直/弯单引号、直/弯双引号、常见短横线与低位逗号会规范为提示中的字形并计为正确。普通难度在词内提前输入空格或 Return 会提交该错误词并跳到下一目标词；含换行提示的词首 Return 会提交空词，严格空格、停止/删除错误或“修正后再前进”则会阻止该跳转。目标词完成后继续输入的额外字符会保留在当前词并计作错误，直到空格或换行提交。隐藏额外字符只影响这些额外字形的呈现；输入仍被接受并参与错误统计和回放。空格分词输入缓冲限制为目标词（含提交分隔符）加 20 个字符，达到上限仍可提交空格。原生引擎将实际输入字符映射到目标位置，因此跳过字符和额外字符的提示状态、词进度、结果历史、错误统计和自由退格不会失去对齐，expert 会在同一提交点失败。
- 禅模式不再以预生成的离线词流模拟。现行实现与参考交互对齐为本机自由输入：空格或换行提交词、任意输入正确、Tab 保留为输入内容且仅 Shift+Enter 结束。禅模式的弹性最低 Burst 以刚提交的本机输入长度计算。
- `MET-34` 增加本机闲置审计：有限测试的计时器不会暂停；结束前连续 5 个一秒输入区间没有文本插入时，结果显示为“闲置无效”，只在当前窗口可见，绝不保存、本机统计、同步或发布。结果记录总用时、闲置时长、闲置占比与有效键入时长；历史和“今日练习”仅累加有效键入时长。旧归档缺失该字段时归零，键盘删除活动仅影响闲置统计、不解除结束前的文本插入要求。
- `MET-35` 补齐参考结果与历史的稳定度维度：Typebar 只使用自己的插入/删除时间回放，分别采样每秒速度和相邻按键间隔，再以 Kogasa 型百分比映射显示稳定度与按键稳定度；最后一个未闭合按键间隔不参与。结果详情、历史行、最高/平均/近 10 次统计和每日平均稳定度图均从同一派生数据读取。回放或有效样本缺失时显示 0%，不会由最终 WPM、提示或汇总统计推断数值。参考网页的事件记录、实现和资产均未复用。
- `MET-45` 补齐活动标签：用户可启用最多五个本机标签，未开始的练习会立即重建并捕获标签；开始后的更改明确仅在下一次重开后生效。完成成绩、重复本轮、设置归档与完整预设都保留该集合。近十次平均、当前 PB、当前设置历史筛选和活动标签个人最佳节奏引导按参考的空集合/任一活动标签语义读取，且所有标签名称均用 Typebar 自有本机数据保存。
- `MET-46` 补齐完成页的标签 PB 反馈：已保存且符合本机 PB 资格的完成成绩，仅以同配置、同语言和同名标签（大小写/重音无关）的本机可比历史计算每个本轮标签的既有 PB；超过时显示增量和皇冠，首次记录明确说明首次 PB。引语、无资格设置和未保存结果不会声称 PB；不读取或保存参考结果数据。
- `MET-47` 补齐结果图的标签 PB 水平线：本轮未超过的已有标签 PB 会以独立灰色点划线和标签绘入本机速度图，并可单独开关且随设置和归档持久化；新 PB 不绘制已过时的旧线。旧归档缺少开关时安全默认显示；水平线只读取本机可比历史，不读取参考或远端成绩。
- `MET-48` 补齐普通本机 PB 的结果反馈与图表标线：已保存且符合资格的完成结果会与不受活动标签筛选影响的同配置本机完成历史比较；首次或超越时显示皇冠与增量，未超越的已有 PB 则以独立灰横线和数值绘入结果图。普通 PB 标线可独立开关，旧归档缺失字段时默认显示；未保存、引语和无资格配置不宣称 PB，也不读取或保存参考/远端成绩。
