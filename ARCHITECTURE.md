# Typebar 架构决策（ADR-001）

## 决策

以“本地优先、服务可替换、内容可追溯”为基础构建独立系统：macOS 客户端绝不请求 Monkeytype 的接口或使用其身份/内容；同步服务只在本地数据完整可用之后增加。

## 客户端边界

| 模块 | 责任 | 依赖方向 |
| --- | --- | --- |
| `TypingEngine` | 纯会话状态、输入规则、指标和完成结果 | Foundation；不依赖 UI 或数据库 |
| `ContentDomain` | 自建词库、引语、文本和语言选择 | 只向引擎提供 prompt |
| `ResultsDomain` | 结果、回放、标签、个人最佳与统计 | 使用引擎值对象与本地存储 |
| `PreferencesDomain` | 配置、预设、主题和导入/导出 | 使用可编码领域对象 |
| `AppKitInputBridge` | IME、键盘事件、焦点和全局快捷键 | 向 SwiftUI / 引擎发送输入事件 |
| SwiftUI feature views | 测试、结果、历史、设置、账户等 UI | 只调用领域服务 |
| `SyncClient` | 排队、冲突处理、认证和网络状态 | 读取本地变更；不阻塞练习 |

## 数据与同步

1. SwiftData 保存结果、预设与保存文本；本机偏好保存设置、主题及只含成绩 UUID/账户/服务器作用域的待发布索引。待发布索引不复制提示、回放或凭据，成绩正文仍以 SwiftData 记录为准。
2. 领域对象使用 `Codable`，以便导出、备份和 API 传输；SwiftData 模型仅是存储适配层。
3. 每条可同步记录都有 UUID、创建/更新时间、删除标记和版本；服务端以变更日志同步，客户端用确定性冲突规则合并。
4. 凭据只进入 Keychain，并按规范化自建服务地址隔离；服务切换不复用或跨域发送 Bearer 令牌。离线数据、导出文件和日志不包含密码、访问令牌或原文密钥。
5. 网络不可用时，测试、结果、设置、内容和统计继续工作；已开启远端发布的完成成绩会按账户与服务器隔离排队，并在联网、同账户恢复或重新开启发布时幂等重试。完整归档同步及其他联网写入仍由用户明确发起，不宣称已有统一队列。
6. 持久容器启动失败时保持原 SwiftData 文件不变，不自动重建或回退到空库；主窗口进入可诊断恢复状态，用户处理磁盘、权限或迁移问题后通过重启重试。

## 服务端边界

独立服务要提供身份、同步、公开资料、好友、排行榜、内容审核和运营接口。它使用我们自己的契约、数据库、限速与反作弊实现；不得代理或镜像 Monkeytype 的生产服务或数据。

## 验收

- `TypingEngine` 单元测试不启动 App 或数据库。
- 存储测试在临时 SwiftData 容器中验证结果往返。
- UI 通过可访问性标识执行端到端流程。
- 服务端以契约测试验证同步、权限和反作弊边界。
- 每一项与 `FUNCTIONAL_INVENTORY.md` 的 ID 对应。
