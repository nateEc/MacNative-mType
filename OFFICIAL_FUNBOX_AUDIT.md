# 官方 Funbox 逐项审计

参考对象固定为 `monkeytypegame/monkeytype` 提交 `91bd24bb8513785c7364cbea29296ff7adafac41` 中的 `packages/funbox/src/list.ts`。该文件只记录行为映射，不复制参考实现的代码、文本、样式、词库或资源。

状态说明：

- **等价实现**：Typebar 有独立的用户可见实现与自动化证据。
- **部分覆盖**：覆盖了主要用途，但互动、视觉、数据源或限制不同。
- **未实现**：仍是兼容性缺口，不能据此宣称完整重写。

2026-09-21 输入审计更正：简化重音输入严格遵循参考的有限字符替换表及逐语言覆盖，而非泛化 Unicode 折叠。未列入表的 `ñ` 维持原样；组合标记也会独立进入替换和大小写位置计算。由于参考以整段大写结果的位置决定多字符替换的字母大小写，较早的 `ß` 扩展会影响后续替换；Typebar 以独立 Swift 实现、针对性测试复现该可观察行为，并补齐 Vietnamese 的 `đ/ơ/ư` 覆盖，不导入参考代码、词表或资产。

2026-09-21 输入回放审计更正：遇错删除不只复现最终文本，还先记录失败键，随后将恢复删除标为自动事件。字符、单词和两种硬档均按此时间线处理；保留原始边界的无空格提示同样以词首判断硬档回退。该观察来自固定参考的输入事件语义，Typebar 使用独立 Swift 事件模型和回归测试实现，未复制事件处理代码或数据。

2026-09-21 连写文字准入更正：固定源码在当前选中语言标为 `joiningScript` 时，若趣味模式包含 `noJoiningScript` 的旋转文字、地震、CRT、字符双写或 ASL，便清空整个趣味模式选择。Typebar 现以独立策略在配置、预设和命令重开路径复现该行为；混排仍按参考只检查当前选中的多语入口，不逐个限制其语言组件。

2026-09-21 视觉动态审计更正：固定源码只在 `nausea`、`round_round_baby`、`choo_choo`、`earthquake` 与 `space_balls` 的元数据标注 `ignoreReducedMotion`，并以页面状态让这五项绕过系统的“减少动态效果”。Typebar 用独立 Swift 策略保留这一边界：前四项有动画的原生视图会在系统偏好下继续运行，静态星点覆盖保留相同元数据但没有额外动效；Typebar 自己明确开启的“减少动态”仍优先停用四项动画。

2026-09-21 内容选项审计更正：固定源码会强制关闭 `arrows`、`ascii`、`specials`、`poetry`、`wikipedia`、`binary` 的标点与数字；`58008`、`IPv4`、`IPv6`、`hexadecimal` 只关闭数字。Typebar 现以独立策略在配置初始化、修饰器更新和 JSON 解码统一写入这些有效值，因此运行、预设、结果、归档与导入不会保留与实际 Funbox 冲突的内容选项。

| 官方 funbox | Typebar 对应能力 | 状态 | 差异/验收线索 |
| --- | --- | --- | --- |
| `58008` | 会计数字流 | 等价实现 | 自有的千分位小数生成器；强制关闭数字；`FUN-06` |
| `mirror` | 镜像练习区 | 等价实现 | 原生水平翻转，不改变输入或计分 |
| `upside_down` | 倒置练习区 | 等价实现 | 原生 180° 旋转，不改变输入或计分 |
| `nausea` | 眩晕练习区 | 等价实现 | 原生周期性透视倾斜与缩放；按源码绕过系统减少动态，Typebar 明确减少动态仍优先 |
| `round_round_baby` | 旋转练习区 | 等价实现 | 原生匀速整面板旋转；按源码绕过系统减少动态，Typebar 明确减少动态仍优先 |
| `simon_says` | Simon 指令 | 等价实现 | 待输入字形隐藏，原生下一键键盘提示强制显示并保持输入计分 |
| `tts` | 听写模式 | 等价实现 | 使用 macOS 本机语音；`ACC-01` |
| `choo_choo` | 旋转文字 | 等价实现 | 原生逐字流式排版和独立 3D 旋转；按源码绕过系统减少动态，Typebar 明确减少动态仍优先 |
| `arrows` | 方向键流 | 等价实现 | 原生 keycode→箭头输入；强制关闭标点和数字；`FUN-07` |
| `rAnDoMcAsE` | 随机大小写 | 等价实现 | 每个 Unicode 字符独立随机大小写；标点与空白保持原样但仍消耗随机选择；`FUN-05` |
| `sPoNgEcAsE` | 交替大小写 | 等价实现 | 每个词都从首 UTF-16 单元小写、第二单元大写的相位重新开始；组合标记和非 BMP 字符按源码宽度推进相位，非字母保持原样 |
| `capitals` | 逐词首字母大写 | 等价实现 | 仅将每个词的首字符改为大写，保留其余字符原样 |
| `layout_mirror` | 镜像键盘 | 等价实现 | 输入映射和下一键提示同步 |
| `layoutfluid` | 布局流动 | 等价实现 | 字数练习按进度切换用户可配置、独立持久化的键盘提示序列，并在切换前三词倒数提醒 |
| `earthquake` | 地震练习区 | 等价实现 | 原生轻微周期偏移；按源码绕过系统减少动态，Typebar 明确减少动态仍优先 |
| `space_balls` | 太空练习区 | 等价实现 | 原生确定性静态星点和主题色光晕，不使用图片资产；保留源码的绕过系统减少动态元数据 |
| `gibberish` | 无意义字串 | 等价实现 | 自有确定性生成器生成 1–7 位 ASCII 小写字母串 |
| `ascii` | ASCII 字符流 | 等价实现 | 自有确定性生成器覆盖全部可打印 ASCII 区间（33–126）；强制关闭标点和数字 |
| `specials` | 特殊符号流 | 等价实现 | 自有确定性生成器覆盖键盘常用标点、括号与运算符；强制关闭标点和数字 |
| `plus_zero` | 专注当前词 | 等价实现 | `FUN-01` |
| `plus_one` | 预读下一词 | 等价实现 | `FUN-01` |
| `plus_two` | 预读后两词 | 等价实现 | `FUN-01` |
| `plus_three` | 预读后三词 | 等价实现 | `FUN-01` |
| `read_ahead_easy` | 预读遮挡（当前词） | 等价实现 | `FUN-01` |
| `read_ahead` | 预读遮挡（当前和下一词） | 等价实现 | `FUN-01` |
| `read_ahead_hard` | 预读遮挡（当前及后两词） | 等价实现 | `FUN-01` |
| `memory` | 记忆模式 | 等价实现 | 首次有效输入后隐藏提示 |
| `nospace` | 无空格 | 等价实现 | 移除显示分隔符，同时保留可安全映射的原始词边界与渲染后词段；拒绝直接输入的常见 Unicode 空白字符，遇错停下和修正后再前进均在原始词末字符提交点阻止错误词；字数进度、expert 难度校验、末字符提交的 Burst、最低单词速度和弹性词长阈值，以及结果词历史、Burst 热力图、错词和慢词复练都逐词运行 |
| `poetry` | 诗性散文 | 等价实现 | 英文等空格分词的 time/words 练习中，用户启用后会以原生 URLSession 请求 PoetryDB 的随机诗歌，显示标题/作者；强制关闭标点和数字；请求失败、超时或用户已开始输入时保持 Typebar 自有离线诗性流，且不保存远端内容到设置 |
| `wikipedia` | 知识短文 | 等价实现 | 所有空格分词的原生语言，以及简体/繁体中文的 time/words 练习中，用户启用后会以原生 URLSession 请求对应 Wikipedia 的随机摘要，并显示标题；强制关闭标点和数字；中文内容由 macOS 的系统词元器分段后以无空格提示呈现。日语假名与罗马字选项均承诺保持所选书写形式，随机百科可能含汉字，故保守使用原创离线知识流。请求失败、超时或用户已开始输入时始终保持 Typebar 自有离线内容，避免将网页代码、资产或固定外部文本纳入项目 |
| `weakspot` | 弱项训练 | 等价实现 | 每次有效输入都在本次 app 生命周期内按字符记录相邻输入间隔与错误罚时；启用后每个目标词从 20 个 Typebar 自有候选中选取平均弱项分最高者，不写入成绩或偏好，且不可产生 PB；持久化回放分析仍由独立“弱项分析”入口提供；`FUN-23`、`MET-02` |
| `pseudolang` | 伪语言词流 | 等价实现 | Typebar 自有生成器 |
| `IPv4` | IPv4 地址流 | 等价实现 | Typebar 自有确定性地址流；强制关闭数字 |
| `IPv6` | IPv6 地址流 | 等价实现 | Typebar 自有确定性地址流；强制关闭数字 |
| `binary` | 二进制流 | 等价实现 | Typebar 自有生成器；强制关闭标点和数字 |
| `hexadecimal` | 十六进制流 | 等价实现 | Typebar 自有生成器；强制关闭数字 |
| `zipf` | Zipf 高频词 | 等价实现 | Typebar 自有词表与秩次加权；参考为 `orderedByFrequency: false` 时会提示词表未按频率排序，未定义时会提示可能不支持，提示持续七秒且不会移除修饰器；`FUN-04` |
| `morse` | 摩斯符号流 | 等价实现 | Typebar 自有编码流 |
| `crt` | CRT 练习区 | 等价实现 | 原生扫描线、色调与边缘光晕，不改变输入或计分 |
| `backwards` | 逐词反写 | 等价实现 | 先反转词序，再按 UTF-16 单元逐词反写并以替换字符修复无效单元；无空格和下划线分隔提示仍作为单个连续片段反写 |
| `ddoouubblleedd` | 字符双写 | 等价实现 | 独立 Unicode 标量双写，标准词间空格保持单个；组合标记与非 BMP 标量均按固定参考的 Unicode 正则语义处理 |
| `instant_messaging` | 即时消息文本 | 等价实现 | 逐词小写；仅词末 ASCII `. ! ?` 变成换行，再清除 `. ( ) ' "`，保留词中问号、方括号、花括号和非 ASCII 句末符；`FUN-08` |
| `underscore_spaces` | 下划线分隔 | 等价实现 | 纯文本变换 |
| `ALL_CAPS` | 全大写 | 等价实现 | 纯文本变换 |
| `polyglot` | 多语混排 | 等价实现 | 使用 Typebar 自有一百零一种可混排 LTR 语言内容 |
| `polyglot`（2026-09-05 更正） | 多语混排 | 等价实现 | 使用 Typebar 自有一百零二种可混排 LTR 语言内容；新增 Swahili 的 `noLazyMode` 边界已由轮转测试覆盖 |
| `polyglot`（2026-09-05 更正） | 多语混排 | 等价实现 | 使用 Typebar 自有一百零三种可混排 LTR 语言内容；新增 Kinyarwanda 的 `noLazyMode` 和 Zipf 高频词边界已由轮转测试覆盖 |
| `polyglot`（2026-09-05 更正） | 多语混排 | 等价实现 | 使用 Typebar 自有一百零四种可混排 LTR 语言内容；新增 Shona 的缺省在线/朗读与 Zipf 未知边界已由轮转测试覆盖 |
| `polyglot`（2026-09-05 更正） | 多语混排 | 等价实现 | 使用 Typebar 自有一百零五种可混排 LTR 语言内容；新增 Belarusian Łacinka 的显式简化输入与缺省在线/朗读边界已由轮转测试覆盖 |
| `polyglot`（2026-09-05 更正） | 多语混排 | 等价实现 | 使用 Typebar 自有一百零七种可混排 LTR 语言内容；新增 Crimean Tatar 拉丁与西里尔书写的独立词流、`noLazyMode` 边界和 `crh`/`crh-CRH` 路径均由轮转测试覆盖 |
| `polyglot`（2026-09-05 更正） | 多语混排 | 等价实现 | 使用 Typebar 自有一百零八种可混排 LTR 语言内容；新增 Klingon 的大小写、词内撇号、`tlh` 在线/朗读路径和 Zipf 未知边界均由轮转测试覆盖 |
| `polyglot`（2026-09-05 更正） | 多语混排 | 等价实现 | 使用 Typebar 自有一百零九种可混排 LTR 语言内容；新增 Quenya 的 `en`／`en-US` 缺省在线/朗读与 Zipf 未知边界均由轮转测试覆盖 |
| `polyglot`（2026-09-05 更正） | 多语混排 | 等价实现 | 使用 Typebar 自有一百一十一种可混排 LTR 语言内容；新增 Viossa 与 Njutro 的独立 idiolect、`noLazyMode` 和 Zipf 不支持边界均由轮转测试覆盖 |
| `asl` | ASL 指语练习 | 等价实现 | 字母到原创原生矢量手势提示、J 弧线/Z 折线运动提示、当前/正确/错误状态与键盘计分均已覆盖；不导入官方专用字体，视觉设计有意不同，且不宣称可替代正式 ASL 教学 |
| `rot13` | ROT13 | 等价实现 | 纯文本变换 |
| `no_quit` | 锁定重开 | 等价实现 | `FUN-01` |

命令面板按固定列表提供 `none + 48` 个原生入口，完整固定名称和 `changeFunbox<name>` 标识均可搜索；会计流保留 `numbers` 别名，IPv4/IPv6 保留 `network` 别名。47 项直接切换上表已有修饰器，`polyglot` 路由自选多语；`weakspot` 作为实时选词修饰器切换并重开，同时保留独立的本机弱项分析入口。命令切换会退出挑战并重开；Memory 必要时切到字数模式，有限专用项不会在无限测试中启用，活动的锁定重开会阻止切换。“全部关闭”只清理固定 funbox 映射，不删除 Typebar 自有纠错、符号流或独立简化输入设置。

## 审计结论

48 项官方 funbox 均已有用户可见的功能等价实现，0 项部分覆盖、0 项未实现。ASL 指语提示以原创原生矢量而非官方专用字体呈现；其他涉及视觉细节、互动、外部内容源或本地替代策略的实现也会持续审计，不能据此宣称逐像素或逐数据源复刻。Egyptian Arabic、Moroccan Arabic、Pashto、Sindhi 与 Yiddish 的固定参考元数据均为 RTL／连写，因而和其余五个 RTL 单语一样刻意排除在 101 种 LTR 多语混排候选之外；Occitan、Oromo、Macedonian、Kazakh、Vietnamese、Jyutping、Pinyin、Western Armenian、Bashkir、Euskera、Frisian、Zulu、Hawaiian、Kabyle、Maltese、toki pona、Xhosa、Tibetan、Kyrgyz、Udmurt 与 Yoruba 的 LTR 路径均已由混排轮转测试覆盖。

无限测试冲突按固定参考的 `noInfiniteDuration` 元数据独立建模：布局流动、专注当前词、预读下一/后二/后三词、记忆模式、诗性散文和知识短文共八项只允许有限长度。原生配置存在这些修饰器时会禁用无限开关并解释原因；旧预设或归档若同时包含无限值与冲突项，则保留无限测试并在解码时安全移除冲突修饰器。其余 funbox 可随 Typebar 自有提示批次循环，不导入参考词表或实现。

2026-09-05 更正：LTR 多语混排候选现为 102 种；Swahili 作为 `noLazyMode: true` 的 LTR 语言已纳入轮转覆盖，普通练习禁用简化输入而自定义文本保留例外。

2026-09-05 更正：LTR 多语混排候选现为 103 种；Kinyarwanda 作为 `noLazyMode: true` 且 `orderedByFrequency: true` 的 LTR 语言已纳入轮转覆盖，普通练习禁用简化输入而自定义文本保留例外，Zipf 使用 Typebar 自有高频词。

2026-09-05 更正：LTR 多语混排候选现为 104 种；Shona 未定义 BCP-47、`noLazyMode` 或词频排序，故知识短文与朗读使用 `en`／`en-US` 缺省路径，保留简化输入与 Zipf 未知提示，并已纳入轮转覆盖。

2026-09-05 更正：LTR 多语混排候选现为 105 种；Belarusian Łacinka 定义 `noLazyMode: false`，未定义 BCP-47 或词频排序，故知识短文与朗读使用 `en`／`en-US` 缺省路径，保留简化输入与 Zipf 未知提示，并已纳入轮转覆盖。

2026-09-05 更正：LTR 多语混排候选现为 107 种；Crimean Tatar 拉丁与西里尔书写的十个固定配置均定义 `noLazyMode: true` 和 `bcp47: crh-CRH`，故两条独立词流均以 `crh`／`crh-CRH` 处理在线知识短文与朗读，普通练习禁用简化输入而自定义文本保留例外，Zipf 使用未知提示，并已纳入轮转覆盖。

2026-09-05 更正：LTR 多语混排候选现为 108 种；Klingon 的两个固定配置均定义 `bcp47: tlh`，未定义 `noLazyMode` 或词频排序，故其大小写敏感、词内撇号词流以 `tlh` 处理在线知识短文与朗读，保留简化输入与 Zipf 未知提示，并已纳入轮转覆盖。

2026-09-05 更正：LTR 多语混排候选现为 109 种；Quenya 的固定配置未定义 BCP-47、`noLazyMode` 或词频排序，故其原创 LTR 词流以 `en`／`en-US` 处理在线知识短文与朗读，保留简化输入与 Zipf 未知提示，并已纳入轮转覆盖。

2026-09-05 更正：LTR 多语混排候选现为 111 种；Viossa 与 Njutro 的固定配置均未定义 BCP-47、RTL 或连写，故两套独立的原创练习 idiolect 均以 `en`／`en-US` 处理在线知识短文与朗读，且 `orderedByFrequency: false` 显示 Zipf 不支持提示。Njutro 额外定义 `noLazyMode: true`，故普通练习禁用简化输入而自定义文本保留例外；两者均已纳入轮转覆盖。

2026-09-05 更正：LTR 多语混排候选现为 112 种；Māori 的固定参考仅为 `maori_1k`，未定义 BCP-47、RTL、连写、`noLazyMode` 或词频排序，故保留 macron 的原创 LTR 词流以 `en`／`en-US` 处理在线知识短文与朗读，保留简化输入与 Zipf 未知提示，并已纳入轮转覆盖。

2026-09-05 更正：LTR 多语混排候选现为 114 种；Lojban 的两个固定选择均定义 `noLazyMode: true`，没有 BCP-47、RTL、连写或词频排序。Typebar 因此将原创 gismu 与 cmavo 词流分离，后者保留 `.` 与 `'` 的输入语义；两者均走 `en`／`en-US` 在线/朗读回退、普通练习禁用简化输入而自定义文本保留例外，并以 Zipf 未知提示纳入轮转覆盖。

## 本机个人最佳资格

参考项目的个人最佳查询会拒绝任一 `canGetPb: false` 的 funbox，并且不会为引语模式保存本地 PB。Typebar 的 `CurrentPersonalBestPolicy` 据此维护显式的资格表：视觉、布局、可见性、听写、反写/双写、ROT13、ASL 和简化重音输入可参与；字符/特殊文本流、大小写与分隔符变换、即时消息、Zipf、多语混排和无空格流不会参与。Typebar 的中英混排与自定义多语也对应 polyglot 的资格限制，因而不会参与。Typebar 自有的“修正后再前进”和“遇错清除当前词”也被额外排除，因为它们改变了输入纠错机制；开启“遇错停下”时，只有 100% 准确率的完成成绩可参与。资格判断不复用参考项目代码或数据。

## 结果页单词 Burst 热力图

参考项目在结果单词历史中提供可选的 burst 热力图。Typebar 依据这一用户可见能力制作了独立的原生实现：从当前完成轮次的输入时间事件计算已尝试词速度，以本轮的五分位划分慢、偏慢、均衡、快、最快五档，并以当前速度单位显示图例。此算法、阈值、文案和界面均为 Typebar 原创；没有有效输入间隔的词保持中性，不伪造速度。该功能见 `MET-15`，不复用参考实现的代码、样式或数据。
